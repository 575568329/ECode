# ECode M1 配置（dev 模式 npm run dev 时读取，覆盖 ~/.ecode/config.json）
# 复制为 .env 并填入真实值

# Astron 的 Anthropic 兼容端点（跑 GLM）
ECODE_BASE_URL=https://astron-endpoint/v1

# API Key（密钥优先环境变量，其次 ~/.ecode/config.json）
ANTHROPIC_API_KEY=sk-your-key-here

# 默认模型（GLM API model 字段，按智谱官方最新）
ECODE_MODEL=glm-5.2
