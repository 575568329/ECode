# M6 三文档审阅问题清单

> **审阅对象**：[M6-技术选型](./M6-技术选型与理由[待实现].md) · [M6-实施方案](./M6-实施方案[待实现].md) · [M6-方案解析](./M6-方案解析[待实现].md)
> **审阅方式**：角色 agent 实读 src/ 源码 9 文件（agent.ts / agent-events.ts / session.ts / context-manager.ts / providers/config.ts / providers/types.ts / tools/types.ts / slash-commands.ts / package.json）+ 全仓 grep 5 类（resolveDataDir / homedir / tool_call / save|load|resume / provider|model）+ 对照 M5 三文档与功能架构支点 13/22/23。每条致命问题均有 file:line 证据，非臆断。
> **审阅日期**：2026-08-09
> **处理状态**：✅ 本轮已修 ｜ 🟡 待用户决策

---

## 总体评价

M6 三文档结构完整、决策清晰、YAGNI 边界明确，TUI/Web 双档设计合理，与 M5/M4 主体架构自洽。**但存在一类系统性事实错误**：多处把 src/ 现状已有能力（`provider`/`model` 注入）错误归属给「M5 阶段0 未实施设计」，对 M5 待做项（支点14 注册式重构、resolveDataDir 抽取）前置标注不彻底。另有 1 处内部矛盾（压缩路由 vs 不改循环）、1 处事件类型臆造（`tool_call`）、1 处悄悄删了功能架构既定的「安全扫描」。**6 致命 + 8 改进，本轮全部已修**。

---

## 🔴 致命问题（6 条）

### F1. `provider`/`model` 字段被错误归属给 M5 阶段0 —— 实际 src/ 现状已有 ✅ 本轮已修

- **问题**：M6 反复声称「M5 阶段0 已把 `provider`/`model` 放进 `RunAgentStreamOptions`」（6 处）。核查 `src/agent.ts:127`（`model?: string`）、`:135`（`provider?: ModelProvider`）——**现状已有**（M2/M3.5 落地），M5 阶段0 只新增 `tools`（M5-实施方案:148-153 确认）。
- **利好**：路由层 `provider`/`model` 注入**不依赖 M5**，现状直接可用。
- **处理**：✅ 三文档 6 处全部改为现状引用；连带调整「M6 依赖 M5」表述（路由的注入不再是 M5 依赖，仅子代理触发点依赖 M5 子代理）。

### F2. 压缩触发点切便宜模型 vs「不改循环」—— 内部矛盾 ✅ 本轮已修

- **问题**：`compressOpts` 在 `runAgentStream` 内部构造（`agent.ts:298-310`，`model = resolvedModel` 主模型）。切便宜模型须**轻微侵入 compressOpts 赋值**，与「循环内部零改动」矛盾。
- **处理**：✅ 明确「压缩触发点需轻微侵入 compressOpts 构造（把 resolvedModel 换成 resolveModelForScenario('compress')），同 M5 阶段0 改 tools 注入级别」；其余三触发点（全局/Skill/子代理）确实调用前注入不改循环。方案解析 §2.2 的「核心循环一行不改」限定表述。

### F3. WS 协议用 `tool_call` 事件类型 —— AgentEvent 实际无此类型 ✅ 本轮已修

- **问题**：协议示例写 `{ "type": "tool_call", "tool": ... }`，但 `agent-events.ts:25-47` AgentEvent 只有 `tool_call_start`/`tool_result`，无 `tool_call`；字段是 `name` 不是 `tool`。line 343「透传 4 种」与冒出的第 5 种矛盾。
- **处理**：✅ 协议改为 `{ "type": "tool_call_start", "id": "...", "name": "read_file", "input": {...} }`；透传清单补 `tool_call_start`。

### F4. `session.ts` 无 `resume` 接口，M6 当现状引用 ✅ 本轮已修

- **问题**：session.ts 导出 `saveSession`/`loadSession`/`listSessions`/`latestSessionId`（无 resume/save/load）；续接靠 `agent.ts` 的 `ResumeContext`；session.ts 是**无状态纯函数库**，无运行时 session 表/锁。
- **处理**：✅ 改为「现有 saveSession/loadSession(listSessions)」；明确续接靠 ResumeContext；明确 session-router 须自建连接映射 + 同 session 串行排队锁（session.ts 无锁）。

### F5. 支点14 注册式重构是 M5 阶段3 前置 —— M6 依赖斜杠命令但未标明 ✅ 本轮已修

- **问题**：`slash-commands.ts` 当前 SlashCommandDef 只有 name+description，无 execute；硬编码 8 条。「注册式重构」是 M5 阶段3 前置（M5-实施方案:455）。M6 多处把「复用支点14 命令中心」当现状。
- **处理**：✅ M6 实施方案 §阶段1 加「🔴 前置：支点14 注册式重构」标注；M6 命令注册（/skill /accept /reject /proposals /channels）依赖此重构。**新增决策点 D6**：支点14 重构归 M5 还是 M6 自带，待用户定。

### F6. Skills 自动生成遗漏「安全扫描」环节 —— 悄悄删了既定设计 ✅ 本轮已修

- **问题**：功能架构 §支点13(line 823) 明确「proposal 审批队列 → **安全扫描** → /accept 转正」。M6 三文档删除了「安全扫描」环节。
- **处理**：✅ 补回安全扫描环节。**新增决策点 D7**：扫描方式（自动模式扫描 vs 人工 review 降级）待用户定——本期默认「自动扫危险模式（rm -rf / 密钥 外发）+ 人工 review 正文」。

---

## 🟡 改进建议（8 条，本轮全修）

| # | 问题 | 处理 |
|---|------|------|
| **I1** | `config.json` 顶层已有 `defaultModel`（config.ts:21），routing 块重复定义 | ✅ routing 块不重复，全局触发点读顶层 defaultModel |
| **I2** | `ModelConfig` 当前无 `cost`（支点17 🟡） | ✅ 22-T4 补「支点17 当前 🟡，cost 未落地，路由可见性后置」 |
| **I3** | homedir 散用实际 4 处（index:272/300, settings-loader:166, config:28），M5 说 3 处偏低 | ✅ M6 阶段0 注明以实际 4 处为准 |
| **I4** | 实施方案未体现「同 session 并发排队」红线（方案解析有，实施缺） | ✅ session-router 加锁接口 + 验收加并发用例 |
| **I5** | M5 用 `tools:`、M6 用 `allowed-tools` 命名不一致未说明 | ✅ 13-T8 补「规范源不同（CC 子代理 vs agentskills.io），语义同」 |
| **I6** | 功能架构说多渠道需子代理隔离，M6 用 session.ts —— 不一致未点出 | ✅ 显式说明调整（支点6 已 ✅；子代理隔离是 L3 维度，多渠道是 L1，不混） |
| **I7** | 「复用 session.ts 天然支持」略夸大（无状态函数库） | ✅ 改为「提供持久化层，运行时隔离自建」 |
| **I8** | resolveDataDir 标注不统一（有的标 M5，有的裸用） | ✅ 统一标注「M5 阶段0 抽出，当前 src/ 不存在」 |

---

## ✅ 核查通过（11 条，核心设计成立）

1. **TUI 现状断言准确**：package.json 仅 ink@7+react@19，无 Web 栈；src/ui/ 15 个 .tsx；CLAUDE.md §4.2 Web 栈未装（Q6 答疑正确）。
2. **UI 复用组件全部存在**：model-picker/permission-dialog/picker-list/status-bar/tool-panel/session-picker/app/input-bar —— 8 个 src/ui/ 全存在。
3. **M5 依赖链准确**：Skills 阶段2←M5 Hooks Stop；路由子代理触发点←M5 子代理 model 字段；resolveDataDir←M5 阶段0。
4. **M5 阶段0 实际范围**：双源确认只新增 tools，未涉及 provider/model（反证 F1）。
5. **providers/config.ts 结构准确**：providers/models 两级；listAvailableModels/getContextWindow/hasCapability 三函数现状存在。
6. **AgentEvent 主要类型准确**：text_delta/tool_call_start/tool_result/usage/completed 存在（F3 的 tool_call 除外）。
7. **压缩用主模型断言准确**：agent.ts:299 compressOpts.model = resolvedModel。
8. **YAGNI 边界合理**：公网暴露/微信个人号/LLM 路由/marketplace 推后均有据。
9. **Skills 格式设计**：三家标准 + 懒加载 + proposal 审批逻辑成立。
10. **前后端分离 + 事件流解耦**：runAgentStream async generator（agent.ts:255）支持多消费者，表述准确。
11. **npm test watch 模式坑属实**：package.json test="vitest" 无 run。

---

## 🟡 审阅暴露的新决策点（追加到决策清单）

| # | 决策点 | 本期默认 | 待用户 |
|---|--------|---------|--------|
| **D6** | 支点14 注册式重构（SlashCommandDef 加 execute + 查表 dispatch）归 M5 阶段3 还是 M6 自带 | M5 阶段3 前置（M6 依赖） | M5/M6 时序 |
| **D7** | Skills proposal「安全扫描」方式：自动扫危险模式 vs 纯人工 review | 自动扫（rm -rf / 密钥 / 外发）+ 人工 review | 扫描严格度 |

---

**结论**：6 致命 + 8 改进本轮全部已修；核心设计 11 条核查成立。新增 D6/D7 待用户审阅。M6 文档可进入「待 M5 落地后实施」状态。
