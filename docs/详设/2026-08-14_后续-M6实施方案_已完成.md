---
layer: tool
status: 已完成（v6 定稿后全量实施，commit 202a769；Skill S-P1~P8 + MCP M-P1~P9 全交付，636/636 测试 + tsc 零 + 真机 smoke 验证：清单注入→LLM 自主调 Skill→按指令回复）
scope: 后续（MVP 后）—— M6 = Skill 系统 + MCP 客户端（两个独立子系统，实施顺序 Skill→MCP）；Plugin 系统 → M7（独立方案，见 docs/详设/2026-08-14_后续-M7-Plugin实施方案_待启动.md）
depends_on:
  - 详设/2026-08-11_ECode-MVP详设_待审核.md（§2.3 工具 / §2.4 心脏铁律 / §0.4 非目标）
  - 详设/2026-08-13_M3-工具与确认实施方案_已完成.md（Tool 接口 / Registry / ConfirmPrompt）
  - 详设/2026-08-14_后续-M5实施方案_已完成.md（§C 扩展能力后置声明 + §15 遗留债清单——M6 的隐性前置依赖）
  - 规范/2026-08-11_MVP-TUI设计规范_已完成.md（键盘/交互基线——面板键位在其上扩展）
---

# ECode 后续 M6 实施方案：Skill 系统 + MCP 客户端

> 日期：2026-08-14 · 状态：**已完成（实施记录见文末 §16）**
> **范围**：M5 原规划 M6=Skill / M7=MCP 分立；v1 应要求合并 Skill+MCP；v3（角色审阅后用户拍板）Plugin 推迟 M7；**v6（用户拍板）Plugin 设计整体拆出为独立 M7 方案**（`docs/详设/2026-08-14_后续-M7-Plugin实施方案_待启动.md`，为 M6 的后继消费者，非前置依赖）——本篇只含 M6 要实施的 Skill + MCP，全部内容均为交付范围。
> **Skill / MCP 关系**：两个**能力原子**（注入指令 / 接外部工具），共享"注册成 Tool/Command 进既有 Registry"的接入方式；Plugin（分发容器）见 M7 独立方案。
> **参考来源**：正文为 ECode 自主设计定稿；外部实现仅作交叉验证，清单集中附录 B，正文不出现来源措辞。

## ★ v6 修订记录（2026-08-14，用户拍板：Plugin 拆出独立 M7 方案）

Plugin 章节自 v3 起就是"M7 储备"，混在本篇正文制造噪音（决策表/测试/风险/附录背着非交付内容）。v6 将第三部分（P1-P8）+ 决策 D17-D21/D24 + Plugin 测试/风险/附录条目**整体迁出**至 `docs/详设/2026-08-14_后续-M7-Plugin实施方案_待启动.md`（决策重编 M7-D1~D7，映射表见彼处）。本篇修订：总览图与三句话去 Plugin 分支；Skill `source:'plugin'` / MCP 配置 plugin 级的引用改为指向 M7；决策表 D17-D21/D24 移除（编号不复用，历史引用见 v5 及更早修订记录与 ADR-026）；D23 表述收敛为 Skill→MCP。**Skill/MCP/面板设计内容零变更**（v5 冻结）。

## ★ v6 审阅修复记录（2026-08-14，架构师 + 资深开发双角色审阅后）

审阅结论：**无 P0，心脏零改动经源码逐条核实成立，命令→面板的 overlay 模式成立**。修复 12 项 P1（均已并入正文）：

1. **SkillRegistry 预留 M7 注入口**（S5 加 `addSource(dir, 'plugin')`；M7 P4.3 对齐——避免 M7 被迫改 M6 已交付接口）
2. **MCP 死连接降级路径**（M4.4：execute 捕获传输类错误 → 置 failed + 清 client 句柄 → 下次调用自动重连；M3.2 状态机补 connected→failed 转移）
3. **手动 skill 转录显示**（现状无"既有截断"——改为 submit 加 `display?` 参数的最小 display/content 分离，进 S-P5；committed 超长 user 文本截断）
4. **install 契约拆层**（S5/S8.3：services 层 `install(candidate, decisions)` 纯落盘 async；LLM merger + 预览交互 + 冲突裁决留在命令层 S-P7；定义 SkillCandidate 与 decisions 入参）
5. **Command.run(args) 契约扩展**（现状 `run()` 无参丢弃参数文本——`run(args?: string)` 向后兼容，M-P6 写明）
6. **分流位置纠正**（命令分流现状在 `InputStream.submit`（非 TuiApp.submit）且 `commandRegistry.match` 有四处调用点——S4.4/S-P5 改为 InputStream 分流 + `onSkillInvoke` 回调交 TuiApp 注入）
7. **SkillPanel 回填反向通道**（输入框文本是 InputStream 内部 state，TuiApp 无法写入——S-P6 加受控插入 prop `insert?: {text, seq}`）
8. **T4 键位矛盾裁决**（j/k、数字直选与"可打印字符即时搜索"互斥——MVP 砍 j/k 与数字直选，后置）
9. **升级模式 frontmatter 规格**（S8.3：frontmatter 整体采用新值（versions 兜底），body 走 section 合并）
10. **merger 输出协议**（S8.3：JSON schema `{sections:[{title, verdict:'add'|'equal'|'conflict', body?}]}`；冲突默认保留现有 + 预览标记；预览 UI MVP 降级为 可弃/重跑，裁决 Select 单层，"并存改名"后置）
11. **connecting 期间断开 + abort 误记 failed**（M4.2 伪码补两分支：abort → 状态回退不计退避；connecting 期间断开 → pendingDisconnect 落地后 close）
12. **metadata cache 并发回写竞争**（M3.5：McpManager 内部回写串行队列）

重要 P2 一并修：failed 过期降级目标统一（有缓存→cached 无→not-connected，挂 30s interval）+ eager 失效后降级为 lazy 语义；skill 与内置命令撞名 load 时 warn；`/mcp.json` 首用批准交互时机（启动 systemMsgs + overlay 批准后二段 start）；ToolRegistry 加 `unregister`（M-P1）；SkillRegistry 加 `clear()`（测试隔离）；onEvent 订阅接线模型（TuiApp setState → 面板读快照）；buildSystemPrompt 的 ctxWindow 缓存（submit 路径缓存 + 切模型刷新）；estimateContextTokens 第二调用点（checkModelWindow 同步传 tools）；makeOnBeforeRequest 收敛为 options 对象（9→10 参触发 AGENTS 1.3）；错误展开区 3-4 行上限；面板与流式并发风险降级（loop 运行中 InputStream inactive，实际不可达）；时间参数化（now()/定时器注入供测试）；附录 A 补 /skill 命令与 TuiAppDeps；M5 债 #4 落点现行编号对照（v3 记录中 S-P6/S-P7 为当时编号，现行 S-P4/S-P8）；实施粒度注记（超 2h 的步实施时二分，见 S9/M7 注）。

## ★ v5 修订记录（2026-08-14，用户拍板：补全二级菜单面板 TUI 设计）

v4 的 TUI 节只有「StatusBar + systemMsgs + 文本列表命令」，缺少用户可见的面板交互。v5 按用户需求（/skill、/mcp 进二级菜单，可查看/选择/操作；/plugin 可浏览市场/搜索/下载/看详情）补全，经三家终端面板源码调研交叉验证（见附录 B 面板交互条目）：

1. **面板基础设施（新增 T1 节）**：`PanelShell` 组件（标题/副标题/窗口化滚动/底部键位提示行）+ viewState 状态机多级导航 + Esc 逐级回退；**挂载复用现有 overlay 机制**（ModelPicker/HistoryPicker 同款：overlay 期间 InputStream inactive、面板独占输入）——ECode 已有基础设施，零新层（M6-D31）。
2. **SkillPanel（新增 T2 节）**：`/skill` 进面板——按来源分组的列表 + 即时搜索；**Enter = 回填 `/skillname ` 到主输入框**（不直接执行，保留 `$ARGUMENTS` 传参机会；回车即无参执行）（M6-D32）。
3. **McpPanel（重写 T3 节，替代 v4 文本列表）**：`/mcp` 进面板——一级 server 列表（状态着色 + failed 光标行自动展开错误）→ Enter 二级 server 详情（信息区 + 操作菜单：查看工具/重连/断开）→ 三级工具列表；重连乐观更新（先置「连接中」立即重绘）；`ctrl+r` 一级列表快捷重连；`/mcp reconnect [name]` 子命令保留（非面板直达）。
4. **PluginPanel（第三部分新增 P6 节，M7 储备；v6 该部分已整体迁出至 M7 方案）**：`/plugin` 进面板——三页签（浏览市场/已安装/添加市场）+ 搜索 + 详情（版本/作者/描述/安装）+ 安装行内进度 + 添加市场输入框。
5. **统一键位规范（T4 节）**：↑↓/j/k 导航（wrap）、Enter 确认、Esc 逐级回退（面板内先清搜索词再退级）、数字 1-9 直选、PageUp/PageDown 翻页、可打印字符即时搜索。
6. 实施拆解重排：Skill S-P6（面板基础+SkillPanel）、S-P7（distill）、S-P8（集成）；MCP M-P6 改为 McpPanel；Plugin P-P5 改为面板。原 S-P6/P7 顺延。

## ★ v4 修订记录（2026-08-14，用户拍板五点）

1. **Skill 双触发面**（修订 M6-D1）：Skill 既可被 LLM 自主调用（SkillTool，原设计），也可被用户当斜杠命令手动调用（`/skill-name 参数文本`）。新增 S4.4 手动触发节——`/` 补全合并列出内置命令与 skill；`$ARGUMENTS` 占位符替换 + 无占位符兜底追加（M6-D25）；两个 frontmatter 布尔（`user-invocable` / `disable-model-invocation`）独立控制两个触发面；手动注入形态为 user 消息 + 立即触发一轮（M6-D26），自动注入形态维持 tool_result（M6-D2）。实施拆解插入 S-P5（原 S-P5/P6 顺延为 S-P6/P7）。
2. **MCP 按需加载**（修订 M6-D15，废弃急连接）：每 server 可配 lifecycle 四态，默认 `lazy`——启动零连接，工具清单来自 metadata cache（M6-D27，`~/.ecode/mcp-cache.json` 单文件 + configHash 失效）；首次调用才连接（并发去重）；空闲默认 10 分钟卸载；keep-alive 类 30s 健康检查自动重连；连接状态机六态（MVP 无 OAuth，needs-auth 后置），failed 60s 自动过期 + 60s 退避。**`required` 字段随急连接模型一并移除**（启动不连则无"启动失败是否 fatal"语义；首次调用失败走既有 is_error recoverable 路径）。
3. **MCP TUI 三层**（新增 M6 节，M6-D30）：StatusBar 常驻一段 `MCP 2/3`（有配置才显示）；systemMsgs 一次性反馈（连接结果）；`/mcp` 命令做管理（状态列表 / reconnect / tools）。全屏 overlay 面板后置（TUI 尚无 overlay 层）。工具暴露 MVP 全量直连注册 + token 计入压缩估算 + 超阈值 warn（M6-D28），proxy 元工具模式后置。
4. **蒸馏升级合并协议**（增强 S8，M6-D29）：同名 skill 再 install 走升级模式——versions 备份（上限 10 版）→ LLM merger 三态判断（补充/等价忽略/冲突上报）→ 确定性 section 级合并（`##` 同名替换、新增追加）→ 预览确认落盘。人审卡点收敛两处：写盘前预览 + 冲突裁决。
5. **全文第一人称重写**：正文只写 ECode 自己的问题→解法→怎么做；所有外部实现出处从正文清除，集中收敛到附录 B（含本轮新增的 colleague-skill / pi / pi-mcp-adapter / claude-code skill-斜杠交互调研）。

## ★ v3 修订记录（2026-08-14，角色审阅 + 用户拍板）

**范围变更：Plugin → M7**（用户拍板）。审阅结论：v2 纳入 Plugin 属范围蠕变——M5 方案 §C 当初承诺的 M6 就是 Skill+MCP；marketplace 四件套估 800-1200 行而 ECode 无插件作者生态；"蹭 Claude 生态"实际只成立于 skills+mcpServers 两类资源（plugin commands 撑不住现有同步 Command 契约）。**ADR-026（AppContext 装配骨架）维持暂缓**：M6 的 PluginLoader 形态（显式拿 Registry 引用 ~20 行分发）正是 ADR-026 复评预言的轻量形态，无需统一骨架（见 M6-D24；v6 已迁 M7-D6）。

**审阅 P1 修订（实施时必须落，已并入相关章节注记）**：
1. **MCP 工具 token 预算（P1-1，最重要）**：M5 压缩估算只算 system+messages，不含 `tools.specs()`——MCP 20+ 工具可达 15K+ token（buffer 才 20000），多 server 时压到 summary 仍可能 400。修法：`estimateContextTokens` 加 tools 参数（specs JSON/4），hook 工厂传入；方案新增「每 server 工具数/schema 预算上限 + 超限 warn」（见 M3.4）。
2. `register()` 对 `skipLocalValidate` 工具跳过 ajv 预编译（外部 schema 含 `$ref`/非法结构会 throw 炸掉整个注册循环）+ adaptTool 做 schema 归一化（剥 `$defs`/外部 `$ref`，oneOf 降级 description，限 properties 数）——MCP 原始 draft-2020-12 schema 直发违反本项目扁平化约束（interface.ts 头注）。
3. 盲确认修复：preview.ts default 分支 pretty-print input JSON（截 40 行）；ConfirmPrompt 加"本会话记住（server 级）"选项（重 MCP 会话逐次确认不可用）。
4. 项目级 `.mcp.json` **首用批准**（hash 持久化 config）——克隆恶意仓库静默 spawn 是最实际攻击面。
5. Claude 生态兼容声明收窄为「skills + mcpServers 两类资源可直接消费」；loader 双目录探测（`.ecode-plugin/` 优先 `.claude-plugin/` 回退）。
6. 项目级 skills 与 `.ecode/` gitignore 冲突：负向豁免 `!.ecode/skills/` 或改独立目录。
7. S8-a 蒸馏降级为「读会话 → LLM 一次性起草 → ConfirmPrompt 预览 → install」最小闭环；DistillSource/Trigger/TurnContext 等启动 L3 再定（YAGNI）。v4 在此基础上补升级合并协议（见 v4 修订点 4）。

**M5 遗留债处置**（详见 M5 方案 §15，债跟子系统走）：#3 chars/4 DRY → **并入 S-P2**（S7.1 代码块自写 `CHARS_PER_TOKEN=4` 会造第三份分叉，S-P2 时收敛进 tokenizer.ts 统一导出）；#4 models.dev 预热 → M5 已修（6d12036），S-P7 集成时依赖已就绪；#1/#5 已修；#2/#6 正交延后。

---

## 0. 总览：两个能力原子，同一接入方式

两者都通过现有 `ToolRegistry` / `CommandRegistry` 接入心脏——**心脏（AgentLoop）完全不感知 skill / mcp 概念**，契合详设 §2.4 铁律。

```
                 ┌──────────────────────────────────────────────────────┐
                 │            AgentLoop（心脏·零改动）                    │
                 │   反复调 LLM → 执行工具 → 回喂（只认 Tool 接口）        │
                 └────────────────────────────┬─────────────────────────┘
                                              │ ToolRegistry / CommandRegistry
            ┌─────────────────────────────────┴─────────────────────────┐
            │                                 │                         │
   ┌────────▼─────────┐            ┌──────────▼─────────┐              │
   │  内置工具（M3）   │            │   SkillTool        │              │
   │  ls/glob/grep/   │            │   (单入口 Tool)    │              │
   │  read/write/     │            │                    │              │
   │  edit/bash       │            │                    │              │
   └──────────────────┘            └─────────┬──────────┘              │
                                             │                         │
                                  ┌──────────▼──────────┐   ┌──────────▼──────────┐
                                  │  SkillRegistry      │   │  McpManager         │
                                  │  发现/解析 SKILL.md  │   │  按需连接/工具缓存   │
                                  │  蒸馏接入点（S8）    │   │  状态机/官方 SDK     │
                                  └──────────▲──────────┘   └──────────▲──────────┘
                                             │                         │
                                             └──────────┬──────────────┘
                                                        │（M7：Plugin 分发容器，独立方案）
```

**两句话**：
1. **Skill** = 把"工作流指令"打包成可复用单元，**双触发面**：LLM 按清单 description 自主调 SkillTool 换取 body；用户在输入框 `/skill-name 参数` 手动触发展开，或 `/skill` 面板浏览选用。
2. **MCP** = 把"外部工具/服务"通过标准协议接入（官方 SDK 按 lazy 连 server → 工具清单入缓存 → 注册成 Tool → 首次调用才真正连接），`/mcp` 面板查看与操作。

**关键共识**：两者都是**注册成 Tool / Command 进既有 Registry**，loop 的 executeTools / confirm / AJV 路径全部复用，心脏零改动。（Plugin 是上层分发容器，见 M7 独立方案。）

---

# 第一部分：Skill 系统

## S1. 核心架构：三层渐进式加载（progressive disclosure）

Skill 区别于"直接写一大段 system prompt"的关键是**省 token**：metadata 常驻 context（几十字），body 仅触发时加载（可能几 KB），附属文件按需 `read_file`。

```
┌─ Level 0：清单（始终在 system prompt，受 token 预算约束，每条 ≤250 字符）─┐
│  <available_skills>                                                      │
│    <skill><name>commit</name><description>按约定提交…</description></skill>│
│  </available_skills>                                                     │
└──────────────────────────────────────────────────────────────────────────┘
                          │ LLM 匹配 description 自主决定
                          ▼
┌─ Level 1：body（SkillTool 调用后，作为 tool_result 注入一次）──────────────┐
│  <skill_content name="commit">                                            │
│    # commit  生成提交的完整步骤…（markdown，建议 <500 行）                 │
│    附属文件目录：<baseDir>（相对路径基于它，需要时 read_file）             │
│  </skill_content>                                                         │
└──────────────────────────────────────────────────────────────────────────┘
                          │ LLM 按 body 指令用已有工具干活
                          ▼
┌─ Level 2：附属文件（LLM 按需 read_file skill 目录里的脚本/模板）───────────┐
│  ~/.ecode/skills/commit/templates/commit-msg.txt                          │
└──────────────────────────────────────────────────────────────────────────┘
```

用户手动触发（S4.4）跳过 Level 0 匹配直接进 Level 1：body 展开为 user 消息。

---

## S2. 目标 & 验收

**目标**：用户/团队能把可复用工作流写成 markdown（SKILL.md），ECode 自动发现；LLM 按需加载执行，用户可斜杠手动触发并传参；并为"从对话蒸馏 skill"提供创建/升级的单一入口。

**验收**：
- **LLM 面**：放 `~/.ecode/skills/commit/SKILL.md`，启动后 system prompt 含其清单项；问"帮我提交"，LLM 自主调 `Skill` 工具加载 commit 指令并执行
- **手动面**（v4）：敲 `/commit 修复登录 bug` → body 展开（`$ARGUMENTS` 替换；无占位符则末尾追加 `ARGUMENTS: …`）作为 user 消息注入并立即触发一轮；`/` 补全里 skill 与内置命令合并列出
- **SkillPanel**（v5）：`/skill` 面板列出全部手动可用 skill（分组+搜索），Enter 回填 `/skillname ` 到输入框
- **触发面开关**（v4）：`user-invocable: false` → 不进 `/` 补全、直接敲拦截提示；`disable-model-invocation: true` → 不进 LLM 清单，SkillTool 调用返回 is_error
- 清单受 token 预算约束（默认 1% 上下文窗），超预算按权重截断（详见 S7）
- 项目级同名覆盖用户级（首个胜出 + LogStore 记 shadowed）
- Skill 不存在 / frontmatter 非法 → SkillTool 返回 `is_error`（recoverable），不崩主流程
- **升级合并**（v4）：对已有 skill 再 install 走升级模式——versions 备份 + section 级合并 + 冲突预览（S8.3）
- **心脏零改动**：`loop.ts` 不出现 `skill` 字样

**不做**（后置，见附录 C）：`fork` 子 agent、`paths` 条件激活、body 内联 shell、url source、热重载、BM25 检索选 skill、`$1`/命名参数占位符。

---

## S3. 数据结构：SKILL.md 格式

### S3.1 文件布局（只支持目录格式）

```
~/.ecode/skills/commit/
├── SKILL.md          ← 必需：YAML frontmatter + markdown body
├── templates/        ← 可选：附属文件（LLM 按需 read_file）
├── scripts/          ← 可选
└── versions/         ← 可选：升级备份（install 升级模式自动生成，上限 10 版）
```
扫描 glob：`*/SKILL.md`。frontmatter 无 name 时回退目录名。

### S3.2 frontmatter（★ AJV 校验）

```yaml
---
name: commit                       # 必填。正则 ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$，1-64 字符
description: 按约定生成提交信息     # 必填。≤1024 字符。写清"何时用+做什么"，触发命脉
when_to_use: 用户要求 git 提交时   # 可选。触发线索，清单里拼到 description 后
user-invocable: true               # 可选，默认 true。false → 不进 / 补全（仅 LLM 可调）
disable-model-invocation: false    # 可选，默认 false。true → 不进 LLM 清单（仅手动）
---
# commit
## 步骤
1. 跑测试（bash: npm test）
2. git diff 看改动
3. 按约定格式写 commit message，主题可带 $ARGUMENTS
```

**校验规则**（`SkillFrontmatterSchema`，解析失败 → 跳过 + warn）：
- `name`：正则校验，缺失回退目录名
- `description`：必填 ≤1024，缺失回退 body 第一段
- `when_to_use`：可选 string
- `user-invocable` / `disable-model-invocation`：可选 boolean，默认 true / false——两个布尔独立控制两个触发面，四个组合都合法（如"仅 LLM"「仅手动"「两面都开"「两面都关=休眠"）
- 未知字段忽略（前向兼容）

**description 写作规范**：触发可靠性 100% 取决于 description。模型倾向 under-trigger，要写得**稍微"激进"**——"用户提到提交/git 时即使没明说 commit 也调用"。触发词前置（清单单条截断 250）。**ECode 无关键词匹配器**——description 是 LLM 面唯一可发现性来源（手动面靠 `/` 补全，不依赖 description 质量）。

### S3.3 规范模型

```ts
// src/services/skill.ts
export interface SkillInfo {
  name: string
  description: string             // 清单展示用（截断 250）
  whenToUse?: string
  body: string                    // markdown 正文（去 frontmatter）
  baseDir: string                 // skill 目录绝对路径（附属文件相对它）
  source: 'user' | 'project' | 'plugin'   // 发现层级
  userInvocable: boolean          // 默认 true；false 不进 / 补全
  disableModelInvocation: boolean // 默认 false；true 不进 LLM 清单
}
```

---

## S4. 流程

### S4.1 发现（多目录分层 + 首个同名胜出）

| 优先级 | 目录 | 用途 |
|---|---|---|
| 1 | 项目级 `<cwd>/.ecode/skills/`（向上遍历到 home；gitignore 负向豁免，见 v3 P1-6） | 团队共享（入库） |
| 2 | 用户级 `~/.ecode/skills/` | 个人习惯 |
| 3 | plugin 级（M7 独立方案，skill 目录由 plugin 贡献） | 第三方分发 |

realpath + name 去重，首个胜出。跨平台：`os.homedir()`、正斜杠、不依赖 shell `~`（AGENTS §2.5）。

**撞名检测**（v6 审阅补）：`load()` 后对内置 CommandRegistry 撞名检测（skill name 允许 `mcp`/`skill` 等正则合法名）→ LogStore warn + `/` 补全里该条加 `(被命令遮蔽)` 标记（内置命令优先分流，见 S4.4）。

### S4.2 清单注入 system prompt（LLM 面入口）

用 `<available_skills>` XML 块注入清单（结构清晰、LLM 解析稳），**受 token 预算约束**（S7）：

```ts
// core/system.ts
export function buildSystemPrompt(skills: SkillInfo[], ctxWindow: number): string {
  const budget = Math.max(2000, Math.floor(ctxWindow * 0.01))   // 1% 窗口，下限 2000 字符
  const listing = renderSkillListing(skills, budget)            // 超预算按权重截断
  return BASE_SYSTEM_PROMPT + (listing ? '\n' + listing : '')
}
```
清单只含 `disableModelInvocation === false` 的 skill。

**ctxWindow 接线**（v6 审阅补）：`resolveContextWindow` 是异步（M5 预热已修但返回 Promise），而 TuiApp 的 submit 路径同步调 `buildSystemPrompt()`——集成时 TuiApp 缓存 ctxWindow（启动解析一次 + `/model` 切换时刷新），submit 传缓存值，不在热路径 await。

### S4.3 触发面一：LLM 自主调（SkillTool 工具调用，无关键词匹配）

```ts
// src/tools/builtin/skill.ts
export const skillTool: Tool = {
  name: 'Skill',
  description: '加载某 Skill 的工作流指令。任务匹配 available_skills 某项描述时调用，传其 name。',
  readonly: true,
  input_schema: { type:'object', properties:{ skill:{ type:'string' } }, required:['skill'] },
  async execute(args, ctx) {
    const info = skillRegistry.get((args as {skill:string}).skill)
    if (!info) return { content:`Skill 不存在。可用：${skillRegistry.list().map(s=>s.name).join(', ')}`, is_error:true }
    if (info.disableModelInvocation)
      return { content:`Skill ${info.name} 设置了 disable-model-invocation，仅限用户手动 /${info.name} 调用`, is_error:true }
    return { content: [                                  // <skill_content> XML 包裹
      `<skill_content name="${info.name}">`, info.body.trim(), '',
      `该 Skill 附属文件目录：${info.baseDir}（相对路径基于此目录，需要时用 read_file 读取）。`,
      '</skill_content>',
    ].join('\n') }
  },
}
```

**body 走 tool_result（非注入新 user message）**：复用 loop 现有 `invokeTool` 回喂路径，零侵入。手动面（S4.4）的 user 消息由 TuiApp 提交层构造，loop 只看到一条普通 user message——两条路径都不需要 loop 特判。

### S4.4 触发面二：用户斜杠手动调（★ v4 新增）

**问题**：LLM 面靠 description 命中，存在 under-trigger；且"我现在就要用这个流程"是确定性意图，不该赌模型判断。**解法**：skill 同时是一条斜杠命令，用户 `/skill-name 参数文本` 直接触发。

**补全合并**：`/` 补全列表 = 内置命令 + skill（过滤 `userInvocable === false`）。skill 条目描述后缀标来源（如 `(skill)`），与内置命令区分；选中回车补全为 `/name `（带尾随空格，提示可接参数）。输入第一个空格后停止命令名匹配，后续文本都是参数。

**分流顺序**（v6 审阅纠正：命令分流现状在 **InputStream.submit** 组件内，且 slash 匹配有四处调用点（补全 effect/handleTextSubmit/useInput 等），合并 skill 时一并改；非 TuiApp.submit）：`/` 开头时内置命令优先 → 未命中查 skill（须 userInvocable）→ 都未命中才按未知命令处理。skill 命中后走新增回调 `onSkillInvoke(name, args)` 交 TuiApp 做展开注入（skillRegistry 经模块级单例 import，与 commandRegistry 先例一致）。

**注入契约**（v6 审阅补，零改动的关键细节）：TuiApp 把**展开全文**作为 `runLoop` 的 `userInput` 传入（loop 的 alreadyUser 去重按 `text === userInput` 命中，传原文会导致 loop 再 push 一条原始输入 → 双 user 消息）；同时 `submit` 加 `display?: string` 参数（最小 display/content 分离）：`active.userInput` 与 committed 渲染显示 display（原始输入 `/commit 修复登录 bug` 一行），消息本体存展开全文供模型消费。committed 层对超长 user 文本截断（~10 行 + `…`）。

**传参展开**（`services/skill.ts` 纯函数）：

```ts
/** 手动触发：body + 参数 → 注入用全文。两条规则保证任何 skill 都不丢参 */
export function expandSkill(info: SkillInfo, args?: string): string {
  let body = info.body
  if (args !== undefined && args !== '') {
    body = body.includes('$ARGUMENTS')
      ? body.replaceAll('$ARGUMENTS', args)                    // ① 有占位符 → 替换
      : `${body}\n\nARGUMENTS: ${args}`                        // ② 无占位符 → 兜底追加
  } else {
    body = body.replaceAll('$ARGUMENTS', '')                   // 未传参剥掉残留占位符
  }
  return [
    `<skill_content name="${info.name}">`, body.trim(), '',
    `该 Skill 附属文件目录：${info.baseDir}（相对路径基于此目录，需要时用 read_file 读取）。`,
    '</skill_content>',
  ].join('\n')
}
```

`$1`/命名参数占位符后置（MVP 只做 `$ARGUMENTS` 一个占位符，够覆盖"给流程一个主题/对象"的绝大多数场景）。

**注入形态**：展开结果作为**一条 user 消息**追加进 messagesRef → 立即触发一轮 LLM 请求。与 LLM 面的对照：

| | LLM 自动调（S4.3） | 手动调（本节） |
|---|---|---|
| 入口 | LLM 发 tool_use('Skill') | 用户敲 `/name args` |
| body 形态 | tool_result（`<skill_content>`） | user 消息（`<skill_content>` 包裹的展开全文） |
| 后续 | LLM 决策，可能在同轮继续调工具 | 立即发起一轮请求 |
| `disable-model-invocation: true` | SkillTool 拒绝（is_error） | 不影响（正是为仅手动设计的） |
| `user-invocable: false` | 不影响 | 补全不显示；直接敲 `/name` 拦截并提示"请让 ECode 使用 ${name} 技能" |

**转录显示**（v6 审阅修正）：显示用户原始输入 `/commit 修复登录 bug` 一行，消息本体存展开全文——经 `submit(display?)` 参数实现（最小 display/content 分离，现状消息渲染无截断、全文直出，必须显式做，见上方注入契约）；committed 层超长 user 文本截断。

### S4.5 执行（inline）

LLM 拿 body 后按步骤用已有工具干活。MVP 只有 inline——skill body 是给当前 loop 的指令，不起新 agent。

---

## S5. 接口

```ts
// src/services/skill.ts
export interface SkillCandidate {          // v6 审阅补：起草/升级的产物契约
  name: string
  description: string
  whenToUse?: string
  userInvocable?: boolean                  // 缺省 true
  disableModelInvocation?: boolean         // 缺省 false
  body: string
}

/** 升级模式的用户裁决：conflict section 的取舍（缺省=保留现有） */
export type SectionDecision = { title: string; verdict: 'keep' | 'adopt' }

export interface SkillRegistry {
  load(): Promise<void>                       // 启动扫描（项目级+用户级）+ 解析 + 去重 + 撞名 warn
  addSource(dir: string): void                // ★ M7 plugin 目录注入口（load 后追加源再扫，M6 不调用但接口冻结）
  get(name: string): SkillInfo | undefined
  list(): SkillInfo[]                         // 全量（含 disableModelInvocation 的——手动面要用）
  listForPrompt(): SkillInfo[]                // LLM 清单（过滤 disableModelInvocation）
  listForCompletion(): SkillInfo[]            // / 补全（过滤 !userInvocable）
  /** 创建/升级唯一落盘入口（v6 审阅拆层：纯落盘 async；LLM merger 与预览/裁决交互在命令层 S-P7） */
  install(c: SkillCandidate, decisions?: SectionDecision[]): Promise<InstallResult>
  clear(): void                               // 测试隔离（对齐 CommandRegistry.clear 先例）
}

export type InstallResult =
  | { mode: 'created'; path: string }
  | { mode: 'upgraded'; path: string; backedUpTo: string; conflicts: string[] }  // conflicts 非空 → 命令层据此弹裁决
```

**install 职责边界**（v6 审阅定）：`install` 只做——同名检测（创建 or 升级）→ versions 备份 → frontmatter 整体采用新值 → body 按 merger 判定结果（`decisions` 覆盖 conflict 项）做 section 级确定性合并 → 落盘。不发起 LLM 请求、不弹 UI。LLM merger 判断与预览/裁决交互属于 `/skill-create` 命令层（S-P7）。

集成（`cli/index.ts` makeDeps）：`await skillReg.load()` → `buildSystemPrompt(skillReg.listForPrompt(), ctxWindow)` → `toolReg.register(skillTool)`；TuiApp 拿 `listForCompletion()` 进 `/` 补全。skillTool 闭包引用模块级 `skillRegistry` 单例（与 commandRegistry 一致）。

---

## S6. 错误处理

| 场景 | 处理 |
|---|---|
| 目录不存在/空 | 静默，清单空 |
| SKILL.md 读失败 / frontmatter 非法 | 跳过 + LogStore warn（不影响其他 skill） |
| SkillTool 调不存在的 name | is_error + 可用列表（recoverable） |
| SkillTool 调 disableModelInvocation 的 | is_error + 提示手动路径（recoverable） |
| 手动敲 `user-invocable:false` 的 | 拦截提示（不发请求），提示改用自然语言让 ECode 调用 |
| 手动敲不存在的 skill 名 | 按未知命令处理（现有行为），错误信息里若近似 skill 名则提示 |

**铁律**：Skill 任何失败都不崩主流程。

---

## S7. Skill 优化

### S7.1 token 预算（MVP 必做）

skill 多了清单会吃 system prompt。MVP 上预算机制（防爆炸）：

```ts
// src/services/skill/listing.ts
const BUDGET_CONTEXT_PERCENT = 0.01          // 1% 上下文窗
const MAX_LISTING_DESC_CHARS = 250           // 单条 description 硬截断
// ★ v3 债处置：不新写 CHARS_PER_TOKEN=4（第三份分叉）——S-P2 时把
//   estimateMessageTokens 收敛进 tokenizer.ts 统一导出，listing/summarize 共用（M5 §15 #3）

function renderSkillListing(skills: SkillInfo[], charBudget: number): string {
  // 1. 每条截断到 250 字符（desc + whenToUse 拼接后）
  // 2. 总长超预算 → 按权重截断（MVP 简化：均匀砍每条到剩余预算均分；极端时降级为 names-only）
  // 3. 无 skill → 返回空串（零开销）
}
```
GLM-5（200K 窗）：1% ≈ 500 tokens ≈ 2000 字符预算，够列 ~20 个 skill。预算由 M5 的 `resolveContextWindow` 提供。

### S7.2 增量推送（MVP 不做，留接口）

system prompt 每轮重算重发。MVP 先"每轮发全量但受预算约束"（实现最简）；清单真成为负担时（skill 数大或窗口小）再上 baseline/update/removed 三态增量——`SkillRegistry` 设计成"可计算变化签名"（name+description 的 hash 序列），为增量预留。

### S7.3 base dir 注入（MVP 必做）

SkillTool 返回与 expandSkill 展开时都注入 baseDir（S4.3/S4.4 已含）。skill 携带附属文件（templates/scripts）时，LLM 知道相对路径基于哪。

### S7.4 优化全景（MVP 做 / 后置）

| 优化点 | MVP | 理由 |
|---|---|---|
| 三层懒加载 | ✅ | skill 灵魂 |
| 双触发面（LLM + 斜杠手动 + 传参） | ✅ | v4 新增，S4.4 |
| submit display 参数（最小 display/content 分离） | ✅ | v6 审阅提为 MVP（现状无截断，手动 skill 必需），S4.4 |
| token 预算 + 单条截断 | ✅ | 防爆炸，S7.1 |
| `when_to_use` + pushy description 规范 | ✅ | LLM 面命中率，S3.2 |
| base dir 注入 | ✅ | 附属文件，S7.3 |
| frontmatter AJV 校验 | ✅ | 天然契合 |
| `$1`/命名参数占位符 | ⏳后置 | `$ARGUMENTS` 已覆盖主场景 |
| 增量推送 | ⏳后置 | 预算约束已够；留签名接口 |
| `paths` 条件激活（gitignore glob） | ⏳后置 | skill 数多才有价值 |
| `fork` 子 agent 执行 | ⏳后置 | 需 sub-agent 能力 |
| body 内联 shell + `${SKILL_DIR}` 变量 | ⏳后置 | 安全风险高 |
| 文件监听热重载 | ⏳后置 | dev 体验，非核心 |
| url source（远端拉） | ⏳后置 | plugin 已覆盖分发 |
| BM25/词法检索选 skill | ⏳远后置 | skill 数 >50 才值得 |
| 转录 display/content 完整分离（isMeta 消息级） | ⏳后置 | MVP 的 display 参数已覆盖手动 skill 场景 |
| 面板 j/k 导航 / 数字 1-9 直选 | ⏳后置 | 与即时搜索键位互斥（T4），MVP 砍掉 |

---

## S8. 扩展性：从对话蒸馏 Skill

### S8.1 定位：L2 半自动是当前可行档位

"会话→skill"的自动化谱系：L1 全手写 → L2 人触发+agent 起草+人确认 → L3 自动上下文蒸馏（产易失笔记，非 skill）→ L4 自动记忆蒸馏（产 AGENTS.md 类 memory，非 skill）→ L5 全自动 skill 生成（研究阶段，尚无跑赢简单基线的实现）。业界最激进的内部实现也停在"人触发 + 访谈起草 + 人确认"——中间隔着"判断什么值得存"这道无人解过的坎。

**ECode 取 L2**：人触发、agent 起草、人确认；全自动判断+固化远期再议（S8.4）。

### S8.2 memory vs skill：分两层，不合并

| 维度 | memory（MEMORY.md / AGENTS.md） | skill（SKILL.md） |
|---|---|---|
| 载入 | 被动（常驻/检索） | 主动（description 命中才触发） |
| 形态 | 自由随笔、索引+topic | 结构化指令（frontmatter+流程+成功判据） |
| 粒度 | 一条洞察/偏好 | 一个完整可复用工作流 |
| token | 持续占用（故限 200 行/分层） | 元数据常驻、body 懒载 |

**两者配合**：memory 是 skill 起草的**输入原料**，但**绝不自动升级**（自动升级正确率无保证，会污染 skill 库）。

### S8.3 设计：L2 最小闭环 + 升级合并协议（★ v4 补全）

**创建**（v3 已降级，维持）：`/skill-create` 命令 → 读当前会话（messagesRef 全量）→ LLM 一次性起草（SkillCandidate）→ 预览 → `install` 落盘。

**升级已有 skill**（v4 新增；对已有同名 skill 再 install 自动走升级模式）：

```
/skill-create 命令层（S-P7）：
  1. LLM merger 判断（新内容 vs 现有内容），输出 JSON 协议（v6 审阅补）：
       { sections: [ { title: '## 步骤', verdict: 'add' | 'equal' | 'conflict', body?: string } ] }
     —— add=新 section（附 body）；equal=等价已有（忽略）；conflict=矛盾（附 body，默认保留现有）
  2. 预览 diff（+ 新增 section / ~ 替换 section / ! 冲突项）：
       预览 UI MVP = ConfirmPrompt 二元（采用 / 放弃重跑）；冲突裁决 = Select 单层（保留现有 / 采用新）
       ——"并存改名"后置；多行编辑后置（现有组件无承载）
  3. 裁决结果以 SectionDecision[] 传给 install
install 落盘层（S5，确定性代码）：
  4. versions 备份：当前 SKILL.md 存档到 <skill>/versions/vN/（上限 10 版，超出挤掉最旧）
  5. frontmatter 整体采用新值（v6 审阅定；versions 有兜底），body 按 merger 判定 + decisions
     覆盖 conflict 项做 section 级合并：## 同名 section 整体替换、无对应 section 追加到末尾
  6. 落盘；conflicts 非空且未裁决时返回清单供命令层弹裁决
```

**设计要点**：
- **LLM 只判断，不拼盘**：merger 的产出是逐 section 三态判定，最终文件由确定性代码按 section 边界组装——LLM 拼全文会引入丢内容/重复段落风险；组装是纯函数，可完整单测（LLM 部分 mock）。
- **人审卡点收敛两处**：写盘前预览 + 冲突裁决。升级永不静默覆盖（有 versions 兜底可回滚，但回滚是兜底不是流程）。
- **版本上限 10**：versions 是安全网不是版本管理系统，超限淘汰最旧。

三抽象（DistillSource/Trigger/Drafter 接口）等 L3 再定（YAGNI）；MVP 的创建+升级闭环不依赖它们。

### S8.4 演进路线（每步可独立交付）

| 步 | 做什么 | 自动度 | 依赖 |
|---|---|---|---|
| **S8-a MVP** | `/skill-create` 创建 + **升级合并协议**（S8.3） | 人触发+确认 | 仅 Skill 基础设施 |
| **S8-b** | SessionMemory（post-turn 抽笔记，服务压缩）—— context distillation，**非 skill** | 自动触发 | sub-agent（后置） |
| **S8-c** | `/learn`：蒸馏"非显然发现"到分层 AGENTS.md —— 持久 memory 层 | 人触发 | S8-b |
| **S8-d** | `ThresholdTrigger`：检测"同一工具序列/纠正模式重复 ≥N 次"→ 主动提议"固化成 skill？"（仍人确认） | 半自动提议 | S8-b/c 做信号源 |
| **S8-e 远期** | 全自动判断+固化 —— 等 LLM 判断力 + 评估闭环成熟 | 全自动 | 研究突破 |

**S8-a 即 M6 的 Skill 子系统交付物之一**（一个 `/skill-create` 命令 + install 双模式）。S8-b 起依赖 sub-agent 能力，后置到独立里程碑。

---

## S9. Skill 实施拆解

| 步 | 内容 | 依赖 | 心脏 |
|---|---|---|---|
| **S-P1** | `services/skill.ts`：SkillInfo + SkillRegistry（扫描/解析/去重/listForPrompt/listForCompletion/addSource/clear/install 双模式 + versions 备份 + section 合并） | 无 | 零 |
| **S-P2** | `services/skill/listing.ts`：token 预算 + 250 截断 + 变化签名（为增量预留）+ **M5 债 #3：estimateMessageTokens 收敛进 tokenizer.ts** | S-P1 | 零 |
| **S-P3** | `tools/builtin/skill.ts`：SkillTool（body→`<skill_content>` + disableModelInvocation 拒绝） | S-P1 | 零 |
| **S-P4** | `core/system.ts`：buildSystemPrompt 加 skills（listForPrompt）+ 预算渲染；TuiApp 缓存 ctxWindow（启动 + 切模型刷新） | S-P2 | 零 |
| **S-P5** | **手动触发面**（v4）：expandSkill 纯函数 + **InputStream.submit 分流**（命令→skill→未知；slash 匹配四处调用点合并 skill；`onSkillInvoke(name,args)` 回调）+ `/` 补全合并（listForCompletion + 来源后缀 + 尾随空格 + 撞名标记）+ **submit `display?` 参数与 committed 超长截断** + userInvocable 拦截 | S-P1 | 零 |
| **S-P6** | **面板**（v5）：`tui/PanelShell.tsx`（标题/副标题/窗口滚动/键位提示行/分组导航/即时搜索——新建组件，复用 Select 的交互语义与 theme）+ `tui/SkillPanel.tsx`（分组列表 + 即时搜索 + Enter 回填）+ **InputStream 受控插入 prop `insert?:{text,seq}`（回填反向通道）** + `/skill` 命令（action 触发 overlay）+ overlay kind 接入 | S-P1,P5 | 零 |
| **S-P7** | `services/skill/distill.ts`：`/skill-create`（读会话→LLM 起草 SkillCandidate→预览（采用/放弃重跑）→install）+ merger 三态 JSON 协议 + 冲突裁决 Select + 升级 diff 预览。依赖注入：LLM 请求通道（TuiApp 闭包 provider）+ Command 异步 action | S-P1,P5 | 零 |
| **S-P8** | `cli/index.ts` makeDeps 集成（skillRegistry 进 TuiAppDeps） | S-P3,P4,P6,P7 | 零 |

全程不动 loop.ts。PanelShell 在 S-P6 落地后即成为面板共用基础设施（MCP 复用）。

> **粒度注记**（v6 审阅）：S-P1 / S-P6 / S-P7 均超 2h 时间盒，实施时二分——S-P1 → 「发现解析查询」+「install 双模式与合并」；S-P6 → 「PanelShell 基础」+「SkillPanel 与命令接入」；S-P7 → 「创建闭环」+「升级合并」。编号保持不变（避免 M5 §15 等历史引用漂移），二分以 checklist 呈现。

---

# 第二部分：MCP 客户端

## M1. 核心架构：按需连接 + 工具缓存 + 状态机

四件套：官方 SDK Client + Transport、适配器（tools/list → ECode Tool）、生命周期管理器（状态机 + 按需连接 + 空闲卸载）。

```
┌─ McpManager（生命周期编排）────────────────────────────────────────┐
│  servers: Map<name, { client?, status, defs, lastUsedAt, inFlight }>│
│  启动：读 metadata cache → 全部工具注册（零连接）                    │
│  lazyConnect(name)：首次调用才连（并发去重 + 60s 失败退避）           │
│  空闲卸载：30s 定时器（unref），idleTimeout 默认 10min → close      │
│  stop()：退出清理全部连接                                            │
└────────────────────────────┬────────────────────────────────────────┘
                             │ 每 server 一个
              ┌──────────────┴──────────────┐
              ▼                             ▼
   ┌─ stdio Transport ──────┐    ┌─ StreamableHTTP Transport ────┐
   │ spawn 子进程           │    │ POST 到 https://host/mcp       │
   │ stdin/stdout JSON-RPC  │    │ stderr → LogStore              │
   └───────────┬────────────┘    └────────────┬───────────────────┘
               │ Client.connect（initialize 握手，SDK 内置）
               ▼
   ┌─ tools/list → 缓存（~/.ecode/mcp-cache.json）+ 适配器 → Registry ─┐
   │  注册与连接解耦：cache 命中 → 不连接也能注册全部工具               │
   │  execute → 未连接先 lazyConnect → client.callTool                │
   │  isError? recoverable / readonly=false 默认确认 / 透传校验       │
   └───────────────────────────────────────────────────────────────────┘
```

**lifecycle 每 server 可配四态**：

| lifecycle | 启动时 | 首次调用 | 掉线 | 空闲 |
|---|---|---|---|---|
| `lazy`（默认） | 不连 | 连 | 不自动重连（60s 退避后可再试） | 10min 断开 |
| `eager` | 连 | — | 不自动重连 | 默认不断开 |
| `keep-alive` | 连 | — | 30s 健康检查自动重连 | 永不断开 |
| `lazy-keep-alive` | 不连 | 连 | 30s 健康检查自动重连 | 永不断开 |

选型逻辑：本地 stdio server 启动便宜、用完可能不用 → lazy；云端 HTTP 启动贵但常驻服务 → keep-alive 系。

---

## M2. 目标 & 验收

**目标**：配置里声明 MCP server，ECode 按需连接、缓存工具清单、注册成 `mcp__server__tool`；LLM 像调内置工具一样调它们；用户随时可见连接状态并可重连。

**验收**：
- **按需加载**：默认 lazy——启动零连接、零子进程，工具清单来自 cache 可正常列出并供 LLM 调用；首次调用某 server 工具时才连接（连接期间状态可见）
- **缓存**：`~/.ecode/mcp-cache.json` 命中 → 不连接直接注册；server 配置变更（configHash 变）→ 缓存失效，下次连接重拉
- stdio server（`npx -y @modelcontextprotocol/server-filesystem <dir>`）→ `mcp__filesystem__*` 可调；HTTP server（url + headers）同样
- 命名 `mcp__<server>__<tool>`，跨 server 重名不冲突
- **默认每次确认**（ConfirmPrompt 复用 M3），即使 readOnlyHint；重 MCP 会话有"本会话记住（server 级）"选项（v3 P1-3）
- server `isError:true` → tool_result is_error（recoverable）
- **失败不阻塞**：任一 server 连接失败 → 状态 failed + warn；60s 内退避（重复调用直接 is_error 提示"N 秒前失败，可 /mcp reconnect"），60s 后自动降级回 cached
- **空闲卸载**：lazy server 10 分钟无调用自动断开（状态回 cached，工具注册保留）
- **TUI**：StatusBar 显示 `MCP 2/3`；`/mcp` 面板三级可达（列表→详情→工具列表），重连/断开可操作且状态实时刷新；failed 行自动展开错误；`/mcp reconnect <name>` 直达生效
- Ctrl+C → 透传 AbortSignal 中断 callTool
- 退出清理子进程（不留孤儿）
- **心脏零改动**

**不做**（后置）：resources/prompts 原语、OAuth（needs-auth 状态随之）、elicitation/MRTR、per-tool 白黑名单、`tools/list_changed` 增量同步、proxy 元工具暴露模式、面板高级项（直连工具 toggle / token 实时估算 / 60s 自动关 / 键位可配置 / 鼠标支持，见 T4）。

---

## M3. 数据结构

### M3.1 配置 schema（业界 de-facto `mcpServers`）

配置来源两处合并（优先级：用户级 < 项目级，同名项目级覆盖 + warn；plugin 级随 M7）：
- **用户级**：`~/.ecode/config.json` 的 `mcpServers`
- **项目级**：`<cwd>/.mcp.json`（团队共享，secret 用 `${ENV_VAR}` 占位符；**首用批准**——v3 P1-4，hash 持久化 config）

```ts
export interface McpServerConfig {
  type: 'stdio' | 'http'
  command?: string; args?: string[]; env?: Record<string,string>; cwd?: string   // stdio
  url?: string; headers?: Record<string,string>                                    // http
  enabled?: boolean                              // 默认 true；false → disabled 状态，不注册工具
  lifecycle?: 'lazy' | 'eager' | 'keep-alive' | 'lazy-keep-alive'   // 默认 lazy（v4）
  idleTimeout?: number                           // 分钟，默认 10（lazy）；eager/keep-alive 系默认 0=不断开
  timeout?: number                               // 单次调用/连接超时 ms，默认 30000
}
```
（v2 的 `required` 字段随急连接模型移除——见 v4 修订点 2。）

**`${ENV_VAR}` 占位符**：加载时替换成 `process.env.XXX`，缺失 warn + skip（呼应 AGENTS §5.2 密钥从环境读）。

### M3.2 连接状态机（v4 新增）

```ts
export type McpServerStatus =
  | 'not-connected'   // 未连接且无缓存（如配置了但从未成功连过）
  | 'cached'          // 未连接但有工具缓存（工具已注册，首次调用时连接）
  | 'connecting'      // 连接中（首次调用 / 重连）
  | 'connected'       // 已连接
  | 'failed'          // 连接失败（60s 内；过后自动降级回 cached/not-connected）
  | 'disabled'        // enabled:false
```

- **failed 自动过期**：失败记时间戳，60s 后由 30s 定时器清除并降级——**有缓存回 `cached`、无缓存回 `not-connected`**（M2/M3.2 统一口径）；"失败"是瞬时态不是常态，避免失败 server 永远挂红。
- **退避**：failed 后 60s 内 lazyConnect 不再尝试（工具调用直接 is_error 带原因与 `/mcp reconnect` 提示），避免每次调用都撞墙。**abort 例外**：用户 Ctrl+C 中断连接不算失败——状态回退、不记退避。
- **connected → failed 转移**（v6 审阅补，防死连接）：lazy/eager 无健康检查，stdio 子进程退出/HTTP 断开后状态仍挂 connected——execute 捕获**传输类错误**（连接重置/管道断裂/Not connected）时置 failed + 清 client 句柄，下次调用自动 lazyConnect 重连（错误以 is_error 回喂 LLM 自纠，重试即真重连）；keep-alive 系另有 30s 健康检查覆盖。
- **eager 失效后**：启动失败 60s 过期即降级为 lazy 语义（下次调用拉起），不隐含重试。
- **needs-auth**（OAuth）后置，MVP 无此态。
- 状态变更是事件源：StatusBar / `/mcp` 面板 / systemMsgs 都订阅同一份快照。**订阅接线**（v6 审阅补）：TuiApp 把 `onEvent` 挂到 McpManager → setState 触发重渲染 → 面板每次渲染读 `status()` 快照（不自己缓存状态）。

### M3.3 McpTool 适配

```ts
import { Client } from '@modelcontextprotocol/sdk/client/index.js'
import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js'
import { StreamableHTTPClientTransport } from '@modelcontextprotocol/sdk/client/streamableHttp.js'

function adaptTool(serverName: string, def: McpToolDef, getClient: () => Promise<Client>, cfg: McpServerConfig): Tool {
  return {
    name: `mcp__${sanitize(serverName)}__${sanitize(def.name)}`,     // 双下划线（事实标准）
    description: def.description ?? `MCP tool ${def.name} (${serverName})`,
    input_schema: normalizeSchema(def.inputSchema) ?? { type:'object', properties:{} },  // v3 P1-2 归一化
    readonly: false,                  // ★ 默认确认（第三方不可信），即使 annotations.readOnlyHint
    skipLocalValidate: true,          // ★ 透传给 server 校验（M4.4）
    timeout_ms: cfg.timeout ?? 30_000,
    async execute(args, ctx) {
      const client = await getClient()      // ★ 未连接先 lazyConnect（并发去重 + 退避检查）
      const r = await client.callTool({ name:def.name, arguments:args ?? {} }, undefined,
        { signal:ctx.signal, timeout:cfg.timeout ?? 30_000, resetTimeoutOnProgress:true, onprogress:()=>{} })
      return { content: renderContent(r.content), is_error: r.isError === true }
    },
  }
}
```
**`skipLocalValidate`**（Tool 接口新增可选字段）：MCP server 是实现方，最懂参数约束；server 在 callTool 时参数不对返回 isError/JSON-RPC error，走既有 recoverable 路径。且避免 AJV 对 server schema `$ref`/复杂结构的兼容问题。Registry.validate 对 `skipLocalValidate` 工具直接 ok；`register()` 对此类工具**跳过 ajv 预编译**（v3 P1-2，防外部 schema throw 炸注册循环）。`normalizeSchema`：剥 `$defs`/外部 `$ref`、oneOf 降级为 description 文本、限 properties 数。

`getClient` 是惰性句柄：cache 命中注册时 client 可能还不存在，execute 时才连接——这是"注册与连接解耦"的落点。

### M3.4 工具暴露与 token 预算

**MVP 全量直连注册**：每个 MCP 工具注册为独立 Tool（`mcp__server__tool`），与内置工具同列。理由：与 Skill/内置工具心智一致、实现最简、绝大多数场景单 server 工具数 <20。

**token 计入压缩估算**（v3 P1-1，必做）：M5 的 `estimateContextTokens` 加 tools 参数（specs JSON/4），onBeforeRequest hook 工厂传入 `toolReg.specs()`——否则 MCP 工具 schema 挤爆真实上下文而压缩器无感。

**超阈值 warn**：单 server 工具数 >20 或估算 schema 超预算份额 → 启动 warn 提示配置 `excludeTools`（后置）/ 关注 token 占用。

**proxy 元工具模式后置**：工具极多时收敛为单一 `mcp` 元工具（search/describe/call 按需取 schema，token 开销恒定）。MVP 不做（M6-D28）。

### M3.5 metadata cache（v4 新增）

```
~/.ecode/mcp-cache.json
{
  "filesystem": {
    "configHash": "a1b2…",       // server 定义的 hash（command/args/url/headers）
    "tools": [ { name, description, inputSchema } ],   // tools/list 结果
    "cachedAt": 1692…
  }
}
```
- **写入时机**：连接成功拉到 tools/list 后回写；**读取时机**：启动时读，configHash 命中 → cached 状态 + 注册全部工具（不连接）。
- **失效**：configHash 不匹配 → 缓存作废，该 server 标 not-connected，首次调用连接后重拉。
- **回写互斥**（v6 审阅补）：读-改-写单文件在 eager 并行连接 + 多 lazy 并发首调下会互相覆盖丢条目——McpManager 内部回写走串行队列（单进程内即可，无需文件锁）。
- **手动刷新**：`/mcp reconnect <name>` 重建连接并回写缓存（工具增删后的人工刷新通道；`tools/list_changed` 增量同步后置）。refreshMetadata 拉到的新工具只增注册不注销（ToolRegistry MVP 无 unregister 语义参与，工具减项重启生效；`unregister` 本身 M-P1 加，供 M7 disable 卸载用）。

---

## M4. 流程

### M4.1 配置加载合并
用户级 + 项目级合并；同名项目级覆盖 + warn；`${ENV_VAR}` 解析；`enabled===false` → disabled（不注册工具）。

**项目级 `.mcp.json` 首用批准**（v3 P1-4；v6 审阅补交互时机）：加载发生在 makeDeps（TUI 尚未 render，无法弹窗）——流程改为**二段启动**：启动读到未批准的项目级配置 → 先跳过该文件完成启动 → systemMsgs 提示「检测到项目级 .mcp.json，需要批准」+ overlay（Select 是/否）→ 批准后 hash 持久化（`~/.ecode/approved-mcp.json`）并调 `mcp.start` 接入；拒绝则本会话忽略（下次启动再问）。

### M4.2 按需连接（lazyConnect）

```ts
// 伪码：并发去重 + 退避 + 状态机推进
async lazyConnect(name: string, signal?: AbortSignal): Promise<Client> {
  const st = this.servers.get(name)!
  if (st.status === 'connected') return st.client!
  if (st.status === 'connecting') return st.connectPromise!        // ★ 并发去重：同 server 共享一个 Promise
  if (st.failedAt && Date.now() - st.failedAt < 60_000)
    throw toAppError({ code:'MCP_BACKOFF', message:`${name} 连接失败（${secs} 秒前），可 /mcp reconnect ${name}` })
  st.status = 'connecting'; this.emitStatus(name)                   // 状态栏/列表实时反馈
  try {
    const client = await this.doConnect(name, signal)               // stdio/http Transport + 握手
    await this.refreshMetadata(name, client)                        // tools/list → 注册增量 → 回写 cache（串行队列）
    st.status = 'connected'; st.lastUsedAt = Date.now(); this.emitStatus(name)
    if (st.pendingDisconnect) { st.pendingDisconnect = false; await this.close(name) }  // ★ v6：connecting 期间用户断开 → 落地即关
    return client
  } catch (e) {
    if (signal?.aborted) {                                           // ★ v6：用户中断 ≠ 故障
      st.status = st.cachedTools ? 'cached' : 'not-connected'; this.emitStatus(name); throw toAppError(e)
    }
    st.failedAt = Date.now(); st.status = 'failed'; this.emitStatus(name)
    throw toAppError(e)
  }
}
```

```ts
// stdio（env 合并 + stderr pipe）
const transport = new StdioClientTransport({
  command: resolveCommand(cfg.command!),        // win32: npx 可能需 cmd /c（M5-M6）
  args: cfg.args ?? [], cwd: cfg.cwd, env: { ...process.env, ...cfg.env }, stderr: 'pipe',
})
transport.stderr?.on('data', c => logger.debug(`mcp.${name}`, 'stderr', { text: c.toString() }))
// http
const transport = new StreamableHTTPClientTransport(new URL(cfg.url!), { requestInit:{ headers: cfg.headers ?? {} } })
const client = new Client({ name:'ecode', version: PKG_VERSION })
await Promise.race([client.connect(transport), timeout(cfg.timeout ?? 30_000)])  // 外包超时兜底
```

启动时只连 `eager` / `keep-alive` 的 server（并行、容错，失败标 failed 不阻塞）；lazy 的全部走 cache 注册。

### M4.3 工具发现注册
cache 命中 → 直接按缓存注册（零连接）；未命中且 eager/keep-alive → 连接后 `tools/list` 注册 + 回写；lazy 且无缓存 → 启动时连一次 bootstrap（拿清单后即断，回 idle）。

### M4.4 调用（isError → recoverable）
LLM 调 `mcp__filesystem__read_file` → validate（skipLocalValidate ok）→ confirm（readonly=false；"本会话记住（server 级）"可选）→ `getClient()`（未连接则 lazyConnect）→ `client.callTool` → isError 转 is_error / JSON-RPC 错误 catch 转 toAppError。content 渲染：text 原样 / image→占位 / resource_link→路径 / resource→text。每次调用 `touch(name)` 更新 lastUsedAt。

**传输层错误处理**（v6 审阅补，死连接降级）：catch 到传输类错误（连接重置/管道断裂/Not connected）→ 置 failed + **清 client 句柄**（下次调用 lazyConnect 自动重连，不撞死句柄）→ 本次以 recoverable is_error 回喂 LLM 自纠。应用级错误（isError/业务错误）不动连接状态。

### M4.5 生命周期（空闲卸载 + 健康检查 + 退出清理）

```
30s interval（unref，不阻退出）三件事：
  ① keep-alive 系未连接 → 重连（失败记 failed 走退避；退避期内跳过本轮回试）
  ② 非 keep-alive 系：lastUsedAt 距今超 idleTimeout 且 inFlight===0 → client.close()
     → 状态回 cached（工具注册保留，下次调用 lazyConnect 重连）
  ③ failed 过期清理：failedAt 距今超 60s → 降级（有缓存回 cached / 无缓存回 not-connected）
退出（process exit handler）：stop() → 全部 client.close()（SDK 发 SIGTERM）
  Windows 孤儿进程复现时再上 taskkill /T 进程树兜底（MVP 先用 SDK 默认）
```

---

## M5. 接口 / 错误处理 / 跨平台

**接口**：
```ts
export interface McpManager {
  start(config: McpConfig, toolReg: ToolRegistry, logger: Logger, onEvent?: (e: McpStatusEvent) => void): Promise<void>
  lazyConnect(name: string, signal?: AbortSignal): Promise<Client>   // 工具 execute 内部用
  reconnect(name?: string): Promise<{ ok: string[]; failed: string[] }>   // /mcp reconnect；省略=全部
  status(): McpServerStatusSnapshot[]      // /mcp 列表 + StatusBar 用（含工具数/失败原因/失败时间）
  stop(): Promise<void>
}
```

**错误二分**：连接失败→recoverable（is_error 回喂 LLM + 状态 failed）；isError→recoverable is_error；超时→recoverable；退避期内调用→recoverable（提示 reconnect）；Ctrl+C→aborted。**脱敏**：env/headers 走 redact.ts（M-P9）。

**跨平台**（AGENTS §2.5）：`resolveCommand` win32 下 npx/npm 包 `cmd /c`（已知 ENOENT 坑）；路径正斜杠；进程清理见 M4.5。

---

## M6. TUI 呈现（三层 + 二级菜单面板，v5 重写）

**问题**：MCP 是唯一"有外部连接状态"的子系统——用户需要知道配了什么、连没连上、失败为什么、怎么重连；skill 数量多后用户需要浏览、查找、选用；这些交互用纯文本列表命令承载不了（无导航、无操作、信息一次性刷过）。

**答案**：三层感知（常态/反馈）+ 二级菜单面板（浏览/操作）。

| 层 | 位置 | 内容 | 时机 |
|---|---|---|---|
| **常驻** | StatusBar 一段 | `MCP 2/3`（已连/启用数）；连接中瞬时态 `MCP 连接中…` | 有启用的 server 才显示；无配置零开销 |
| **一次性** | systemMsgs | 启动 eager/keep-alive 连接结果；reconnect 结果；skill 升级结果 | 事件发生时 |
| **面板** | overlay（二级菜单） | `/skill` 浏览选用 skill；`/mcp` 查看/操作 server | 用户主动 |

### T1. 面板基础设施：PanelShell + overlay 挂载 + 多级导航

**挂载复用现有 overlay 机制**（ModelPicker / HistoryPicker / Wizard 同款）：TuiApp 的 `overlay` state 加 `skill-panel` / `mcp-panel` 两个 kind，面板组件渲染在对话流下方当前轮区域；overlay 期间 `InputStream inactive`（输入框停用）、面板独占键盘（含 Ctrl+C 语义）——这些行为 ECode 已有，面板零新层。

**PanelShell 组件**（`src/tui/PanelShell.tsx`，**新建组件**——复用 Select 的交互语义与 theme，不从 Select 改造：Select 无窗口滚动/搜索/分组行/键位提示行，能力面差距大）：

```
┌─ PanelShell ──────────────────────────────────────┐
│ 标题（bold）              副标题（dim，如 3 个服务）│
│ ─────────────────────────────────────────────────│
│  ↑ 可选：搜索行（有 query 时显示 过滤词▏）          │
│  列表区（窗口化滚动，超 12 行滚动 + 边缘提示）      │
│  …                                                │
│ ─────────────────────────────────────────────────│
│  底部键位提示行（dim）：↑↓ 选择 · 回车 确认 · Esc 返回│
└───────────────────────────────────────────────────┘
```

- **标题 + 副标题 + 底部键位提示行**：三个面板统一壳，提示行随当前视图动态生成（如 MCP 详情视图追加 `ctrl+r 重连`）。
- **窗口化滚动**：列表超 12 行只渲染窗口（光标为中心 clamp），窗口上方/下方显示 `↑ 还有 N 项` / `↓ 还有 N 项`——避免长列表刷屏（ECode 非 alt-screen，面板高度必须受控）。
- **多级导航 = viewState 状态机**：`{view:'list'} → {view:'detail', item} → …`，每级是独立渲染分支；**Esc 逐级回退**（详情→列表→退出面板）。不用 accordion 行内展开（Esc 语义清晰、渲染简单、每级可用满宽）。
- **分组列表**：组标题行（bold + dim 配置来源路径）不可选中；光标只落在条目行。分组数据拍平成 `{type:'header'|'item'}` 数组导航（分组列表不能直接用扁平 Select，自管 selectedIndex）。

### T2. SkillPanel（`/skill`）

**一级（唯一级）**：skill 列表。

```
 Skill                        3 个技能 · 项目级 1 / 用户级 2
 ─────────────────────────────────────────────────────
 项目级（.ecode/skills/）
   commit        按约定生成提交信息
   review        审查当前 diff 并给结论            仅手动
 用户级（~/.ecode/skills/）
   doc-dist      从对话蒸馏文档技能
 ─────────────────────────────────────────────────────
 ↑↓ 选择 · 回车 选用 · 输入即搜索 · Esc 退出
```

- 行 = `name + description（截断）+ 标记`；`disable-model-invocation: true` 的标「仅手动」，`user-invocable: false` 的**不出现**（面板就是手动面）。
- **即时搜索**：任意可打印字符进入过滤（name + description，不区分大小写子串匹配），backspace 删字符，Esc **先清搜索词、再退面板**（两级 Esc 语义）。
- **Enter = 选用（回填模式）**：把 `/skillname `（带尾随空格）回填主输入框并关面板——用户直接回车 = 无参执行（`$ARGUMENTS` 剥除），或续写参数再回车。不直接执行的理由：保留传参机会；回填让用户看到将要执行的命令，符合"所见即所发"。
- **空态**：`暂无技能。创建：/skill-create；目录：~/.ecode/skills/<name>/SKILL.md`。

### T3. McpPanel（`/mcp`）

**一级：server 列表**。

```
 MCP 服务                     3 个 · 已连接 1
 ─────────────────────────────────────────────────────
   filesystem    ✓ 已连接      11 个工具
   github        ○ 已缓存      20 个工具（未连接，调用时自动连接）
   db            ✗ 失败        12 秒前：spawn npx ENOENT …
                  （错误详情自动展开：光标停在该行时）
 ─────────────────────────────────────────────────────
 ↑↓ 选择 · 回车 详情 · ctrl+r 重连 · Esc 退出
```

- 行 = `名称 + 状态标签（着色）+ 工具数`。状态标签：`✓ 已连接`（绿）/ `○ 已缓存`（dim）/ `… 连接中`（黄）/ `✗ 失败`（红）/ `⊘ 已禁用`（dim）——与 M3.2 状态机一一对应。
- **failed 光标行自动展开错误**：光标停在失败 server 上时，错误信息（红色、词级换行、超长 URL 逐字符硬切，**≤4 行截断 + `…`，完整信息进详情视图**——防顶爆窗口高度）渲染在该行正下方；光标移开即收起。不需要额外按键。
- **ctrl+r**：对光标 server 直接重连（免进二级）；正在连接/已禁用的行忽略。

**二级：server 详情**（Enter 进入）。

```
 db（stdio · 项目级 .mcp.json）
 ─────────────────────────────────────────────────────
 状态        ✗ 失败（12 秒前）
 命令        npx -y @modelcontextprotocol/server-db
 lifecycle   lazy · 空闲 10 分钟断开
 工具        8 个（缓存）
 ─────────────────────────────────────────────────────
   查看工具
   重连
   断开                    ← 已连接时显示；未连接时为「连接」
   返回列表
 ─────────────────────────────────────────────────────
 ↑↓ 选择 · 回车 执行 · Esc 返回
```

- 上半**信息区**（dim 键值对）：状态（含失败原因摘要）/ 类型与命令或 url / lifecycle 配置 / 来源（用户级 config 或项目级 .mcp.json 路径）/ 工具数（缓存或实连）。
- 下半**操作菜单**（Select）：
  - **查看工具** → 三级：工具列表（`name + description 截断`，只读），Esc 返回详情。
  - **重连**：乐观更新——按下立即置「… 连接中」并重绘（不等网络），完成后刷新状态 + metadata cache；结果行内反馈（成功→状态变已连接；失败→状态变失败 + 错误展开）。
  - **断开**：`client.close()` → 状态回「已缓存」（**工具注册保留**，下次调用自动重连）；connecting 中按断开 → 置 pendingDisconnect（连接落地即关，见 M4.2）；已断开时显示「连接」（手动触发 lazyConnect）。
  - **返回列表**（Esc 等价）。
- 禁用的 server：详情可看信息，操作只有「启用」（写配置 enabled:true，提示重启或下版热生效）→ MVP 简化为提示配置路径，不做写配置操作。

**子命令直达**（不走面板，带 systemMsgs 反馈）：`/mcp reconnect [name]`（省略=全部非禁用项）。v4 的 `/mcp tools` 文本清单移除（面板三级覆盖）。

### T4. 统一键位规范（面板通用，TUI 规范的面板扩展节）

| 键 | 行为 |
|---|---|
| `↑` / `↓` | 移动光标（首尾环绕） |
| `Enter` | 确认（列表=进入/选用；操作菜单=执行） |
| `Esc` | 逐级回退：先清搜索词 → 退上一级视图 → 退出面板（恢复输入框） |
| `PageUp` / `PageDown` | 翻页（±窗口高） |
| 任意可打印字符 | 进入即时搜索（backspace 删字符）——**因此 j/k 导航与数字直选不进 MVP**（与可打印字符即搜索互斥，v6 审阅裁决；后置可加显式搜索模式切换键再引入） |
| `Ctrl+C` | 面板期间=退出面板（不中断 loop，同 ConfirmPrompt 语义） |
| `ctrl+r`（MCP 一级） | 重连光标 server |

**渲染约束**：宽度=终端全宽（对齐现有组件）；高度受控（窗口化滚动 12 行 + 提示行）；ambiguous 字符宽度问题沿用现有 symbols 方案；面板打开期间后台 loop 若在流式输出，面板仍钉在当前轮位置不动（Static 之上），流式完成后 systemMsgs 反馈。

**后置**：面板内直连工具 toggle / token 实时估算（等 M6-D28 proxy 模式一起）、60s 无操作自动关、键位可配置（keybindings 覆盖）、鼠标支持。

---

## M7. MCP 实施拆解

| 步 | 内容 | 依赖 | 心脏 |
|---|---|---|---|
| **M-P1** | 装 `@modelcontextprotocol/sdk@^1.30`；Tool 加 `skipLocalValidate?` + Registry 分支（validate ok + register 跳过 ajv 预编译）+ **Registry 加 `unregister(name)`**（M7 disable 卸载用；refreshMetadata MVP 只增不减） | 无 | 零 |
| **M-P2** | `services/mcp/manager.ts`：McpManager（六态状态机 / lazyConnect 并发去重 / 60s 退避与过期 / 空闲卸载 / 健康检查 / stop；**now() 与定时器参数化注入**——60s/10min/30s 单测不用真等） | M-P1 | 零 |
| **M-P3** | adaptTool（normalizeSchema + getClient 惰性句柄 + 传输错误清句柄降级）+ renderContent（isError→is_error，signal+resetTimeoutOnProgress） | M-P2 | 零 |
| **M-P4** | `services/mcp/cache.ts`：metadata cache（读/写/configHash 失效/**回写串行队列**） | M-P2 | 零 |
| **M-P5** | `services/config.ts`：Config 增 `mcpServers`（lifecycle/idleTimeout）+ `.mcp.json` + `${ENV_VAR}` + 合并去重 + 首用批准（hash 存 `~/.ecode/approved-mcp.json`，二段启动流见 M4.1） | 无 | 零 |
| **M-P6** | **Command 契约扩展**：`run(args?: string)`（向后兼容；InputStream 把首个空格后文本传入）+ `CommandResult.action` 加 `open-mcp-panel`；`commands/mcp.ts` + `tui/McpPanel.tsx`：三级面板（列表→详情→工具）+ 状态着色 + failed 错误展开（≤4 行截断）+ 重连/断开（乐观更新；connecting 中断开= pendingDisconnect）+ ctrl+r 快捷 + `/mcp reconnect` 直达（v5，复用 PanelShell） | M-P2,S-P6 | 零 |
| **M-P7** | TUI 接线：StatusBar MCP 段 + systemMsgs + overlay kind 接入 + **mcpManager/onEvent 进 TuiAppDeps**（TuiApp setState → 面板读 status() 快照） | M-P2 | 零 |
| **M-P8** | resolveCommand（win32 cmd /c）+ 退出进程清理 + **estimateContextTokens 加 tools 参数**（v3 P1-1；两个调用点同步改：hook 工厂 + TuiApp.checkModelWindow）+ **makeOnBeforeRequest 收敛为 options 对象**（现 9 参再加 tools 触发 AGENTS 1.3 Builder/参数对象规范） | M-P2 | 零 |
| **M-P9** | makeDeps 集成 + redact.ts 增 MCP env/headers 规则 | M-P2,P5 | 零 |

全程不动 loop.ts。

> **粒度注记**（v6 审阅）：M-P2 / M-P6 超 2h 时间盒，实施时二分——M-P2 → 「连接核心（状态机/lazyConnect/退避）」+「生命周期定时器（卸载/健康检查/过期）」；M-P6 → 「一级列表 + StatusBar 订阅」+「二三级详情与操作」。编号不变，二分以 checklist 呈现。

---

# 决策结论（M6）

> D17-D21 / D24（Plugin 范式/清单/资源/优先级/安全/装配）已随 v6 拆分迁至 M7 方案（重编 M7-D1~D7），编号不复用；历史引用见各版修订记录与 ADR-026。

| # | 决策点 | 结论 | 理由 |
|---|---|---|---|
| **M6-D1** | Skill 触发方式 | **双触发面**：LLM 自主调 SkillTool（主）+ 用户 `/skill-name args` 手动调（v4 修订） | description 驱动 LLM 命中是主要路径；确定性意图不赌模型判断，斜杠补齐 |
| **M6-D2** | Skill body 注入（LLM 面） | 作为 tool_result（`<skill_content>`） | 零侵入心脏；复用既有回喂路径 |
| **M6-D3** | Skill 文件格式 | 目录 `name/SKILL.md` + frontmatter（AJV） | 目录携附属文件；缺字段回退 |
| **M6-D4** | Skill 发现层级 | 项目级 > 用户级 > plugin 级；首个胜出 | 项目级更具体；plugin 基底层 |
| **M6-D5** | Skill MVP 优化 | token 预算(1%)+250截断+when_to_use+base dir+AJV | 防爆炸+命中率，成本极低 |
| **M6-D6** | Skill 增量推送 | MVP 暂不做（预算约束已够），留变化签名接口 | YAGNI；M5 压缩后视清单负担再上 |
| **M6-D7** | Skill 蒸馏自动化 | **MVP 做 L2 半自动**（`/skill-create` 访谈起草 + 升级合并）；L3-L5 后置 | 全自动"判断什么值得存"业界尚无成熟实现；人审卡点收敛两处 |
| **M6-D8** | memory vs skill | 分两层不合并；memory 可作 skill 起草输入但不自动升级 | 自动升级正确率无保证会污染 skill 库 |
| **M6-D9** | MCP SDK | 官方 `@modelcontextprotocol/sdk@^1.30` | 官方 SDK 是唯一稳定实现，自研 JSON-RPC 无收益 |
| **M6-D10** | MCP transport | stdio + Streamable HTTP 都做 | stdio 社区主流；HTTP 接云端 |
| **M6-D11** | MCP 工具命名 | `mcp__<server>__<tool>` | 事实标准；防冲突 |
| **M6-D12** | MCP 工具确认 | 统一默认确认（readonly=false） | annotations 不可信；第三方代码 |
| **M6-D13** | MCP 参数校验 | 透传 server（skipLocalValidate） | server 最权威；避 AJV 兼容问题 |
| **M6-D14** | MCP 配置 | 用户级 config + 项目级 .mcp.json（plugin 级随 M7） | 对齐业界；secret 从 env 读 |
| **M6-D15** | MCP 生命周期 | **按需连接**：lazy 默认 + eager/keep-alive 可选 + 空闲卸载 + failed 60s 过期退避（v4 修订，废弃急连接） | 启动零连接降延迟与进程开销；metadata cache 解耦注册与连接；required 字段随急连接移除 |
| **M6-D16** | resources/prompts | MVP 不做（只 tools） | tools 是主价值；另两类后置 |
| **M6-D22** | 与心脏关系 | Skill/MCP 都注册成 Tool/Command 进 Registry，loop.ts 零改动 | 契合详设 §2.4 铁律 |
| **M6-D23** | 实施顺序 | Skill → MCP | 共用 PanelShell（S-P6 先落）；MCP 外部依赖重、独立验收 |
| **M6-D25** | Skill 手动传参 | `$ARGUMENTS` 替换 + 无占位符兜底追加；`$1`/命名参数后置 | 两条规则保证任何 skill 不丢参；MVP 一个占位符够用 |
| **M6-D26** | Skill 双路径注入形态 | 自动=tool_result；手动=user 消息 + 立即触发一轮 | 自动复用回喂零侵入；手动是 TuiApp 层构造普通 user 消息，loop 零特判 |
| **M6-D27** | MCP metadata cache | `~/.ecode/mcp-cache.json` 单文件；configHash 失效；reconnect 手动刷新 | lazy 默认的前提：注册与连接解耦；单文件够用（KISS） |
| **M6-D28** | MCP 工具暴露 | MVP 全量直连注册 + token 计入压缩估算 + 超阈值 warn；proxy 元工具后置 | 与内置工具心智一致；token 估算兜底爆炸风险 |
| **M6-D29** | 蒸馏升级合并 | versions 备份（≤10）+ merger 三态 + section 级确定性合并 + 预览确认 | LLM 只判断不拼盘；升级永不静默覆盖且有回滚 |
| **M6-D30** | MCP TUI | StatusBar 常驻段 + systemMsgs 一次性 + `/mcp` **二级面板**（列表→详情→操作，v5 修订）；`/mcp reconnect` 直达 | 三层覆盖"常态感知/结果反馈/浏览操作"；文本列表承载不了导航与操作 |
| **M6-D31** | 面板基础设施 | PanelShell（标题/副标题/窗口滚动 12 行/键位提示行）+ viewState 状态机 + Esc 逐级回退；**挂载复用现有 overlay 机制** | ModelPicker/HistoryPicker 已验证 overlay+独占输入可行，零新层；多级状态机比 accordion 的 Esc 语义清晰 |
| **M6-D32** | SkillPanel 选用行为 | Enter = 回填 `/skillname ` 到主输入框（不直接执行） | 保留 `$ARGUMENTS` 传参机会；所见即所发（回填后可见将执行的命令）；无参场景多敲一次回车可接受 |

---

# 测试

## Skill
- **单测**：SkillRegistry.load（扫描/frontmatter 合法非法/字段回退/去重/优先级）；listing（token 预算截断/250/无 skill 零开销/变化签名）；skillTool（存在→`<skill_content>`/不存在→is_error/disableModelInvocation→is_error）；expandSkill（`$ARGUMENTS` 替换/兜底追加/未传参剥占位符）；install 创建模式 + 升级模式（versions 备份/section 同名替换/新 section 追加/冲突标记/上限淘汰）；listForPrompt/listForCompletion 过滤
- **TUI 测试**（ink-testing-library，Esc 断言注意 ~20ms flush 延迟，见《ECode自测方法规范》——ink-testing-library 层只覆盖渲染与键盘分流；**node-pty 真机层为人工验证手段，不进 vitest**（原生模块 Windows 安装成本高））：`/` 补全合并（skill 出现 + 来源后缀 + userInvocable:false 不出现 + 撞名标记）；`/commit 修复 x` 手动触发（user 消息含展开全文/立即请求/display 显示原文）；空格后停止命令名匹配；**SkillPanel**（分组渲染/搜索过滤/Esc 先清词再退/Enter 回填主输入框并关面板/空态）；**McpPanel**（三级导航与 Esc 逐级回退/状态标签着色/failed 光标行错误展开≤4 行/重连乐观态→结果刷新/断开后状态回已缓存/ctrl+r 快捷）。McpManager 状态事件驱动重渲染的测试模式：组件外触发 onEvent → flush → 断言（M-P2 时间参数化后 60s/10min/30s 场景用注入时钟）
- **集成**：放 commit/SKILL.md → MockProvider → LLM 调 Skill → tool_result 含 body → 引用步骤；`/skill-create` 读会话产出合法 SKILL.md；项目级覆盖用户级；同名再 install 走升级

## MCP
- **单测**：adaptTool（命名/sanitize/readonly=false/skipLocalValidate/normalizeSchema）；validate（skipLocalValidate 直接 ok / 内置仍 AJV）；配置合并（`${ENV_VAR}`/enabled/lifecycle 默认值）；lazyConnect（并发去重共享 Promise/退避期内拒绝/状态机推进）；cache（命中注册零连接/configHash 失效/回写）；空闲卸载（超时断开/inFlight>0 不断/keep-alive 不卸）；renderContent（多类型）
- **TUI 测试**：StatusBar MCP 段（2/3/连接中/无配置不显示）；`/mcp` 面板各状态行渲染与三级导航；`/mcp reconnect` 后状态刷新（并入上方 Skill/MCP 面板测试条目，MCP 侧断言状态机联动）
- **集成**：in-process mock MCP server（SDK Server + in-memory transport）→ cache 注册 → 首次调用才连接（断言启动零连接）→ `mcp__mock__*` 可调；LLM 调 → confirm → callTool → 结果回喂；isError→is_error；连接失败标 failed 且不阻塞；失败 60s 后降级；AbortSignal 中断；stop 后无孤儿

（Plugin 测试 → M7 方案）

---

# 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| MCP SDK 大版本断裂（client@2.0 无状态协议） | 中 | 锁 `^1.30`；适配层薄；列开放问题 |
| MCP server 第三方代码不安全 | 高 | 默认每次确认（D12）+ .mcp.json 首用批准；readOnlyHint 不豁免 |
| **lazy 首调延迟**（连接+握手+list 在调用路径上） | 中 | StatusBar 瞬时态"连接中…"；cache 命中时注册零延迟；超时兜底 30s |
| **metadata cache 陈旧**（server 工具增删未感知） | 中 | configHash 覆盖配置变更；`/mcp reconnect` 人工刷新；`tools/list_changed` 后置 |
| stdio 子进程孤儿（Windows） | 中 | SDK close 兜底；taskkill /T 后置 |
| npx Windows ENOENT | 中 | resolveCommand cmd /c 降级 |
| server schema 含 `$ref` AJV 不兼容 | 低 | skipLocalValidate 透传（D13）+ normalizeSchema 归一化 |
| Skill 清单膨胀吃 system prompt | 低 | token 预算（D5）；M5 压缩后上增量 |
| description 差致 LLM under-trigger | 中 | pushy 规范 + 短 desc warn + **斜杠手动面兜底**（v4：确定性场景用户可直调） |
| MCP 工具 schema 挤爆上下文 | 中 | token 计入压缩估算（M3.4）+ 超阈值 warn；proxy 模式后置 |
| **蒸馏污染 skill 库**（自动升级错误） | 中 | memory/skill 分层（D8）；install 单一入口 + 人审卡点两处 + versions 回滚 |
| 手动 skill 展开全文进转录刷屏 | 中 | submit `display?` 参数显示原始输入 + committed 超长截断（v6 提为 MVP） |
| **面板与流式输出并发**（面板打开时 loop 在跑） | 低 | 实际不可达：loop 运行中 InputStream inactive 无法输命令开面板；仅 /model 等 picker 同理，沿用现有语义即可 |
| **面板期间状态变化**（MCP 在面板打开时断线/连上） | 中 | McpManager 状态事件驱动面板重渲染（onEvent 订阅）；MVP 面板内操作后主动刷新快照兜底 |
| **重连乐观态与实际结果竞态** | 低 | 乐观置 connecting 后以回调终态为准；面板每次渲染读快照不读本地缓存 |
| `.mcp.json` 泄露 secret | 高 | `${ENV_VAR}` 占位符；脱敏；不入库 |
| MCP 工具与内置重名 | 低 | `mcp__` 前缀隔离 |

---

# 附录

## A. 关键文件改动

| 文件 | 改动 | 子系统 |
|---|---|---|
| `src/services/skill.ts` | **新**：SkillInfo + SkillRegistry + expandSkill + install 双入口（创建/升级+versions+section 合并） | Skill |
| `src/services/skill/listing.ts` | **新**：token 预算 + 截断 + 变化签名 | Skill |
| `src/services/skill/distill.ts` | **新**：`/skill-create`（起草）+ merger 三态 prompt + 升级 diff 预览 | Skill |
| `src/tools/builtin/skill.ts` | **新**：SkillTool | Skill |
| `src/core/system.ts` | buildSystemPrompt 加 skills（listForPrompt）+ 预算渲染 | Skill |
| `src/tui/InputStream.tsx` | submit 分流（命令→skill→未知，slash 匹配四处调用点合并）+ onSkillInvoke 回调 + `/` 补全合并（listForCompletion + 来源后缀 + 尾随空格 + 撞名标记）+ 受控插入 prop `insert?:{text,seq}`（面板回填通道） | Skill |
| `src/tui/PanelShell.tsx` | **新**（v5）：面板壳（标题/副标题/窗口滚动 12 行/键位提示行/分组导航/即时搜索）——Skill/MCP 面板共用（M7 PluginPanel 复用） | Skill/MCP |
| `src/tui/SkillPanel.tsx` | **新**（v5）：分组列表 + Enter 回填选用 + 空态 | Skill |
| `src/tui/McpPanel.tsx` | **新**（v5）：三级视图（server 列表→详情→工具）+ 状态着色 + 错误展开 + 重连/断开 | MCP |
| `src/tui/TuiApp.tsx` | submit 加 `display?` 参数 + committed 超长 user 截断 + onSkillInvoke 展开注入 + StatusBar MCP 段 + MCP systemMsgs + overlay 新 kind（skill-panel/mcp-panel）+ **TuiAppDeps 扩展（skillRegistry/mcpManager/onEvent 接线）** + ctxWindow 缓存 | Skill/MCP |
| `src/services/mcp/manager.ts` | **新**：McpManager（状态机/lazyConnect/退避/空闲卸载/健康检查 + status 事件流供面板/StatusBar 订阅） | MCP |
| `src/services/mcp/cache.ts` | **新**：metadata cache（configHash） | MCP |
| `src/services/mcp/adapt.ts` | **新**：adaptTool + normalizeSchema + renderContent | MCP |
| `src/commands/mcp.ts` | **新**：`/mcp`（无参=面板 + reconnect 直达） | MCP |
| `src/tools/interface.ts` + `registry.ts` | Tool 加 `skipLocalValidate?` + validate 分支 + register 跳过预编译 | MCP |
| `src/services/tokenizer.ts` | estimateContextTokens 加 tools 参数（M3.4 + M5 债 #3 收敛点） | MCP/Skill |
| `src/services/config.ts` | Config 增 `mcpServers`（lifecycle/idleTimeout）+ `.mcp.json` + `${ENV_VAR}` + 首用批准 hash | MCP |
| `src/commands/registry.ts` | Command 契约扩展 `run(args?)` + action 加面板触发值；`/skill` `/skill-create` `/mcp` 注册 | Skill/MCP |
| `src/services/compaction/hook.ts` | makeOnBeforeRequest 收敛 options 对象 + 加 tools 入参（两个调用点：cli/index 与 TuiApp 同步改） | MCP |
| `src/services/redact.ts` | 增 MCP env/headers 规则（plugin 占位符随 M7） | MCP |
| `src/cli/index.ts` | makeDeps：Skill→MCP 顺序集成 + exit handler 调 mcp.stop | 两者 |
| `src/core/loop.ts` | **不动**（铁律验证：无 skill/mcp/plugin 字样） | — |
| `package.json` | 增 `@modelcontextprotocol/sdk@^1.30`（MCP 需要；Skill 零依赖） | MCP |

## B. 参考来源

> 正文为 ECode 自主设计；以下外部实现仅作交叉验证与选型依据，按子系统归类。

**Skill**：
- claude-code：skill 与斜杠命令同构（同一 Command 结构）；`user-invocable` / `disable-model-invocation` 双布尔；`$ARGUMENTS`/`$1`/命名参数占位替换 + 无占位符兜底追加；手动注入 isMeta user 消息；1% token 预算 + 250 截断；sent-Set 增量推送；paths 条件激活；fork 子 agent；内联 shell
- opencode：`<available_skills>` XML 清单；baseline/update/removed 三态增量；base dir 注入；文件采样上限
- codex：BM25/词法检索选 skill；隐式调用检测；skills-lock
- skill-creator（公开元 skill）：pushy description 规范；三层渐进披露；examples beat rules

**Skill 蒸馏**：
- colleague-skill / dot-skill（20k stars，上海 AI Lab）：四角色 prompt 管线（intake→analyzer→builder→merger→correction）；`##` section 级合并协议（同名替换/新增追加，升级不产生重复段落）；versions 自动存档上限 10 版 + 回滚；人审卡点收敛两处（写盘前预览 + 冲突裁决）；LLM 只判断、确定性代码写盘——**全部显式触发，无自动触发**
- claude-code skillify（内部）：4 轮访谈起草（名称→大纲→逐步骤含成功判据→触发时机）；读 session memory + user messages 作原料
- opencode `/learn`：蒸馏"非显然发现"写 AGENTS.md（memory 层，非 skill）
- arXiv 2606.20363：GUI agent 全自动 skill 生成，未跑赢 frequency baseline——全自动档位尚不成立的实证

**面板交互**（v5 调研，T1-T4 的交叉验证；Plugin 面板部分对应 M7 方案 P6）：
- claude-code：面板 = "local-jsx 命令"（斜杠命令返回 React 节点挂到 REPL 专用槽位，面板期间主输入停用）；MCP 面板四级 viewState 状态机（list→server 菜单→tools→tool 详情，Esc 逐级回退）；键位集中配置（Select/Confirmation/Plugin 三张表）；分组列表自管 selectedIndex；Dialog 壳=标题+副标题+底部键位提示行
- pi-mcp-adapter：面板组件协议（handleInput/render/done）；扁平 visibleItems 导航（server 行 Enter 展开工具子行）；60s 无操作自动关（每次按键重置）；failed 光标行自动展开错误（词级换行+超长词硬切）；即时搜索双模式（name 随打随滤 / ? 切 desc 模态）+ fuzzy 打分（server 名命中给工具行 0.6 加权）；重连乐观更新（先置 connecting 立即重绘）；MAX_VISIBLE=12 窗口滚动+进度点；dirty 未保存提示 + Esc 三级语义（清搜索→确认丢弃→退出，Discard/Keep 默认 Keep）；面板不写配置只产出 diff
- opencode：DialogSelect（居中模态 + 搜索自动聚焦 + 分组列表 + footer 动作区，Tab 在动作按钮间循环）；DialogMcp 仅 85 行（行=server+状态+footer 徽标，Enter=toggle 连接/断开，Esc 才关面板）；DialogSkill「选择即回填」（onSelect 把 `/skillname ` 写进输入框并关面板）——ECode SkillPanel 回填模式的直接依据
- codex：ListSelectionView（占输入框位置的带边框面板）；←→ 切 Tab 页；空格 toggle；Enter=执行行 actions；selected_description 高亮行显示上下文操作提示；/plugins 四 Tab（All/Installed/Curated/Add marketplace）+ Loading/Ready/Failed 状态机 + Add marketplace 文本输入

**MCP**：
- MCP 官方规范（modelcontextprotocol.io）：两类 transport（stdio/Streamable HTTP）；三类原语（tools 主价值）；`tools/call` isError 二分契合 recoverable
- pi-mcp-adapter（pi 官方推荐扩展，npm v2.25）：lazy/eager/keep-alive/lazy-keep-alive 四态 lifecycle；metadata cache + configHash（注册与连接解耦）；六态状态机 + failed 60s 过期与退避；30s 健康检查 + 10min 空闲卸载（lastUsedAt/inFlight）；footer/notify/panel 三层 UI；proxy vs direct 两种工具暴露；连接并发去重（connectPromises Map）
- pi 本体（coding-agent）：刻意不含内置 MCP（core small 原则的印证）；deferred tool loading（setActiveTools + 工具结果消息记录 addedToolNames + Anthropic defer_loading 原生序列化）——proxy 模式的协议级参考
- opencode：convertTool 适配骨架；急连接 + 容错（ECode v2 曾采，v4 废弃改 lazy）
- codex：进程清理与安全面

**Plugin**：随 v6 拆分迁至 M7 方案附录 B（ZCode 本地插件缓存四件套一手验证 / claude-code 三层 schema 与加载器 / codex core-plugins / opencode 进程内 SDK 反例）。

## C. 开放问题 / 后置项

**Skill 后置**：增量推送（D6）、`$1`/命名参数占位符、paths 条件激活、fork 子 agent、内联 shell + `${SKILL_DIR}`、url source、热重载、BM25 检索、转录 display/content 分离（isMeta）、**蒸馏 L3-L5**（S8-b~e：SessionMemory/`/learn`/ThresholdTrigger/全自动）。

**MCP 后置**：resources/prompts、OAuth（交互式，needs-auth 态随之）、elicitation/MRTR、per-tool 白黑名单（excludeTools）、`tools/list_changed` 增量同步、proxy 元工具暴露模式、面板高级项（直连工具 toggle / token 实时估算 / 60s 自动关 / 键位可配置 / 鼠标支持）、env_clear 高安全选项、**协议演进**（`@modelcontextprotocol/client@2.0` 无状态核心/`server/discover`——持续关注，适配层薄，升级面小）。

**Plugin 后置**：随 v6 拆分迁至 M7 方案附录 C（hooks/agents/userConfig/依赖解析/git-subdir/企业策略/autoUpdate/keychain）。

**里程碑建议**：两个子系统解耦，独立 PR 交付。顺序 **Skill（S-P1~P8）→ MCP（M-P1~P9）**，各自独立验收不互相阻塞；Plugin（M7）待 M6 交付后按独立方案启动。

---

## 16. 实施记录（2026-08-14，commit 202a769）

**交付**：S-P1~P8 + M-P1~P9 全量（48 文件 +5887 行；测试 636/636 + tsc 零；真机 smoke 验证通过）。

**关键落地与方案差异**（其余严格按方案）：
- **distill 升级路径补一个纯函数**：`patchBodyFromVerdicts`——install 吃「干净补丁 + decisions」双输入（merger 判定先过滤出补丁 body，等价/被 keep 的节不进补丁），S8.3 流程图第 5 步的确定性实现。
- **McpManager 依赖注入重构**：connectFn/now/cache 构造注入（方案 M4.2 伪码的直接连接改为工厂）——SDK 连接器（adapt.ts createSdkConnectFn）与状态机解耦，manager 单测零子进程。
- **Ctrl+C 语义细化**：T4 表的「面板 Ctrl+C=退出」实现为直接退出（不清搜索词的两级语义只归 Esc）——与 ConfirmPrompt 先例一致。
- **stdin 测试经验**：ink-testing-library 下 `\u001b[B` 合并写会被解析成单事件（逐次写 + flush）；Esc 单发需要 flush 后才送达；onCursor 类 effect 级联需双 flush。
- **PanelShell 补 onCursor 回调**（McpPanel failed 行随光标展开错误需要；方案 T3 隐含但接口未列——实施时补）。
- **SkillPanel/补全/手动分流共用 skillRegistry 单例**（与 commandRegistry 同范式，方案 S5 预期）。
- **M7 依赖冻结确认**：SkillRegistry.addSource / ToolRegistry.unregister / McpManager（subscribe/toolsOf）均已交付，M7 方案 v2 已按此更新。

**遗留**（记入下方债清单，均不阻塞）：
1. npx/cmd 包装的 spawnSpec 在 win32 的真实 npx server 未真机验证（单测覆盖逻辑；需配真实 server 验证 cmd /c 链）。
2. MCP stderr pipe 未接 LogStore（M4.2 注释留口；调试需要时接）。
3. `resolveCommand` win32 对 npx 返回原值 + spawnSpec 包 cmd（两函数职责有重叠，M7 P6.1 统一收敛到 proc.ts）。
4. 真机 TUI 面板（/skill /mcp）未跑 node-pty 真机验证（组件测试覆盖渲染与键盘；按自测规范属人工验证层）。
