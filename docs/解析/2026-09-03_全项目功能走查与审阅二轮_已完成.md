# 2026-09-03 全项目功能走查与审阅（第二轮·全功能清单）

> 状态：已完成（走查+审阅+P1×7 全修+P2 快赢 12 项；全量门 1971+3skip 零红）
> 范围：全项目功能面（第一轮 docs/解析/2026-09-03_0831-0902全功能走查与审阅_已完成.md 已覆盖 0831-0902 提交窗口；本轮补全 M1-M14 早期成熟面）
> 方法：功能清单提取 → 斜杠命令 pty 全量清扫 → 工具集真 LLM 轮次 → 服务面协议驱动（含隔离 daemon）→ 四角色审阅 → 修复

---

## 一、全项目功能清单与覆盖矩阵

### 1. 斜杠命令（20 条，registry.ts 全量）

| 命令 | 功能 | 本轮验证 |
|------|------|---------|
| /help | 命令帮助 | ✅ pty 清扫 |
| /clear | 清屏 | ✅ |
| /model | 模型切换面板 | ✅（并行会话同日另修附着态假切换 b1cb267/8c14066） |
| /history | 历史会话恢复 | ✅（+doublesc 探针回归） |
| /setup | 配置向导 | ✅ 首屏 |
| /compact | 手动压缩 | ✅（协议层 session/compact 真跑回执 ok） |
| /cost | 成本查询 | ✅ |
| /stats | 用量统计 | ✅（第一轮 web 面板+本轮 TUI） |
| /devices | 设备管理面板 | ✅（第一轮深测：QR/吊销/relay 状态） |
| /skill /skill-create | 技能面板/创建 | ✅（创建流程含诚实报错实证：「蒸馏失败：起草输出未包含 JSON」对 mock 正确暴露） |
| /mcp /plugin | MCP/插件面板 | ✅ |
| /warnings | 告警中心 | ✅ |
| /sandbox | 沙箱四档面板 | ✅（面板+协议档位真拦真放） |
| /undo | git 撤销 | ✅（沙盒仓库+真仓库提示形态） |
| /rewind | 快照回退 | ✅（**本轮修复：新建文件回退删除**） |
| /doctor | 自检 prompt 注入 | ✅ |
| /restart | 重启 | ✅（restart 探针 5/5） |
| /config | 配置面板三页签 | ✅ |

**新探针 `scripts/pty-slash-sweep.cjs` 入库**：20 条逐条驱动（两段式回车），探针工程三课：Esc 连按间隔须在双击退出窗（500ms）外；空草稿双击 Esc 开 /rewind 面板吞键；覆盖层清场用单刀 Ctrl+C（第二刀撞「再按一次退出」长窗杀 TUI）。

### 2. 工具集（16 个，builtin 全量）

| 工具 | 本轮验证 |
|------|---------|
| ls / glob / grep / read_file / write_file / edit_file / bash | ✅ 真 LLM 轮次 7/7（沙盒项目内逐项执行+报告） |
| todo | ✅（同轮自动使用 7/7 清单跟踪+面板渲染探针） |
| task（子代理） | ✅ 真 LLM 派发计算任务回传正确（+两个子代理探针） |
| ask_user | ✅ 协议全链（requested→respond→resolved→completed 实测 4.5s/8.2s 全序；无应答者时非交互降级文案引导模型自决——设计使然） |
| web_search | ✅ 真搜「北京天气」10 条结果（bing RSS 免费层） |
| web_fetch | ✅ 抓取 example.com 标题正确 |
| skill | ✅（系统提示清单注入+双触发面——功能测试批已锁，slash 面板本轮过） |
| 后台任务（bash run_in_background + task_output/task_stop） | ✅ 真跑 ping 后台+task_output 等待完成取输出 |

### 3. 服务面

| 功能 | 验证 | 结果 |
|------|------|------|
| ECODE.md 两级注入 | 沙盒项目 ECODE.md 暗号 → 模型答出 | ✅ |
| hooks（UserPromptSubmit） | 隔离 daemon+config 数组 schema | ✅ 触发落盘（**注意：hooks 配置在用户级 config.json 且必须是数组形态** `[{event, handler:{kind:'command',command}}]`——对象映射形态会被「必须是数组，已忽略」警告拒绝） |
| 沙箱四档 | sandbox/set read-only→写被拦；default→写真放行（审批经 canAnswer 客户端放行） | ✅ |
| 多模态图片 | 协议 path 形态（ImagePayload={path,mime}——inline data 不支持，TUI 粘贴即落盘引用） | ✅ 模型 thinking 描述 1×1 图片真实颜色+正文答对宽高 |
| 压缩 | session/compact 协议真跑 | ✅ |
| checkpoint+/rewind | 写入→快照→回退 | ⚠️ **发现 P1 真 bug 已修**（见 §二） |
| 会话恢复/fork/--history | doublesc/restart 探针 | ✅ |
| 沙箱 Tab 切换/acceptEdits | sensitive-cards/grid 探针+单测 | ✅ |

### 4. 端与链路（第一轮已深测，本轮回归）

TUI 附着 daemon/失联自愈 ✅（attach 10/10）；relay/E2EE/设备配对 ✅（公网 6/6）；web 全链路 ✅（真浏览器）；飞书 IM（config 无凭据——G-IM 真机门已过记录在案）；微信 gateway（待用户 wechat-login 真机门，挂账不变）。

## 二、本轮发现与修复（提交 5fad166）

### 真 bug ×2（已修）

**R2-1（P1）rewind 对新建文件失效**：checkpoint 对「快照时刻不存在的文件」原语义是「无基线可拍，跳过」——`write_file` 新建文件的 onBeforeWrite 快照为空 → revert 无从删除 → **/rewind 回退了消息但新建文件全部残留**。真机 dogfood 复现（写 rewind-target.txt → rewind/exec ok → 文件仍在）。修复：ENOENT 记 **absent 基线**（`CheckpointFileRef.absent`），revert 到该点=删除该路径（还原前自动快照闭环「撤销撤销」）；detectExternalChanges 跳过 absent 项；+2 回归锁。

**R2-2（P2）审批零应答者文案误导**：serve 会话无可应答客户端（canAnswer 订阅）时副作用工具 fail-closed 返回裸 false → loop 冠「用户已取消」——模型向用户转述「你取消了」（dogfood 实证模型原文「写入被取消」）。修复：`APPROVAL_NO_CHANNEL_FEEDBACK` 专用反馈串（D-T8 超时串同款透传语义），如实说明无应答渠道并引导模型换只读方式推进。

**R2-3（P2，并行会话遗留）hook context 双重前缀**：e268776 归属根治把 hook context 改为预注入消息时，session 侧预加一次 `[hook context]` + preInjected 统一包装再加一次 → 模型侧看到双重前缀。修复+断言锁。

### 驱动侧经验（非产品问题，记录防复发）

- `?project=` 查询参数形式被 /api/cmd **静默忽略**（落默认宿主）——项目路由只有路径形式 `/api/p/<path>/cmd`（API 契约如此，但静默回退对脚本客户端是可用性陷阱——记入审阅账）
- 副作用工具在 serve 会话需 **canAnswer=1 订阅者**应答审批；纯观察订阅（无 canAnswer）= 零应答者 fail-closed（产品语义，见 R2-2 文案修复）
- pending 审批对新 canAnswer 连接重播=设计内（多端审批语义+15min 超时收敛），非泄漏
- 并行会话 34 笔新提交到达后主干带 4 红（归属 meta 化后旧字符串形态测试没跟上）——本轮顺手适配修复（commitThinking×2/ScrollbackDesign/TuiAppHooks）

## 三、四角色审阅（第二轮）

四席并行审成熟面（M1-M8 心脏/压缩/历史/skill/mcp/hooks/多模态/websearch）+ 5fad166 改动。发现 **P1×7 + P2×12 修复项 + 挂账若干**，无 P0。

| 席位 | P1 | 代表发现 |
|------|----|---------|
| 架构席 | 2 | gate 同步化中断盲区（quality/autoCommit 在 Ctrl+C 后照跑）/工具软超时输家 reject 无人接（迟到 reject=unhandledRejection 杀 TUI 主进程） |
| 开发席 | 2 | task_output 长等不响应 abort（readonly 池 5min 盲区）/absent 检测豁免（静默删用户手改） |
| 安全席 | 3 | MCP 批准门盲批外传（http 型 headers 的 ${VAR} 密钥外传面不可见）/absent 静默删除/RewindPanel 确认清单≠实际还原范围 |
| 测试席 | 3 | NO_CHANNEL loop 透传零覆盖（修复动机的另一半）/slash-sweep 累积缓冲假绿+/undo 真仓库风险/openai meta 零锁（e268776 宣称只兑现一半） |

核验通过面：absent 多版本排序/撤销闭环推导正确（架构席逐场景验证）；压缩链生命周期（中断→摘要 AbortError→降级不追加 boundary）防御完整；HistoryStore meta 七枚举投影一致；中断三处传播（provider/压缩/MCP）闭环（唯 task_output 一处断点已修）；沙箱 checkWrite fail-closed/sensitive 词法+realpath 双判/permissions 三层求值/MCP env deny-list 在位；checkpoint 无仓库外越界删除（gitDirtyFiles resolve 锚定 cwd）。

## 四、审阅修复（P1×7 全修+P2 快赢 12 项，提交 7ab1f9d）

**P1**：gate 后 abort 复查（早退不跑 quality/autoCommit）；工具软超时 execute 侧 .catch 吸收迟到 reject；task_output wait 接 ctx.signal race（中断带 offset 立即返回）；detectExternalChanges 对 absent 对称检测（存在=将删除，删前明示）；RewindPanel 确认页改还原范围并集+absent 标注+文案中性化+失败不吞；MCP 批准卡带 server 清单+host+${VAR} 引用名摘要+approve 批准即所见；测试席三锁（NO_CHANNEL 集成/openai meta+document 降级/slash-sweep 增量断言+沙盒接线）。

**P2（12 项）**：absent 判定收窄（对象库写独立 catch）；revert per-plan 容错+failed 汇总；revert 计划段+copyForResume 入串行链（TOCTOU）；detectExternalChanges metas 预载（面板 O(点²)→O(点)）；write_file tmp finally；bash 后台分支补写前快照；停滞段 usage 补账（两 provider）；max_tokens 截断 tool_use 源头配对占位；redact Bearer+JWT；glob 敏感过滤对齐 grep；distill JSON 先整体 parse（body 含代码围栏不再截断起草）；GIF magic 第 4 字节+IPv6 localnet fe80::/10 全段。

## 五、挂账与后续

**2026-09-04 清偿批（886c19e+0c4d467，已推送）**：上文挂账中的可单方面修复项已全部清偿——webfetch 流式截断（边读边截+cancel，+2 测试锁）/bash absent 兜底 v2（实例化基线：begin 一次 git status 拍写前快照记 seq 锚+end 差集补录进锚点；实施审阅 3×P1 全修：子代理断链/错点误删/单槽污染）/web resync 运行态对账（setSessionRunning+session/list 权威）/websearch 熔断补+超时文案/pdf needle 无空格/wall<240→并发哨兵/tasks.test throw/stallWatchdog 余量 8-12×/invite+findBySecret 常量时/blockedCommands 口径/AGENTS.md 状态行/loop-guard 假钟未做（挂账保留）。**剩余挂账（需用户参与或属新批次）**：

bash absent 兜底根治（快照后二次 git status 差集补拍——后台 npm install 类新建文件回退仍依赖下一轮快照）；webfetch 响应体流式截断（当前全量读入后才查 512KB 上限=内存 DoS 面）；blockedCommands「硬拒」承诺口径降为「常见形态拦截」（shell 元字符固有绕过）；MCP 项目级 env 展开白名单（批准卡摘要已缓解外传可见性）；/skill-create 全流程测试（唯一真裸奔面）；loop-guard 等待豁免假钟（mock 时间推进失真）；wall<240 墙钟断言改并发计数（flake 家族）；tasks.test `if(!r.ok) return` 改 throw；「已知 flake」归因须附 stash 对照（提交规范）。

**既有挂账重申**：会话列表虚拟化/微信真机 G-R4/mermaid 方案拍板/第一轮九项 P2（web 深链确认弹窗/web 运行态对账/finalTextCap 死分支/degradeToLocal 单调保护/stallWatchdog 假 timer/wechat mock 真机锚等）。

**并行会话协作记录**：本轮期间另一环境合入 34 笔（归属 meta 化 e268776/等待根治/同名折叠/429 分流/model 切换修复等）——主干曾带 4 红（meta 化后旧字符串形态测试没跟上）本轮顺手适配；hook context 双重前缀为其归属根治 P2 遗留，本轮修复+断言锁。
