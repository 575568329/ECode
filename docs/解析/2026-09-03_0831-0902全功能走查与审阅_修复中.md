# 2026-08-31~09-02 全功能走查与四角色审阅

> 状态：修复中（走查+修复批已交付，审阅进行中）
> 范围：2026-08-31 00:00 → 09-03（HEAD=4451f7c+走查修复批 02c1185），共 86 笔提交
> 方法：四层走查（单测基础门 / pty 探针真机 / serve 协议层 / web 真浏览器）→ 四角色并行审阅 → 修复 → 回归

---

## 一、走查范围盘点

86 笔提交归四大功能面：

| 功能面 | 代表提交 | 内容 |
|--------|---------|------|
| 输入体验批（08-31） | 190386c/74355a2/87a6382/e604110/2a8364d | 双击 Esc 清空草稿（armed 确认式）/粘贴 token 化 [Pasted #N]/Ctrl+C 全局兜底九面板/Ctrl+T 时间线同构/backToList |
| T 线 TUI 附着 daemon（08-31~09-01） | 54aee4e→e93b24e→92b388c→49780b4→98b067e→ddc342a | T1 协议面宿主权威化/T2 数据读面命令化/T3+T4 入口 daemon 化+MultiTransport/T5 StatusBar daemon 段/审阅修复 P0×3/轮中失联自愈/resurrect 入口验活 |
| R 线配对与远程接入（09-01） | bb73d88→40aa67b→5018d5f→ae1b469→3a12eaa→58066b3→5c5f644→a490651 | R1 设备注册表+per-device token/R2 完整 relay/R3 E2EE 全链路/R4 微信 ClawBot/R5 web 设备面板+多机/四角色审阅修复 P0×2+P1×8/实机部署+浏览器三修//devices 面板化 |
| 活动流+杂项（09-02） | a3fe9d9→11dd1eb→f334b97→c8898cd→2bae057→907ac7c→fbb6503→198ed5f→0b93e29→4451f7c | B1-B6 思考链路+timelineReducer+TimelineView+ActivityBar+web 对等/R1-R4 审阅修复/挂账清扫+PTY 验证/OpenAI·智谱思考打通/Ctrl+C 立即停三重硬化/signal 传参翻案/STREAM_STALL 看门狗/沙箱会话级/归档人专属/纠偏审查接线/审查器附注卡/状态栏精简+内存可见 |

另含 todo 面板系列（✓ 符号 3e7e311/全完成自动收起 6801791/3 项上限+完成排尾 a783a66）与提交即锁死（750e06c）。

## 二、四层走查结果

### 2.1 基础门

| 门 | 结果 |
|----|------|
| vitest 全量 | **走查前 9 红**（4 文件）→ 修复后 **1845 过 + 3 skip 零红** |
| tsc --noEmit | 净 |
| web vitest | 62/62 |
| web build | 452.79 KB（gzip 140.34 KB） |

9 个红测试全是**测试滞后于拍板的行为变更**，非产品回归：

| 测试 | 滞后于 | 拍板内容 |
|------|--------|---------|
| tests/tui/formatAgentLine.test.ts（1） | 3af0e5d | 子代理 transcript 正文图标 ●→◆（与对话栅格 D3 二次翻案同款） |
| tests/tui/viewport.test.ts（1） | 2bae057 | 退化线不含条件段（todo 面板曾把 30 行终端抬进 degraded=时间线整体不渲染）——22+双条改为不退化、content 收缩 |
| tests/tui/ToolGroupView.test.tsx（5） | c8898cd/bebd861 | ToolGroupView 薄壳化：expanded/onToggle/done props 退役，折叠标记只随输出行；maxTools 成为新折叠面；D15 动态区副作用完成后自动展开受 expandCap 封顶 |
| tests/tui/ToolExpandTimeMark.test.tsx（2） | 同上 | 只读工具 Static 恒收起——时间标记要挂副作用工具的 ▾ 输出行才可见 |

修复：更新断言锁定新行为（含新增 ToolLine mode='dynamic'/'static' 契约四用例、maxTools 三用例），提交 02c1185。

### 2.2 pty 探针层（17 个真机探针全绿）

| 探针 | 结果 | 备注 |
|------|------|------|
| pty-input-clear-probe | 全过 | 双击 Esc 清空/armed 解除/超时/粘贴 token |
| pty-doublesc-probe | 全过 | 双击 Esc rewind+@ 下拉守卫 |
| pty-ctrlc-matrix-probe | 全过 | Ctrl+C 五态矩阵 |
| pty-grid-check | 四查过 | ❯ 顶格/⌕ 工具图标/◆ 正文/排队行 |
| runonce-approval-probe | 双路径过 | --yes 放行/ask fail-closed |
| pty-sensitive-cards-probe | 全过 | sensitive 卡串行化 |
| pty-ctrlt-probe | 全过 | busy 中开执行时间线 |
| dogfood-e2e-input | 18/18 | 粘贴 token 数据级断言/时间线 ❯/双击 Esc 后草稿不外发 |
| pty-todopanel-probe | 4/4 | **断言滞后修复**：[x]/[->]/[ ] → ✓/▸/○（3e7e311） |
| tui-devices-probe | 3/3 | 面板开/配对生成/QR 块字符+自动吊销（需 daemon 在跑——环境前置非代码问题） |
| pty-attach-probe | 10/10 | **A7 探针修复**：伪造注册文件版本已骗不过 P1-7（版本比对以 health.version 为准）——改为假 daemon /api/health 回假版本 |
| pty-overscreen-probe | 全过 | **环境修复**：真 pty 全程无 ESC[3J；曾因 TUI 自动附着 daemon 打真 GLM |
| pty-wedge-probe | 全过 | 4 轮含 80-delta 长流未复现楔死 |
| pty-interrupt-latency-probe | 全过 | **探针三修**：双位置读日志（daemon 用户级 logs/serve/ 子目录）+t0 过滤防旧轮混样+杀泄漏 daemon；实测见 §2.4 |
| pty-mem-probe | 过 | 进程树 RSS 稳定（一轮对话前后 324.6→324.9MB） |
| pty-restart-probe | 5/5 | **断言滞后修复**：状态栏 `ECode · … · 0 tok` → `· T0 ·`（32ce80e 精简批） |
| pty-altscreen / pty-interject-check | 全过 | alt 序列/插话留痕 |

**探针层系统性发现（本走查最有价值的产出之一）**：T 线后 TUI「无注册即自动拉起 daemon 并附着」成为默认路径，所有 mock LLM 的 pty 探针若不设 `ECODE_FORCE_EMBEDDED=1`，轮次跑进 daemon 进程（无 mock env）→ 请求打真 GLM 端点（烧配额）且探针指纹串（如 END-OF-R）永不到达=假失败。**隔离 USERPROFILE 也不够**——沙盒 home 无注册照样 spawn（pty-interrupt-latency-probe 实证：日志显示 daemon spawn→attached，轮次全部跑在 daemon）。已修 4 个探针（overscreen/wedge/semialive/keys-realtest），此后新增 mock 探针必须带此 env（已写入探针注释+memory）。

另一探针卫生问题：探针 rmSync 沙盒 home 前不杀自拉 daemon → 泄漏 detached 进程（本次走查抓到并清理了一只，pid 7492）。interrupt 探针已改为「先读沙盒 server.json 的 pid 杀进程再删目录」。

### 2.3 serve 协议层（对真 daemon 实测）

daemon：本机 `tsx src/cli/index.ts serve`（端口 51199），用户真实 config（relay=wss://nodetime.cn/win-dev-01）。

| 检查 | 结果 |
|------|------|
| GET /api/health | ok+id+version+name |
| GET /api/relay/status | **connected:true，generation 7，租约活**，phoneConnectUrl 有效 |
| relay-e2e-probe --remote（公网 6 断言） | ①控制腿 host-hello-ack ②配对 offer 带 relay 段+钉公钥 ③E2EE 握手 ④cmd 往返 ⑤事件透传 ⑥rebind 后数据腿可用——**6/6** |
| 设备回路 | POST /api/devices 创建（offer 含 secret/daemonPubKeyB64/projects/relay 段+持久 invite）→ 列表可见 → revoke 后列表消失（走查中双重创建虚惊一次，均已清理，终态=用户原有 2 台设备） |
| 归档门（人专属） | /cmd 发 session/archive → **403 HUMAN_ONLY_COMMAND**（拦截在信封解析前，无冷会话拉起副作用）；POST /api/archive 归档→meta 落盘→恢复往返全通 |
| timeline 帧（真 LLM 轮次） | 订阅 mux + prompt 触发 todo 工具轮：**thinking×31 帧（GLM 思考链路真机实证）+ thinking/ended 配对 + item/started·executing·completed 带 digest + itemId 同源配对 + turn 完成**全过 |
| /api/stats | 四维+命中率+三维度分布正常（byDay 缺 09-01/09-02 行**不是 bug**：本机那两日无用户会话，UTC 晚间记录归入本地次日行——已用磁盘 stats 记录逐日核对） |

### 2.4 实测基线数据（Ctrl+C 立即停链路）

```
按键→宿主收到     15ms
宿主收到→abort 浮现 10ms
abort 浮现→轮收尾    1ms
按键→UI 回空闲（墙钟）109ms
```

fbb6503/198ed5f/0b93e29 三批「立即停」硬化在真机上的最终数字——用户感知中断延迟 ≈109ms。

### 2.5 web 真浏览器（Playwright MCP，Chromium）

| 检查 | 结果 |
|------|------|
| 连接/token 输入 | 过（错 token 401 后正确重试） |
| 项目侧栏+会话列表 | 5 项目全显；时间分组（今日/过去 7 天）+搜索框+已归档入口+逐条重命名/归档 |
| 设备面板 | 配对表单/relay 状态联动（已连接 gen 7）/设备列表/吊销（confirm 弹窗）/CLI 指引；**生成配对→二维码本地渲染可扫**（视觉核验：定位角完整、尺寸充分）+深链（含 relay 段+钉公钥，base64url 解码核对） |
| 统计面板 | 四维卡片+按模型/按天/按项目三组分布渲染完整 |
| 对话页 | 用户消息（复制/重发）+assistant 回复（复制）+ModelChip（glm-5.3-flash）+composer 常驻 |
| 控制台 | 除首个错 token 的 401 外零错误 |

已知挂账（非本窗口回归）：会话列表无虚拟化（数百会话全渲染，aria 树巨大——W 线 virtua 已拍板待实施）。

## 三、走查结论

**0831-0902 窗口产品码零回归。** 全部 16 项发现均为测试/探针资产滞后于拍板变更或环境假设过时，无一处需要改产品码。走查修复批（9 测试+7 探针）提交 02c1185，全量门 1845+3skip 零红。

---

## 四、四角色审阅（进行中，待回填）

- 架构席：审 T/R 线生命周期与状态机、分层边界、并发模型、timeline 不变式
- 开发席：审边界条件、资源泄漏、竞态、错误处理、数值时序
- 安全席：审认证授权、E2EE 健全性、注入越界、中继信任模型、微信网关
- 测试席：审假覆盖、mock 形态失真、flake、覆盖缺口、探针可打穿性

（审阅报告与修复记录见 §五/§六，回填后本文档转已完成）

## 五、审阅修复（待回填）

## 六、挂账与后续（待回填）
