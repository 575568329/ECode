# ECode 开发规划

> **本项目是开源产品**，用 TypeScript + Node.js 手写一个对标 Claude Code / opencode 的 AI coding agent。
> **拒绝框架黑盒**：agent loop、Provider 适配、上下文管理都自己写，可以用工具库，但核心逻辑不假手于框架。
>
> **定位变更**（2026-07-30）：项目从"学习项目（理解优先、YAGNI）"转型为"开源产品（交付优先）"。质量标准随之提高——测试覆盖、错误恢复、权限、文档都是**必须项**，不再"用到再加"。本文档已按交付定位重写；技术骨架（认知盲区清单 / 分层架构 / 选型）保持不变。

---

## 一、项目定位（North Star）

用 TypeScript + Node.js **手写**一个生产可用、可开源的 AI coding agent（对标 Claude Code / opencode），**拒绝框架黑盒**，自主掌控 agent 的每一个环节。

**三个不可动摇的原则：**
1. **交付优先** — 每个模块都要稳定、完整、可维护。能跑只是起点，错误恢复 / 边界处理 / 测试覆盖是硬性要求，不是"以后再说"。
2. **手写核心** — agent loop、Provider 适配、上下文管理都自己写。可以用工具库（如 fast-glob），但核心逻辑不假手于框架。
3. **测试驱动** — 核心逻辑走 TDD（Red → Green → Refactor），每个功能有测试兜底，重构有安全网。

> 理解仍是手段——手写核心本身就要求吃透原理——但不再是"第一目的"。目标是交付一个真正能日常使用、能开源让别人用的产品。

---

## 二、已确定的产品决策（8 个）

| # | 决策点 | 选择 | 理由 |
|---|--------|------|------|
| 1 | 定位 | 生产可用的开源工具 | 能解决实际问题才有价值 |
| 2 | 技术路线 | 手写 loop + 逐步模块化 | 自主可控，长期可维护 |
| 3 | 模型策略 | 多模型 + 配置驱动 + CLI 切换 | 兼容多方模型是核心能力 |
| 4 | Provider | 2 个核心（Claude + OpenAI-compatible） | 各家兼容 OpenAI/Anthropic 协议，无需为每个模型写代码 |
| 5 | 预置模型 | GLM-5.2 + DeepSeek（其他用户自配，Gemini 暂不支持） | 参考 cc-switch 思路，配置即扩展 |
| 6 | 工具集 | 实用 MVP（read/write/edit/list/grep/glob/bash/ask_user） | 覆盖真实开发场景 |
| 7 | 交互 | 沉浸式 REPL + slash 命令 + 流式渲染 + Session 持久化 + 上下文自动整理 | 对标 Claude Code 核心产品力，详见 M3.5 |
| 8 | 权限 + 输出 | 分级权限系统 + 混合流式（文本流式/工具非流式） | 安全性与体验并重 |

**关键技术调研结论：**
- **Token 计数无通用 tokenizer**，跨模型差异 ±30%（中文更大）。方案：**仿 Claude Code 用 `length/4` 粗估**（零依赖；Claude Code 官方即如此，`claude-code-main/services/tokenEstimation.ts:203-208`；ai-tokenizer 对 GLM/DeepSeek 无原生 encoding，2026-08-03 源码核查后弃用），Anthropic 侧可选 count_tokens API 校准，阈值设 80%。
- **OpenAI-compatible ≠ tokenizer 兼容** — GLM/DeepSeek 别盲目套 tiktoken。
- **模型工具支持度要探测不能假设**（continue.dev 的 `PROVIDER_TOOL_SUPPORT` 模式）。

---

## 三、认知盲区清单（调研发现的"我不知道我不知道"）

> 这是早期调研最大的价值。以下模块是初版规划遗漏的，但真实生产 agent 缺了会直接不可用/不安全。**按交付优先级排期。**

### P0 — 缺了 agent 直接不可用/不安全
- **P0-1 System Prompt 工程** — 分层拼装（行为约束+工具说明+项目上下文），非一坨字符串。Claude Code 系统提示约 4200 token，恒在最前、永不进 history。
- **P0-2 文件编辑正确性** — 难点不是替换，是**匹配失败后的恢复**：把文件真实行回喂给 LLM 重试。aider 数据：编辑格式选对能把重构得分从 20% 拉到 61%。
- **P0-3 Tool result 截断** — 一次大文件读取就能撑爆上下文导致死循环。基准：Bash 30000 字符、Read 2000 行/单行 2000 字符。
- **P0-4 错误恢复** — API 退避（区分 429/529/500/504）+ 工具失败回喂 `is_error` + 死循环防护（迭代上限 25-50 + 重复动作检测）。
- **P0-5 强制后置验证** — 别信模型自报"完成"（22.6% 失败源于此），强制跑 build/test/lint。

### P1 — "能用"到"好用/可维护"
- **P1-1 项目记忆（CLAUDE.md 类）** — 启动注入 + 压缩后重注入。
- **P1-2 Repo Map** — tree-sitter + 图排序表示大代码库（v1 用简化版：文件树+关键签名）。
- **P1-3 中断机制** — Ctrl+C 用 AbortController，难在中断后保持 tool_use/tool_result 配对。
- **P1-4 并行工具调用** — 只读工具 Promise.all 并发。
- **P1-5 成本追踪** — 用 API 返回的 usage，别用本地估算。
- **P1-6 可观测性** — Run>Step>Message 三层结构化日志，debug 生命线。
- **P1-7 可测试性** — Provider 层依赖注入 + LLM VCR（record/replay）。temperature=0 测不出确定性。
- **P1-8 交互式 CLI 体验** — 对标 Claude Code 核心产品力。当前是 one-shot（跑完退出），缺沉浸式 REPL / slash 命令 / 流式渲染 / 中断 / 富文本 / TUI。详见 M3.5。

### P2 — 预留接口，v2 实现
- **P2-1 Subagent** — 独立上下文的子 agent（架构预留"loop 可递归 fresh context"）。
- **P2-2 MCP client** — 工具注册表预留"外部工具源"抽象。
- **P2-3 Hooks / P2-4 Git 自动化**

---

## 四、推荐分层架构

```
┌─────────────────────────────────────────────┐
│  UI / Entry 层  (CLI/REPL, 可选 HTTP server)  │
├─────────────────────────────────────────────┤
│  Agent Core 层                                │
│   - AgentLoop (状态机: gather→act→verify)      │
│   - SessionManager (state + 持久化)            │
│   - ContextManager (compaction/prune/token)   │
│   - Orchestrator (subagent, P2 预留空壳)       │
├─────────────────────────────────────────────┤
│  Tool 层                                       │
│   - ToolRegistry (注册/schema/能力探测)         │
│   - ToolExecutor (并发 + 截断 + is_error 回喂)  │
│   - PermissionGate (分级权限)                  │
│   - 各工具: read/write/edit/grep/bash/...      │
├─────────────────────────────────────────────┤
│  Provider 层                                   │
│   - ProviderAbstraction (统一 stream 接口)      │
│   - ProviderTransform (消息规范化)             │
│   - ClaudeProvider / OpenAIProvider           │
│   - RetryPolicy (指数退避 + jitter)            │
└─────────────────────────────────────────────┘
   横切: Observability │ CostTracker │ SystemPromptBuilder │ RepoMap
```

**参考项目分工：**
- **Claude Code** — context 管理、subagent 隔离、截断策略的数值基准
- **opencode** — Provider 抽象（transform 层）、Session/Message/Part 状态模型、目录分包
- **aider** — repo map、编辑格式正确性（最硬核两块）
- **cline** — Plan/Act 双模式、per-mode 模型切换
- **continue.dev** — 工具能力探测（别假设所有模型支持 tool）

---

## 五、里程碑规划（交付目标驱动）

> 每个里程碑 = 一组紧密相关的交付目标。**先定验收标准（什么算"完成"），再 TDD 实现，最后用验收标准检验。**
> 每个里程碑内部走 TDD：定义验收 → 写测试 → 实现 → 重构 → 满足验收。

### 🎯 M1：Agent 的心脏 —— 最小可运行 loop（**骨架已完成，补全中**）
**交付目标：**
- LLM 工具调用完整协议跑通：`tool_use` / `tool_result` / `stop_reason` 往返
- agent 是 `while` 循环，终止条件正确（已修复终止判断 bug）
- messages 累积（append 不重建）、tool_use/tool_result id 配对

**当前进度：**
- ✅ loop 能跑、`read_file` + `bash` 两工具、运行时日志（主摘要 + raw 旁路双层）
- ✅ 终止判断 bug 已修（误报"达到最大迭代"）
- ⬜ 补全到"真能用"（见下）

**M1 补全清单（真能用标准）：**
1. 测试基础设施 + 纯函数测试（truncate / executeTool 分发）
2. System Prompt 分层（P0-1）
3. `edit_file` 工具 + 匹配失败恢复（P0-2）
4. `grep` + `glob` 搜索工具
5. 错误恢复：API 重试（429/529/500）+ 死循环防护加强（P0-4）
6. 后置验证：强制 build/test，不信模型自报完成（P0-5）

**验收：**
- 手画一次"读文件 → 编辑 → 验证"任务的完整消息流转图
- 单元测试覆盖纯函数，agent loop 有 Mock SDK 集成测试
- 能稳定完成"改一处代码并跑通测试"的真实任务

---

### 🎯 M2：多模型适配 —— Provider 抽象层
**交付目标：**
- 抹平 Claude `tool_use` 与 OpenAI `function_call` 协议差异
- 一套代码接多个模型，加新模型只改配置不改代码
- 模型能力探测（不假设所有模型支持 tool）

**技术产出：**
- `ModelProvider` 统一接口 + `ProviderTransform` 规范化层
- ClaudeProvider + OpenAIProvider 两个实现
- 配置系统：预置 GLM-5.2 / DeepSeek，用户可自配
- CLI 切换：`ecode --model deepseek "..."`、`ecode --list-models`

**验收：**
- 同一任务在 Claude 和 OpenAI 协议下都能完成
- 新增一个模型仅需配置，零代码改动（有测试证明）

---

### 🎯 M3："记不住"问题 —— 上下文管理 + Session（数据层）

> 📄 **详细方案**:[M3-实施方案](./M3-实施方案.md)(怎么做:5 Phase/接口/文件清单)·[M3-方案解析](./M3-方案解析.md)(为什么:压缩原理/踩坑)。**两文档待审阅,确认后进入 TDD。**

> **前置准备（M2 → M3 之间的地基工作，已部分完成 ✅）**
>
> 来源:对比 Vercel AI SDK 后提炼的「立即可做」借鉴点,详见 `docs/借鉴Vercel-AI-SDK对比报告.md`。
> 这些是 M3 正式开发前的一次性投资——不做则 M3 压缩/流式代码会更脆弱或需回头补。
>
> | # | 借鉴点 | 状态 | 价值 |
> |---|--------|------|------|
> | P-1 | ① 工具 execute try/catch + 参数校验(loop 不炸底线) | ✅ 已完成 | agent 健壮性底线 |
> | P-2 | ⑤ warnings + stopReason 双字段(信息保真) | ✅ 已完成 | M3 压缩触发判断不再误判 |
> | P-3 | ⑧ exhaustive check + ⑨ safeParseJSON 不吞错 | ✅ 已完成 | 防漏 + 保留模型原始输出 |
>
> **已识别但推迟到 M3 之中的借鉴点:**
> - ② 并行工具执行(M3 实现过程中自然带)
> - ④ 重试 + 错误体系升级(M3 流式/复杂错误恢复前)
> - ⑩ 流式接口签名占位(M3 实现 stream() 前先定好)
> - ③ AbortController 可中断(M3 流式时建好,M3.5 中断直接用)
> - ⑥ 内部格式 v2(providerOptions + tool_result 判别联合)→ 建议纳入 M3 启动第一步

**交付目标：**
- 长对话不"越聊越傻"：token 计数 + 80% 阈值触发 LLM 摘要压缩
- Tool result 截断防撑爆（Bash 30000 字符 / Read 2000 行基准）
- Session 持久化（数据层）+ `--continue` 续接（REPL 交互体验见 M3.5）

**技术产出：**
- `length/4` token 粗估（零依赖，仿 Claude Code）+ 分层 token 计数
- ContextManager：压缩时保护 tool_use 配对不被破坏
- Session 持久化（JSON，数据层）

**验收：**
- 一次上下文压缩的完整过程可解释（保留什么、压缩什么、如何不破坏 tool 链）
- 超长会话能持续工作不崩

---

### 🎯 M3.5：交互式 CLI 体验 —— 让 agent 能和人持续对话
> 对标 Claude Code 的核心产品力：不是"跑完就退出"的脚本，而是一个沉浸式终端。
> **与 M3 的边界**：M3 管"会话数据层"（Session 存储 / 上下文压缩）；M3.5 管"交互呈现层"（人怎么和 agent 对话）。**依赖 M3 的 Session / 流式能力。**

**交付目标（MVP —— 能对标的最低门槛）：**
- 沉浸式 REPL：提示符（如 `>`）持续对话，跑完不退出，可追问
- 流式渲染：LLM 回复逐字输出到终端（接 M3 流式数据）
- slash 命令：至少 `/help` `/clear` `/model` `/exit`
- 中断：Esc / Ctrl+C 中断当前生成（AbortController 最小版）

**交付目标（进阶 —— 后续迭代）：**
- 富文本渲染：markdown / 代码语法高亮 / diff 着色
- 工具调用可视化：实时展示工具在干什么、可折叠
- 多行输入 / 历史记录 ↑↓ / Tab 补全
- TUI 分区布局（对标 Claude Code 的 ink）

**技术选型（已定，详见 `M3.5-技术选型与理由.md`）：**
- UI：**Ink + React**（官方 npm Ink 6，不 fork；对标 CCode）。OpenTUI 出局（Zig native，Node 22 走不通）。
- 推进：**两段式**——① agent core 事件化（UI 无关）→ ② Ink REPL
- 富文本：`marked` / `cli-highlight`
- **权限档 A（CCode 式极简）+ CLAUDE.md 兼容读取已并入本里程碑**（原独立 M3.6 取消，M4 权限主体前移至此）
> 决策依据：`M3.5-CLI工具调研对比.md`（四工具调研）+ `M3.5-技术选型与理由.md`。

**验收：**
- 能持续对话、追问，不退出
- 流式逐字输出肉眼可见
- slash 命令可用
- 能中断一次跑飞的生成

---

### 🎯 M4："信任问题" —— 安全进阶 + 正确性 + 健壮性
> ⚠️ **范围调整（2026-08）**：权限档 A（dangerous 二元 + 弹窗 + allow 白名单）+ CLAUDE.md 兼容读取**已并入 M3.5**。本里程碑聚焦**权限进阶 + 正确性闭环**。

**交付目标：**
- 权限档 B/C（真兼容 `.claude/settings.json`、四档模式 default/acceptEdits/plan/bypass、deny/ask 规则、Bash 完整命令解析、yes-always 持久化）
- edit 工具"匹配失败回喂真实行 + 重试"闭环
- 错误恢复三条路径完整（API 退避 / 工具回喂 / 死循环防护）
- 强制后置验证
- 混合流式输出

**验收：**
- edit 匹配失败时 agent 能自我纠正
- 权限系统能拦截 `rm -rf` 类危险操作
- 有真实任务的端到端测试

---

### 🎯 M5+：进阶（可观测性 / Repo Map / Subagent）
前四个里程碑跑通、工具真正能日常使用后，按使用痛点迭代：
- 并行工具调用、中断机制、成本追踪、结构化日志、DI+VCR 测试
- Repo Map（v1 简化版 → v2 tree-sitter）
- Subagent、MCP client、Hooks

---

## 六、技术栈与工程约定

- **语言**：TypeScript，strict 模式，禁止 `any`（不得已用 `unknown`）
- **运行时**：Node.js（多端运行，暂不用 Bun 以降低环境复杂度）
- **测试**：Vitest，核心逻辑 TDD，目标高覆盖率
- **提交**：Conventional Commits（feat/fix/refactor/docs/chore）
- **关键依赖**：`@anthropic-ai/sdk`、`openai`（M3 token 计数用内置 `length/4`，无新增依赖）
- **配置**：`~/.ecode/config.json`（模型配置）+ 项目根 `ECODE.md`（项目记忆，类比 CLAUDE.md）
- **质量门禁**：提交前 `npm run build`（tsc strict）+ `npm test` 全绿

---

## 七、工作节奏

1. 每个里程碑/子任务开始：**先更新文档与方案**（验收标准、接口契约），对齐后再写代码
2. 方案确认 → TDD 实现（Red → Green → Refactor）
3. 实现完成 → 用验收标准检验 + 跑 build/test
4. 每完成一个里程碑更新本文档进度勾选 + `docs/memory/`
5. 重要决策与踩坑沉淀到 `docs/memory/decisions.md` / `debugging.md`

---

**创建日期**：2026-07-21
**最近更新**：2026-07-30（从学习项目转型为开源产品，重写定位/原则/里程碑/工作节奏）
**当前状态**：M1 补全中（骨架已完成，补到"真能用"）
**下一步**：M1 补全清单①测试基础设施
