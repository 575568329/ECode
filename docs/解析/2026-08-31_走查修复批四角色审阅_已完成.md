# 2026-08-31 走查修复批四角色审阅（已完成）

> 对象：走查修复批三提交 `4ad4211`（D1+D9 host）/ `d145b27`（D2+D4+D7+探针）/ `f4fe593`（文档），diff 范围 `006cd94..f4fe593`。
> 形式：四角色并行只读审阅（架构师/资深开发/安全/测试），结论交叉比对后由监看方裁定处置。

## 一、总裁决

**架构师/资深开发/安全三席「可合入」（无 P0/P1），测试席「需补测后合入」**——唯一 P1 是 D1 的改动点（runOnce 接线）无自动化钉（契约测试钉的是 broker 语义层，回退接线不红）。全部实质发现已处置：2 项 P1（含补钉）+ 5 项 P2 当批修讫，其余挂账。

## 二、审阅确认项（抽核心）

- D1 修复语义完整：canAnswer 两侧（InMemoryChannel/mux/web）语义一致；sensitive 无 auto-approve 分支，`--yes` 不豁免承诺保持；runOnce 被动订阅**不减帧**（passive 照收全量事件）；sandbox/set 提档在单次模式得 `NEED_CLIENT` 拒绝（与 D6 fail-closed 口径一致）。
- D9 双保险正确：cancelPending/timeout/respond 三方同步互斥无双发；enqueueConfirm 无死锁环（task 只 await broker 应答）、实例级不串台；中断收敛方向是拒绝不是放行，不构成审批绕过；串行化不损多端应答（只推迟发布时点，先答先得/claim/replayPending 不变）。
- D2 端口 read() 穿透子组件 ref 取活值，父组件重渲染与否无关——端口模式本义；Esc 三态语义无破坏。
- D4 实际修复了既有跨项目泄漏：旧代码把全部无主会话映射到 daemon 启动目录所在项目（ownsSession 随之放行）；修复后 ownsSession/session/list/restore 同源收口，无「列表排除但 restore 可读」不一致。
- 测试钉力经回退推理核验：串行化例双重断言真红、中断收敛例双保险真红、D4 例恒等式构造与 worker cwd 取值无关。**无虚报。**

## 三、发现与处置

| # | 级别 | 发现（来源） | 处置 |
|---|------|-------------|------|
| 1 | P1 | D1 调用点无自动化钉：回退 runOnce 接线，既有测试不红（测试席） | **当批补** `scripts/runonce-approval-probe.cjs`——print 模式子进程 + mock SSE，断言 `--yes` 放行执行/ask fail-closed 拒绝喂回/双路径限时退出，6/6 过；旧代码两路径挂死必红 |
| 2 | P2 | 子代理两处 confirm（session.ts mountedBridge/session.confirmTool）未透传当轮 signal——中断后子代理卡悬空 900s 成僵尸卡（三席交叉确认） | **当批修**：两处补传 `this.abort.signal` |
| 3 | P2 | sandbox/set 提档卡与 mcp-permission 卡不入串行队列——TUI 单槽顶掉残余路径（架构师+安全） | **当批修**：两处入 enqueueConfirm + 接 signal（broker.permission 增加 signal 参） |
| 4 | P2 | listMetas 空串 cwd 仍会经 realpathSync('') 误命中（资深开发+架构师） | **当批修**：真值判定拉齐 collectProjectCwds 口径 |
| 5 | P2 | enqueueConfirm `then(task, task)` 第二参为死分支，注释误导（资深开发） | **当批修**：改 `then(task)`，注释改述真实保障（链尾吞错重赋值） |
| 6 | P2 | 「排队卡+中断→第二张永不落卡」交叉竞态无测试（测试席） | **当批补** approval-serialize 例3：中断后 whenIdle 限时收敛 + requested/resolved 零孤儿 + 轮不进第二轮；顺带修 MockProvider 保真（尊重 signal） |
| 7 | P2 | 例2 race 输家 Promise 潜在 unhandledRejection（测试席卫生项） | **当批修**：clearTimeout 收口 |
| 8 | P2 | doublesc 探针缺 @ 下拉守卫分支（read()===true）与失败现场（测试席） | **当批补**：E6（@ 开着双击不误开）/E7（关下拉清字符后恢复），失败 dump 末 30 行，7/7 过 |
| 9 | P3 | HttpTransport.subscribe 无 canAnswer 形参（零实例化，陈旧工件）；subagent.ts makeConfirmQueue 文档残留；Folded 测试过期 Ctrl+O hint prop | 残留两处当批清；HttpTransport 挂账（复活前必修） |

## 四、挂账（范围外既有，非本批回归，随 R 线/按需）

- 飞书 gateway 为唯一客户端时审批 fail-closed 取消（被动订阅不撑起应答面）——功能缺口非安全缺口；
- `approval/respond` decision 无运行时校验，垃圾值按 once 放行（持凭据者同权，不提权）——R 线协议收口判拒；
- multi.ts 冷 restore 路径归属校验前移（幻影载入 + briefs 标题跨项目可见）；
- `approvalTimeoutMs=0` 时串行队列可被无界挂起（config 注释已有 0=不限口径，使用警示随文档）；
- 探针临时目录不清理（%TEMP% 残留，informational）。

## 五、处置后全量门

vitest **1643 passed + 3 skipped**（149 文件，净增例3）、tsc 净、dist 重建、探针复跑：runonce-approval **6/6**、doublesc **7/7**、sensitive-cards **3/3**、wedge 4 轮全过。

## 六、提交

- `审阅修复批` fix(host)：P2 ①②③④⑤（子代理 signal/提档与权限卡入队/空串 cwd/死分支）
- `审阅补测批` test(scripts)：P1 补钉 + 例3 + race 卫生 + doublesc 扩展 + P3 残留清理
- 本文档
