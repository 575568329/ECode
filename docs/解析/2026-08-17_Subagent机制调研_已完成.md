# Subagent 机制调研（子代理的调用纪律 / 硬限制 / 提示词模板 / 运行时展示）

> 2026-08-17，subagent 立项前调研（观察区项：M7 §H2 SubagentStart/Stop 事件 + plugin `agents` 组件）。起因用户三问：怎么指派、怎么防止主 LLM 滥用、提示词模板怎么让子代理准确完成任务、运行时能否展开查看且不破坏活动区。本地一手源码（claude-code-main / opencode；codex 无 subagent、aider/pi 无此功能）。**本文只记事实与 ECode 视角的落点推演，不是实施方案**——方案待立项时另立文档。

---

## 1. ECode 视角的核心结论（先说落点）

**1. subagent 对 ECode 是零新基建的能力。** AgentLoop 是心脏且无状态复入——子代理 = 宿主工具的 execute 里嵌一个新 `runLoop`（全新 messages、同 provider/config/ToolRegistry），跑完把最终 assistant 文本作为 tool_result 返回。两个 Registry 分支面的既定优势：`task` 就是又一个 `register()` 的普通工具，心脏零改动。子代理与父对话的唯一通道 = 入参 prompt + 返回文本，上下文天然隔离。

**2. 防"主 LLM 什么都丢给 subagent"，业界的答案不是硬限制，是工具 description 的反例教学。** 两家都在 description 里写"何时**不要**用"清单（读指定文件用 Read / 找类定义用 Grep / 2-3 个文件内搜索用 Read / 与 agent 定义无关的任务），外加"已委派的活不要自己重复做"。没有频率限制、没有成本提示、没有默认禁用。**而且两家都主动鼓励并发调用**（"Launch multiple agents concurrently whenever possible"）——业界共识是**防错用（该用轻工具时别开子代理），不防多用**。ECode 沿此方向：调用纪律写在 task 工具 description，不建开关类基建（对齐"Agent 优先不产品化"）。

**3. 真正的硬限制只有一条被两家共同实施：递归一层封顶。** CC 在工具过滤层对子代理**移除 Agent 工具**（硬编码）；opencode 用深度计数（`subagent_depth` 默认 1）+ 权限派生（子代理无显式 allow 则自动补 `task: deny`）。并发与迭代在两家都**没有**硬上限（CC 的 maxTurns 是 agent 定义可选项，无全局默认；opencode 无独立限制）。

**4. 提示词是两层模板：入口工具 description 教主 LLM 怎么写任务书，子代理 system prompt 定角色与返回契约。** 关键细节（ECode 模板要抄的）：
- 主侧："像给刚进门的聪明同事 briefing——它没看过这段对话"；**反"委派理解"**："不要写'根据你的发现修 bug'——那是把综合推给子代理；要写能证明你自己理解了的 prompt（文件路径/行号/具体改什么）"；明确告知"写代码还是只调研"。
- 子侧：擅长领域声明 + 行为准则 + **返回契约**（"完成时回复简明报告，调用者会转述给用户，只要要点"）；只读型子代理配一整段**只读宣言**（"你没有编辑工具，尝试会失败"）。

**5. 运行时展示的业解：折叠一行 + 进度流式渲染 + 终端高度自适应降级 + 全量事后可看。** CC 运行中把子代理每条 tool 消息压成一行实时滚；**视口不够时自动切超压缩摘要**（行数预算 = 工具数×每工具预估行数 + 缓冲 > 终端行数时降级）——这正是"展开查看不破坏活动区"的现成答案，与 ECode 防超屏体系（动态区任何组件不得超视口）同一约束。完整 transcript 落盘事后看。

**6. 返回契约：最终 assistant 文本一条。** CC 硬截断 100k 字符；opencode XML 包裹（`<task id state>`）且 task_id 可续接同一子会话（二段对话）。"结果对用户不可见、主 agent 必须转述"写进 usage notes——ECode 的 tool_result 走 ToolGroupView 展示，天然满足"用户能看"且无需转述强制。

---

## 2. ECode 立项时要做的决策（问题清单，非答案）

| # | 决策点 | 事实输入 |
|---|---|---|
| S1 | 子代理类型集：只内置调研型（explore 类，只读白名单）还是也内置全能型（tools:['*']） | CC 两者都内置；opencode general/explore 双型 |
| S2 | 递归封顶实现：CC 式工具过滤硬编码 / opencode 式深度可配 | 两家默认行为等价（一层） |
| S3 | 迭代上限：跟主循环 maxIterations / 子代理独立默认（CC 无默认的风险=跑飞） | CC maxTurns 可选无默认 |
| S4 | 运行时 UI 折叠行格式与展开键位（Ctrl+O 体系内还是独立键）；进度流要不要进动态区（高度预算） | CC 高度自适应降级先例 |
| S5 | agent 清单注入位置：工具 description 内嵌（CC 缓存击穿教训：占 cache_creation 10.2%）vs 消息流注入 | ECode skill 清单已有 1% 预算机制可复用思路 |
| S6 | 与 M7 观察区 SubagentStart/Stop hook 事件、plugin `agents` 组件的关系（一起做还是先内置） | — |
| S7 | 子代理的 usage/成本归并展示（StatusBar）与告警中心事件 | — |

---

## 3. 与 ECode 既有机制的映射（立项时的复用面）

- **工具注册**：`task` 工具 `register()` 进 ToolRegistry，readonly:false（副作用语义——占运行槽）。
- **权限系统**：子代理工具集裁剪可复用 M9-P5 权限规则的资源维度扩展（`Agent(x)` 规则位已留，四维模型）。
- **hooks**：SubagentStart/Stop 事件挂 HookRunner（观察区已有位）。
- **checkpoint**：子代理改文件与主循环同走 onBeforeWrite 快照（同一会话目录，/rewind 粒度自然覆盖）。
- **quality 回喂**：afterTools 的轮末聚合对子代理轮同样生效（EDIT_TOOLS 判定按工具名，与哪个循环无关）。
- **成本**：子代理 usage 回传主循环 onUsage 即可归并（Statusbar/`/cost` 零改动）。

---

## 附录 A：claude-code 一手实现锚点（本地源码）

| 事实 | 锚点 |
|---|---|
| 入口工具名 Agent（别名 Task），description 动态拼装 | `src/tools/AgentTool/AgentTool.tsx:230`（description() 返回 'Launch a new agent'）、`prompt.ts getPrompt()` |
| "When NOT to use" 四条反例 | `src/tools/AgentTool/prompt.ts`（whenNotToUseSection） |
| 鼓励并发 | 同上 concurrencyNote（非 pro 订阅）："single message with multiple tool uses" |
| 反"委派理解"教学 | 同上 writingThePromptSection："Never delegate understanding…include file paths, line numbers" |
| fork 模式（继承上下文的自分身）| `forkSubagent.ts`（feature gate；"Don't peek"——禁止中途读 output_file 拉噪音进上下文；"Don't race"——禁止编造未返回的结果） |
| 递归一层封顶 | `agentToolUtils.ts:191`：`filterToolsForAgent` 对非主线程跳过 Agent 工具解析（子代理工具表无 Agent） |
| maxTurns 可选无全局默认 | `runAgent.ts:756` `maxTurns ?? agentDefinition.maxTurns`；agent 定义 frontmatter `loadAgentsDir.ts:89` |
| general-purpose 定义（tools:['*']）| `built-in/generalPurposeAgent.ts`：SHARED_PREFIX（"complete the task fully—don't gold-plate"）+ 返回契约（"caller will relay this to the user, so it only needs the essentials"） |
| Explore 定义（disallowedTools 黑名单：Agent/Edit/Write/Notebook）+ 只读宣言 | `built-in/exploreAgent.ts`（"READ-ONLY MODE…attempting to edit will fail"，bash 仅限 ls/git status/log/diff/cat 等只读） |
| agent 清单缓存击穿治理 | `prompt.ts shouldInjectAgentListInMessages`：动态清单占 fleet cache_creation 10.2%，改注入消息流 |
| 结果硬截断 100k | `AgentTool.tsx` `maxResultSizeChars: 100_000` |
| UI：进度流式 + 高度自适应降级 | `UI.tsx renderToolUseProgressMessage`：`shouldUseCondensedMode = rows < 工具数×每工具行数估计 + 缓冲`；折叠态仅显示 3-5 词 description |
| 每 agent 独立颜色 | `agentColorManager.ts`（多子代理并行时视觉区分） |

## 附录 B：opencode 一手实现锚点

| 事实 | 锚点 |
|---|---|
| 入口工具 task，description 存 task.txt（与 CC 文案同源的四条反例 + 8 条 usage notes） | `packages/opencode/src/tool/task.ts` + `task.txt` |
| "已委派不要重复做" | task.txt usage note 2："do not duplicate that work yourself. Continue with non-overlapping tasks" |
| 递归深度可配默认 1 | `task.ts`：沿 parentID 数链 `depth >= (cfg.subagent_depth ?? 1)` 即拒 |
| 子代理权限派生：父 deny 规则继承 + 子代理无显式 allow 自动补 task/todowrite deny | `packages/opencode/src/agent/subagent-permissions.ts` |
| 内置 agent 双型 | `packages/opencode/src/agent/agent.ts`：general（多步任务，仅 deny todowrite）/ explore（白名单 grep/glob/read/bash/webfetch + PROMPT_EXPLORE 模板） |
| explore 子代理模板（绝对路径返回/不用 emoji/禁止改状态） | `packages/opencode/src/agent/prompt/explore.txt` |
| task_id 续接同一子会话 | task.ts 入参 `task_id`："resume a previous task…continue the same subagent session" |
| 背景运行实验开关 | `OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`（默认关；完成后合成消息注入父会话） |
| 返回 XML 包裹 | `renderOutput`：`<task id state="completed"><task_result>` |

## 附录 C：其余项目

- **codex（codex-rs）**：无 subagent（目录扫查无对应模块；M7 调研时其 MCP/schema 演进中亦无）。
- **aider / pi / openclaw**：无此功能（目录结构确认；pi 的 fork 在 M7 调研时确认是 skill frontmatter 概念，非子代理执行）。
