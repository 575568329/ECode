# 开发日志 2026-07-28

## 今日工作

### M1 骨架搭建
- 创建 `package.json`（ESM + tsx/tsc 混合 + Vitest）
- 创建 `tsconfig.json`（strict + NodeNext）
- 创建 `.env.example`
- 创建 `src/index.ts`（入口占位）
- 安装依赖并验证骨架运行正常
- Git 提交: `8cbaceb`

### M1 Agent Loop 实现
- `src/tools.ts` — read_file + bash 工具定义与执行器
  - 工具采用 Anthropic Tool 格式（name/description/input_schema）
  - 执行结果截断防止撑爆上下文（30KB）
  - 错误处理 + is_error 标记
- `src/agent.ts` — 完整 Agent Loop
  - `while (stop_reason === 'tool_use')` 逻辑（上限 25 轮）
  - messages 累加（append 方式）
  - tool_use/tool_result id 配对
  - 多 tool_use block 处理（一次性调多个工具）
  - 实时输出显示（文本 + 工具执行状态）
- `src/index.ts` — CLI 入口（`ecode <任务>`）
- 编译验证通过（`tsc --noEmit` + `npm run build`）

### 文档
- `docs/03-Anthropic-SDK-参数详解.md` — SDK 完整参数说明
  - Client 初始化参数
  - messages.create() 所有参数（model/system/max_tokens/tools/tool_choice/temperature/top_p/top_k/stop_sequences/stream/metadata）
  - Response 结构（stop_reason/usage/content block）
  - Content Block 类型（text/tool_use/tool_result）
  - 错误类型与排查
  - 实验清单（换模型/调温度/改 tool_choice/不加 tools）

### 讨论要点
- LLM 返回格式由 API 协议决定，我们只需按协议类型做分支
- 不能控制 LLM 行为，所以 TDD 的核心是 mock 而非真调用
- OpenAI 与 Claude 工具调用协议的主要差异（arguments 字符串 vs 对象、tool_result 格式、stop_reason 命名）
- 用户要求写详细的参数文档，方便一一实验理解

## 下一步
- [x] 继续写 `docs/04-OpenAI-vs-Anthropic-API协议对比.md`
- [ ] 设定 `ANTHROPIC_API_KEY` 后实际跑一次 agent loop
- [ ] 用户将通过实验不同参数来理解协议差异

## 碎碎念
- 当前使用的 `@anthropic-ai/sdk` 版本: 0.32.1
- Claude Code 模型: deepseek-v4-flash
- 项目默认模型: claude-sonnet-4-20250514
