# M5 文档审阅问题清单（待用户审阅）

> **生成**：2026-08-08，角色 agent（资深架构师 + CC/MCP 领域专家）审阅 M5 三文档后产出。
> **审阅范围**：[技术选型](./M5-技术选型与理由[已完成].md) + [实施方案](./M5-实施方案[已完成].md) + [方案解析](./M5-方案解析[已完成].md)。
> **核实方式**：所有问题附 CC/opencode 源码 `file:line` 或官方文档 URL（agent 用了 33 次工具核查）。
>
> **总评**：三文档整体质量高、可进入实施。MCP 规范/SDK/传输演进事实、ECode 挂点定位、安全核心设计（权限⊆/hook 只能收紧/stdio RCE 防护）**全部经核查成立**。但需先修 3 致命问题 + 若干事实/精度问题。

---

## 🔴 致命问题（3 条，需先修再进 plan mode）

### F1. `resolveDataDir()` 函数在 ECode 源码中**不存在**，但被当作"现有资产"反复引用
- **现象**：M5 三文档多处写「走 `resolveDataDir`（§9.3 跨平台）」并列为现有资产。全仓 grep **零命中**——现状是分散内联：
  - `src/permission/settings-loader.ts:113` → `resolve(homedir(), '.ecode')`
  - `src/providers/config.ts:28` → `join(homedir(), '.ecode', 'config.json')`
  - `src/index.ts:272,300` → `loadInstructions([homedir(), ...])`
- **影响**：子代理 loader / MCP registry / user 级 hooks 加载都依赖这个"现成入口"。按文档直接调用会编译失败；临时内联又违反 §9.3（跨 Windows/WSL 两套 home 必对不上）。
- **建议**：M5 阶段 0 **新增前置小任务**——把 3 处内联抽成 `src/paths.ts` 的 `resolveDataDir()`（纯提取不改语义，符合 §1.7），M5 各模块复用。或文档明示「本里程碑需新建此函数」。
- **状态**：✅ **已修（M5 阶段0 落地，commit 8cf262c）**：新增 `src/paths.ts` 的 `resolveDataDir()`，`providers/config.ts` + `permission/settings-loader.ts` 接入；`index.ts` 的 `homedir()` 用途不同（指令搜索根 ≠ `.ecode` 数据目录）按 §1.7 未动。

### F2. 「CC 5 种 hook handler 含 `mcp_tool`」**凭空捏造**
- **现象**：技术选型 12-T3 写 CC 有 5 种 handler（command/http/**mcp_tool**/prompt/agent）。
- **核实**：CC 源码 `schemas/hooks.ts:176-189` 实际是 `z.discriminatedUnion('type', [BashCommandHook, PromptHook, AgentHook, HttpHook])`——**4 种持久化**（command/prompt/http/agent）+ 1 进程内 `callback`（不可持久化）。**`mcp_tool` 类型根本不存在**。
- **影响**：损坏文档"禁止臆测"承诺；ECode 若日后对齐 CC mcp_tool 会发现无此先例。
- **建议**：改为「CC 4 种持久化 handler（command/prompt/http/agent）+ 1 进程内 callback；ECode 一期只做 command」。
- **状态**：✅ 已修（技术选型 12-T3）

### F3. ✅ 子代理权限继承：**用户确认 A 方案（继承全部）**——夜间自动化子代理须带权限
- **现象**：
  - **支点讨论记录 L661-663（已锁定）**：子代理「默认继承主代理当前模式 + **全部权限规则**…可被人设 tools 收紧」= **A 方案**（继承全部含 allow）。
  - **M5 技术选型 9-T3 / 方案解析 Q3**：倾向 **B 方案**（只继承 deny，allow 子代理自决）。
- **问题**：文档标为「⚠️ 待审阅」，但实为**重新讨论已锁定决策**。若用户未察觉，会以为是细节待定而非方向反转。
- **A vs B 行为差异**：A 下子代理复用主代理已批准命令不打扰；B 下子代理每个新危险操作弹窗到主对话（防代持）。
- **审阅建议**：**选 B**（只继承 deny）——子代理是"不可信分身"（人设可被构造），最小权限最安全；opencode 双源佐证扎实；ask 归主对话不"打扰"。但**必须用户明确确认是否反转支点 9 已锁定决策**。
- **实施细节**：选 B 后，子代理应有**独立 AllowList 实例**（不回写主），而非实施方案 §五现写的「传同一实例」。
- **状态**：✅ **已确认 A（2026-08-09 用户）**。理由：夜间/无人值守自动化执行时子代理须带权限跑，B 方案每个危险操作弹窗打断、自动化无法跑通。A 方案子代理共享主 AllowList 实例，不建独立实例；人设 tools 可收紧。

---

## 🟡 改进建议（15 条，事实/精度问题）

> 已标注哪些本轮已修。未修的可在实施过程分批修订。

| # | 问题 | 核实 | 建议 | 状态 |
|---|------|------|------|------|
| I1 | 多 hook 优先级写「deny > defer > ask > allow」 | CC `types/permissions.ts:44` 无 defer，实际 `utils/hooks.ts:2820-2846` | 改「deny > ask > allow」 | ✅ 已修 |
| I2 | CC HOOK_EVENTS「30+」夸大 | `coreTypes.ts:25-53` 精确 27 个 | 统一「源码 27，文档略多 30+，以源码为准」 | ⬜ 待修 |
| I3 | ~~「683 测试」与实测 542 不符~~ | **复核：683 正确**（`npx vitest run` = **683 passed / 61 文件，全绿**），审阅者误数（grep `it(`/`test(` 漏 `.tsx` 等少计为 542）。**不修**，683 保留。 | ✅ 无需修（683 属实） |
| I4 | CC runAgent.ts 行号 mcpClients:524 与 abortController:700 标反 | 源码：524 是 abortController，mcpClients 在 650/685 | 改 mcpClients:650,685 / abortController:524 | ⬜ 待修 |
| I5 | frontmatter 清单遗漏 `color` 字段 | 源码 16 frontmatter 字段（含 color）+ 正文=17 | 清单补 color | ⬜ 待修 |
| I6 | opencode 命名 `mcp__server__tool` 错 | `catalog.ts:117-119` 实际 `server_tool`（单下划线无 mcp__） | 速查表分两行 | ⬜ 待修 |
| I7 | opencode `task.ts:188 findLast` 行号错 | 实际 213 行（188 是 `model,`） | 改 task.ts:213 | ⬜ 待修 |
| I8 | opencode 顶层 mcp 字段位置错 | 顶层在 `config.ts:113-115`，`mcp.ts` 仅类型 | 改正位置 | ⬜ 待修 |
| I9 | CC `ALL_AGENT_DISALLOWED_TOOLS` 源码与文档矛盾 | `constants/tools.ts:36-46`：非 ant 用户**无条件禁** Agent 工具（非深度受限才禁） | Q4 补注「源码与文档不一致」 | ⬜ 待修 |
| I10 | CC 加载优先级漏 plugin/flag/managed 三层 | `loadAgentsDir.ts:193-221`：built-in→plugin→user→project→flag→managed | 补全（ECode 一期只做 built-in/user/project） | ⬜ 待修 |
| I11 | CC stopHooks 位置错 | 注入+防递归在 `query.ts:1282-1306`（非 stopHooks.ts:65-81） | 补 query.ts 行号 | ⬜ 待修 |
| I12 | **MCP↔hook 联动机制几乎未设计**（关键场景遗漏） | 支点讨论记录 L710/837 锁定「MCP 装/卸时 hooks 跟随」 | 阶段 2/3 各加「MCP↔hook 联动」节 | ⬜ 待修（设计层留挂点） |
| I13 | ✅ MCP「独立注册」已确认（去待审阅标） | 支点讨论记录 L697-710 已锁定 | 已去掉「待审阅」 | ✅ 已确认 A（2026-08-09） |
| I14 | CC processHookJSONOutput 三通道归属 + stdout 顶层无 permissionDecision | exit code 2 在 executeHooks；顶层 schema 无 permissionDecision | 注明 | ⬜ 待修 |
| I15 | ToolDefinition.parameters 字面量类型 vs MCP inputSchema 兼容 | `tools/types.ts` type 是 `'object'` 字面量，MCP 是 string | adapter 补类型断言注 | ⬜ 待修 |

---

## 🟢 亮点（审阅确认做对的）

1. **主动核实纠偏机制**：7 处「🔴 核实纠偏」抽查 4 处（HTTP+SSE 废弃日期 2025-03-26、DCR 废弃、CC frontmatter 无 glob、CC 默认深度 3）**全部通过**。
2. **MCP 规范/SDK/传输演进事实准确**：规范 2026-07-28、SDK v1.30.0、HTTP+SSE 废弃自 2025-03-26、DCR 废弃、stateless/discover/MRTR/SEP-2596、RFC 9207——全部属实。
3. **ECode 现状挂点定位精确**：17/20 完全属实，agent.ts 关键挂点偏差 ≤1 行。
4. **安全核心设计经核查全部成立**：权限⊆（CC toolHooks.ts:332-433 确认 hook allow 不 bypass deny/ask）、stdio RCE（opencode 确实没做）、子代理防递归双保险属实。
5. **5 阶段划分清晰可实施**：依赖关系 + TDD 步骤 + 接口设计 + 验收完整，可直接进 plan mode。
6. **`npm test` 是 watch 模式警告属实**（package.json `"test": "vitest"` 无 run）——真实坑。
7. **三源对比取证扎实**：每条选型给双源佐证，非单源臆断。

---

## 🆕 前端 UI 设计草案审阅（2026-08-09，角色 agent 第二轮）

> 审阅对象：[M5-前端UI设计草案[已完成]](./M5-前端UI设计草案[已完成].md)。架构师 + CC/MCP 专家二轮审阅。
> **核心发现**：草案的子代理/hook 进度事件（`subagent_progress` / `hook_progress`）与 [实施方案 §1.1/§八](./M5-实施方案[已完成].md)「子代理黑盒」**冲突** → 降级两处决策点保持黑盒（M5 不加新事件）。

### 决策点降级（⚠️ 待用户确认，原默认 C/B）

| 决策 | 草案原推荐 | 审阅后降级 | 理由 |
|------|-----------|-----------|------|
| ① 子代理进度块 | C（单行实时 + pager） | **M5 黑盒（无 `subagent_progress` 事件），实时可观测推 M6+** | 实施方案 §1.1/§八 已锁黑盒；实时进度须给 agent loop 加事件 + UI 动态区，侵入核心不值当；M5 先黑盒保主上下文 |
| ④ hook 运行时显示 | B（CC 折中显示一行） | **A 全静默（仅 `--debug`）** | 同理：hook 实时显示须 `hook_progress` 事件；M5 hook 拒绝经现有 deny / ToolDone 反馈即可 |

> 用户原默认采纳草案 C/B；现因架构冲突降级。**待用户知晓确认**。降级后草案的「AgentEvent 加 `subagent_*`/`hook_progress`」「chat-view 子代理进度块 / hook 运行时行」「`subagent_result` kind」三条**推 M6+ 或删除**。

### 修正项（已并入草案）
- **5 态图标 typo**：草案 `✓/○/▲/✗/○`（`○` 重复）→ 4 图标 5 态（connected/connecting/failed/disabled + 空闲）。
- **FOLD mcp__ 前缀分支**：MCP 工具名 `mcp__server__tool` 是动态的，`FOLD_STRATEGIES` 现按精确名查 → 需加前缀分支（`name.startsWith('mcp__')`），否则 MCP 工具折叠兜底失效。
- **MCP prompt 触发口径**：草案「双源共识」→ 实为 **CC 单源路径**（opencode 不做 prompt 注入），改「CC 路径」。

### 补充项（并入草案 / M5 实施）
- 页脚错误信息用红色（status-bar 错误态）。
- `PendingPermission` 加 `source` 字段（区分 subagent / 主对话来源）。
- `/help` 过滤 `source=builtin`（MCP 动态命令不进 help）。
- `subagent_result` DisplayMessage kind **删除** → 子代理结论复用 Task 工具的 ToolDone（黑盒下无需独立 kind）。
- hook deny 经 ToolDone 红色 X 反馈（复用现有 error kind）。
- `yes_all` 语义（MCP 首次审批三选项）须在 permission 层明确（本 server 全部放行 vs 全局）。

### 范围决定（非审阅，记录备查）
- **resolveDataDir 不触碰 index.ts**：`index.ts` 的 `homedir()` 用于指令搜索根（CLAUDE.md 等），语义 ≠ `.ecode` 数据目录，按 §1.7 不在阶段0 范围。
- **斜杠命令注册式重构校正**：实施方案阶段3 写「~55 行」偏低；实测迁移 8 case + dispatch + adapter ≈ **~200 行**（草案口径更准）。

---

## ✅ 两处取舍已确认（2026-08-09 用户）

### 取舍 1：子代理权限继承 —— **选 A（继承全部）**
用户决策：子代理继承主代理当前模式 + 全部权限规则（denyRules + 已批准 allow）。**理由**：夜间自动化执行子代理须带权限跑，B（只继承 deny）每个危险操作弹窗打断、自动化无法跑通。**实施细节**：A 方案子代理共享主 AllowList 实例（不建独立实例），人设 tools 可收紧。三文档已更新（技术选型 9-T3 / 方案解析 Q3 / 实施方案 §五）。

### 取舍 2：MCP 配置 —— **选 A（独立注册表）**
用户决策：与支点 10 已锁定一致，独立注册表 `~/.ecode/mcp/registry.json`。**理由**：防 config 连坐删 + 夜间自动化配置稳定。**实施提醒**：`/mcp` 首次运行须显式告知「MCP 配置在 registry.json 不在 config.json」。

---

**处理状态**：F1/F2/F3/I1/I13 已修/确认；I12 留挂点；其余 I 项实施时分批修。➕ 2026-08-09 **M5 三支点全部交付**（阶段0 地基 + 阶段1 子代理 + 阶段2 Hooks 引擎 + 阶段3 MCP R1-R7 + H1 settings.json hooks 配置加载，**819 测试全绿**，tsc clean）。待用户审阅。

---

## 🆕 阶段2 Hooks 延期 + 阶段3 MCP 实施就绪评估（2026-08-09 夜间自主推进）

> 用户指令："按计划执行 m5 阶段内容，有问题先记录，我明天审阅。"以下为自主模式下标记的待审项。

### 阶段2 Hooks —— 已交付 / 延期

**已交付**（commit `e7148a4`，749 测试绿）：
- ✅ `runner.ts`：CC 三通道解析（exit 2 / 顶层 decision / 嵌套 hookSpecificOutput.permissionDecision）+ 可注入 exec + 超时失败降级 allow
- ✅ `inject.ts`：createHookGate 多 hook 聚合（deny 最严胜出 + modifiedInput 整体替换 + matcher CI 通配 `*`/exact/pipe/前缀）
- ✅ `system-hooks.ts`：SYSTEM_HOOKS 强制叠加框架（`getEffectiveHooks` 恒前置、用户不可移除）
- ✅ agent.ts 接线：Pre（check 前：deny 跳执行 / 改输入 → effectiveInput）+ Post（validateAfterEdit 后：deny 回喂反馈）；无有效 hook → hookGate=undefined → 字节级零回归
- ✅ `opts.hooks` / `opts.hooksExec` 注入口

**⏸ 延期待审**（自主模式安全边界，不擅自动安全关键+用户配置项）：

| 项 | 原因 | 风险 | 建议 |
|---|---|---|---|
| ~~**settings.json 配置加载**~~ | ~~hooks 字段 schema + settings-loader 接线~~ | ~~用户配置入口~~ | ✅ **已交付**（buildHooksFromSettings + loadPermissionSettings hooks 解析 + index.ts passthrough + 8 测试） |
| **SYSTEM_HOOKS 内容** | 具体 hook 是 command handler=spawn shell，跨平台 shell 分流 §9.3 需打磨 | Windows cmd.exe vs Unix bash 命令兼容性；M4 path-guard 已覆盖核心安全网 | 中风险；先定 shell 分流策略（复用 bash 工具同款 Git Bash 分流 or node -e） |
| **4 事件流钩子** (SessionStart/End/UserPromptSubmit/Stop) | 需事件流 yield 基础设施（当前 agent 只 yield 决定型事件）；Stop 打回需抽 `emitCompleted()` 收口 | 事件流钩子只通知不等决策，设计简洁但需 refactor 4 处 completed yield | 低风险；纯增量（不改现有 Pre/Post），可后置 |
| **PostToolUse modifiedOutput** | CC 协议未定义标准 PostToolUse 输出替换字段（已联网核实，无对标文档） | 猜字段名→静默丢弃→假绿 | 不做是正确选择；等 CC 出标准再接 |

### 阶段3 MCP —— registry 已交付 / 核心 SDK+spawn 待审

**已交付**（registry.ts，9 测试绿）：
- ✅ `src/mcp/registry.ts`：McpRegistryEntry 类型 + loadMcpRegistry / saveMcpRegistry（走 resolveDataDir，独立于 config.json，文件缺失/损坏降级空数组）
- ✅ registry 物理路径 `~/.ecode/mcp/registry.json`，走 resolveDataDir §9.3 跨平台

**⏸ 待审决策（安全关键 + 新依赖 + UI 重构，不擅自动）**：

| # | 待审项 | 性质 | 风险 | 状态 |
|---|-------|------|------|------|
| R1 | **SDK 安装** | 新依赖 `@modelcontextprotocol/sdk@1.30.0` | 低 | ✅ **已装**（`npm install`，subpath import 验证通过） |
| R2 | **client.ts stdio spawn + RCE allowlist** | 🔴 **安全红线** | 高 | ✅ **已交付**（commit 74e5d39：SAFE_COMMAND_HEADS 19 个白名单 + validateMcpCommand 纯函数 + connectMcpServer 依赖注入 + 30 测试） |
| R3 | **斜杠命令注册式重构** | UI 结构重构 ~200 行 | 中 | ✅ **已交付**（registerCommand/unregisterCommand/findCommandHandler + dispatch；758→全绿） |
| R4 | **adapter.ts (tool + prompt)** | 依赖 SDK 类型形状 | 中 | ✅ **已交付**（adaptMcpTool/adaptMcpPrompt，13 测试） |
| R5 | **loader.ts + 接线** | 依赖 R2+R4 | — | ✅ **已交付**（loader 74e5d39 + 接线 ed666ed：useEffect 加载 + getRunOpts 合并 + 卸载断开） |
| R6 | **`/mcp` 斜杠命令 UI** | 依赖 R3 | — | ✅ **已交付**（ed666ed：list/enable/disable + SLASH_COMMANDS 9 命令） |
| R7 | **MCP Prompts → 动态斜杠命令** | 依赖 R4+R5 | — | ✅ **已交付**（client.ts listPrompts + loader prompts 汇总 + use-agent-stream registerCommand + 卸载注销） |
| H1 | **Hooks settings.json 配置加载** | 用户配置入口 | — | ✅ **已交付**（settings-loader hooks 解析 + index.ts hooks passthrough + app.tsx hooks prop + 8 测试全绿） |
