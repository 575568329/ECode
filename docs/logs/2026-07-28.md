# 全量日志 2026-07-28

> **格式**: 时间线 + 详细操作记录，每条包含输入/输出/文件变更

---

## 19:09 — 项目了解 & 规划确认

- **操作**: 查看项目结构和设计文档
- **命令**: `ls -la /mnt/d/Study/ECode/`、`find ... | head -40`
- **输入**: 项目目录、README.md、docs/ 下 3 份文档
- **输出**: 确认项目为 ECode — 手写 AI Coding Agent 学习项目
- **状态**: 规划完成，M1 骨架搭建待执行
- **文档**: docs/00-学习型开发规划.md、docs/01-技术栈选型与理由.md、docs/02-M1骨架搭建方案.md
- **决策**: 按 M1 骨架方案执行

---

## 19:15 — 创建 package.json

- **命令**: Write `/mnt/d/Study/ECode/package.json`
- **内容**: 31 行，ESM 配置
  - `"type": "module"` — ESM 模块系统
  - scripts: dev(tsx) / build(tsc) / start(node) / test(vitest)
  - dependencies: `@anthropic-ai/sdk@^0.32.1`
  - devDependencies: `tsx@^4.19.2` / `typescript@^5.7.2` / `vitest@^2.1.8`
  - `"engines": { "node": ">=18.0.0" }`
- **文件变更**: 新建 package.json

---

## 19:15 — 创建 tsconfig.json

- **命令**: Write `/mnt/d/Study/ECode/tsconfig.json`
- **内容**: strict 模式 + NodeNext 模块解析
  - `target: ES2022`, `module: NodeNext`, `moduleResolution: NodeNext`
  - `strict: true`, `noUnusedLocals`, `noUnusedParameters`
  - `sourceMap: true`, `declaration: true`
- **文件变更**: 新建 tsconfig.json

---

## 19:15 — 创建 .env.example + src/index.ts

- **命令**: Write `.env.example` + `mkdir -p src` + Write `src/index.ts`
- **.env.example**: 占位 `ANTHROPIC_API_KEY=sk-ant-api03-...` + 可选 `ANTHROPIC_MODEL`
- **index.ts**: shebang + `console.log('ECode M1 骨架已就绪...')`
- **文件变更**: 新建 .env.example、src/ 目录、src/index.ts

---

## 19:16 — npm install

- **命令**: `npm install`
- **时序**: 41 秒
- **输出**: 83 packages added, 1 deprecated warning (node-domexception)
- **文件变更**: node_modules/、package-lock.json

---

## 19:17 — 验证骨架

- **命令**: `npm run dev`
- **输出**: `ECode M1 骨架已就绪，开始实现 agent loop...` ✅
- **命令**: `npm run build`
- **输出**: tsc 编译通过 ✅

---

## 19:19 — 初次 Git 提交失败 → 配置身份

- **命令**: `git commit -m "feat(m1): ..."`
- **错误**: `Author identity unknown` — git 未配置 user.name/email
- **处理**: `git config user.email/user.name` 补上
- **提交成功**: `8cbaceb` — feat(m1): 搭建项目骨架 - TS + ESM + tsx/tsc 混合 + Vitest
  5 files changed, 2470 insertions(+)

---

## 19:30 — 学习 Agent Loop 协议原理

- **讨论**: 
  1. Agent Loop = while 循环，`stop_reason === 'tool_use'` 时继续
  2. `tool_use.id` ↔ `tool_result.tool_use_id` 配对约束
  3. messages 累加（append 不是重建）的原因
  4. 用户提关键问题：我们不能控制 LLM 返回格式
  5. 回答案：格式由 API 协议固定，我们的循环只做类型分支
  6. 用户问 OpenAI 标准差异 → 对比两种协议的 tool calling

- **决策**: 
  - TDD 中核心测试是 mock（模拟 LLM 各种返回），不是真调 API
  - M1 专注 Claude 协议，M2 做 Provider 抽象层

---

## 19:45 — 实现 src/tools.ts

- **命令**: Write `/mnt/d/Study/ECode/src/tools.ts`
- **内容**: 130 行
  - `ToolDefinition` 接口（name/description/input_schema）
  - `toolDefinitions[]` — read_file + bash 两个工具定义
  - `executeTool(name, input)` — 工具执行函数
  - `truncate()` — 结果截断（30KB 上限，防撑爆上下文）
  - `executeReadFile()` — 用 `readFileSync`，错误捕获 + `isError`
  - `executeBash()` — 用 `execSync`，10MB buffer/30s timeout，错误捕获

---

## 19:45 — 实现 src/agent.ts

- **命令**: Write `/mnt/d/Study/ECode/src/agent.ts`
- **内容**: 120 行
  - `runAgent(task)` — 主循环
  - `for (let i = 0; i < 25; i++)` — 上限保护
  - 处理 content block：`text` 输出到控制台 / `tool_use` 加入执行队列
  - LLM 回复整体 pushed 到 messages（包括 text + tool_use）
  - 逐一执行工具，`tool_result` 回传（id 配对验证）
  - `is_error` 标记在 tool_result 上
  - 输出格式：emoji 标记 + 工具名 + 结果预览（前 5 行）

---

## 19:45 — 更新 src/index.ts

- **命令**: Write `/mnt/d/Study/ECode/src/index.ts`
- **内容**: 覆盖占位代码
  - shebang 保留
  - JSDoc 说明用法+环境变量
  - `process.argv[2]` 取任务（手写 CLI，M1 不引 commander）
  - 无参数时打印用法并 exit(1)
  - `runAgent(task).catch()` 捕获未预期错误

---

## 19:48 — 编译验证

- **命令**: `npx tsc --noEmit` → 无输出 ✅
- **命令**: `npm run build` → tsc 编译通过 ✅

---

## 19:50 — 撰写 docs/03-Anthropic-SDK-参数详解.md

- **命令**: Write `/mnt/d/Study/ECode/docs/03-Anthropic-SDK-参数详解.md`
- **内容**: 约 300 行，覆盖 7 大章节
  1. Client 初始化（apiKey/baseURL/maxRetries/timeout/anthropicVersion/...）
  2. messages.create() 所有参数（18 个参数逐一说明+实验建议）
     - model（模型表）、system（字符串/数组）、max_tokens
     - tools（JSON Schema 完整示例）
     - tool_choice（auto/any/tool 三种策略对比）
     - temperature/top_p/top_k（随机性控制）
     - stop_sequences、stream、metadata
  3. Response 结构（id/type/role/content/stop_reason/usage）
  4. Content Block 类型（text/tool_use/tool_result + 数组示例）
  5. 错误类型（6 种 + HTTP 状态码映射 + 排查表）
  6. 实验清单（换模型/调温度/改 tool_choice/不加 tools）
  7. 参考链接

---

## 19:50 — 收到新增日志要求

- **消息**: 用户要求从现在开始写日志，方便调试
- **操作**: 创建 `docs/logs/` 目录 + 初始日志文件

---

## 20:00 — 撰写 docs/04-OpenAI-vs-Anthropic-API协议对比.md

- **命令**: Write `/mnt/d/Study/ECode/docs/04-OpenAI-vs-Anthropic-API协议对比.md`
- **内容**: 约 280 行，覆盖 10 大章节
  1. 核心差异总览（15 维度对比表）
  2. 工具调用请求格式（Anthropic content block vs OpenAI tool_calls）
  3. 工具结果回传格式（Anthropic user.tool_result vs OpenAI role:tool）
  4. System 消息位置
  5. 完整消息序列（逐轮对比）
  6. Streaming 对比（事件结构/参数流式/复杂度）
  7. 错误响应对比
  8. 工具定义格式对比
  9. Provider 抽象层设计思路（M2 预告）
  10. 实验清单

---

## 20:05 — 日志相关 Git 提交

- **命令**: `git add docs/logs/`
- **错误**: `docs/logs` 被 `.gitignore` 的 `logs/` 规则排除
- **修复**: `.gitignore` 中 `logs/` → `/logs/`（只排除根目录 logs/）
- **文件变更**: .gitignore — 第 20 行 `logs/` → `/logs/`
- **提交**: `aa05261` — chore: 添加开发日志 docs/logs/

---

## 20:10 — 用户确认需要全量日志

- **讨论**: 目前日志是工作纪要，非全量
- **要求**: 全量日志 = 详细操作日志 + 调试日志 + 时间线日志
- **决策**: 补充今日日志为全量格式 + 给 Agent 加运行时日志系统 + 定标准

---

## 🔧 当前代码文件总览

```
src/
├── index.ts      # 33 行 — CLI 入口
├── agent.ts      # 92 行 — Agent Loop
└── tools.ts      # 100 行 — 工具定义与执行

docs/
├── 00-学习型开发规划.md      # 214 行 — 总纲
├── 01-技术栈选型与理由.md    # 252 行 — 选型
├── 02-M1骨架搭建方案.md      # 240 行 — 搭建方案
├── 03-Anthropic-SDK-参数详解.md  # ~300 行
├── 04-OpenAI-vs-Anthropic-API协议对比.md  # ~280 行
└── logs/
    └── 2026-07-28.md        # 本文

依赖: @anthropic-ai/sdk@0.32.1, tsx@4.x, typescript@5.x, vitest@2.x
编译: tsc --noEmit / npm run build 均通过 ✅
```

## 待办
- [ ] 给 Agent 添加运行时日志系统（API 请求/响应自动落文件）
- [ ] 设定 ANTHROPIC_API_KEY 后实际跑一次
- [ ] 用户逐一实验 SDK 参数

## 环境信息
| 项 | 值 |
|----|-----|
| Claude Code 模型 | deepseek-v4-flash |
| 项目默认模型 | claude-sonnet-4-20250514 |
| @anthropic-ai/sdk | 0.32.1 |
| 当前分支 | main |
| 最新 commit | aa05261 |
