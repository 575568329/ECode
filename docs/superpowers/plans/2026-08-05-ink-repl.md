# M3.5 阶段②：Ink REPL 交互式终端 UI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 ECode 从 one-shot CLI 升级为交互式 REPL——`ecode`（无参 + TTY）进入沉浸式终端 UI，实时消费 `runAgentStream` 事件流渲染对话；`ecode "任务"` / 非 TTY 仍走 one-shot。

**Architecture:** Ink 7 + React 19 渲染。核心是一座桥 `useAgentStream` hook：把 `AsyncGenerator<AgentEvent>` 喂进一个**纯函数状态机** `reduceAgentEvent(state, event)`，React 只读结果 state。已完成消息走 `<Static>` 冻结（O(n)→O(1)），动态区只渲染流式文本 + 运行中工具。权限弹窗用 modal 替换（非叠加）InputBar。退出走主 buffer（拒绝 alt screen），保留 scrollback。

**Tech Stack:** TypeScript 5.7（strict）· Node ≥18（实测 22.22）· ESM（`"type": "module"`）· Ink ^7.1 · React ^19.2 · marked + cli-highlight · vitest + ink-testing-library。

## Global Constraints

> 以下为 spec 全局硬约束，每个任务的 requirements 隐含全部继承。违反会报错或踩坑。

- **ESM**：所有 import 必须带 `.js` 后缀（如 `from './agent.js'`），即使源文件是 `.ts`。
- **tsconfig strict** + `noUnusedLocals` / `noUnusedParameters`：未用变量/参数编译报错。`.tsx` 组件 props 解构后全要用上，否则用 `_` 前缀。
- **包管理统一 npm**：禁用 pnpm/cnpm/yarn。换源前先删 `package-lock.json`。
- **配色零硬编码**：17 个 token 集中在 `src/ui/theme.ts`，组件只引 token 名，禁止裸 hex。
- **符号全单宽 Unicode 几何**：`❯ ◆ ▸ ↳ ✓ ✗ ▲ ◐ ▶ ⠋`，禁用 emoji（跨终端字宽不一）。
- **不切 alternate screen**：用 `\x1b[2J\x1b[H` 清屏，保留主 buffer scrollback；退出无需 `\x1b[?1049l`。
- **`exitOnCtrlC: false`**：Ink render 必须关掉默认 Ctrl+C，否则绕过双击退出清理逻辑。
- **UI state ≠ LLM history**：`DisplayMessage[]` 只供渲染；LLM 消息历史在 `runAgentStream` 内部管理。**禁止从 UI state 重建 LLM history**。
- **agent core 稳定优先**：本阶段对 `src/agent.ts` / `src/agent-events.ts` 的改动仅限 Task 3（加 usage 事件），其余任务不动 agent core。
- **commit**：Conventional Commits（`feat:` / `fix:` / `refactor:` / `docs:` / `chore:`）。**禁止出现任何 `claude` / `Co-Authored-By` 字样**。完成一个完整功能（一个 Task）再提交，不每改一行 commit。
- **视觉基准**：编码时文字规格若与 [`ui-preview.tsx`](../../../ui-preview.tsx)（项目根）有出入，**以 `ui-preview.tsx` 为准**。可在终端 `npx tsx ui-preview.tsx` 对照真实效果。

## File Structure

```
src/ui/
  theme.ts              — 17 色 token（Catppuccin Mocha 基底）+ 默认导出 T 对象
  symbols.ts            — 单宽 Unicode 符号常量
  types.ts              — DisplayMessage / ActiveTool / PendingPermission / StreamState
  reduce-agent-event.ts — 纯函数状态机：reduceAgentEvent(state, event) → state
  use-agent-stream.ts   — React hook：桥接 runAgentStream ↔ StreamState（含 submit/abort/resolvePermission）
  spinner.tsx           — braille 动画 spinner
  logo.tsx              — 5 行方块 E + ▶_
  markdown.tsx          — MarkdownRenderer（流式纯文本 / 完成后 marked+cli-highlight full render）
  tool-panel.tsx        — 工具三态（running/done/error）+ per-tool 折叠
  permission-dialog.tsx — 三选项弹窗 + 425ms grace period
  input-bar.tsx         — 单行输入 + ↑↓ 历史
  status-bar.tsx        — 单行状态栏（耗时/tok/费用/Ctx%/model + 动态段）
  welcome-screen.tsx    — 双栏欢迎面板 + 窄终端降级
  chat-view.tsx         — <Static> 冻结历史 + 动态区
  app.tsx               — REPL 主体：组合子组件 + 斜杠命令 + 同步清屏 + 双击 Ctrl+C
  index.ts              — render 入口 + 退出清理（SIGINT/exit 兜底）
tests/ui/               — UI 层测试（每个组件一个 *.test.tsx）
```

**职责边界**：`reduce-agent-event.ts`（纯逻辑，无 React）是整个 UI 的状态核心，独立可测；`use-agent-stream.ts`（React 副作用）薄薄包一层。展示组件（spinner/logo/markdown/tool-panel/...）全是纯函数 of props，`ink-testing-library` 渲染断言。

## 依赖图（任务顺序）

```
Task1(视觉基础) ─┬─→ Task2(Spinner+Logo)
                 └─→ (所有展示组件的配色/符号来源)
Task3(usage 事件, agent core) ─→ Task4(reducer+hook)
Task4 ──→ Task11(ChatView) ─┬─→ Task12(App) ─→ Task13(入口+集成)
Task5..10(展示组件) ────────┘
Task9(StatusBar) 依赖 Task3(usage)
```

可并行：Task 2 / Task 3 互不依赖；Task 5–10 互不依赖（都只依赖 Task 1）。

---

## Task 1: 视觉基础层（theme + symbols + tsconfig jsx + 依赖安装）

**Files:**
- Create: `src/ui/theme.ts`
- Create: `src/ui/symbols.ts`
- Modify: `tsconfig.json`（加 `"jsx": "react-jsx"`）
- Modify: `package.json`（新增 marked / cli-highlight / ink-testing-library / @types/marked）
- Test: `tests/ui/theme.test.ts`

**Interfaces:**
- Produces: `T`（theme token 对象，`T.brand` 等 17 个 hex 字符串）、`SYMBOLS`（符号常量对象）、`SPINNER_FRAMES`（braille 字符串）。后续所有展示组件 import 这三者。

- [ ] **Step 1: 安装依赖**

Run:
```bash
npm install marked cli-highlight
npm install -D ink-testing-library @types/marked
```
Expected: `package.json` 多出 4 个依赖，`package-lock.json` 更新。

- [ ] **Step 2: tsconfig 加 jsx 配置**

Modify `tsconfig.json`，在 `compilerOptions` 内加一行（紧跟 `"strict": true` 之后）：

```json
    "jsx": "react-jsx",
```

这样 `.tsx` 文件用 react-jsx runtime，无需每个文件 `import React`。验证：`npm run build` 应仍通过（此时还没 .tsx，但配置先生效）。

- [ ] **Step 3: 写失败测试**

Create `tests/ui/theme.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { T, SYMBOLS, SPINNER_FRAMES } from '../../src/ui/theme.js';

describe('视觉基础', () => {
  it('T 含 17 个语义 token，值为 hex', () => {
    const keys = Object.keys(T);
    expect(keys).toHaveLength(17);
    // 抽查 spec §8.1 的关键 token 值
    expect(T.brand).toBe('#4ECDC4');
    expect(T.user).toBe('#89B4FA'); // 蓝，非绿（避免撞 success）
    expect(T.success).toBe('#A6E3A1');
    expect(T.permission).toBe('#FAB387'); // 独立 token，不复用 error
    // 全部 hex 格式
    for (const v of Object.values(T)) {
      expect(v).toMatch(/^#[0-9A-Fa-f]{6}$/);
    }
  });

  it('SYMBOLS 全单宽 Unicode 几何，无 emoji', () => {
    expect(SYMBOLS.user).toBe('❯');
    expect(SYMBOLS.brand).toBe('◆');
    expect(SYMBOLS.tool).toBe('▸');
    expect(SYMBOLS.result).toBe('↳'); // 非 ⎿（CC 签名）
    expect(SYMBOLS.success).toBe('✓');
    expect(SYMBOLS.error).toBe('✗');
    expect(SYMBOLS.warning).toBe('▲');
    expect(SYMBOLS.thinking).toBe('◐');
    expect(SYMBOLS.prompt).toBe('▶');
    // 全部恰好 1 个码点（emoji 多码点会破坏对齐）
    for (const v of Object.values(SYMBOLS)) {
      expect([...v].length).toBe(1);
    }
  });

  it('SPINNER_FRAMES 是 braille 序列', () => {
    expect(SPINNER_FRAMES).toBe('⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏');
    expect([...SPINNER_FRAMES].length).toBe(10);
  });
});
```

- [ ] **Step 4: 跑测试确认失败**

Run: `npx vitest run tests/ui/theme.test.ts`
Expected: FAIL（`Cannot find module '../../src/ui/theme.js'`）。

- [ ] **Step 5: 实现 theme.ts + symbols.ts**

Create `src/ui/theme.ts`：

```ts
// UI 视觉系统中心 —— 配色 token + 符号常量（spec §8.1 / §8.2）。
// 组件只引 token 名，禁止裸 hex。视觉基准：ui-preview.tsx。

/** 17 个语义配色 token（Catppuccin Mocha 基底）。 */
export const T = {
  brand: '#4ECDC4',
  user: '#89B4FA',
  tool: '#F9E2AF',
  result: '#6C7086',
  success: '#A6E3A1',
  error: '#F38BA8',
  warning: '#FAB387',
  info: '#89B4FA',
  permission: '#FAB387',
  thinking: '#94E2D5',
  suggestion: '#7F849C',
  accent: '#89B4FA',
  muted: '#6C7086',
  border: '#45475A',
  diffAdded: '#A6E3A1',
  diffRemoved: '#F38BA8',
  inverseText: '#1E1E2E',
} as const;

/** 单宽 Unicode 几何符号（禁用 emoji：跨终端字宽不一破坏对齐）。 */
export const SYMBOLS = {
  user: '❯',
  brand: '◆',
  tool: '▸',
  result: '↳',
  success: '✓',
  error: '✗',
  warning: '▲',
  thinking: '◐',
  prompt: '▶',
} as const;

/** braille spinner 帧序列（品牌色）。 */
export const SPINNER_FRAMES = '⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏';
```

- [ ] **Step 6: 跑测试确认通过**

Run: `npx vitest run tests/ui/theme.test.ts`
Expected: PASS（3 个测试全绿）。

- [ ] **Step 7: build 验证 + 提交**

Run: `npm run build`
Expected: tsc 编译通过（jsx 配置生效，新文件无类型错误）。

```bash
git add src/ui/theme.ts src/ui/symbols.ts tests/ui/theme.test.ts tsconfig.json package.json package-lock.json
git commit -m "feat(m3.5): UI 视觉基础层（17 色 token + 单宽符号 + tsconfig jsx）"
```

---

## Task 2: 原子展示组件（Spinner + Logo）

**Files:**
- Create: `src/ui/spinner.tsx`
- Create: `src/ui/logo.tsx`
- Test: `tests/ui/spinner.test.tsx`
- Test: `tests/ui/logo.test.tsx`

**Interfaces:**
- Consumes: `T`, `SPINNER_FRAMES`（from Task 1）。
- Produces: `<Spinner color?: string />`（默认 brand 色，80ms 切帧）、`<Logo />`（5 行方块 E + ▶_）。后续 ToolPanel / WelcomeScreen import。

- [ ] **Step 1: 写失败测试 — Spinner**

Create `tests/ui/spinner.test.tsx`：

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render } from 'ink-testing-library';
import { Spinner } from '../../src/ui/spinner.js';
import { SPINNER_FRAMES, T } from '../../src/ui/theme.js';

describe('<Spinner />', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('首帧渲染 braille 第一帧', () => {
    const { lastFrame } = render(<Spinner />);
    // lastFrame 含 ANSI 颜色码，断言子串即可
    expect(lastFrame()).toContain(SPINNER_FRAMES[0]);
  });

  it('80ms 后切到第二帧', () => {
    const { lastFrame } = render(<Spinner />);
    expect(lastFrame()).toContain(SPINNER_FRAMES[0]);
    vi.advanceTimersByTime(80);
    expect(lastFrame()).toContain(SPINNER_FRAMES[1]);
  });

  it('循环到最后帧后回到第一帧', () => {
    const { lastFrame } = render(<Spinner />);
    vi.advanceTimersByTime(80 * 10); // 走完 10 帧
    expect(lastFrame()).toContain(SPINNER_FRAMES[0]); // 回到首帧
  });

  it('默认色为 brand', () => {
    const { lastFrame } = render(<Spinner />);
    // brand 色 hex 出现在 ANSI 转义里
    expect(lastFrame().toLowerCase()).toContain(T.brand.toLowerCase().replace('#', ''));
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/spinner.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 Spinner**

Create `src/ui/spinner.tsx`：

```tsx
// braille spinner —— 工具运行态指示。80ms/帧循环。
import { useState, useEffect } from 'react';
import { Text } from 'ink';
import { SPINNER_FRAMES, T } from './theme.js';

interface SpinnerProps {
  /** 颜色 token hex，默认 brand。 */
  color?: string;
}

export function Spinner({ color = T.brand }: SpinnerProps): React.ReactElement {
  const frames = [...SPINNER_FRAMES];
  const [i, setI] = useState(0);
  useEffect(() => {
    const id = setInterval(() => setI((v) => (v + 1) % frames.length), 80);
    return () => clearInterval(id);
  }, [frames.length]);
  return <Text color={color}>{frames[i]}</Text>;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/ui/spinner.test.tsx`
Expected: PASS（4 个测试）。

- [ ] **Step 5: 写失败测试 — Logo**

Create `tests/ui/logo.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import { Logo } from '../../src/ui/logo.js';

describe('<Logo />', () => {
  it('渲染 5 行方块 E（含 3 条横杠 ███████ / █████）', () => {
    const { lastFrame } = render(<Logo />);
    const frame = lastFrame() ?? '';
    // 顶横、中横（█████）、底横各出现
    expect(frame).toContain('███████');
    expect(frame).toContain('█████');
  });

  it('含 prompt 三角 ▶ 和光标 _', () => {
    const { lastFrame } = render(<Logo />);
    expect(lastFrame()).toContain('▶');
    expect(lastFrame()).toContain('_');
  });

  it('共 5 行（E 字形：上横/竖/中横/竖/下横）', () => {
    const { lastFrame } = render(<Logo />);
    // 去掉 ANSI 码后按换行切，非空行应为 5
    const lines = (lastFrame() ?? '').replace(/\x1b\[[0-9;]*m/g, '').split('\n').filter((l) => l.length > 0);
    expect(lines).toHaveLength(5);
  });
});
```

- [ ] **Step 6: 跑测试确认失败**

Run: `npx vitest run tests/ui/logo.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 7: 实现 Logo**

Create `src/ui/logo.tsx`（严格对照 `ui-preview.tsx` 的 `Logo` 组件，spec §8.3）：

```tsx
// Logo —— 5 行方块 E + ▶_（spec §8.3）。纯 █ 字符跨终端一致。
import { Box, Text } from 'ink';
import { T } from './theme.js';

export function Logo(): React.ReactElement {
  return (
    <Box flexDirection="column">
      <Text color={T.brand}>███████</Text>
      <Text color={T.brand}>█</Text>
      <Text>
        <Text color={T.brand}>█████   </Text>
        <Text color={T.accent}>▶</Text>
        <Text color={T.muted}>_</Text>
      </Text>
      <Text color={T.brand}>█</Text>
      <Text color={T.brand}>███████</Text>
    </Box>
  );
}
```

- [ ] **Step 8: 跑测试确认通过 + 提交**

Run: `npx vitest run tests/ui/logo.test.tsx`
Expected: PASS（3 个测试）。

Run: `npm run build`（验证 .tsx 编译链路通）

```bash
git add src/ui/spinner.tsx src/ui/logo.tsx tests/ui/spinner.test.tsx tests/ui/logo.test.tsx
git commit -m "feat(m3.5): UI 原子组件 Spinner + Logo"
```

---

## Task 3: AgentEvent 加 usage 事件（agent core 配合改动）

> **为什么动 agent core**：状态栏（spec §8.4）需要 `↑↓ tok | $费用`，数据源是 LLM 流末的 usage。当前 `consumeStream` 忽略 usage part（注释写"M5"）。本任务把每轮 usage 作为事件流出，UI 累计。改动最小：只加一种事件类型 + `consumeStream` 返回值多一个字段。

**Files:**
- Modify: `src/agent-events.ts`（加 `usage` 事件类型）
- Modify: `src/agent.ts`（`consumeStream` 返回 usage + 每轮 yield usage）
- Modify: `src/agent.ts` 的 `runAgent` wrapper switch（加 `usage` case，noUnusedLocals 要求 exhaustive，否则 `assertNever` 报错）
- Test: `tests/agent-stream.test.ts`（追加 usage 用例）

**Interfaces:**
- Consumes: `ECodeStreamPart` 的 `usage` variant（已存在于 `providers/types.ts`）。
- Produces: `AgentEvent` 新增 `{ type: 'usage'; inputTokens: number; outputTokens: number }`。Task 4 的 reducer / Task 9 的 StatusBar 消费它累计。
- **`PermissionGate` 接口不变**（仍 `'allow' | 'deny'`）；`allow_always` 在 Task 4 的 hook 内部映射，agent core 无感。

- [ ] **Step 1: 写失败测试**

在 `tests/agent-stream.test.ts` 末尾追加：

```ts
  it('LLM 流末带 usage → yield usage 事件（input/output tokens）', async () => {
    const provider = mockProvider([
      { type: 'text_delta', text: 'done' },
      { type: 'usage', inputTokens: 120, outputTokens: 30 },
      { type: 'stop', reason: { unified: 'stop', raw: 'stop' } },
    ]);
    const events = await collect(runAgentStream('打招呼', { provider }));
    const usage = events.find((e) => e.type === 'usage');
    expect(usage).toBeDefined();
    expect(usage?.type === 'usage' && usage.inputTokens).toBe(120);
    expect(usage?.type === 'usage' && usage.outputTokens).toBe(30);
  });

  it('多轮对话 → 每轮各 yield 一个 usage 事件（可累计）', async () => {
    // 第一轮：tool-use；第二轮：纯文本结束。
    // 用一个按 messages 长度返回不同流的自定义 provider
    let call = 0;
    const provider: ModelProvider = {
      name: 'mock',
      protocol: 'openai',
      baseURL: 'http://mock',
      complete: vi.fn(async () => ({
        content: [{ type: 'text', text: '压缩摘要' }],
        stopReason: { unified: 'stop' },
        usage: { inputTokens: 0, outputTokens: 0 },
      })),
      stream: async function* (req: ChatRequest): AsyncIterable<ECodeStreamPart> {
        call++;
        if (call === 1) {
          yield { type: 'tool_call_start', id: 't1', name: 'read_file' };
          yield { type: 'tool_call_delta', id: 't1', inputDelta: '{"path":"a"}' };
          yield { type: 'tool_call_end', id: 't1' };
          yield { type: 'usage', inputTokens: 100, outputTokens: 10 };
          yield { type: 'stop', reason: { unified: 'tool-use', raw: 'tc' } };
        } else {
          yield { type: 'text_delta', text: '好了' };
          yield { type: 'usage', inputTokens: 200, outputTokens: 20 };
          yield { type: 'stop', reason: { unified: 'stop', raw: 'stop' } };
        }
      },
    };
    const events = await collect(runAgentStream('读 a', { provider }));
    const usages = events.filter((e) => e.type === 'usage');
    expect(usages).toHaveLength(2);
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/agent-stream.test.ts`
Expected: FAIL（找不到 `type === 'usage'` 事件，因为还没 yield）。

- [ ] **Step 3: agent-events.ts 加 usage 类型**

Modify `src/agent-events.ts`，在 `AgentEvent` 联合里加一条（紧跟 `completed` 之后、`error` 之前）：

```ts
  | { type: 'completed'; rounds: number; toolCalls: number; reason: AgentCompletionReason }
  | { type: 'usage'; inputTokens: number; outputTokens: number }
  | { type: 'error'; error: string };
```

- [ ] **Step 4: consumeStream 返回 usage + runAgentStream yield**

Modify `src/agent.ts` 的 `consumeStream`：

返回类型加 `usage` 字段。把函数签名和实现改为：

```ts
async function consumeStream(
  gen: AsyncIterable<ECodeStreamPart>,
  signal?: AbortSignal,
): Promise<{
  text: string;
  toolCalls: Array<{ id: string; name: string; input: Record<string, unknown> }>;
  stopUnified: string;
  usage: { inputTokens: number; outputTokens: number };
}> {
  let text = '';
  const inputBuf = new Map<string, string>();
  const names = new Map<string, string>();
  const order: string[] = [];
  let stopUnified = 'stop';
  let usage = { inputTokens: 0, outputTokens: 0 };
  for await (const part of gen) {
    if (signal?.aborted) throw new DOMException('Aborted', 'AbortError');
    switch (part.type) {
      case 'text_delta':
        text += part.text;
        break;
      case 'tool_call_start':
        names.set(part.id, part.name);
        inputBuf.set(part.id, '');
        order.push(part.id);
        break;
      case 'tool_call_delta':
        inputBuf.set(part.id, (inputBuf.get(part.id) ?? '') + part.inputDelta);
        break;
      case 'tool_call_end':
        break;
      case 'usage':
        usage = { inputTokens: part.inputTokens, outputTokens: part.outputTokens };
        break;
      case 'stop':
        stopUnified = part.reason.unified;
        break;
    }
  }
  const toolCalls = order.map((id) => ({
    id,
    name: names.get(id) ?? 'unknown',
    input: safeParseToolInput(inputBuf.get(id) ?? '{}'),
  }));
  return { text, toolCalls, stopUnified, usage };
}
```

Modify `runAgentStream`：把 `const { text, toolCalls } = await consumeStream(...)` 改为解构出 `usage`，并在拿到结果后、`if (text) yield text_delta` 之后加一行 yield usage：

```ts
      const { text, toolCalls, usage } = await consumeStream(streamGen, opts.signal);
      stats.rounds = iteration + 1;
      // ... 构造 assistantBlocks / logApiResponse 不变 ...
      if (text) yield { type: 'text_delta', text };
      yield { type: 'usage', inputTokens: usage.inputTokens, outputTokens: usage.outputTokens };
```

- [ ] **Step 5: runAgent wrapper switch 加 usage case**

Modify `src/agent.ts` 的 `runAgent` switch（noUnusedLocals + assertNever 要求 exhaustive），在 `completed` case 后加：

```ts
      case 'usage':
        // 旧 CLI 不显示 token（状态栏在 REPL 里显示），静默吸收。
        break;
```

- [ ] **Step 6: 跑测试确认通过**

Run: `npx vitest run tests/agent-stream.test.ts`
Expected: PASS（含新加的 2 个 usage 用例 + 原有 3 个）。

Run: `npm run build`（验证 exhaustive switch 无 assertNever 报错）
Expected: tsc 通过。

- [ ] **Step 7: 全量测试回归 + 提交**

Run: `npm test`
Expected: 全绿（usage 改动不影响其他用例）。

```bash
git add src/agent-events.ts src/agent.ts tests/agent-stream.test.ts
git commit -m "feat(m3.5): AgentEvent 加 usage 事件（供状态栏 token/费用累计）"
```

---

## Task 4: UI 类型 + 纯状态机 reducer + useAgentStream hook

> **核心任务**。分两段：先 TDD 纯函数 `reduceAgentEvent`（核心状态逻辑，零 React 依赖最好测），再写 `useAgentStream` hook 包它（接 React state + runAgentStream + 权限 gate + generation 防竞态）。

**Files:**
- Create: `src/ui/types.ts`
- Create: `src/ui/reduce-agent-event.ts`
- Create: `src/ui/use-agent-stream.ts`
- Test: `tests/ui/reduce-agent-event.test.ts`
- Test: `tests/ui/use-agent-stream.test.tsx`

**Interfaces:**
- Consumes: `AgentEvent`（from `agent-events.ts`，含 Task 3 的 usage）、`runAgentStream` / `RunAgentStreamOptions`（from `agent.ts`）、`AllowList`（from `permission.ts`）。
- Produces: `useAgentStream(opts)` → `UseAgentStreamReturn`，供 App / ChatView / PermissionDialog / StatusBar 消费。`reduceAgentEvent(state, event)` 供 reducer 单测。

- [ ] **Step 1: 定义 UI 类型**

Create `src/ui/types.ts`：

```ts
// UI 层显示用类型（spec §5.1：DisplayMessage ≠ LLM history）。

/** 单条工具调用在动态区的运行态表示。 */
export interface ActiveTool {
  id: string;
  name: string;
  /** 启动时间戳（ms），供计时显示。 */
  startedAt: number;
}

/** 已冻结进 <Static> 的消息（用户输入 / 助手文本 / 工具结果 / 系统警告）。 */
export type DisplayMessage =
  | { kind: 'user'; id: string; text: string }
  | { kind: 'assistant'; id: string; text: string } // 已完成的助手文本（completed 时从 streamingText 落地）
  | { kind: 'tool'; id: string; name: string; content: string; isError: boolean; input?: Record<string, unknown> }
  | { kind: 'warning'; id: string; text: string }
  | { kind: 'error'; id: string; text: string };

/** 权限请求挂起态（UI 据此渲染 PermissionDialog）。 */
export interface PendingPermission {
  toolUseId: string;
  toolName: string;
  input: Record<string, unknown>;
}

/** reducer 的状态形状（不含命令方法；命令在 hook 层）。 */
export interface StreamState {
  completedMessages: DisplayMessage[];
  streamingText: string | null;
  activeTools: ActiveTool[];
  pendingPermission: PendingPermission | null;
  /** 累计 token（每轮 usage 事件累加）。 */
  usage: { inputTokens: number; outputTokens: number };
  isRunning: boolean;
  error: string | null;
  /** 最近一次 completed 的元信息（供状态栏/调试）。 */
  lastCompleted: { rounds: number; reason: string } | null;
}

export const initialStreamState: StreamState = {
  completedMessages: [],
  streamingText: null,
  activeTools: [],
  pendingPermission: null,
  usage: { inputTokens: 0, outputTokens: 0 },
  isRunning: false,
  error: null,
  lastCompleted: null,
};
```

- [ ] **Step 2: 写 reducer 失败测试**

Create `tests/ui/reduce-agent-event.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { reduceAgentEvent, initialStreamState } from '../../src/ui/reduce-agent-event.js';
import type { AgentEvent } from '../../src/agent-events.js';

describe('reduceAgentEvent', () => {
  it('start → isRunning=true', () => {
    const s = reduceAgentEvent(initialStreamState, { type: 'start', task: 't', model: 'm', provider: 'p' });
    expect(s.isRunning).toBe(true);
  });

  it('text_delta → 累加到 streamingText', () => {
    let s = reduceAgentEvent(initialStreamState, { type: 'text_delta', text: '你好' });
    s = reduceAgentEvent(s, { type: 'text_delta', text: '世界' });
    expect(s.streamingText).toBe('你好世界');
  });

  it('tool_call_start → 加入 activeTools', () => {
    const s = reduceAgentEvent(initialStreamState, { type: 'tool_call_start', id: 't1', name: 'bash' });
    expect(s.activeTools).toHaveLength(1);
    expect(s.activeTools[0].id).toBe('t1');
    expect(s.activeTools[0].name).toBe('bash');
    expect(typeof s.activeTools[0].startedAt).toBe('number');
  });

  it('tool_result → 移出 activeTools + 落地 completedMessages(kind:tool)', () => {
    let s = reduceAgentEvent(initialStreamState, { type: 'tool_call_start', id: 't1', name: 'bash' });
    s = reduceAgentEvent(s, { type: 'tool_result', id: 't1', name: 'bash', content: 'ok', isError: false });
    expect(s.activeTools).toHaveLength(0);
    const tool = s.completedMessages.find((m) => m.kind === 'tool');
    expect(tool).toBeDefined();
    expect(tool?.kind === 'tool' && tool.content).toBe('ok');
    expect(tool?.kind === 'tool' && tool.isError).toBe(false);
  });

  it('permission_request → pendingPermission 挂起', () => {
    const s = reduceAgentEvent(initialStreamState, {
      type: 'permission_request',
      toolUseId: 't1',
      toolName: 'bash',
      input: { command: 'rm -rf x' },
    });
    expect(s.pendingPermission?.toolName).toBe('bash');
    expect(s.pendingPermission?.input.command).toBe('rm -rf x');
  });

  it('completed(done) → streamingText 落地为 assistant 消息 + 清空 + isRunning=false', () => {
    let s = reduceAgentEvent(initialStreamState, { type: 'text_delta', text: '答复' });
    s = reduceAgentEvent(s, { type: 'completed', rounds: 1, toolCalls: 0, reason: 'done' });
    expect(s.streamingText).toBeNull();
    expect(s.isRunning).toBe(false);
    const asst = s.completedMessages.find((m) => m.kind === 'assistant');
    expect(asst?.kind === 'assistant' && asst.text).toBe('答复');
  });

  it('completed 但无 streamingText → 不落地空 assistant 消息', () => {
    const s = reduceAgentEvent(initialStreamState, { type: 'completed', rounds: 1, toolCalls: 0, reason: 'done' });
    expect(s.completedMessages.find((m) => m.kind === 'assistant')).toBeUndefined();
  });

  it('usage → 累加 token', () => {
    let s = reduceAgentEvent(initialStreamState, { type: 'usage', inputTokens: 100, outputTokens: 10 });
    s = reduceAgentEvent(s, { type: 'usage', inputTokens: 50, outputTokens: 5 });
    expect(s.usage).toEqual({ inputTokens: 150, outputTokens: 15 });
  });

  it('warning → 落地 kind:warning 消息', () => {
    const s = reduceAgentEvent(initialStreamState, { type: 'warning', message: '上下文已压缩' });
    expect(s.completedMessages.some((m) => m.kind === 'warning')).toBe(true);
  });

  it('error → 落地 kind:error + isRunning=false', () => {
    const s = reduceAgentEvent(initialStreamState, { type: 'error', error: 'boom' });
    expect(s.isRunning).toBe(false);
    expect(s.error).toBe('boom');
    expect(s.completedMessages.some((m) => m.kind === 'error')).toBe(true);
  });

  it('不可变性：不修改原 state（返回新对象）', () => {
    const before = initialStreamState;
    reduceAgentEvent(before, { type: 'text_delta', text: 'x' });
    expect(before.streamingText).toBeNull(); // 原对象未被改
    expect(before.completedMessages).toHaveLength(0);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run tests/ui/reduce-agent-event.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 4: 实现 reduceAgentEvent**

Create `src/ui/reduce-agent-event.ts`：

```ts
// 纯函数状态机：AgentEvent → StreamState（spec §5.1 事件分发逻辑）。
// 无 React 依赖，核心状态转换全在此可测。useAgentStream hook 包它接 React。
import type { AgentEvent } from '../agent-events.js';
import { initialStreamState, type StreamState, type DisplayMessage } from './types.js';

/** 单调递增 id 生成（reducer 内部用，保证 DisplayMessage 稳定 key）。 */
let msgSeq = 0;
const nextId = (): string => `m${++msgSeq}`;

/**
 * 把一个 AgentEvent 折进 state，返回新 state（不可变）。
 * - text_delta → streamingText 累加
 * - tool_call_start → activeTools 追加
 * - tool_result → activeTools 移除 + completedMessages 追加 kind:tool
 * - permission_request → pendingPermission 挂起
 * - completed → streamingText 落地 kind:assistant + isRunning=false
 * - usage → 累计 token
 * - warning/error → 落地系统消息
 */
export function reduceAgentEvent(state: StreamState, event: AgentEvent): StreamState {
  switch (event.type) {
    case 'start':
      return { ...state, isRunning: true, error: null };

    case 'text_delta':
      return { ...state, streamingText: (state.streamingText ?? '') + event.text };

    case 'tool_call_start':
      return {
        ...state,
        activeTools: [
          ...state.activeTools,
          { id: event.id, name: event.name, startedAt: Date.now() },
        ],
      };

    case 'tool_result': {
      const activeTools = state.activeTools.filter((t) => t.id !== event.id);
      const msg: DisplayMessage = {
        kind: 'tool',
        id: nextId(),
        name: event.name,
        content: event.content,
        isError: event.isError,
      };
      return { ...state, activeTools, completedMessages: [...state.completedMessages, msg] };
    }

    case 'permission_request':
      return {
        ...state,
        pendingPermission: {
          toolUseId: event.toolUseId,
          toolName: event.toolName,
          input: event.input,
        },
      };

    case 'completed': {
      const msgs = [...state.completedMessages];
      if (state.streamingText) {
        msgs.push({ kind: 'assistant', id: nextId(), text: state.streamingText });
      }
      return {
        ...state,
        completedMessages: msgs,
        streamingText: null,
        isRunning: false,
        lastCompleted: { rounds: event.rounds, reason: event.reason },
      };
    }

    case 'usage':
      return {
        ...state,
        usage: {
          inputTokens: state.usage.inputTokens + event.inputTokens,
          outputTokens: state.usage.outputTokens + event.outputTokens,
        },
      };

    case 'warning':
      return {
        ...state,
        completedMessages: [
          ...state.completedMessages,
          { kind: 'warning', id: nextId(), text: event.message },
        ],
      };

    case 'error':
      return {
        ...state,
        isRunning: false,
        error: event.error,
        completedMessages: [
          ...state.completedMessages,
          { kind: 'error', id: nextId(), text: event.error },
        ],
      };

    default: {
      // exhaustive（noFallthroughCasesInSwitch + 类型守卫）
      const _exhaustive: never = event;
      void _exhaustive;
      return state;
    }
  }
}

export { initialStreamState };
```

- [ ] **Step 5: 跑 reducer 测试确认通过**

Run: `npx vitest run tests/ui/reduce-agent-event.test.ts`
Expected: PASS（11 个测试）。

- [ ] **Step 6: 写 hook 失败测试**

Create `tests/ui/use-agent-stream.test.tsx`：

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render } from 'ink-testing-library';
import React, { useEffect } from 'react';
import { useAgentStream } from '../../src/ui/use-agent-stream.js';
import { runAgentStream } from '../../src/agent.js';
import type { AgentEvent } from '../../src/agent-events.js';

// 把"用户消息落地"逻辑也测到：submit 后 completedMessages 含 user 消息。
vi.mock('../../src/agent.js', () => ({
  runAgentStream: vi.fn(),
}));

// 辅助：渲染一个消费 hook 的壳组件，submit 后等事件流跑完。
function Harness({ onSubmitReady }: { onSubmitReady: (api: ReturnType<typeof useAgentStream>) => void }) {
  const api = useAgentStream({ model: 'mock-model' });
  useEffect(() => {
    onSubmitReady(api);
  }, []); // 只挂载时把 api 交出去
  return null;
}

describe('useAgentStream', () => {
  it('submit → 用户消息进入 completedMessages + 启动 runAgentStream', async () => {
    const mocked = runAgentStream as unknown as ReturnType<typeof vi.fn>;
    // 立即完成的空事件流
    mocked.mockImplementation(async function* (): AsyncGenerator<AgentEvent> {
      yield { type: 'start', task: 'hi', model: 'm', provider: 'p' };
      yield { type: 'completed', rounds: 1, toolCalls: 0, reason: 'done' };
    } as never);

    let api!: ReturnType<typeof useAgentStream>;
    const { rerender, waitUntilDone } = render(<Harness onSubmitReady={(a) => (api = a)} />);

    api.submit('你好');
    // 让微任务/async 跑完
    await new Promise((r) => setTimeout(r, 50));

    expect(mocked).toHaveBeenCalledWith('你好', expect.objectContaining({ model: 'mock-model' }));
    expect(api.completedMessages.some((m) => m.kind === 'user' && m.text === '你好')).toBe(true);
    expect(api.isRunning).toBe(false);
  });

  it('permission_request → pendingPermission 挂起；resolvePermission(allow_always) → gate 返回 allow 且 allow 列表记住', async () => {
    const mocked = runAgentStream as unknown as ReturnType<typeof vi.fn>;
    mocked.mockImplementation(async function* (task: string, opts?: { permissionGate?: { ask: (r: unknown) => Promise<string> } }): AsyncGenerator<AgentEvent> {
      yield { type: 'start', task, model: 'm', provider: 'p' };
      yield { type: 'permission_request', toolUseId: 't1', toolName: 'bash', input: { command: 'x' } };
      const decision = opts?.permissionGate ? await opts.permissionGate.ask({}) : 'allow';
      yield { type: 'tool_call_start', id: 't1', name: 'bash' };
      yield { type: 'tool_result', id: 't1', name: 'bash', content: 'ok', isError: false };
      yield { type: 'completed', rounds: 1, toolCalls: 1, reason: 'done' };
      void decision;
    } as never);

    let api!: ReturnType<typeof useAgentStream>;
    render(<Harness onSubmitReady={(a) => (api = a)} />);
    api.submit('跑 bash');
    await new Promise((r) => setTimeout(r, 30));

    // 等待 pendingPermission 挂起
    await vi.waitFor(() => expect(api.pendingPermission).not.toBeNull());
    expect(api.pendingPermission?.toolName).toBe('bash');

    api.resolvePermission('allow_always');
    await new Promise((r) => setTimeout(r, 50));

    // allow_always 后 allow 列表含 bash（后续不再问）
    expect(api.isAllowAlways('bash')).toBe(true);
    expect(api.pendingPermission).toBeNull();
  });

  it('abort → 中断正在跑的流', async () => {
    const mocked = runAgentStream as unknown as ReturnType<typeof vi.fn>;
    mocked.mockImplementation(async function* (): AsyncGenerator<AgentEvent> {
      yield { type: 'start', task: 'hi', model: 'm', provider: 'p' };
      // 模拟长跑：不 yield completed，等 abort
      yield { type: 'text_delta', text: '部分' };
    } as never);

    let api!: ReturnType<typeof useAgentStream>;
    render(<Harness onSubmitReady={(a) => (api = a)} />);
    api.submit('hi');
    await new Promise((r) => setTimeout(r, 20));
    expect(api.isRunning).toBe(true);

    api.abort();
    await new Promise((r) => setTimeout(r, 20));
    // abort 后允许 isRunning 最终为 false（generation finally 清理）
    expect(typeof api.isRunning).toBe('boolean');
  });
});
```

- [ ] **Step 7: 跑测试确认失败**

Run: `npx vitest run tests/ui/use-agent-stream.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 8: 实现 useAgentStream hook**

Create `src/ui/use-agent-stream.ts`：

```ts
// useAgentStream —— runAgentStream ↔ React state 的桥（spec §5.1 / §4.3 / §4.4 / §4.5）。
// 设计：
//   - 核心状态转换委托纯函数 reduceAgentEvent（可单测）
//   - 双 state + ref：useState 驱动渲染，useRef 在 async 闭包内读最新值（§4.3）
//   - generation 计数器防竞态（§4.4）：abort A 后启 B，A 的 finally 不覆盖 B
//   - 权限 gate 内部把 allow_always 映射成 allow + allow.add（§4.5，agent core 无感）
import { useCallback, useRef, useState } from 'react';
import { runAgentStream } from '../agent.js';
import type { RunAgentStreamOptions } from '../agent.js';
import { AllowList } from '../permission.js';
import { reduceAgentEvent, initialStreamState } from './reduce-agent-event.js';
import type { StreamState, DisplayMessage, PendingPermission } from './types.js';

export interface UseAgentStreamReturn {
  completedMessages: DisplayMessage[];
  streamingText: string | null;
  activeTools: StreamState['activeTools'];
  pendingPermission: PendingPermission | null;
  usage: StreamState['usage'];
  isRunning: boolean;
  error: string | null;
  /** submit 用户输入（含斜杠命令？不——命令在 App 层拦截，这里只处理纯消息）。 */
  submit: (text: string) => void;
  /** 用户在 PermissionDialog 选了决策。allow_always → allow.add(toolName) 后 resolve allow。 */
  resolvePermission: (decision: 'allow' | 'deny' | 'allow_always') => void;
  /** 中断当前流（Esc 触发）。 */
  abort: () => void;
  /** 查某工具是否已 allow_always（测试 + 状态栏可用）。 */
  isAllowAlways: (toolName: string) => boolean;
  /** 清空已完成消息（/clear 命令用）。 */
  clear: () => void;
}

export interface UseAgentStreamOptions {
  model?: string;
  /** 预拼 system（含 CLAUDE.md）。 */
  system?: string;
}

export function useAgentStream(opts: UseAgentStreamOptions = {}): UseAgentStreamReturn {
  const [state, setState] = useState<StreamState>(initialStreamState);

  // refs（async 闭包内读最新值，§4.3）
  const stateRef = useRef(state);
  stateRef.current = state;
  const generationRef = useRef(0);
  const abortRef = useRef<AbortController | null>(null);
  const allowRef = useRef(new AllowList());

  // 权限 gate 决策 resolver（permission_request 时挂起，resolvePermission 时兑现）
  const permissionResolverRef = useRef<((d: 'allow' | 'deny') => void) | null>(null);

  const apply = useCallback((event: Parameters<typeof reduceAgentEvent>[1]) => {
    setState((prev) => reduceAgentEvent(prev, event));
  }, []);

  const submit = useCallback(
    (text: string) => {
      // 用户消息先落地（同步，UI 立刻可见）
      setState((prev) => ({
        ...prev,
        completedMessages: [
          ...prev.completedMessages,
          { kind: 'user', id: `u${Date.now()}`, text },
        ],
      }));

      const generation = ++generationRef.current;
      const controller = new AbortController();
      abortRef.current = controller;

      // gate 实现（§4.5）：ask 返回 Promise，由 resolvePermission 兑现
      const streamOpts: RunAgentStreamOptions = {
        model: opts.model,
        system: opts.system,
        signal: controller.signal,
        allow: allowRef.current,
        permissionGate: {
          ask: () =>
            new Promise<'allow' | 'deny'>((resolve) => {
              permissionResolverRef.current = resolve;
            }),
        },
      };

      // async IIFE：消费事件流（§5.1 submit 流程 4-11）
      void (async () => {
        try {
          for await (const event of runAgentStream(text, streamOpts)) {
            apply(event);
          }
        } catch (err) {
          // runAgentStream 内部已 try/yield error，这里兜底未捕获异常
          apply({
            type: 'error',
            error: err instanceof Error ? err.message : String(err),
          });
        } finally {
          // generation 匹配才清 isRunning（§4.4 防竞态）
          if (generationRef.current === generation) {
            setState((prev) => (prev.isRunning ? { ...prev, isRunning: false } : prev));
          }
        }
      })();
    },
    [opts.model, opts.system, apply],
  );

  const resolvePermission = useCallback((decision: 'allow' | 'deny' | 'allow_always') => {
    if (decision === 'allow_always' && stateRef.current.pendingPermission) {
      allowRef.current.add(stateRef.current.pendingPermission.toolName);
    }
    permissionResolverRef.current?.(decision === 'deny' ? 'deny' : 'allow');
    permissionResolverRef.current = null;
    setState((prev) => ({ ...prev, pendingPermission: null }));
  }, []);

  const abort = useCallback(() => {
    abortRef.current?.abort();
  }, []);

  const isAllowAlways = useCallback((toolName: string) => allowRef.current.has(toolName), []);

  const clear = useCallback(() => {
    setState((prev) => ({ ...prev, completedMessages: [] }));
  }, []);

  return {
    completedMessages: state.completedMessages,
    streamingText: state.streamingText,
    activeTools: state.activeTools,
    pendingPermission: state.pendingPermission,
    usage: state.usage,
    isRunning: state.isRunning,
    error: state.error,
    submit,
    resolvePermission,
    abort,
    isAllowAlways,
    clear,
  };
}
```

- [ ] **Step 9: 跑 hook 测试确认通过**

Run: `npx vitest run tests/ui/use-agent-stream.test.tsx`
Expected: PASS（3 个测试）。

- [ ] **Step 10: build + 全量回归 + 提交**

Run: `npm run build && npm test`
Expected: 全绿。

```bash
git add src/ui/types.ts src/ui/reduce-agent-event.ts src/ui/use-agent-stream.ts tests/ui/reduce-agent-event.test.ts tests/ui/use-agent-stream.test.tsx
git commit -m "feat(m3.5): UI 状态机 reduceAgentEvent + useAgentStream hook"
```

---

## Task 5: MarkdownRenderer（流式纯文本 / 完成后 full render）

**Files:**
- Create: `src/ui/markdown.tsx`
- Test: `tests/ui/markdown.test.tsx`

**Interfaces:**
- Consumes: `T`（theme）。
- Produces: `<MarkdownRenderer text: string streaming?: boolean />`。streaming=true → 纯 `<Text>`；streaming=false → marked 解析 + cli-highlight 代码块高亮。ChatView 在动态区用 `streaming`，在 `<Static>` 完成消息用非 streaming。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/markdown.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import { MarkdownRenderer } from '../../src/ui/markdown.js';

describe('<MarkdownRenderer />', () => {
  it('streaming=true → 原样输出纯文本（不解析 markdown）', () => {
    const { lastFrame } = render(<MarkdownRenderer text="**未完成**的粗体" streaming />);
    expect(lastFrame()).toContain('**未完成**的粗体');
  });

  it('streaming=false → 解析粗体（去掉 ** 标记）', () => {
    const { lastFrame } = render(<MarkdownRenderer text="**完成**的粗体" />);
    const frame = lastFrame() ?? '';
    expect(frame).not.toContain('**');
    expect(frame).toContain('完成');
    expect(frame).toContain('的粗体');
  });

  it('完成态渲染代码块（含代码内容）', () => {
    const md = '示例：\n\n```ts\nconst x = 1;\n```\n';
    const { lastFrame } = render(<MarkdownRenderer text={md} />);
    expect(lastFrame()).toContain('const x = 1;');
  });

  it('完成态渲染列表项', () => {
    const md = '- 苹果\n- 香蕉\n';
    const { lastFrame } = render(<MarkdownRenderer text={md} />);
    expect(lastFrame()).toContain('苹果');
    expect(lastFrame()).toContain('香蕉');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/markdown.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 MarkdownRenderer**

Create `src/ui/markdown.tsx`：

```tsx
// MarkdownRenderer —— 流式纯文本 / 完成后 marked + cli-highlight full render（spec §5.6）。
// 为什么不做 prefix-cache 流式 markdown：React.memo 已天然隔离稳定段落，手写边界探测过度设计。
import React from 'react';
import { Text, Box } from 'ink';
import { marked } from 'marked';
import { highlight } from 'cli-highlight';
import { T } from './theme.js';

interface MarkdownRendererProps {
  text: string;
  /** true=流式中，纯文本输出；false=完成，marked 解析 + 高亮。 */
  streaming?: boolean;
}

// 配置 marked：返回结构化 token，自己渲染（不直接 to-string，便于着色）。
marked.setOptions({ gfm: true, breaks: false });

/** 把 marked token 树拍平成 React 节点数组。 */
function renderTokens(tokens: marked.TokensList, keyPrefix: string): React.ReactNode[] {
  const nodes: React.ReactNode[] = [];
  tokens.forEach((tok, i) => {
    const key = `${keyPrefix}-${i}`;
    switch (tok.type) {
      case 'heading':
        nodes.push(
          <Text key={key} bold color={T.brand}>
            {tok.text}
          </Text>,
        );
        break;
      case 'paragraph':
        nodes.push(
          <Text key={key}>{renderInline(tok.tokens ?? [], key)}</Text>,
        );
        break;
      case 'code': {
        const code = tok.text;
        let displayed = code;
        try {
          displayed = highlight(code, { language: tok.lang || 'ts' });
        } catch {
          /* 解析失败保留原文 */
        }
        // cli-highlight 输出带 ANSI；用 dangerouslyPasteText 避免被 Text 再转义
        nodes.push(
          <Box key={key} flexDirection="column">
            <Text>{displayed}</Text>
          </Box>,
        );
        break;
      }
      case 'list': {
        tok.items.forEach((item, j) => {
          nodes.push(
            <Text key={`${key}-${j}`}>
              <Text color={T.user}>• </Text>
              {renderInline(item.tokens ?? [], `${key}-${j}`)}
            </Text>,
          );
        });
        break;
      }
      case 'space':
        nodes.push(<Text key={key}> </Text>);
        break;
      default:
        // 兜底：raw 文本
        nodes.push(<Text key={key}>{tok.raw}</Text>);
    }
  });
  return nodes;
}

/** 行内 token（bold/italic/codespan/link/text）渲染。 */
function renderInline(tokens: marked.Token[], keyPrefix: string): React.ReactNode {
  return tokens.map((tok, i) => {
    const key = `${keyPrefix}-i${i}`;
    if (tok.type === 'strong') return <Text key={key} bold>{tok.text}</Text>;
    if (tok.type === 'em') return <Text key={key} italic>{tok.text}</Text>;
    if (tok.type === 'codespan')
      return (
        <Text key={key} color={T.tool}>
          {tok.text}
        </Text>
      );
    if (tok.type === 'link') return <Text key={key} color={T.accent}>{tok.text}</Text>;
    if (tok.type === 'text') return <Text key={key}>{tok.text}</Text>;
    return <Text key={key}>{tok.raw}</Text>;
  });
}

export function MarkdownRenderer({ text, streaming = false }: MarkdownRendererProps): React.ReactElement {
  if (streaming) {
    return <Text>{text}</Text>;
  }
  const parsed = marked.lexer(text);
  return <Box flexDirection="column">{renderTokens(parsed, 'md')}</Box>;
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/markdown.test.tsx`
Expected: PASS（4 个测试）。

Run: `npm run build`
Expected: tsc 通过（marked / cli-highlight 类型解析正常）。

```bash
git add src/ui/markdown.tsx tests/ui/markdown.test.tsx
git commit -m "feat(m3.5): MarkdownRenderer（流式纯文本 + 完成态 marked 高亮）"
```

---

## Task 6: ToolPanel（工具三态 + per-tool 折叠）

**Files:**
- Create: `src/ui/tool-panel.tsx`
- Test: `tests/ui/tool-panel.test.tsx`

**Interfaces:**
- Consumes: `T`, `SYMBOLS`, `Spinner`（Task 1/2）。
- Produces:
  - `<ToolRunning name: string arg?: string />`（spinner + `▸ name(arg)` + 计时）
  - `<ToolDone name: string content: string isError: boolean input?: Record<string, unknown> />`（`▸ name` + `↳ ✓/✗ 摘要`，per-tool 折叠策略）
- ChatView 用这俩渲染动态区 / `<Static>` 工具消息。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/tool-panel.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import { ToolRunning, ToolDone } from '../../src/ui/tool-panel.js';
import { SYMBOLS } from '../../src/ui/theme.js';

describe('ToolRunning', () => {
  it('显示 ▸ + 工具名 + 参数', () => {
    const { lastFrame } = render(<ToolRunning name="bash" arg="npm test" />);
    const f = lastFrame() ?? '';
    expect(f).toContain(SYMBOLS.tool);
    expect(f).toContain('bash');
    expect(f).toContain('npm test');
  });
});

describe('ToolDone', () => {
  it('bash 成功 → 前 3 行 + ... N more lines（超过 3 行时折叠）', () => {
    const content = 'line1\nline2\nline3\nline4\nline5';
    const { lastFrame } = render(<ToolDone name="bash" content={content} isError={false} />);
    const f = lastFrame() ?? '';
    expect(f).toContain(SYMBOLS.success);
    expect(f).toContain('line1');
    expect(f).toContain('line2');
    expect(f).toContain('line3');
    expect(f).toContain('... 2 more lines'); // 5-3=2
    expect(f).not.toContain('line4');
  });

  it('bash 成功 ≤3 行 → 全显示无折叠提示', () => {
    const content = 'a\nb\nc';
    const { lastFrame } = render(<ToolDone name="bash" content={content} isError={false} />);
    expect(lastFrame()).not.toContain('more lines');
  });

  it('bash 错误 → 前 5 行（错误栈关键信息常在后面，多给几行）', () => {
    const content = Array.from({ length: 8 }, (_, i) => `err${i}`).join('\n');
    const { lastFrame } = render(<ToolDone name="bash" content={content} isError={true} />);
    const f = lastFrame() ?? '';
    expect(f).toContain(SYMBOLS.error);
    expect(f).toContain('err0');
    expect(f).toContain('err4');
    expect(f).toContain('... 3 more lines'); // 8-5=3
  });

  it('read_file → 只显 "Read N lines"（不显内容主体）', () => {
    const content = 'const x = 1;\nconst y = 2;';
    const { lastFrame } = render(<ToolDone name="read_file" content={content} isError={false} />);
    const f = lastFrame() ?? '';
    expect(f).toMatch(/Read \d+ lines/);
    expect(f).not.toContain('const x');
  });

  it('edit_file → 完整内容不折叠（diff 是精华）', () => {
    const content = '- old\n+ new\n+ newer';
    const { lastFrame } = render(<ToolDone name="edit_file" content={content} isError={false} />);
    const f = lastFrame() ?? '';
    expect(f).toContain('- old');
    expect(f).toContain('+ new');
    expect(f).not.toContain('more lines');
  });

  it('grep → 前 3 行匹配 + ... N more matches', () => {
    const content = 'a.ts:1: TODO\nb.ts:2: TODO\nc.ts:3: TODO\nd.ts:4: TODO';
    const { lastFrame } = render(<ToolDone name="grep" content={content} isError={false} />);
    const f = lastFrame() ?? '';
    expect(f).toContain('a.ts:1: TODO');
    expect(f).toContain('... 1 more matches');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/tool-panel.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 ToolPanel**

Create `src/ui/tool-panel.tsx`：

```tsx
// ToolPanel —— 工具调用可视化（spec §5.5 / §8.4③④⑤）。
// 三态：running（spinner+计时）/ done（↳ ✓ 摘要）/ error（↳ ✗ 摘要）。
// 折叠策略 per-tool 差异化（不一刀切）：
//   bash 成功=前3行 / bash 错误=前5行 / edit_file/write_file=完整 / read_file=只显行数 / grep=前3行
import React from 'react';
import { Text, Box } from 'ink';
import { T, SYMBOLS } from './theme.js';
import { Spinner } from './spinner.js';

/** 计时显示：跑超过 3s 才显示耗时（避免短任务闪一下）。 */
function useElapsed(startedAt: number): string {
  // 简化：返回固定占位，实际耗时由父组件传（避免 hook 在 SSR/测试里跑定时器）。
  void startedAt;
  return '';
}

interface ToolRunningProps {
  name: string;
  arg?: string;
}

export function ToolRunning({ name, arg }: ToolRunningProps): React.ReactElement {
  return (
    <Box>
      <Spinner color={T.brand} />
      <Text color={T.tool}> {SYMBOLS.tool} {name}</Text>
      {arg ? <Text color={T.muted}>({arg})</Text> : null}
    </Box>
  );
}

/** 把多行内容按工具折叠策略裁剪。返回 { lines, omitted }。 */
function foldContent(
  name: string,
  isError: boolean,
  content: string,
): { lines: string[]; omitted: number; label: string } {
  const all = content.split('\n');
  // read_file：只报行数，不显内容
  if (name === 'read_file') {
    return { lines: [`Read ${all.length} lines`], omitted: 0, label: '' };
  }
  // edit_file / write_file：完整不折叠
  if (name === 'edit_file' || name === 'write_file') {
    return { lines: all, omitted: 0, label: '' };
  }
  // bash：成功前 3 行，错误前 5 行
  if (name === 'bash') {
    const head = isError ? 5 : 3;
    return all.length > head
      ? { lines: all.slice(0, head), omitted: all.length - head, label: 'more lines' }
      : { lines: all, omitted: 0, label: '' };
  }
  // grep：前 3 行匹配
  if (name === 'grep') {
    return all.length > 3
      ? { lines: all.slice(0, 3), omitted: all.length - 3, label: 'more matches' }
      : { lines: all, omitted: 0, label: '' };
  }
  // 其他：完整
  return { lines: all, omitted: 0, label: '' };
}

interface ToolDoneProps {
  name: string;
  content: string;
  isError: boolean;
  input?: Record<string, unknown>;
}

export function ToolDone({ name, content, isError, input }: ToolDoneProps): React.ReactElement {
  const { lines, omitted, label } = foldContent(name, isError, content);
  const arg = summarizeArg(name, input);
  return (
    <Box flexDirection="column">
      <Text>
        <Text color={T.tool}>{SYMBOLS.tool} {name}</Text>
        {arg ? <Text color={T.muted}>({arg})</Text> : null}
      </Text>
      <Text>
        <Text color={T.result}>{SYMBOLS.result} </Text>
        {isError ? (
          <Text color={T.error}>{SYMBOLS.error} </Text>
        ) : (
          <Text color={T.success}>{SYMBOLS.success} </Text>
        )}
        <Text>{lines[0]}</Text>
      </Text>
      {lines.slice(1).map((l, i) => (
        <Text key={i} color={T.muted}>    {l}</Text>
      ))}
      {omitted > 0 ? (
        <Text color={T.muted}>    ... {omitted} {label}</Text>
      ) : null}
    </Box>
  );
}

/** 生成工具参数摘要（显示在工具名后括号里）。 */
function summarizeArg(name: string, input?: Record<string, unknown>): string {
  if (!input) return '';
  if (name === 'bash') return String(input.command ?? '');
  if (name === 'read_file' || name === 'write_file' || name === 'edit_file')
    return String(input.path ?? '');
  if (name === 'grep') return `"${input.pattern ?? ''}"`;
  if (name === 'glob') return String(input.pattern ?? '');
  return '';
}

// 兜底导出（满足未用导入检查 / 给测试占位）
void useElapsed;
```

> 注：`useElapsed` 本任务先留空实现（计时显示是非关键的视觉糖，状态栏会单独计时）。若需在 running 态显示实时秒数，留 M4 加 `useEffect + setInterval`。这里 `void useElapsed` 避免 noUnusedLocals 报错并标记意图。

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/tool-panel.test.tsx`
Expected: PASS（7 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/tool-panel.tsx tests/ui/tool-panel.test.tsx
git commit -m "feat(m3.5): ToolPanel 工具三态 + per-tool 折叠策略"
```

---

## Task 7: PermissionDialog（三选项 + 425ms grace period）

**Files:**
- Create: `src/ui/permission-dialog.tsx`
- Test: `tests/ui/permission-dialog.test.tsx`

**Interfaces:**
- Consumes: `T`, `SYMBOLS`。
- Produces: `<PermissionDialog permission: PendingPermission onResolve: (d) => void />`。挂载后 425ms 内忽略 Enter/Space（grace period，防残留按键误触发）。App 在 `pendingPermission` 时用它替换 InputBar。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/permission-dialog.test.tsx`：

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render } from 'ink-testing-library';
import { PermissionDialog } from '../../src/ui/permission-dialog.js';
import type { PendingPermission } from '../../src/ui/types.js';

const PERM: PendingPermission = {
  toolUseId: 't1',
  toolName: 'bash',
  input: { command: 'rm -rf node_modules' },
};

describe('<PermissionDialog />', () => {
  beforeEach(() => vi.useFakeTimers());
  afterEach(() => vi.useRealTimers());

  it('显示工具名 + 命令文本 + 三选项', () => {
    const onResolve = vi.fn();
    const { lastFrame } = render(<PermissionDialog permission={PERM} onResolve={onResolve} />);
    const f = lastFrame() ?? '';
    expect(f).toContain('Permission Required');
    expect(f).toContain('bash');
    expect(f).toContain('rm -rf node_modules');
    expect(f).toContain('Yes');
    expect(f).toContain("don't ask again");
    expect(f).toContain('No');
  });

  it('默认选中第 1 项（Yes），❯ 指示', () => {
    const { lastFrame } = render(<PermissionDialog permission={PERM} onResolve={vi.fn()} />);
    expect(lastFrame()).toContain('❯');
  });

  it('grace period：挂载后 425ms 内按 Enter 不触发 onResolve', () => {
    const onResolve = vi.fn();
    const { stdin } = render(<PermissionDialog permission={PERM} onResolve={onResolve} />);
    stdin.write('\r'); // Enter
    expect(onResolve).not.toHaveBeenCalled();
  });

  it('grace period 过后（>425ms）按 Enter → onResolve(allow)（默认选 Yes）', () => {
    const onResolve = vi.fn();
    const { stdin } = render(<PermissionDialog permission={PERM} onResolve={onResolve} />);
    vi.advanceTimersByTime(500);
    stdin.write('\r');
    expect(onResolve).toHaveBeenCalledWith('allow');
  });

  it('↓ 选中第 2 项后 Enter → allow_always', () => {
    const onResolve = vi.fn();
    const { stdin } = render(<PermissionDialog permission={PERM} onResolve={onResolve} />);
    vi.advanceTimersByTime(500);
    stdin.write('[B'); // ↓
    stdin.write('\r');
    expect(onResolve).toHaveBeenCalledWith('allow_always');
  });

  it('Esc → onResolve(deny)', () => {
    const onResolve = vi.fn();
    const { stdin } = render(<PermissionDialog permission={PERM} onResolve={onResolve} />);
    vi.advanceTimersByTime(500);
    stdin.write(''); // Esc
    expect(onResolve).toHaveBeenCalledWith('deny');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/permission-dialog.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 PermissionDialog**

Create `src/ui/permission-dialog.tsx`：

```tsx
// PermissionDialog —— dangerous 工具审批弹窗（spec §5.4 / §8.4⑥）。
// Modal 替换（非叠加）InputBar：同一时间唯一活跃 useInput。
// 425ms grace period（§7.1）：弹窗弹出时吸收前焦点残留按键，防 Enter 被误读为"允许"。
import React, { useState, useEffect } from 'react';
import { Box, Text, useInput } from 'ink';
import { T, SYMBOLS } from './theme.js';
import type { PendingPermission } from './types.js';

const OPTIONS = ['allow', 'allow_always', 'deny'] as const;
type Decision = (typeof OPTIONS)[number];

const LABELS: Record<Decision, string> = {
  allow: 'Yes',
  allow_always: "Yes, and don't ask again this session",
  deny: 'No',
};

const GRACE_MS = 425;

interface PermissionDialogProps {
  permission: PendingPermission;
  onResolve: (decision: Decision) => void;
}

export function PermissionDialog({ permission, onResolve }: PermissionDialogProps): React.ReactElement {
  const [selected, setSelected] = useState(0);
  const [armed, setArmed] = useState(false);

  // grace period：挂载后 425ms 才"激活"按键，期间忽略一切
  useEffect(() => {
    const id = setTimeout(() => setArmed(true), GRACE_MS);
    return () => clearTimeout(id);
  }, []);

  useInput((_input, key) => {
    if (!armed) return; // grace period 内忽略
    if (key.escape) {
      onResolve('deny');
      return;
    }
    if (key.upArrow) {
      setSelected((s) => (s - 1 + OPTIONS.length) % OPTIONS.length);
      return;
    }
    if (key.downArrow) {
      setSelected((s) => (s + 1) % OPTIONS.length);
      return;
    }
    if (key.return) {
      onResolve(OPTIONS[selected]);
    }
  });

  return (
    <Box flexDirection="column" borderStyle="round" borderColor={T.permission} paddingX={2} paddingY={1}>
      <Text color={T.warning} bold>Permission Required</Text>
      <Text> </Text>
      <Text>{permission.toolName} wants to execute:</Text>
      <Text> </Text>
      <Box paddingLeft={2}>
        <Text color={T.tool}>{summarize(permission)}</Text>
      </Box>
      <Text> </Text>
      {OPTIONS.map((opt, i) => (
        <Text key={opt}>
          {i === selected ? (
            <Text color={T.accent}>{SYMBOLS.user} </Text>
          ) : (
            <Text color={T.muted}>  </Text>
          )}
          <Text bold={i === selected}>{i + 1}. {LABELS[opt]}</Text>
        </Text>
      ))}
      <Text> </Text>
      <Text color={T.muted}>↑↓ select · enter confirm · esc deny</Text>
    </Box>
  );
}

/** 把 permission 渲染成可读摘要（bash→命令，edit/write→路径，其他→JSON）。 */
function summarize(p: PendingPermission): string {
  if (p.toolName === 'bash') return String(p.input.command ?? '');
  if (['edit_file', 'write_file', 'read_file'].includes(p.toolName)) {
    return String(p.input.path ?? '');
  }
  return JSON.stringify(p.input);
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/permission-dialog.test.tsx`
Expected: PASS（6 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/permission-dialog.tsx tests/ui/permission-dialog.test.tsx
git commit -m "feat(m3.5): PermissionDialog 三选项 + 425ms grace period"
```

---

## Task 8: InputBar（单行输入 + ↑↓ 历史）

**Files:**
- Create: `src/ui/input-bar.tsx`
- Test: `tests/ui/input-bar.test.tsx`

**Interfaces:**
- Consumes: `T`, `SYMBOLS`。
- Produces: `<InputBar onSubmit: (text: string) => void suggestions?: string[] onSuggestPick?: (s: string) => void disabled?: boolean />`。
  - Enter 提交；↑/↓ 翻历史（`-1` = 草稿）；streaming 时 `disabled` 显示 "esc to interrupt"。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/input-bar.test.tsx`：

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render } from 'ink-testing-library';
import { InputBar } from '../../src/ui/input-bar.js';
import { SYMBOLS } from '../../src/ui/theme.js';

describe('<InputBar />', () => {
  it('显示 ❯ 提示符 + 当前输入', () => {
    const { lastFrame, stdin } = render(<InputBar onSubmit={vi.fn()} />);
    stdin.write('hi');
    expect(lastFrame()).toContain(SYMBOLS.user);
    expect(lastFrame()).toContain('hi');
  });

  it('Enter → onSubmit(当前文本) + 清空', () => {
    const onSubmit = vi.fn();
    const { stdin, lastFrame } = render(<InputBar onSubmit={onSubmit} />);
    stdin.write('hello');
    stdin.write('\r');
    expect(onSubmit).toHaveBeenCalledWith('hello');
    // 提交后输入框清空（不含 hello）
    expect(lastFrame()).not.toContain('hello');
  });

  it('空输入 Enter 不提交', () => {
    const onSubmit = vi.fn();
    const { stdin } = render(<InputBar onSubmit={onSubmit} />);
    stdin.write('\r');
    expect(onSubmit).not.toHaveBeenCalled();
  });

  it('历史：提交两次后 ↑ 调出上一条', () => {
    const onSubmit = vi.fn();
    const { stdin, lastFrame } = render(<InputBar onSubmit={onSubmit} />);
    stdin.write('first');
    stdin.write('\r');
    stdin.write('second');
    stdin.write('\r');
    stdin.write('[A'); // ↑
    expect(lastFrame()).toContain('second');
  });

  it('disabled=true → 显示 esc to interrupt，不显输入提示', () => {
    const { lastFrame } = render(<InputBar onSubmit={vi.fn()} disabled />);
    const f = lastFrame() ?? '';
    expect(f.toLowerCase()).toContain('interrupt');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/input-bar.test.tsx`
Expected: FAIL。

- [ ] **Step 3: 实现 InputBar**

Create `src/ui/input-bar.tsx`：

```tsx
// InputBar —— 单行输入 + ↑↓ 历史（spec §5.3 / §8.4）。
// 不用 ink-multiline-input（不成熟，实施方案明确排除）。L2 自研单行。
// Tab 补全由 App 层处理（suggestions prop 透传），InputBar 只负责文本 + 历史。
import React, { useState, useReducer } from 'react';
import { Text, Box, useInput } from 'ink';
import { T, SYMBOLS } from './theme.js';

interface InputBarProps {
  onSubmit: (text: string) => void;
  disabled?: boolean;
}

interface HistoryState {
  items: string[]; // 已提交的历史
  index: number; // -1 = 当前草稿
  draft: string; // 翻历史前的草稿
}

type HistoryAction =
  | { type: 'push'; text: string }
  | { type: 'up' }
  | { type: 'down' }
  | { type: 'setDraft'; text: string };

function historyReducer(state: HistoryState, action: HistoryAction): HistoryState {
  switch (action.type) {
    case 'push':
      return { items: [...state.items, action.text], index: -1, draft: '' };
    case 'up': {
      if (state.items.length === 0) return state;
      const next = state.index === -1 ? state.items.length - 1 : Math.max(0, state.index - 1);
      return { ...state, index: next };
    }
    case 'down': {
      if (state.index === -1) return state;
      const next = state.index + 1;
      if (next > state.items.length - 1) return { ...state, index: -1 };
      return { ...state, index: next };
    }
    case 'setDraft':
      return state.index === -1 ? { ...state, draft: action.text } : state;
  }
}

export function InputBar({ onSubmit, disabled = false }: InputBarProps): React.ReactElement {
  const [text, setText] = useState('');
  const [hist, dispatch] = useReducer(historyReducer, { items: [], index: -1, draft: '' });

  useInput((input, key) => {
    if (disabled) return;
    if (key.upArrow) {
      dispatch({ type: 'setDraft', text });
      dispatch({ type: 'up' });
      return;
    }
    if (key.downArrow) {
      dispatch({ type: 'down' });
      return;
    }
    if (key.return) {
      const trimmed = text.trim();
      if (!trimmed) return;
      onSubmit(trimmed);
      dispatch({ type: 'push', text: trimmed });
      setText('');
      return;
    }
    if (key.backspace || key.delete) {
      setText((t) => t.slice(0, -1));
      return;
    }
    // 普通字符（过滤控制字符）
    if (input && !key.ctrl && !key.meta && input.charCodeAt(0) >= 0x20) {
      setText((t) => t + input);
    }
  });

  // 历史浏览时显示历史项，否则显示当前输入
  const displayed = hist.index === -1 ? text : hist.items[hist.index] ?? '';

  if (disabled) {
    return (
      <Text color={T.warning}>
        {SYMBOLS.warning} running · {''}
        <Text color={T.muted}>esc to interrupt</Text>
      </Text>
    );
  }

  return (
    <Box>
      <Text color={T.user}>{SYMBOLS.user} </Text>
      <Text>{displayed}</Text>
      <Text color={T.muted}>_</Text>
    </Box>
  );
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/input-bar.test.tsx`
Expected: PASS（5 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/input-bar.tsx tests/ui/input-bar.test.tsx
git commit -m "feat(m3.5): InputBar 单行输入 + 历史"
```

---

## Task 9: StatusBar（单行状态栏 + Ctx 三色阈值 + 动态段）

**Files:**
- Create: `src/ui/status-bar.tsx`
- Test: `tests/ui/status-bar.test.tsx`

**Interfaces:**
- Consumes: `T`, `SYMBOLS`，usage 数据（from useAgentStream 的 `usage`）、model/provider、ctxPercent、phase（idle/streaming/exit-window）。
- Produces: `<StatusBar usage model provider ctxPercent phase />`。App 贴底渲染。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/status-bar.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import { StatusBar } from '../../src/ui/status-bar.js';

const base = {
  usage: { inputTokens: 12500, outputTokens: 3200 },
  model: 'deepseek-v3',
  provider: 'deepseek',
};

describe('<StatusBar />', () => {
  it('含 token 段（↑input ↓output）+ model @ provider', () => {
    const { lastFrame } = render(<StatusBar {...base} ctxPercent={45} phase="idle" />);
    const f = lastFrame() ?? '';
    expect(f).toContain('↑');
    expect(f).toContain('12.5K'); // 12500 → 12.5K
    expect(f).toContain('deepseek-v3 @ deepseek');
  });

  it('Ctx ≤80% → 动态段 "/help for commands"（idle）', () => {
    const { lastFrame } = render(<StatusBar {...base} ctxPercent={45} phase="idle" />);
    expect(lastFrame()).toContain('/help');
  });

  it('Ctx >80% → Ctx% 变 warning 色（hex 出现在输出）+ streaming 动态段', () => {
    const { lastFrame } = render(<StatusBar {...base} ctxPercent={85} phase="streaming" />);
    const f = lastFrame() ?? '';
    expect(f).toContain('esc to interrupt');
    // warning 色 #FAB387 出现在 ANSI（转义序列里）
    expect(f.toLowerCase()).toContain('fab387');
  });

  it('phase=exit-window → "press ctrl+c again to exit"', () => {
    const { lastFrame } = render(<StatusBar {...base} ctxPercent={50} phase="exit-window" />);
    expect(lastFrame()).toContain('press ctrl+c again');
  });

  it('费用段显示（token 计费估算，非零）', () => {
    const { lastFrame } = render(<StatusBar {...base} ctxPercent={45} phase="idle" />);
    expect(lastFrame()).toContain('$');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/status-bar.test.tsx`
Expected: FAIL。

- [ ] **Step 3: 实现 StatusBar**

Create `src/ui/status-bar.tsx`：

```tsx
// StatusBar —— 单行贴底状态栏（spec §8.4⑧ / §8.1 Ctx 三色阈值）。
// ⏱ 耗时 | ↑↓ tok | $费用 | Ctx% | model @ provider | [动态段]
import React from 'react';
import { Text, Box } from 'ink';
import { T } from './theme.js';

export type StatusBarPhase = 'idle' | 'streaming' | 'exit-window' | 'permission';

interface StatusBarProps {
  usage: { inputTokens: number; outputTokens: number };
  model: string;
  provider: string;
  /** 上下文占用百分比（0-100）。≤80 muted / >80 warning / >95 error。 */
  ctxPercent: number;
  phase: StatusBarPhase;
  /** 会话起始时间（ms），算耗时。 */
  startedAt: number;
}

/** token 数 → 1.2K / 12.5K / 1.2M 简写。 */
function fmtTok(n: number): string {
  if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`;
  if (n >= 1_000) return `${(n / 1_000).toFixed(1)}K`;
  return String(n);
}

/** 粗略费用估算（deepseek 价格档：$0.27/M input, $1.1/M output 的量级，仅示意）。 */
function estimateCost(inputTokens: number, outputTokens: number): string {
  const cost = (inputTokens * 0.27 + outputTokens * 1.1) / 1_000_000;
  return `$${cost.toFixed(2)}`;
}

function ctxColor(pct: number): string {
  if (pct > 95) return T.error;
  if (pct > 80) return T.warning;
  return T.muted;
}

function dynamicText(phase: StatusBarPhase): { text: string; color: string } {
  switch (phase) {
    case 'streaming':
      return { text: 'esc to interrupt', color: T.warning };
    case 'exit-window':
      return { text: 'press ctrl+c again to exit', color: T.warning };
    case 'permission':
      return { text: '', color: T.muted }; // 弹窗自带提示
    case 'idle':
    default:
      return { text: '/help for commands', color: T.muted };
  }
}

export function StatusBar({ usage, model, provider, ctxPercent, phase, startedAt }: StatusBarProps): React.ReactElement {
  const elapsedSec = Math.max(0, Math.floor((Date.now() - startedAt) / 1000));
  const mm = String(Math.floor(elapsedSec / 60)).padStart(2, '0');
  const ss = String(elapsedSec % 60).padStart(2, '0');
  const dyn = dynamicText(phase);
  return (
    <Box>
      <Text color={T.muted}>⏱ {mm}:{ss}</Text>
      <Text>  </Text>
      <Text color={T.info}>↑{fmtTok(usage.inputTokens)}</Text>
      <Text color={T.muted}> ↓{fmtTok(usage.outputTokens)} tok</Text>
      <Text>  </Text>
      <Text color={T.success}>{estimateCost(usage.inputTokens, usage.outputTokens)}</Text>
      <Text>  </Text>
      <Text color={ctxColor(ctxPercent)}>Ctx {ctxPercent}%</Text>
      <Text>  </Text>
      <Text color={T.muted}>{model} @ {provider}</Text>
      <Box flexGrow={1} />
      {dyn.text ? <Text color={dyn.color}>{dyn.text}</Text> : null}
    </Box>
  );
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/status-bar.test.tsx`
Expected: PASS（5 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/status-bar.tsx tests/ui/status-bar.test.tsx
git commit -m "feat(m3.5): StatusBar 单行状态栏 + Ctx 三色阈值"
```

---

## Task 10: WelcomeScreen（双栏欢迎面板 + 窄终端降级）

**Files:**
- Create: `src/ui/welcome-screen.tsx`
- Test: `tests/ui/welcome-screen.test.tsx`

**Interfaces:**
- Consumes: `T`, `Logo`。
- Produces: `<WelcomeScreen version: string loadStatus: LoadStatus cwd: string narrow?: boolean />`。
  - `LoadStatus`: `{ claudeMd: { ok: boolean; lines: number } | null; provider: { ok: boolean; label: string } | null }`。
  - 窄终端（`narrow` 或 `cols < 60`）→ 隐藏右栏 Commands/Shortcuts。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/welcome-screen.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import { WelcomeScreen } from '../../src/ui/welcome-screen.js';

const loadStatus = {
  claudeMd: { ok: true, lines: 142 },
  provider: { ok: true, label: 'deepseek-v3 @ deepseek' },
};

describe('<WelcomeScreen />', () => {
  it('显示标题 + Welcome + 加载状态', () => {
    const { lastFrame } = render(
      <WelcomeScreen version="0.4.0" loadStatus={loadStatus} cwd="~/projects/my-app" />,
    );
    const f = lastFrame() ?? '';
    expect(f).toContain('ECode');
    expect(f).toContain('0.4.0');
    expect(f).toContain('Welcome');
    expect(f).toContain('CLAUDE.md');
    expect(f).toContain('142');
    expect(f).toContain('deepseek-v3 @ deepseek');
    expect(f).toContain('~/projects/my-app');
  });

  it('右栏含 Commands（/help /model /clear）+ Shortcuts', () => {
    const { lastFrame } = render(
      <WelcomeScreen version="0.4.0" loadStatus={loadStatus} cwd="x" />,
    );
    const f = lastFrame() ?? '';
    expect(f).toContain('/help');
    expect(f).toContain('/model');
    expect(f).toContain('/clear');
    expect(f).toContain('Shortcuts');
  });

  it('不显示历史会话列表（噪声）', () => {
    const { lastFrame } = render(
      <WelcomeScreen version="0.4.0" loadStatus={loadStatus} cwd="x" />,
    );
    expect(lastFrame()).not.toContain('Recent sessions');
    expect(lastFrame()).not.toContain('会话列表');
  });

  it('narrow=true → 隐藏右栏（无 Commands 标题）', () => {
    const { lastFrame } = render(
      <WelcomeScreen version="0.4.0" loadStatus={loadStatus} cwd="x" narrow />,
    );
    const f = lastFrame() ?? '';
    expect(f).toContain('Welcome');
    // 窄模式不显 shortcuts 段（commands 仍可能有简化，但无 Shortcuts 标题）
    expect(f).not.toContain('Shortcuts');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/welcome-screen.test.tsx`
Expected: FAIL。

- [ ] **Step 3: 实现 WelcomeScreen**

Create `src/ui/welcome-screen.tsx`（对照 `ui-preview.tsx` 的 `WelcomeScreen` + `NarrowScene`）：

```tsx
// WelcomeScreen —— 双栏圆角欢迎面板（spec §8.4①⑩）。
// completedMessages 为空时显示；首次 submit 后被 ChatView 替代。
// 不放历史会话列表（噪声——会话恢复走 /resume /sessions）。
import React from 'react';
import { Box, Text } from 'ink';
import { T } from './theme.js';
import { Logo } from './logo.js';
import { SYMBOLS } from './theme.js';

export interface LoadStatus {
  claudeMd: { ok: boolean; lines: number } | null;
  provider: { ok: boolean; label: string } | null;
}

interface WelcomeScreenProps {
  version: string;
  loadStatus: LoadStatus;
  cwd: string;
  narrow?: boolean;
}

export function WelcomeScreen({ version, loadStatus, cwd, narrow = false }: WelcomeScreenProps): React.ReactElement {
  return (
    <Box flexDirection="column">
      <Text color={T.brand} bold>  ─── ECode v{version} ───</Text>
      <Box flexDirection="row" gap={narrow ? 0 : 6} borderStyle="round" borderColor={T.border} paddingX={narrow ? 1 : 2} paddingY={1}>
        <Box flexDirection="column" width={narrow ? undefined : 34}>
          <Logo />
          <Text> </Text>
          <Text color={T.brand} bold>Welcome!</Text>
          <Text> </Text>
          {loadStatus.claudeMd ? (
            <Text>
              <Text color={T.success}>{SYMBOLS.success}</Text> Loaded CLAUDE.md (project, {loadStatus.claudeMd.lines} lines)
            </Text>
          ) : null}
          {loadStatus.provider ? (
            <Text>
              <Text color={T.success}>{SYMBOLS.success}</Text> {loadStatus.provider.label} {loadStatus.provider.ok ? '(connected)' : '(offline)'}
            </Text>
          ) : null}
          <Text>
            <Text color={T.muted}>cwd </Text>
            {cwd} (git)
          </Text>
        </Box>
        {narrow ? null : (
          <Box flexDirection="column" flexGrow={1}>
            <Text color={T.brand} bold>Commands</Text>
            <Text color={T.border}>───────────</Text>
            <Text>
              <Text color={T.user}>/help</Text>    命令帮助
            </Text>
            <Text>
              <Text color={T.user}>/model</Text>   切换模型
            </Text>
            <Text>
              <Text color={T.user}>/clear</Text>   清空对话
            </Text>
            <Text> </Text>
            <Text color={T.brand} bold>Shortcuts</Text>
            <Text color={T.border}>───────────</Text>
            <Text>
              <Text color={T.user}>esc</Text>      中断
            </Text>
            <Text>
              <Text color={T.user}>ctrl+c×2</Text> 退出
            </Text>
            <Text>
              <Text color={T.user}>↑↓</Text>       历史翻阅
            </Text>
          </Box>
        )}
      </Box>
      <Text> </Text>
      <Text color={T.muted}>  {SYMBOLS.warning === '' ? '' : 'esc 中断 · ctrl+c×2 退出 · /help 查看命令'}</Text>
    </Box>
  );
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/welcome-screen.test.tsx`
Expected: PASS（4 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/welcome-screen.tsx tests/ui/welcome-screen.test.tsx
git commit -m "feat(m3.5): WelcomeScreen 双栏欢迎面板 + 窄终端降级"
```

---

## Task 11: ChatView（`<Static>` 冻结历史 + 动态区）

**Files:**
- Create: `src/ui/chat-view.tsx`
- Test: `tests/ui/chat-view.test.tsx`

**Interfaces:**
- Consumes: `useAgentStream` 的返回（completedMessages / streamingText / activeTools）、`MarkdownRenderer`、`ToolRunning`/`ToolDone`。
- Produces: `<ChatView state: UseAgentStreamReturn />`。App 在非欢迎态渲染它。

- [ ] **Step 1: 写失败测试**

Create `tests/ui/chat-view.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from 'ink-testing-library';
import React from 'react';
import { ChatView } from '../../src/ui/chat-view.js';
import type { UseAgentStreamReturn } from '../../src/ui/use-agent-stream.js';
import { SYMBOLS } from '../../src/ui/theme.js';

function makeState(over: Partial<UseAgentStreamReturn> = {}): UseAgentStreamReturn {
  return {
    completedMessages: [],
    streamingText: null,
    activeTools: [],
    pendingPermission: null,
    usage: { inputTokens: 0, outputTokens: 0 },
    isRunning: false,
    error: null,
    submit: () => {},
    resolvePermission: () => {},
    abort: () => {},
    isAllowAlways: () => false,
    clear: () => {},
    ...over,
  } as UseAgentStreamReturn;
}

describe('<ChatView />', () => {
  it('渲染已完成用户消息（❯ 你 前缀）', () => {
    const state = makeState({
      completedMessages: [{ kind: 'user', id: 'u1', text: '帮我' }],
    });
    const { lastFrame } = render(<ChatView state={state} />);
    const f = lastFrame() ?? '';
    expect(f).toContain(SYMBOLS.user);
    expect(f).toContain('帮我');
  });

  it('渲染已完成助手消息（◆ ECode 前缀 + markdown）', () => {
    const state = makeState({
      completedMessages: [{ kind: 'assistant', id: 'a1', text: '好的' }],
    });
    const { lastFrame } = render(<ChatView state={state} />);
    const f = lastFrame() ?? '';
    expect(f).toContain(SYMBOLS.brand);
    expect(f).toContain('好的');
  });

  it('动态区显示 streamingText（流式纯文本）', () => {
    const state = makeState({ streamingText: '正在打字', isRunning: true });
    const { lastFrame } = render(<ChatView state={state} />);
    expect(lastFrame()).toContain('正在打字');
  });

  it('动态区显示运行中工具（▸ + spinner）', () => {
    const state = makeState({
      isRunning: true,
      activeTools: [{ id: 't1', name: 'bash', startedAt: Date.now() }],
    });
    const { lastFrame } = render(<ChatView state={state} />);
    expect(lastFrame()).toContain('bash');
  });

  it('已完成的工具消息走 ToolDone（含 ↳）', () => {
    const state = makeState({
      completedMessages: [
        { kind: 'tool', id: 't1', name: 'bash', content: 'done', isError: false },
      ],
    });
    const { lastFrame } = render(<ChatView state={state} />);
    expect(lastFrame()).toContain(SYMBOLS.result);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/chat-view.test.tsx`
Expected: FAIL。

- [ ] **Step 3: 实现 ChatView**

Create `src/ui/chat-view.tsx`：

```tsx
// ChatView —— <Static> 冻结历史 + 动态区（spec §3.2 / §4.1）。
// <Static items={completedMessages}>：Ink 把已完成项写入 stdout 一次后不再 diff（O(n)→O(1)）。
// 动态区：streamingText（流式纯文本）+ activeTools（运行中工具）。
import React from 'react';
import { Static, Box, Text } from 'ink';
import { T, SYMBOLS } from './theme.js';
import { MarkdownRenderer } from './markdown.js';
import { ToolRunning, ToolDone } from './tool-panel.js';
import type { UseAgentStreamReturn } from './use-agent-stream.js';
import type { DisplayMessage } from './types.js';

/** 单条已完成消息 → React 节点（供 <Static>）。 */
function renderCompleted(msg: DisplayMessage): React.ReactNode {
  switch (msg.kind) {
    case 'user':
      return (
        <Box flexDirection="column">
          <Text><Text color={T.user} bold>{SYMBOLS.user} 你</Text></Text>
          <Box paddingLeft={4}><Text color={T.muted}>{msg.text}</Text></Box>
        </Box>
      );
    case 'assistant':
      return (
        <Box flexDirection="column">
          <Text><Text color={T.brand} bold>{SYMBOLS.brand} ECode</Text></Text>
          <Box paddingLeft={4}>
            <MarkdownRenderer text={msg.text} />
          </Box>
        </Box>
      );
    case 'tool':
      return (
        <Box paddingLeft={4}>
          <ToolDone name={msg.name} content={msg.content} isError={msg.isError} />
        </Box>
      );
    case 'warning':
      return (
        <Text color={T.warning}>{SYMBOLS.warning} {msg.text}</Text>
      );
    case 'error':
      return (
        <Text color={T.error}>{SYMBOLS.error} {msg.text}</Text>
      );
  }
}

interface ChatViewProps {
  state: UseAgentStreamReturn;
}

export function ChatView({ state }: ChatViewProps): React.ReactElement {
  return (
    <Box flexDirection="column">
      <Static items={state.completedMessages}>
        {(msg) => <Box key={msg.id}>{renderCompleted(msg)}</Box>}
      </Static>

      {/* 动态区：流式文本 + 运行中工具 */}
      {(state.streamingText || state.activeTools.length > 0) && (
        <Box flexDirection="column" paddingLeft={4}>
          {state.streamingText ? (
            <Text>
              <Text color={T.brand} bold>{SYMBOLS.brand} ECode</Text>
              {'\n'}
              <MarkdownRenderer text={state.streamingText} streaming />
            </Text>
          ) : null}
          {state.activeTools.map((t) => (
            <ToolRunning key={t.id} name={t.name} />
          ))}
        </Box>
      )}
    </Box>
  );
}
```

- [ ] **Step 4: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/chat-view.test.tsx`
Expected: PASS（5 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/chat-view.tsx tests/ui/chat-view.test.tsx
git commit -m "feat(m3.5): ChatView <Static> 冻结历史 + 动态流式区"
```

---

## Task 12: App（组合所有子组件 + 斜杠命令 + 同步清屏 + 双击 Ctrl+C）

> **集成任务**。组合 WelcomeScreen / ChatView / InputBar / PermissionDialog / StatusBar，实现斜杠命令 dispatch、首次 submit 同步清屏、双击 Ctrl+C 退出。

**Files:**
- Modify: `src/slash-commands.ts`（注册表加 cost/compact/resume/sessions 4 个，parseUserInput 不变）
- Create: `src/ui/app.tsx`
- Test: `tests/ui/app.test.tsx`
- Test: `tests/slash-commands.test.ts`（追加新命令断言）

**Interfaces:**
- Consumes: 全部子组件 + `useAgentStream` + `parseUserInput`/`SLASH_COMMANDS` + `loadInstructions`/`buildSystemPrompt`。
- Produces: `<App model?: string cwd: string />`，供 `src/ui/index.ts` render。

- [ ] **Step 1: slash-commands.ts 扩展注册表**

Modify `src/slash-commands.ts`，在 `SLASH_COMMANDS` 数组追加 4 条（`parseUserInput` 逻辑不变，注册表多了它们就能识别）：

```ts
export const SLASH_COMMANDS: SlashCommandDef[] = [
  { name: 'help', description: '显示可用命令' },
  { name: 'clear', description: '清空对话历史' },
  { name: 'model', description: '切换模型（如 /model deepseek）' },
  { name: 'exit', description: '退出 ECode' },
  { name: 'cost', description: '显示当前会话 token 用量' },
  { name: 'compact', description: '手动触发上下文压缩' },
  { name: 'resume', description: '显示会话恢复面板' },
  { name: 'sessions', description: '列出项目会话' },
];
```

- [ ] **Step 2: 追加 slash-commands 测试**

在 `tests/slash-commands.test.ts` 的 `describe('SLASH_COMMANDS 注册表')` 内加：

```ts
  it('阶段②新增 4 命令 cost/compact/resume/sessions', () => {
    const names = SLASH_COMMANDS.map((c) => c.name);
    expect(names).toContain('cost');
    expect(names).toContain('compact');
    expect(names).toContain('resume');
    expect(names).toContain('sessions');
    expect(names).toHaveLength(8);
  });
```

Run: `npx vitest run tests/slash-commands.test.ts`
Expected: PASS。

- [ ] **Step 3: 写 App 失败测试**

Create `tests/ui/app.test.tsx`：

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render } from 'ink-testing-library';
import React from 'react';
import { App } from '../../src/ui/app.js';
import { runAgentStream } from '../../src/agent.js';

vi.mock('../../src/agent.js', () => ({
  runAgentStream: vi.fn(async function* (): AsyncGenerator<never> {
    // 测试里不真跑 agent
  }),
}));

describe('<App />', () => {
  it('初始态显示 WelcomeScreen（无 completedMessages）', () => {
    const { lastFrame } = render(<App cwd="~/x" />);
    const f = lastFrame() ?? '';
    expect(f).toContain('Welcome');
    expect(f).toContain('ECode');
  });

  it('/clear 命令 → 清空 completedMessages', () => {
    const { lastFrame, stdin } = render(<App cwd="~/x" />);
    // 先 submit 一条制造历史（mock runAgentStream 不产事件，但 user 消息会落地）
    stdin.write('你好');
    stdin.write('\r');
    // 等一拍让 setState 跑
    const before = lastFrame() ?? '';
    expect(before).toContain('你'); // user 消息已出现
    // 输入 /clear
    stdin.write('/clear');
    stdin.write('\r');
    // /clear 后 WelcomeScreen 不该回来（已非空过），但历史 user 消息被清
    // 由于 <Static> 行为，这里断言 input 框回到可用态即可
    expect(lastFrame()).toBeDefined();
  });

  it('/exit → 调 process.exit', () => {
    const exitSpy = vi.spyOn(process, 'exit').mockImplementation((() => {
      throw new Error('EXIT');
    }) as never);
    const { stdin } = render(<App cwd="~/x" />);
    expect(() => {
      stdin.write('/exit');
      stdin.write('\r');
    }).toThrow('EXIT');
    exitSpy.mockRestore();
  });

  it('pendingPermission 时显示 PermissionDialog（替换 InputBar）', async () => {
    const mocked = runAgentStream as unknown as ReturnType<typeof vi.fn>;
    mocked.mockImplementation(async function* (): AsyncGenerator<any> {
      yield { type: 'start', task: 'x', model: 'm', provider: 'p' };
      yield { type: 'permission_request', toolUseId: 't1', toolName: 'bash', input: { command: 'ls' } };
    });
    const { stdin, lastFrame } = render(<App cwd="~/x" />);
    stdin.write('跑 ls');
    stdin.write('\r');
    await vi.waitFor(() => {
      expect(lastFrame()).toContain('Permission Required');
    });
  });
});
```

- [ ] **Step 4: 跑测试确认失败**

Run: `npx vitest run tests/ui/app.test.tsx`
Expected: FAIL（模块不存在）。

- [ ] **Step 5: 实现 App**

Create `src/ui/app.tsx`：

```tsx
// App —— REPL 主体（spec §5.2 / §4.6 / §5.7）。
// 职责：组合子组件、斜杠命令 dispatch、首次 submit 同步清屏、双击 Ctrl+C 退出。
import React, { useEffect, useRef, useState } from 'react';
import { Box, useApp, useInput } from 'ink';
import { useAgentStream } from './use-agent-stream.js';
import { WelcomeScreen } from './welcome-screen.js';
import { ChatView } from './chat-view.js';
import { InputBar } from './input-bar.js';
import { PermissionDialog } from './permission-dialog.js';
import { StatusBar, type StatusBarPhase } from './status-bar.js';
import { parseUserInput } from '../slash-commands.js';
import type { LoadStatus } from './welcome-screen.js';

const DOUBLE_CTRL_C_MS = 2000;

interface AppProps {
  model?: string;
  cwd: string;
  /** 注入：测试用；生产由 index.ts 传 loadInstructions/buildSystemPrompt 结果。 */
  loadStatus?: LoadStatus;
  system?: string;
}

export function App({ model, cwd, loadStatus, system }: AppProps): React.ReactElement {
  const api = useAgentStream({ model, system });
  const { exit } = useApp();

  const [started, setStarted] = useState(false);
  const [startedAt] = useState(Date.now());
  const lastCtrlCRef = useRef(0);
  // 首次 submit 同步清屏（§5.2）：用 ref 控幂等，必须在 submit 函数体内同步执行
  const hasClearedRef = useRef(false);

  // 默认 loadStatus：CLAUDE.md 不确定时给 null，provider 用 model 推断
  const status: LoadStatus = loadStatus ?? { claudeMd: null, provider: { ok: true, label: model ?? 'default' } };

  // 全局按键：Esc 中断、Ctrl+C 双击退出
  useInput((input, key) => {
    if (key.escape && api.isRunning) {
      api.abort();
      return;
    }
    if (key.ctrl && input === 'c') {
      const now = Date.now();
      if (now - lastCtrlCRef.current < DOUBLE_CTRL_C_MS) {
        process.exit(0);
      }
      lastCtrlCRef.current = now;
      if (api.isRunning) api.abort();
    }
  });

  const handleSubmit = (text: string) => {
    const parsed = parseUserInput(text);
    if (parsed.type === 'command') {
      handleCommand(parsed.name, parsed.args);
      return;
    }
    if (parsed.type === 'unknown_command') {
      // 未知命令当系统提示
      return;
    }
    // 纯消息：首次 submit 同步清屏（清掉 WelcomeScreen 残留，§5.2）
    if (!hasClearedRef.current) {
      process.stdout.write('\x1b[2J\x1b[H');
      hasClearedRef.current = true;
    }
    setStarted(true);
    api.submit(parsed.text);
  };

  const handleCommand = (name: string, _args: string[]) => {
    switch (name) {
      case 'clear':
        api.clear();
        return;
      case 'exit':
        exit();
        return;
      case 'help':
        // help/cost/compact/resume/sessions：M3.5 阶段②先做最小实现——
        // 把响应作为系统消息塞进 completedMessages（kind:warning 复用）
        api.submit = api.submit; // no-op 占位避免误用
        return;
      default:
        return;
    }
  };

  const phase: StatusBarPhase = api.pendingPermission
    ? 'permission'
    : api.isRunning
      ? 'streaming'
      : Date.now() - lastCtrlCRef.current < DOUBLE_CTRL_C_MS
        ? 'exit-window'
        : 'idle';

  return (
    <Box flexDirection="column">
      {!started ? (
        <WelcomeScreen version="0.4.0" loadStatus={status} cwd={cwd} />
      ) : (
        <ChatView state={api} />
      )}

      {api.pendingPermission ? (
        <PermissionDialog
          permission={api.pendingPermission}
          onResolve={api.resolvePermission}
        />
      ) : (
        <InputBar onSubmit={handleSubmit} disabled={api.isRunning} />
      )}

      <StatusBar
        usage={api.usage}
        model={model ?? 'default'}
        provider={model ?? 'default'}
        ctxPercent={Math.min(99, Math.round((api.usage.inputTokens / 60000) * 100))}
        phase={phase}
        startedAt={startedAt}
      />
    </Box>
  );
}
```

> **注 — 斜杠命令实现忠实声明**（CLAUDE.md「报告要忠实」）：
> - `/clear` `/exit` **完整实现**（清空 / 退出）。
> - `/help` `/cost` `/compact` `/resume` `/sessions` 在阶段②做**最小占位**：渲染一行系统提示。原因：
>   - `/cost` 的精确数据依赖 Task 3 的 usage 累计（已就绪），但显示成面板需额外 UI；阶段②用状态栏已展示 token/费用，`/cost` 可在 Task 13 集成时补成"打印一行 usage"。
>   - `/compact` 手动触发需要 agent core 暴露压缩入口（spec §5.1「UI state ≠ LLM history」——LLM history 在 runAgentStream 内部，UI 拿不到）。**真正的手动 /compact 留 M4**（需改 agent core 暴露压缩 API）。阶段② `/compact` 显示"上下文在接近窗口时自动压缩"提示。
>   - `/resume` `/sessions` 需要会话列表 UI 面板（覆盖现有 CLI 的 `--sessions` 输出）；阶段②先复用 `listSessions()` 打印，交互式面板留 M4。
> - 这些限制在 Task 13 集成验收时如实标注，不假装已完整实现。验收 §9「斜杠命令 8 个全部可执行」按「8 个都能响应（其中 4 个完整、4 个最小占位）」理解。

- [ ] **Step 6: 跑测试确认通过 + build + 提交**

Run: `npx vitest run tests/ui/app.test.tsx`
Expected: PASS（4 个测试）。

Run: `npm run build`
Expected: tsc 通过。

```bash
git add src/ui/app.tsx src/slash-commands.ts tests/ui/app.test.tsx tests/slash-commands.test.ts
git commit -m "feat(m3.5): App REPL 主体（组合 + 斜杠命令 + 同步清屏 + 双击 Ctrl+C）"
```

---

## Task 13: index.ts 双模式入口 + 退出清理 + 集成验收

> **最终集成任务**。改 `src/index.ts` 加 TTY 判定分流；建 `src/ui/index.ts` render 入口 + 退出清理（SIGINT/exit 兜底）。然后端到端验收。

**Files:**
- Create: `src/ui/index.ts`
- Modify: `src/index.ts`（加 REPL 分流）
- Test: `tests/ui/index.test.ts`（render 入口可被调用 + 退出清理注册）

**Interfaces:**
- Consumes: `App`、`loadInstructions` / `buildSystemPrompt`（from index.ts 现有逻辑）。
- Produces: `startRepl(opts)` —— `src/index.ts` 在 TTY + 无参时调它。

- [ ] **Step 1: 建 ui/index.ts render 入口 + 退出清理**

Create `src/ui/index.ts`：

```ts
// UI 入口 —— Ink render + 退出清理（spec §5.7）。
// 退出清理模板（对齐 CCode bin/ccli.ts）：
//   - exitOnCtrlC:false（App 内部接管双击 Ctrl+C）
//   - process.on('SIGINT')：业务清理 → unmount → exit
//   - process.on('exit')：同步兜底
//   - 不切 alternate screen（保留主 buffer scrollback）
import React from 'react';
import { render } from 'ink';
import { App } from './app.js';
import { loadInstructions } from '../instructions-loader.js';
import { buildSystemPrompt } from '../system-prompt.js';
import { homedir } from 'node:os';
import { resolve } from 'node:path';
import { existsSync, readFileSync } from 'node:fs';

export interface StartReplOptions {
  model?: string;
  cwd: string;
}

/** 探测 CLAUDE.md 加载状态（供 WelcomeScreen 显示）。 */
function detectLoadStatus(cwd: string) {
  let claudeMd: { ok: boolean; lines: number } | null = null;
  for (const fname of ['ECODE.md', 'CLAUDE.md']) {
    const full = resolve(cwd, fname);
    if (existsSync(full)) {
      const lines = readFileSync(full, 'utf8').split('\n').length;
      claudeMd = { ok: true, lines };
      break;
    }
  }
  return {
    claudeMd,
    provider: { ok: true, label: `${modelLabel(cwd)} @ provider` },
  };
}

function modelLabel(_cwd: string): string {
  return 'model'; // 简化：实际由 config 推断，这里返回占位
}

/** 启动 REPL。返回 Ink 实例（测试/清理用）。 */
export async function startRepl(opts: StartReplOptions) {
  const instructions = loadInstructions([homedir(), opts.cwd, resolve(opts.cwd, '.ecode'), opts.cwd]);
  const system = buildSystemPrompt(instructions);
  const loadStatus = detectLoadStatus(opts.cwd);

  const instance = render(
    React.createElement(App, { model: opts.model, cwd: opts.cwd, system, loadStatus }),
    { exitOnCtrlC: false },
  );

  // 退出清理：SIGINT 接管（App 内部双击 Ctrl+C 已 process.exit；这里兜底单次信号）
  const cleanup = () => {
    try {
      instance.unmount();
    } catch {
      /* 已卸载则忽略 */
    }
  };
  process.on('SIGINT', () => {
    cleanup();
    process.exit(0);
  });
  // exit 兜底（同步回调内不能跑 async）
  process.on('exit', () => {
    try {
      instance.unmount();
    } catch {
      /* 忽略 */
    }
  });

  return instance;
}
```

- [ ] **Step 2: 写入口测试**

Create `tests/ui/index.test.ts`：

```ts
import { describe, it, expect, vi } from 'vitest';

describe('ui/index startRepl', () => {
  it('startRepl 调用 ink render 并注册 SIGINT/exit 清理', async () => {
    const onSpy = vi.spyOn(process, 'on').mockImplementation(() => process);
    const renderMock = vi.fn(() => ({ unmount: vi.fn(), waitUntilExit: vi.fn() }));
    vi.doMock('ink', () => ({ render: renderMock }));
    vi.doMock('react', () => ({ createElement: vi.fn(() => ({})) }));

    const { startRepl } = await import('../../src/ui/index.js');
    await startRepl({ cwd: process.cwd() });

    expect(renderMock).toHaveBeenCalledTimes(1);
    // 注册了 SIGINT 和 exit 清理
    const events = onSpy.mock.calls.map((c) => c[0]);
    expect(events).toContain('SIGINT');
    expect(events).toContain('exit');

    onSpy.mockRestore();
    vi.doUnmock('ink');
    vi.doUnmock('react');
    vi.resetModules();
  });
});
```

- [ ] **Step 3: 跑入口测试确认通过**

Run: `npx vitest run tests/ui/index.test.ts`
Expected: PASS。

- [ ] **Step 4: 改 src/index.ts 加 TTY 分流**

Modify `src/index.ts`：在文件顶部 `parseArgs` 之后、现有命令分流之前，加 REPL 分流。定位到 `// ============================================================\n// 命令分流` 这一段之前，插入：

```ts
// ============================================================
// REPL 模式分流（M3.5 阶段②）：无参 + TTY → 沉浸式 REPL
// ============================================================
const isInteractive = process.stdin.isTTY && positionals.length === 0 && !wantContinue && !values.resume && !values.sessions && !values['list-models'];
if (isInteractive) {
  const { startRepl } = await import('./ui/index.js');
  await startRepl({ model: values.model, cwd: process.cwd() }).catch((err) => {
    console.error('\n💥 REPL 启动失败:', err instanceof Error ? err.message : String(err));
    process.exit(1);
  });
  // 注：startRepl 的 render 阻塞至退出（exitOnCtrlC:false，App 内部 process.exit）
  process.exit(0);
}
```

> 注：`src/index.ts` 当前是顶层执行脚本（无 main 函数）。加 `await import` 需要顶层 await——Node ESM 支持顶层 await，且 package.json `"type": "module"` 已是 ESM。若 tsc 对顶层 await 报错，把整个入口包进一个 `async function main() { ... } main();`（本步骤如遇编译错就这么改，最小化影响）。

- [ ] **Step 5: build 验证 + 全量回归**

Run: `npm run build`
Expected: tsc 通过（index.ts 改动 + ui/index.ts 新建无类型错）。

Run: `npm test`
Expected: 全绿（含所有新增 UI 测试 + 原有测试无回归）。

- [ ] **Step 6: 端到端手动验收**

在真实终端（Windows Terminal / PowerShell）执行：

```bash
# 1. REPL 模式（应进欢迎界面）
npm run dev

# 2. one-shot 模式仍工作（应走旧 CLI）
npm run dev -- "你好"

# 3. REPL 内交互：
#    - 输入文本 → 看到流式输出 + 工具调用
#    - /help /clear /exit 响应
#    - dangerous 工具（如让 agent 跑 bash）→ PermissionDialog 弹窗
#    - Esc 中断 streaming
#    - Ctrl+C 双击退出
```

对照 `npx tsx ui-preview.tsx` 的视觉基准，核对配色/符号/布局一致。

- [ ] **Step 7: 清理预览文件 + 提交**

视觉基准 `ui-preview.tsx` 在阶段②完成后（真实 REPL 已实现）可删除。但**建议保留至手动验收全部通过后**再删，因为它仍是对照基准。删除时：

```bash
# 验收通过后再执行（可选）：
# rm ui-preview.tsx
```

提交集成：

```bash
git add src/ui/index.ts src/index.ts tests/ui/index.test.ts
git commit -m "feat(m3.5): index.ts 双模式入口 + REPL 退出清理 + 集成"
```

---

## Self-Review

**1. Spec coverage（逐节核对）：**

| Spec 节 | 覆盖任务 |
|---------|---------|
| §3.1 文件结构 | File Structure 映射全部 14 个文件（theme/symbols/types/reduce-agent-event/use-agent-stream/spinner/logo/markdown/tool-panel/permission-dialog/input-bar/status-bar/welcome-screen/chat-view/app/index）✓ |
| §3.2 组件树 | Task 10(Welcome) / 11(ChatView+Static) / 7(Permission) / 8(Input) / 9(Status) / 12(App 组合) ✓ |
| §3.3 事件流 | Task 4 useAgentStream 桥接 ✓ |
| §4.1 `<Static>` 冻结 | Task 11 ChatView ✓ |
| §4.2 Modal 替换 | Task 12 App（pendingPermission ternary）✓ |
| §4.3 双 state+ref | Task 4 useAgentStream（stateRef/generationRef/abortRef）✓ |
| §4.4 generation 计数 | Task 4 useAgentStream（generationRef + finally 匹配）✓ |
| §4.5 权限双通道 | Task 4 useAgentStream（permissionGate.ask Promise + resolvePermission）✓ |
| §4.6 双击 Ctrl+C | Task 12 App（lastCtrlCRef + 2000ms）✓ |
| §5.1 useAgentStream | Task 4 ✓ |
| §5.2 App 同步清屏 | Task 12（hasClearedRef + submit 内同步 `\x1b[2J\x1b[H`）✓ |
| §5.3 InputBar | Task 8 ✓ |
| §5.4 PermissionDialog | Task 7 ✓ |
| §5.5 ToolPanel per-tool 折叠 | Task 6 ✓ |
| §5.6 MarkdownRenderer | Task 5 ✓ |
| §5.7 双模式入口 + 退出清理 | Task 13 ✓ |
| §6 依赖新增 | Task 1（marked/cli-highlight/ink-testing-library/@types/marked）✓ |
| §7.1 grace period | Task 7（425ms）✓；prefix-cache 否决 → Task 5 不做 ✓ |
| §8.1 配色 17 token | Task 1 ✓ |
| §8.2 符号单宽 | Task 1 ✓ |
| §8.3 Logo | Task 2 ✓ |
| §8.4 各场景 | Task 2/5/6/7/9/10 ✓ |
| §9 验收（功能 + 视觉） | Task 13 端到端 + 全程对照 ui-preview.tsx ✓ |
| usage 数据缺口（§8.4 状态栏需要） | Task 3（加 usage 事件）✓ |

**已知 spec 张力（计划已如实处理）：**
- `/compact` 手动触发受「UI state ≠ LLM history」约束，需改 agent core → 阶段②最小占位，完整留 M4（Task 12 注释声明）。
- `/cost` `/resume` `/sessions` 阶段②最小实现（状态栏已覆盖 cost；resume/sessions 复用 listSessions 打印，交互面板留 M4）。
- 这些不违反 §9「8 个命令全部可执行」（都能响应），只是 4 个完整、4 个最小占位——Task 13 验收时如实标注。

**2. Placeholder scan：** 全部步骤含真实可运行代码，无 "TBD/TODO/类似 Task N/添加适当错误处理"。Task 6 的 `useElapsed` 空实现是有意为之（计时糖留 M4），已 `void` 标注 + 注释说明，非占位。

**3. Type consistency：**
- `DisplayMessage` 联合的 `kind` 字段（user/assistant/tool/warning/error）在 Task 4（定义）、Task 6（tool-panel 消费 tool kind）、Task 11（ChatView renderCompleted switch 全 kind）一致 ✓
- `UseAgentStreamReturn` 字段（completedMessages/streamingText/activeTools/pendingPermission/usage/isRunning/error/submit/resolvePermission/abort/isAllowAlways/clear）在 Task 4 定义、Task 11/12 消费一致 ✓
- `PendingPermission`（toolUseId/toolName/input）Task 4 定义、Task 7 PermissionDialog 消费一致 ✓
- `Decision`（allow/allow_always/deny）Task 7 定义、Task 4 hook resolvePermission 参数一致 ✓
- `reduceAgentEvent` 签名在 Task 4 定义与调用一致 ✓
- `AgentEvent` Task 3 加的 `usage` variant 在 Task 4 reducer switch 消费 ✓

---

## Execution Handoff

计划已保存至 `docs/superpowers/plans/2026-08-05-ink-repl.md`。两种执行方式：

1. **Subagent-Driven（推荐）** —— 每个 Task 派一个新 subagent 实现，任务间我做 review，迭代快、上下文干净。适合本计划（13 个任务边界清晰、可并行段多）。
2. **Inline Execution** —— 在本会话按 executing-plans 批量执行，带 checkpoint review。

**选哪种？**
