# M3.5 阶段①：Agent Core 事件化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 ECode 的 agent core 从「直接 console.log 的 `Promise<void>`」改造为「产出 `AgentEvent` 的 `AsyncGenerator`」，同时落地 CLAUDE.md/ECODE.md 4 层注入、dangerous 权限判定、斜杠命令解析、OpenAI/Claude 流式实现 —— 全部 UI 无关，为阶段②（Ink REPL）提供可订阅的事件源。

**Architecture:** agent core 改造为 `AsyncGenerator<AgentEvent>`，消费方（旧 CLI wrapper 或阶段② Ink）订阅事件渲染。新增三块纯逻辑（instructions-loader / permission / slash-commands）UI 无关、可独立单测。Provider 层把已占位的 `stream()` 填实。权限通过「注入 `permissionGate` 回调」实现决策点：遇 dangerous 工具 → 调 gate → deny 时回喂 `is_error` 的 tool_result（保持 tool_use/tool_result id 配对不断裂）。中断走 AbortController，中断后补发空 tool_result 保配对。

**Tech Stack:** TypeScript 5.7（ESM，strict + noUnusedLocals/Parameters）/ Node 22 / Vitest 2.1 / `@anthropic-ai/sdk` ^0.32 / `openai` ^7.3。**阶段①零新增依赖**。

---

## Global Constraints

> 本节来自 ECode `CLAUDE.md` §九（红线）+ 项目硬约束。**每个 task 的需求都隐含包含本节**，不重复写。

- **ESM**：import 必须带 `.js`（如 `from './agent.js'`），即使源文件是 `.ts`。
- **tsc strict** + `noUnusedLocals` / `noUnusedParameters` / `noImplicitReturns` / `noFallthroughCasesInSwitch`：未用变量/参数、switch 漏分支都会编译报错。switch 处理判别联合时**必须**写 `default: const _: never = x;` exhaustive check（参考 `transform.ts:50-53`）。
- **tool_use/tool_result 配对铁律**：`tool_result.tool_use_id` 必须等于 `tool_use.id`，不配对 → API 400。中断/拒绝时**必须**补发对应 id 的 tool_result（内容可为 error），不能漏。
- **测试约定**：Vitest，测试放 `tests/`（平铺）或 `tests/<模块>/`（子目录）；import 源文件带 `.js`；mock 外部 SDK 用 `vi.mock('openai', ...)` + `vi.hoisted`（参考 `tests/providers/openai.test.ts`）；测试方法命名 `should_<预期行为>_when_<前置>` 或中文描述句。
- **包管理**：统一 `npm`，禁用 pnpm/cnpm/yarn。
- **commit**：Conventional Commits（`feat:`/`fix:`/`refactor:`/`docs:`/`test:`）；**不出现任何 claude / `Co-Authored-By` 字样**；完成一个完整功能（一个 task 走完红绿）再提交，禁止每改一行 commit。
- **不重构周边代码**（CLAUDE.md §1.7）：只改本 task 列明的文件，不顺手改无关代码。
- **异常处理**：禁止空 catch、禁止吞异常；异常信息必须含上下文。
- **每次 task 结束**：跑 `npm run build`（tsc clean）+ 相关测试全绿，再进 commit。

---

## 现状基线（动手前必读）

| 文件 | 现状 | 本计划是否动 |
|------|------|-------------|
| `src/agent.ts` | `runAgent(task, model?, opts?): Promise<void>`，循环内 `console.log` + `provider.complete()` 非流式 | Task 9/10 改造 |
| `src/providers/types.ts` | `ModelProvider.stream(request): AsyncIterable<ECodeStreamPart>` **已占位**；`ECodeStreamPart` 6 变体已定义（L83-89） | Task 7 加可选 `signal` |
| `src/providers/{openai,claude}.ts` | `stream()` 抛 `not implemented`（openai:39-41 / claude:40-42） | Task 7/8 填实现 |
| `src/tools/types.ts` | `ToolDefinition` 有 `execute` / `parallelizable`，**无 `dangerous`** | Task 4 加字段 |
| `src/tools/registry.ts` | 5 工具声明式（read_file/bash/edit_file/grep/glob） | Task 4 标 bash dangerous |
| `src/system-prompt.ts` | `buildSystemPrompt(): string` 无参，拼 IDENTITY+BEHAVIOR+TOOL_GUIDE | Task 3 加 instructions 参数 |
| `src/index.ts` | `parseArgs` → `runAgent`，单次跑完退出 | Task 10 接 AbortController + 注入 |
| `tests/` | 18 个测试文件，vitest，风格见 `tests/system-prompt.test.ts` / `tests/providers/openai.test.ts` | 各 task 新增 |

**已有类型（直接复用，勿重复定义）**：`ECodeMessage` / `ECodeContentBlock` / `ECodeResponse` / `ECodeStopReason` / `ECodeStreamPart` / `ECodeToolDefinition`（`providers/types.ts`）；`ToolDefinition` / `ToolResult`（`tools/types.ts`）；`RunAgentOptions` / `ResumeContext`（`agent.ts`）。

---

## File Structure

### 新建文件（4 个源 + 4 个测试）

| 文件 | 职责 | 单一边界 |
|------|------|---------|
| `src/agent-events.ts` | `AgentEvent` 判别联合类型 + 类型守卫 | 事件契约中心，所有事件化模块围绕它 |
| `src/instructions-loader.ts` | CLAUDE.md/ECODE.md 4 层读取（ECODE.md 优先 → CLAUDE.md 回退） | 纯 IO 读取，不做注入/拼装 |
| `src/permission.ts` | `shouldAsk()` 判定 + `AllowList` 内存集合 | 纯权限判定逻辑，无 UI/无回调 |
| `src/slash-commands.ts` | `parseSlashCommand()` 解析 + 命令注册表 | 纯解析，命令执行交给消费方 |
| `tests/agent-events.test.ts` | 类型守卫测试 | — |
| `tests/instructions-loader.test.ts` | 4 层回退 + ECODE.md 优先测试 | — |
| `tests/permission.test.ts` | shouldAsk 真值表 + AllowList 增删测试 | — |
| `tests/slash-commands.test.ts` | 解析分流测试 | — |

### 修改文件

| 文件 | 改动 |
|------|------|
| `src/tools/types.ts` | `ToolDefinition` 加 `dangerous?: boolean` |
| `src/tools/registry.ts` | `bash` 标 `dangerous: true`，其余不变 |
| `src/system-prompt.ts` | `buildSystemPrompt(instructions?)` 加可选项目上下文段 |
| `tests/system-prompt.test.ts` | 加 instructions 注入测试 |
| `src/providers/types.ts` | `stream(request, options?: { signal?: AbortSignal })` 加可选 options |
| `src/providers/openai.ts` | 实现 `stream()`（SDK `stream:true` + chunk 翻译） |
| `src/providers/claude.ts` | 实现 `stream()`（SDK `.stream()` + 事件翻译） |
| `tests/providers/openai.test.ts` | 加 stream chunk 流测试 |
| `tests/providers/claude.test.ts` | 加 stream 事件流测试 |
| `src/agent.ts` | 新增 `runAgentStream(): AsyncGenerator<AgentEvent>`；旧 `runAgent` 改为消费事件的 thin wrapper |
| `tests/agent-stream.test.ts`（新建） | mock provider 测事件序列 + 权限拦截 + 中断配对 |
| `src/index.ts` | 构造 AbortController（Ctrl+C）+ 读 CLAUDE.md 注入 |

---

## Task 1：AgentEvent 类型契约

**Files:**
- Create: `src/agent-events.ts`
- Test: `tests/agent-events.test.ts`

**Interfaces:**
- Consumes: `ECodeStopReason`（from `./providers/types.js`）
- Produces: `AgentEvent`（判别联合）+ `isXxxEvent` 类型守卫。后续所有 task 的事件产出口径。

**设计说明（Why 这个事件集）：**
- `text_delta`：流式文本增量（接 Provider stream 的 text_delta chunk）。
- `tool_call_start` / `tool_result`：工具调用的起止（UI 可折叠面板）。
- `permission_request`：dangerous 工具触发 gate 前，通知消费方「即将请求权限」（可观测/日志用；决策本身走 `permissionGate` 回调同步返回，不走事件往返，避免 AsyncGenerator 反向注入复杂度）。
- `warning`：transform 降级、压缩、重复动作检测等非致命提示。
- `completed`/`error`：终止事件，reason 区分正常结束/达上限/重复/中断。

- [ ] **Step 1：写失败测试（类型守卫）**

`tests/agent-events.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { isTextDeltaEvent, isPermissionRequestEvent, type AgentEvent } from '../src/agent-events.js';

describe('AgentEvent 类型守卫', () => {
  it('isTextDeltaEvent 正确收窄 text_delta 事件', () => {
    const e: AgentEvent = { type: 'text_delta', text: '你好' };
    expect(isTextDeltaEvent(e)).toBe(true);
    if (isTextDeltaEvent(e)) expect(e.text).toBe('你好');
  });

  it('isPermissionRequestEvent 正确收窄 permission_request 事件', () => {
    const e: AgentEvent = {
      type: 'permission_request',
      toolUseId: 'u1',
      toolName: 'bash',
      input: { command: 'rm -rf x' },
    };
    expect(isPermissionRequestEvent(e)).toBe(true);
  });

  it('非匹配类型返回 false', () => {
    const e: AgentEvent = { type: 'completed', rounds: 1, toolCalls: 0, reason: 'done' };
    expect(isTextDeltaEvent(e)).toBe(false);
  });
});
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/agent-events.test.ts`
Expected: FAIL —— `Failed to resolve import '../src/agent-events.js'`（文件不存在）。

- [ ] **Step 3：写最小实现**

`src/agent-events.ts`:
```ts
// AgentEvent —— agent core 事件化的中心契约（UI 无关）。
// 消费方（旧 CLI wrapper / 阶段② Ink）订阅 runAgentStream 产出的事件流渲染。
import type { ECodeStopReason } from './providers/types.js';

/** 请求权限的事件（可观测用，决策本身走 permissionGate 回调） */
export interface PermissionRequestEvent {
  type: 'permission_request';
  toolUseId: string;
  toolName: string;
  input: Record<string, unknown>;
}

/** agent 终止原因 */
export type AgentCompletionReason = 'done' | 'max-iterations' | 'repeated' | 'aborted';

/** agent 事件流 —— 判别联合，靠 type 字段收窄 */
export type AgentEvent =
  | { type: 'start'; task: string; model: string; provider: string; logFile?: string }
  | { type: 'text_delta'; text: string }
  | { type: 'tool_call_start'; id: string; name: string }
  | { type: 'tool_result'; id: string; name: string; content: string; isError: boolean }
  | PermissionRequestEvent
  | { type: 'warning'; message: string }
  | { type: 'completed'; rounds: number; toolCalls: number; reason: AgentCompletionReason }
  | { type: 'error'; error: string };

// ---- 类型守卫（消费方收窄用）----
export const isTextDeltaEvent = (e: AgentEvent): e is Extract<AgentEvent, { type: 'text_delta' }> =>
  e.type === 'text_delta';

export const isPermissionRequestEvent = (
  e: AgentEvent,
): e is PermissionRequestEvent => e.type === 'permission_request';

export const isCompletedEvent = (
  e: AgentEvent,
): e is Extract<AgentEvent, { type: 'completed' }> => e.type === 'completed';
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/agent-events.test.ts`
Expected: PASS（3 个用例）。

- [ ] **Step 5：tsc clean 检查**

Run: `npm run build`
Expected: 0 error。

- [ ] **Step 6：commit**

```bash
git add src/agent-events.ts tests/agent-events.test.ts
git commit -m "feat(m3.5): AgentEvent 判别联合 + 类型守卫"
```

---

## Task 2：CLAUDE.md/ECODE.md 4 层 loader

**Files:**
- Create: `src/instructions-loader.ts`
- Test: `tests/instructions-loader.test.ts`

**Interfaces:**
- Consumes: `node:fs` / `node:path`
- Produces: `loadInstructions(dirs: string[]): string`（拼好的项目上下文，空则返回 `''`）。Task 3 的 system-prompt 消费它。

**设计说明（Why 4 层 + ECODE.md 优先）：**
- 对齐 Claude Code 的 `['CCODE.md', 'CLAUDE.md']` 回退：每层目录先找 `ECODE.md`（ECode 品牌），没有再找 `CLAUDE.md`（兼容用户已有记忆文件）。
- 4 层：全局 home / 项目根 / 配置目录（`.ecode/`）/ cwd。每层独立读取，命中即拼一段（带来源标注），全部缺失返回空串。
- 纯 IO 函数，传入目录数组（由调用方解析，loader 不假设 cwd/home —— 呼应 CLAUDE.md §9.3「别散用 os.homedir()」，home 由调用方通过 `resolveDataDir` 之类入口传入，本函数只读）。

- [ ] **Step 1：写失败测试**

`tests/instructions-loader.test.ts`:
```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { loadInstructions } from '../src/instructions-loader.js';

describe('loadInstructions', () => {
  let root: string;

  beforeEach(() => {
    root = mkdtempSync(join(tmpdir(), 'ecode-instr-'));
  });
  afterEach(() => rmSync(root, { recursive: true, force: true }));

  it('某层有 CLAUDE.md 时读取其内容', () => {
    writeFileSync(join(root, 'CLAUDE.md'), '# 项目规则\n用中文');
    expect(loadInstructions([root])).toContain('用中文');
  });

  it('同层 ECODE.md 优先于 CLAUDE.md', () => {
    writeFileSync(join(root, 'CLAUDE.md'), 'from-claude');
    writeFileSync(join(root, 'ECODE.md'), 'from-ecode');
    const result = loadInstructions([root]);
    expect(result).toContain('from-ecode');
    expect(result).not.toContain('from-claude');
  });

  it('多层目录各自命中时全部拼接（带来源标注）', () => {
    const sub = join(root, 'sub');
    mkdirSync(sub);
    writeFileSync(join(root, 'ECODE.md'), '顶层规则');
    writeFileSync(join(sub, 'CLAUDE.md'), '子目录规则');
    const result = loadInstructions([root, sub]);
    expect(result).toContain('顶层规则');
    expect(result).toContain('子目录规则');
  });

  it('所有层都无记忆文件时返回空串（不抛错）', () => {
    expect(loadInstructions([root])).toBe('');
  });

  it('目录不存在时优雅返回空串（不抛错）', () => {
    expect(loadInstructions([join(root, 'nope')])).toBe('');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/instructions-loader.test.ts`
Expected: FAIL —— import 无法解析。

- [ ] **Step 3：写最小实现**

`src/instructions-loader.ts`:
```ts
// CLAUDE.md / ECODE.md 兼容读取 —— 4 层回退，ECODE.md 优先。
// 对齐 Claude Code 的 ['CCODE.md','CLAUDE.md'] 回退思想：ECode 品牌名优先，兼容用户已有 CLAUDE.md。
// 纯 IO：调用方传入目录数组（home/cwd 等由上层解析），本函数只读不假设路径。
import { readFileSync, existsSync } from 'node:fs';
import { join } from 'node:path';

/** 每层目录的候选文件名（优先级从高到低） */
const CANDIDATES = ['ECODE.md', 'CLAUDE.md'] as const;

/**
 * 从候选目录读取项目记忆并拼接。
 * @param dirs 目录数组（如 [homeDir, projectRoot, configDir, cwd]）
 * @returns 拼好的项目上下文；全部缺失返回 ''
 */
export function loadInstructions(dirs: string[]): string {
  const sections: string[] = [];
  for (const dir of dirs) {
    const hit = readFirstFound(dir);
    if (hit) sections.push(hit);
  }
  return sections.join('\n\n');
}

/** 单层目录：按 CANDIDATES 顺序找第一个存在的文件，读内容（带来源标注）。 */
function readFirstFound(dir: string): string {
  for (const fname of CANDIDATES) {
    const full = join(dir, fname);
    if (existsSync(full)) {
      // 读不到（权限/编码）会抛 —— 调用方 catch 降级为空，不让记忆读取杀 agent
      const content = readFileSync(full, 'utf8').trim();
      if (content) return `## 项目记忆（${fname} @ ${dir}）\n${content}`;
    }
  }
  return '';
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/instructions-loader.test.ts`
Expected: PASS（5 个用例）。

- [ ] **Step 5：tsc clean + commit**

```bash
npm run build
git add src/instructions-loader.ts tests/instructions-loader.test.ts
git commit -m "feat(m3.5): CLAUDE.md/ECODE.md 4层 loader（ECODE 优先）"
```

---

## Task 3：system-prompt 接受 instructions 注入

**Files:**
- Modify: `src/system-prompt.ts:32-34`
- Test: `tests/system-prompt.test.ts`（追加用例）

**Interfaces:**
- Consumes: Task 2 的 `loadInstructions()` 输出（`string`）
- Produces: `buildSystemPrompt(instructions?: string): string`

- [ ] **Step 1：写失败测试（追加到现有文件）**

在 `tests/system-prompt.test.ts` 末尾的 `describe` 块内追加：
```ts
  it('传入 instructions 时拼入项目记忆段', () => {
    const prompt = buildSystemPrompt('## 项目记忆\n禁止用 any');
    expect(prompt).toContain('项目记忆');
    expect(prompt).toContain('禁止用 any');
  });

  it('不传 instructions 时不含项目记忆段（向后兼容）', () => {
    const prompt = buildSystemPrompt();
    expect(prompt).not.toContain('项目记忆');
  });
```

记得在文件顶部 import 行追加 `buildSystemPrompt` 已 import，无需改。

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/system-prompt.test.ts`
Expected: FAIL —— `Expected substring: "项目记忆"`（当前 buildSystemPrompt 无参，忽略入参）。

- [ ] **Step 3：改实现**

`src/system-prompt.ts`，把 `buildSystemPrompt` 改为：
```ts
/**
 * 拼装完整 system prompt。分层组合，便于后续按里程碑扩展。
 * @param instructions 项目记忆（CLAUDE.md/ECODE.md 内容，Task 2 产出）；空则不拼该段。
 */
export function buildSystemPrompt(instructions?: string): string {
  const sections = [IDENTITY, BEHAVIOR, TOOL_GUIDE];
  if (instructions && instructions.trim()) {
    sections.push(instructions.trim());
  }
  return sections.join('\n\n---\n\n');
}
```

- [ ] **Step 4：跑测试确认通过（含旧用例无回归）**

Run: `npx vitest run tests/system-prompt.test.ts`
Expected: PASS（原 3 个 + 新 2 个 = 5 个）。

- [ ] **Step 5：tsc clean + commit**

```bash
npm run build
git add src/system-prompt.ts tests/system-prompt.test.ts
git commit -m "feat(m3.5): buildSystemPrompt 接受 instructions 注入"
```

---

## Task 4：ToolDefinition.dangerous + bash 标记

**Files:**
- Modify: `src/tools/types.ts:3-19`
- Modify: `src/tools/registry.ts:30-44`（bash 条目）
- Test: `tests/tools.test.ts`（追加用例）

**Interfaces:**
- Produces: `ToolDefinition.dangerous?: boolean`。Task 5 的 `shouldAsk` 消费它。

- [ ] **Step 1：写失败测试（追加到 `tests/tools.test.ts`）**

先确认现有 `tests/tools.test.ts` 的 import（应有 `toolDefinitions`），在合适 `describe` 内追加：
```ts
  it('bash 工具标记为 dangerous（需权限审批）', () => {
    const bash = toolDefinitions.find((t) => t.name === 'bash');
    expect(bash?.dangerous).toBe(true);
  });

  it('只读工具（read_file/grep/glob）未标 dangerous', () => {
    const readonly = toolDefinitions.filter((t) =>
      ['read_file', 'grep', 'glob'].includes(t.name),
    );
    for (const t of readonly) {
      expect(t.dangerous ?? false).toBe(false);
    }
  });
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/tools.test.ts`
Expected: FAIL —— `Expected: true, Received: undefined`（bash 未标 dangerous）。

- [ ] **Step 3：改实现**

`src/tools/types.ts`，在 `ToolDefinition` 接口内（`parallelizable` 后）加：
```ts
  /** P5 预留：标记工具是否只读可并行（如 glob/grep 可并发，edit_file 不行） */
  parallelizable?: boolean;
  /**
   * 是否危险操作（需权限审批）。
   * 档A（CCode 式）：bash 等副作用工具标 true，触发 permissionGate 询问；
   * read_file/grep/glob 等只读工具不标（undefined 视为 false，直接放行）。
   */
  dangerous?: boolean;
```

`src/tools/registry.ts`，bash 条目加 `dangerous: true`（在 `execute` 前/后均可，建议放 description 后）：
```ts
  {
    name: 'bash',
    description:
      '执行 shell 命令。当你需要运行命令、安装依赖、编译代码、运行脚本时使用这个工具。',
    dangerous: true,
    parameters: {
      // ... 不变
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/tools.test.ts`
Expected: PASS（原用例 + 新 2 个）。

- [ ] **Step 5：tsc clean + commit**

```bash
npm run build
git add src/tools/types.ts src/tools/registry.ts tests/tools.test.ts
git commit -m "feat(m3.5): ToolDefinition.dangerous + bash 标记"
```

---

## Task 5：权限判定纯逻辑（shouldAsk + AllowList）

**Files:**
- Create: `src/permission.ts`
- Test: `tests/permission.test.ts`

**Interfaces:**
- Consumes: Task 4 的 `ToolDefinition.dangerous`
- Produces: `shouldAsk(toolName, isDangerous, allow)` + `AllowList` 类 + `PermissionGate` 接口。Task 9 的 runAgentStream 消费（`PermissionGate` 是权限决策回调的契约）。

**设计说明（Why 纯逻辑 + 不含回调）：**
- 档A 规则极简：**已批准过（在 AllowList）→ 直接放行；否则 dangerous → 需询问；非 dangerous → 放行**。
- 询问/弹窗本身是 UI 的事（阶段② Ink 的 PermissionDialog），核心只提供「是否需要问」的判定 + 「记住批准」的集合。
- `AllowList` 内存版（会话级 always-allow）；持久化（`.ecode/settings.local.json`）留 M4，本 task 只预留接口形状（`has`/`add`），实现用内存 Set。

- [ ] **Step 1：写失败测试**

`tests/permission.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { shouldAsk, AllowList } from '../src/permission.js';

describe('shouldAsk', () => {
  it('dangerous 且未在 allow 列表 → true（需询问）', () => {
    const allow = new AllowList();
    expect(shouldAsk('bash', true, allow)).toBe(true);
  });

  it('dangerous 但已在 allow 列表 → false（已批准，放行）', () => {
    const allow = new AllowList();
    allow.add('bash');
    expect(shouldAsk('bash', true, allow)).toBe(false);
  });

  it('非 dangerous（undefined）→ false（只读放行）', () => {
    const allow = new AllowList();
    expect(shouldAsk('read_file', false, allow)).toBe(false);
  });
});

describe('AllowList', () => {
  it('add 后 has 返回 true', () => {
    const allow = new AllowList();
    expect(allow.has('bash')).toBe(false);
    allow.add('bash');
    expect(allow.has('bash')).toBe(true);
  });

  it('不同工具名互不影响', () => {
    const allow = new AllowList();
    allow.add('bash');
    expect(allow.has('edit_file')).toBe(false);
  });
});
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/permission.test.ts`
Expected: FAIL —— import 无法解析。

- [ ] **Step 3：写最小实现**

`src/permission.ts`:
```ts
// 权限判定纯逻辑（档A：CCode 式 dangerous 二元）。
// 只决定「是否需要询问」+ 记住「已批准」。询问/弹窗本身由 UI 层（阶段②）注入回调实现。

/**
 * 会话级允许列表（always-allow）。
 * 内存版：Set<toolName>。持久化（.ecode/settings.local.json）留 M4，接口形状预留。
 */
export class AllowList {
  private readonly granted = new Set<string>();

  has(toolName: string): boolean {
    return this.granted.has(toolName);
  }

  /** 记住某工具已批准（本会话后续不再询问）。 */
  add(toolName: string): void {
    this.granted.add(toolName);
  }
}

/**
 * 判定一次工具调用是否需要请求权限。
 * 档A规则：已在 allow 列表 → 放行；否则仅 dangerous 工具需询问。
 */
export function shouldAsk(toolName: string, isDangerous: boolean, allow: AllowList): boolean {
  if (allow.has(toolName)) return false;
  return isDangerous;
}

/**
 * 权限决策回调契约（注入式）：UI 层（阶段② Ink 的 PermissionDialog）实现 ask，
 * 阶段①测试用 mock。runAgentStream 遇 dangerous 工具时调它，据返回值放行/拒绝。
 */
export interface PermissionGate {
  ask(req: { toolName: string; input: Record<string, unknown> }): Promise<'allow' | 'deny'>;
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/permission.test.ts`
Expected: PASS（5 个用例）。

- [ ] **Step 5：tsc clean + commit**

```bash
npm run build
git add src/permission.ts tests/permission.test.ts
git commit -m "feat(m3.5): 权限判定 shouldAsk + AllowList"
```

---

## Task 6：斜杠命令解析 + 注册表

**Files:**
- Create: `src/slash-commands.ts`
- Test: `tests/slash-commands.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `parseUserInput(input)` → `SlashCommand | { type: 'message'; text: string }`。消费方据此决定走命令还是发给 LLM。

**设计说明（Why 纯解析、不含执行）：**
- 阶段①只做「识别 + 分流」：输入 `/help` → 返回命令描述；输入 `你好` → 返回 message。
- 命令**执行**（/clear 清屏、/exit 退出、/model 切换）依赖 UI/进程控制，放阶段②。本 task 的 `SlashCommand` 只带 `name` + `args`，执行由消费方 switch。
- 注册表声明式，新增命令加一条即可。

- [ ] **Step 1：写失败测试**

`tests/slash-commands.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseUserInput, SLASH_COMMANDS } from '../src/slash-commands.js';

describe('parseUserInput', () => {
  it('以 / 开头且命中注册表 → 返回 SlashCommand', () => {
    expect(parseUserInput('/help')).toEqual({ type: 'command', name: 'help', args: [] });
  });

  it('带参数的命令 → args 拆分', () => {
    expect(parseUserInput('/model deepseek')).toEqual({
      type: 'command',
      name: 'model',
      args: ['deepseek'],
    });
  });

  it('非 / 开头 → 返回 message', () => {
    expect(parseUserInput('帮我读文件')).toEqual({ type: 'message', text: '帮我读文件' });
  });

  it('/ 开头但未注册 → 返回 unknown command（不静默当 message）', () => {
    expect(parseUserInput('/foobar')).toEqual({ type: 'unknown_command', raw: '/foobar' });
  });

  it('空串 → message（空文本）', () => {
    expect(parseUserInput('')).toEqual({ type: 'message', text: '' });
  });
});

describe('SLASH_COMMANDS 注册表', () => {
  it('包含 MVP 四命令 help/clear/model/exit', () => {
    const names = SLASH_COMMANDS.map((c) => c.name);
    expect(names).toContain('help');
    expect(names).toContain('clear');
    expect(names).toContain('model');
    expect(names).toContain('exit');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/slash-commands.test.ts`
Expected: FAIL —— import 无法解析。

- [ ] **Step 3：写最小实现**

`src/slash-commands.ts`:
```ts
// 斜杠命令解析 + 注册表（纯逻辑，UI 无关）。
// 阶段①只做「识别 + 分流」，命令执行（清屏/退出/切模型）由消费方处理。

export interface SlashCommandDef {
  name: string;
  description: string;
}

/** MVP 命令注册表（声明式，新增命令加一条） */
export const SLASH_COMMANDS: SlashCommandDef[] = [
  { name: 'help', description: '显示可用命令' },
  { name: 'clear', description: '清空对话历史' },
  { name: 'model', description: '切换模型（如 /model deepseek）' },
  { name: 'exit', description: '退出 ECode' },
];

export type ParsedInput =
  | { type: 'command'; name: string; args: string[] }
  | { type: 'unknown_command'; raw: string }
  | { type: 'message'; text: string };

/**
 * 解析用户输入：/ 开头且命中注册表 → command；/ 开头未命中 → unknown_command；否则 → message。
 */
export function parseUserInput(input: string): ParsedInput {
  const trimmed = input.trim();
  if (!trimmed.startsWith('/')) {
    return { type: 'message', text: input };
  }
  const parts = trimmed.slice(1).split(/\s+/);
  const name = parts[0];
  const args = parts.slice(1);
  const known = SLASH_COMMANDS.some((c) => c.name === name);
  if (!known) {
    return { type: 'unknown_command', raw: trimmed };
  }
  return { type: 'command', name, args };
}
```

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/slash-commands.test.ts`
Expected: PASS（6 个用例）。

- [ ] **Step 5：tsc clean + commit**

```bash
npm run build
git add src/slash-commands.ts tests/slash-commands.test.ts
git commit -m "feat(m3.5): 斜杠命令解析 + 注册表"
```

---

## Task 7：OpenAI Provider stream() 实现

**Files:**
- Modify: `src/providers/types.ts:103`（stream 签名加 options）
- Modify: `src/providers/openai.ts:39-41`（填实现）
- Test: `tests/providers/openai.test.ts`（追加用例）

**Interfaces:**
- Consumes: `toOpenAIRequest(request)`（transform.ts）
- Produces: `stream(request, options?)` 返回 `AsyncIterable<ECodeStreamPart>`，支持 `options.signal` 中断。Task 9 消费。

**设计说明：**
- OpenAI SDK 流式：`client.chat.completions.create({ ...params, stream: true })` 返回 `Stream<ChatCompletionChunk>`，迭代 chunk。
- chunk 关键字段：`choices[0].delta.content`（string|null，文本增量）；`choices[0].delta.tool_calls`（数组，每个含 `index`/`id`/`function{name, arguments}`，arguments 是**增量 JSON 字符串**）；`choices[0].finish_reason`（null|'stop'|'tool_calls'|...）。
- 翻译映射：delta.content → `text_delta`；tool_calls 首次出现（带 id/name）→ `tool_call_start`，后续 arguments 增量 → `tool_call_delta`；finish_reason 非 null → `stop`；最后 chunk 的 usage → `usage`。
- `withRetry` 不套流式（流已开始不宜整体重试）；signal 传给 SDK（openai SDK 支持 `signal` 在 request options）。

- [ ] **Step 1：写失败测试（追加到 `tests/providers/openai.test.ts`）**

```ts
  it('stream() 把 OpenAI chunk 流翻译成 ECodeStreamPart', async () => {
    // mock create 返回一个异步迭代器（模拟 SDK Stream）
    const chunks = [
      { choices: [{ delta: { content: '你' }, finish_reason: null }] },
      { choices: [{ delta: { content: '好' }, finish_reason: null }] },
      { choices: [{ delta: {}, finish_reason: 'stop' }], usage: { prompt_tokens: 2, completion_tokens: 2 } },
    ];
    mockCreate.mockResolvedValue(
      (async function* () {
        for (const c of chunks) yield c;
      })(),
    );
    const provider = new OpenAIProvider({ apiKey: 'fake' });
    const parts = [];
    for await (const p of provider.stream(baseReq)) parts.push(p);
    expect(parts).toEqual([
      { type: 'text_delta', text: '你' },
      { type: 'text_delta', text: '你好'.slice(1) }, // '好'
      { type: 'usage', inputTokens: 2, outputTokens: 2 },
      { type: 'stop', reason: { unified: 'stop', raw: 'stop' } },
    ]);
  });
```

> 注：`'你好'.slice(1)` 即 `'好'`，直接写 `'好'` 也可；这里用 slice 避免 LLM 既有知识写具体字符失真（CLAUDE.md debugging #004），实际可写 `text: '好'`。**用 `text: '好'`**。

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/providers/openai.test.ts`
Expected: FAIL —— stream() 抛 `not implemented`。

- [ ] **Step 3：先改 types.ts 签名（加可选 options）**

`src/providers/types.ts:99-103`，把 stream 行改为：
```ts
  /**
   * 流式：逐 chunk 返回。
   * @param options.signal 可选 AbortSignal，中断时停止迭代（M3.5 中断机制用）。
   */
  stream(request: ChatRequest, options?: { signal?: AbortSignal }): AsyncIterable<ECodeStreamPart>;
```

- [ ] **Step 4：填 openai.ts 实现**

`src/providers/openai.ts`，把 stream 方法改为：
```ts
  async *stream(request: ChatRequest, options?: { signal?: AbortSignal }): AsyncIterable<ECodeStreamPart> {
    // stream_options.include_usage：OpenAI 流式默认不返回 usage，必须显式开启，否则 chunk.usage 恒 undefined
    // stream: true as const 是字面量类型，触发 SDK 的 streaming 重载（返回 Stream<ChatCompletionChunk>）
    const params = {
      ...toOpenAIRequest(request),
      stream: true as const,
      stream_options: { include_usage: true },
    };
    // openai SDK 支持 signal；流已开始不套 withRetry（整体重试不适用流式）
    // 若 strict 下 create 重载未正确推断为 streaming，对 params 加 `as ChatCompletionCreateParamsStreaming` 断言兜底
    const stream = await this.client.chat.completions.create(
      params,
      options?.signal ? { signal: options.signal } : undefined,
    );
    // tool_call 参数增量按 index 累积：首次(id/name)发 start，后续 arguments 增量发 delta
    const toolCallMeta = new Map<number, { id: string; name: string }>();
    for await (const chunk of stream as AsyncIterable<OpenAI.Chat.ChatCompletionChunk>) {
      const choice = chunk.choices?.[0];
      if (!choice) continue;
      const delta = choice.delta;
      if (delta?.content) {
        yield { type: 'text_delta', text: delta.content };
      }
      if (delta?.tool_calls) {
        for (const tc of delta.tool_calls) {
          if (tc.id && tc.function?.name && !toolCallMeta.has(tc.index)) {
            toolCallMeta.set(tc.index, { id: tc.id, name: tc.function.name });
            yield { type: 'tool_call_start', id: tc.id, name: tc.function.name };
          }
          if (tc.function?.arguments) {
            const id = toolCallMeta.get(tc.index)?.id;
            if (id) yield { type: 'tool_call_delta', id, inputDelta: tc.function.arguments };
          }
        }
      }
      if (choice.finish_reason) {
        if (chunk.usage) {
          yield { type: 'usage', inputTokens: chunk.usage.prompt_tokens ?? 0, outputTokens: chunk.usage.completion_tokens ?? 0 };
        }
        yield { type: 'stop', reason: mapOpenAIStopReasonStreaming(choice.finish_reason) };
      }
    }
  }
```

并在文件底部加一个流式 stop_reason 映射（复用 transform.ts 的逻辑思路，但 transform 的 `mapOpenAIStopReason` 未导出 —— 这里本地写一份轻量版，避免为复用而改 transform 增加面）：
```ts
/** 流式 finish_reason → ECodeStopReason（与 transform.mapOpenAIStopReason 同语义，独立避免循环依赖） */
function mapOpenAIStopReasonStreaming(reason: string): import('./types.js').ECodeStopReason {
  switch (reason) {
    case 'stop':
      return { unified: 'stop', raw: reason };
    case 'tool_calls':
      return { unified: 'tool-use', raw: reason };
    case 'length':
      return { unified: 'length', raw: reason };
    case 'content_filter':
      return { unified: 'content-filter', raw: reason };
    default:
      return { unified: 'other', raw: reason };
  }
}
```

> 注意 `noUnusedLocals`：`toolCallMeta` 若某轮只收到 delta 无 start（异常情况），`id` 为 undefined 时跳过 delta，已处理。

- [ ] **Step 5：修正测试中的 text 期望**

把测试里 `{ type: 'text_delta', text: '你好'.slice(1) }` 改为 `{ type: 'text_delta', text: '好' }`（明确）。

- [ ] **Step 6：跑测试确认通过**

Run: `npx vitest run tests/providers/openai.test.ts`
Expected: PASS（原 3 个 + stream 1 个）。

- [ ] **Step 7：tsc clean + commit**

```bash
npm run build
git add src/providers/types.ts src/providers/openai.ts tests/providers/openai.test.ts
git commit -m "feat(m3.5): OpenAI Provider stream() 流式实现"
```

---

## Task 8：Claude Provider stream() 实现

**Files:**
- Modify: `src/providers/claude.ts:40-42`（types.ts 已在 Task 7 改好）
- Test: `tests/providers/claude.test.ts`（追加用例）

**Interfaces:**
- Consumes: `toAnthropicRequest(request)`（transform.ts）
- Produces: `stream(request, options?)`。Task 9 消费。

**设计说明：**
- Anthropic SDK 流式：`client.messages.stream(params)` 返回 `MessageStream`，其 `.stream` 是 `AsyncIterable<RawMessageStreamEvent>`。
- 事件类型（判别联合，靠 type 收窄）：
  - `message_start`：含 `message.usage.input_tokens`。
  - `content_block_start`：`index` + `content_block`（type 为 `text` 或 `tool_use`，tool_use 带 `id`/`name`）→ 发 `tool_call_start`。
  - `content_block_delta`：`delta.type === 'text_delta'` → `text_delta`；`delta.type === 'input_json_delta'` → `tool_call_delta`（需用 content_block_start 记下的 id）。
  - `content_block_stop`：`index` → 发 `tool_call_end`。
  - `message_delta`：`delta.stop_reason` + `usage.output_tokens` → 发 `usage`。
- signal 传给 `messages.stream(params, { signal })`。

- [ ] **Step 1：写失败测试（追加到 `tests/providers/claude.test.ts`）**

先看 `tests/providers/claude.test.ts` 顶部的 mock 模式（应与 openai.test.ts 同构：`vi.mock('@anthropic-ai/sdk', ...)` + `mockCreate`/`mockStream`）。追加：
```ts
  it('stream() 把 Anthropic 事件流翻译成 ECodeStreamPart', async () => {
    const events = [
      { type: 'message_start', message: { usage: { input_tokens: 5 } } },
      { type: 'content_block_start', index: 0, content_block: { type: 'text', text: '' } },
      { type: 'content_block_delta', index: 0, delta: { type: 'text_delta', text: '你好' } },
      { type: 'content_block_stop', index: 0 },
      {
        type: 'content_block_start',
        index: 1,
        content_block: { type: 'tool_use', id: 't1', name: 'read_file', input: {} },
      },
      { type: 'content_block_delta', index: 1, delta: { type: 'input_json_delta', partial_json: '{"path":"x"}' } },
      { type: 'content_block_stop', index: 1 },
      { type: 'message_delta', delta: { stop_reason: 'tool_use' }, usage: { output_tokens: 8 } },
    ];
    // mock messages.stream 返回带 [Symbol.asyncIterator] 的对象
    mockStream.mockResolvedValue({
      stream: (async function* () {
        for (const e of events) yield e;
      })(),
    });
    const provider = new ClaudeProvider({ apiKey: 'fake' });
    const parts = [];
    for await (const p of provider.stream(baseReq)) parts.push(p);
    const types = parts.map((p) => p.type);
    // 实现里 message_delta 先 yield usage 再 yield stop（output_tokens 来自 message_delta）
    expect(types).toEqual([
      'text_delta',
      'tool_call_start',
      'tool_call_delta',
      'tool_call_end',
      'usage',
      'stop',
    ]);
    const start = parts.find((p) => p.type === 'tool_call_start');
    expect(start).toMatchObject({ id: 't1', name: 'read_file' });
  });
```

> 若现有 claude.test.ts 的 mock 叫法不同（如 `mockCreate` 而非 `mockStream`），按实际调整：需同时 mock `messages.create`（complete 用）和 `messages.stream`（stream 用）。参考 openai.test.ts 的 `vi.hoisted` 模式，在 mock class 里加 `stream = mockStream`。

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/providers/claude.test.ts`
Expected: FAIL —— stream() 抛 `not implemented`，或 mockStream 未定义（需先在文件顶部 mock 里加 `stream`）。

- [ ] **Step 3：补 mock（若 claude.test.ts 顶部 mock class 缺 stream）**

在 `vi.mock('@anthropic-ai/sdk', ...)` 的 class 内加：
```ts
const { mockCreate, mockStream } = vi.hoisted(() => ({ mockCreate: vi.fn(), mockStream: vi.fn() }));
vi.mock('@anthropic-ai/sdk', () => ({
  default: class MockAnthropic {
    messages = { create: mockCreate, stream: mockStream };
  },
}));
```

- [ ] **Step 4：填 claude.ts 实现**

`src/providers/claude.ts`，把 stream 方法改为：
```ts
  async *stream(request: ChatRequest, options?: { signal?: AbortSignal }): AsyncIterable<ECodeStreamPart> {
    const params = toAnthropicRequest(request);
    const msgStream = this.client.messages.stream(
      params,
      options?.signal ? { signal: options.signal } : undefined,
    );
    // 按 index 记 tool_use 的 id（content_block_start 给 id，delta 只给 partial_json）
    const toolIdByIndex = new Map<number, string>();
    for await (const ev of msgStream.stream as AsyncIterable<Anthropic.RawMessageStreamEvent>) {
      switch (ev.type) {
        case 'content_block_start': {
          const block = ev.content_block;
          if (block.type === 'tool_use') {
            toolIdByIndex.set(ev.index, block.id);
            yield { type: 'tool_call_start', id: block.id, name: block.name };
          }
          break;
        }
        case 'content_block_delta': {
          const d = ev.delta;
          if (d.type === 'text_delta') {
            yield { type: 'text_delta', text: d.text };
          } else if (d.type === 'input_json_delta') {
            const id = toolIdByIndex.get(ev.index);
            if (id) yield { type: 'tool_call_delta', id, inputDelta: d.partial_json };
          }
          break;
        }
        case 'content_block_stop': {
          const id = toolIdByIndex.get(ev.index);
          if (id) yield { type: 'tool_call_end', id };
          break;
        }
        case 'message_delta': {
          // stop_reason + output_tokens 在 message_delta 的 usage
          if (ev.usage) {
            yield { type: 'usage', inputTokens: 0, outputTokens: ev.usage.output_tokens };
          }
          yield { type: 'stop', reason: mapAnthropicStopReasonStreaming(ev.delta.stop_reason) };
          break;
        }
        default:
          // message_start / ping 等不产出 ECodeStreamPart，跳过
          break;
      }
    }
  }
```

文件底部加流式 stop_reason 映射（与 transform.ts `mapAnthropicStopReason` 同语义，独立避免循环依赖）：
```ts
function mapAnthropicStopReasonStreaming(
  reason: string | null | undefined,
): import('./types.js').ECodeStopReason {
  switch (reason) {
    case 'end_turn':
      return { unified: 'stop', raw: reason };
    case 'tool_use':
      return { unified: 'tool-use', raw: reason };
    default:
      return { unified: 'length', raw: reason ?? undefined };
  }
}
```

- [ ] **Step 5：跑测试确认通过**

Run: `npx vitest run tests/providers/claude.test.ts`
Expected: PASS（原用例 + stream 1 个）。

- [ ] **Step 6：tsc clean + commit**

```bash
npm run build
git add src/providers/claude.ts tests/providers/claude.test.ts
git commit -m "feat(m3.5): Claude Provider stream() 流式实现"
```

---

## Task 9：runAgentStream AsyncGenerator 核心

**Files:**
- Modify: `src/agent.ts`（新增 `runAgentStream`，**暂不动** `runAgent` —— Task 10 才改成 wrapper）
- Test: `tests/agent-stream.test.ts`（新建）

**Interfaces:**
- Consumes:
  - Task 1 `AgentEvent`
  - Task 4 `ToolDefinition.dangerous`
  - Task 5 `shouldAsk` / `AllowList` / `PermissionGate`
  - Task 7/8 `provider.stream()`
  - 已有 `executeTool` / `buildSystemPrompt` / `maybeCompress` / `persistSession` / `withRetry` / `initRuntimeLog` / `logApiRequest` / `logApiResponse` / `logToolExecution` / `logError` / `finalizeRuntimeLog`
- Produces: `runAgentStream(task, opts): AsyncGenerator<AgentEvent>`。Task 10 消费。

**关键类型（权威定义见 Step 3）：**
- `PermissionGate`：Task 5 已在 `permission.ts` 定义并导出（`ask(req) → 'allow'|'deny'`），此处 import 复用，不重复定义。
- `RunAgentStreamOptions extends RunAgentOptions`（继承 `resumed`）：

```ts
export interface RunAgentStreamOptions extends RunAgentOptions {
  model?: string;
  system?: string;                     // 预拼好的 system（含 CLAUDE.md），不传则 buildSystemPrompt()
  signal?: AbortSignal;                // 中断
  allow?: AllowList;                   // 会话级允许列表（不传则内部 new）
  permissionGate?: PermissionGate;     // 权限决策回调（不传 = 全部放行，兼容无 UI / 测试）
  provider?: ModelProvider;            // 依赖注入（测试用 mock；生产用 createProvider）
}
```
（warning 走 `yield` event 暴露，无需额外 `onWarning` 回调 —— YAGNI。）

**核心流程（yield 事件 + 权限 hook + 中断配对）：**
1. `yield start`（task/model/provider）。
2. 进 loop（最多 MAX_ITERATIONS）：
   - `maybeCompress`（不变）；压缩则 `yield warning`。
   - 调 `provider.stream(request, { signal })`，迭代 chunk：
     - text_delta → `yield text_delta`
     - tool_call_start/delta/end → 累积 input（按 id 拼接 inputDelta JSON）
     - usage/stop → 收尾，组装本轮 `ECodeResponse.content`（text + tool_call）
   - 无 tool_call → `yield completed(reason:'done')`，return。
   - 有 tool_call → push assistant 消息；逐个工具：
     - 查 dangerous + `shouldAsk` → 需询问时：
       - `yield permission_request`（可观测）
       - `await permissionGate.ask()`（无 gate = 放行）
       - deny → 构造 `isError=true` 的 tool_result（内容"用户拒绝执行"），**yield tool_result 后跳过 executeTool**（但必须 push tool_result 保配对）。
       - allow 且 gate 返回 → `allow.add(name)`（会话记住，可选；档A 简化为 allow 即记住）。
     - 放行 → `executeTool` → `yield tool_result`。
     - push tool_result（id 配对）。
   - 重复动作检测 → `yield warning` + `yield completed(reason:'repeated')`，return。
3. signal 中断：迭代中 `signal.aborted` 抛 `AbortError` → catch → **为所有未配对的 tool_use 补发 is_error tool_result**（保配对）→ `yield completed(reason:'aborted')`，return。
4. 达 MAX_ITERATIONS → `yield completed(reason:'max-iterations')`。
5. 任意异常 → `yield error`（不 re-throw，让消费方决定；但保留 session 落盘）。

- [ ] **Step 1：写失败测试（mock provider.stream + 测事件序列）**

采用 **provider 注入方案**（`RunAgentStreamOptions.provider?`，呼应 CLAUDE.md §架构「Provider 依赖注入」）：测试直接传 mockProvider，省去 `vi.doMock` factory 的时机坑。

`tests/agent-stream.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest';
import { runAgentStream } from '../src/agent.js';
import type { AgentEvent } from '../src/agent-events.js';
import type { ECodeStreamPart, ModelProvider, ChatRequest } from '../src/providers/types.js';

/** 造一个返回固定 chunk 流的 mock provider（complete 给桩，summarize 压缩时用）。 */
function mockProvider(parts: ECodeStreamPart[]): ModelProvider {
  return {
    name: 'mock',
    protocol: 'openai',
    baseURL: 'http://mock',
    complete: vi.fn(async () => ({
      content: [{ type: 'text', text: '压缩摘要' }],
      stopReason: { unified: 'stop' },
      usage: { inputTokens: 0, outputTokens: 0 },
    })),
    stream: async function* (_req: ChatRequest): AsyncIterable<ECodeStreamPart> {
      for (const p of parts) yield p;
    },
  };
}

const collect = async (gen: AsyncGenerator<AgentEvent>): Promise<AgentEvent[]> => {
  const out: AgentEvent[] = [];
  for await (const e of gen) out.push(e);
  return out;
};

describe('runAgentStream', () => {
  it('纯文本回复 → start / text_delta / completed(done)', async () => {
    const provider = mockProvider([
      { type: 'text_delta', text: '你好' },
      { type: 'stop', reason: { unified: 'stop', raw: 'stop' } },
    ]);
    const events = await collect(runAgentStream('打招呼', { provider }));
    expect(events[0].type).toBe('start');
    expect(events.some((e) => e.type === 'text_delta' && e.text === '你好')).toBe(true);
    const done = events.find((e) => e.type === 'completed');
    expect(done?.type === 'completed' && done.reason).toBe('done');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

Run: `npx vitest run tests/agent-stream.test.ts`
Expected: FAIL —— `runAgentStream is not a function`（未导出）。

- [ ] **Step 3：实现 runAgentStream（采用 provider 注入）**

在 `src/agent.ts` 顶部加 import（只加缺失项；`noUnusedLocals` 下每个都要被用到）：
```ts
import type { AgentEvent } from './agent-events.js';
import type { ModelProvider, ECodeStreamPart, ECodeContentBlock } from './providers/types.js';
import { shouldAsk, AllowList } from './permission.js';
import type { PermissionGate } from './permission.js';
```

（`ECodeMessage` / `ECodeToolResultOutput` / `executeTool` / `buildSystemPrompt` / `maybeCompress` / `createProvider` / `hasCapability` / `getDefaultModel` / `persistSession` / `timestampId` / `MAX_ITERATIONS` / 各 `logXxx` / `ECodeSession` / `ECodeSessionStats` —— 均已在 agent.ts 现有 import（L6/L17）或同文件定义，复用，不重复 import。`ECodeContentBlock` 原文件未 import，故上方新增。）

在 `runAgent` 上方新增：
```ts
export interface RunAgentStreamOptions extends RunAgentOptions {
  model?: string;
  system?: string;
  signal?: AbortSignal;
  allow?: AllowList;
  permissionGate?: PermissionGate;
  provider?: ModelProvider; // 依赖注入（测试用 mock；生产用 createProvider）
}

/** 把流式 chunk 累积成本轮 ECodeResponse.content（text + tool_call 完整 input）。 */
async function consumeStream(
  gen: AsyncIterable<ECodeStreamPart>,
  signal?: AbortSignal,
): Promise<{ text: string; toolCalls: Array<{ id: string; name: string; input: Record<string, unknown> }>; stopUnified: string }> {
  let text = '';
  const inputBuf = new Map<string, string>();      // id → 累积的 input JSON 字符串
  const names = new Map<string, string>();         // id → name
  const order: string[] = [];                      // 保持 tool_call 顺序
  let stopUnified = 'stop';
  for await (const part of gen) {
    if (signal?.aborted) throw new DOMException('Aborted', 'AbortError');
    switch (part.type) {
      case 'text_delta':
        text += part.text;
        break;
      case 'tool_call_start':
        names.set(part.id, part.name);
        inputBuf.set(part.id, '');
        order.push(part.id);
        break;
      case 'tool_call_delta':
        inputBuf.set(part.id, (inputBuf.get(part.id) ?? '') + part.inputDelta);
        break;
      case 'tool_call_end':
        break; // 累积已随 delta 完成
      case 'usage':
        break; // usage 暂不用于事件（成本追踪 M5）
      case 'stop':
        stopUnified = part.reason.unified;
        break;
    }
  }
  const toolCalls = order.map((id) => ({
    id,
    name: names.get(id) ?? 'unknown',
    input: safeParseToolInput(inputBuf.get(id) ?? '{}'),
  }));
  return { text, toolCalls, stopUnified };
}

function safeParseToolInput(s: string): Record<string, unknown> {
  try {
    return JSON.parse(s) as Record<string, unknown>;
  } catch {
    return { _parseError: 'tool_call input 非法 JSON', _raw: s };
  }
}

export async function* runAgentStream(
  task: string,
  opts: RunAgentStreamOptions = {},
): AsyncGenerator<AgentEvent> {
  const resolvedModel = opts.model ?? getDefaultModel();
  const provider = opts.provider ?? createProvider(resolvedModel);
  const useTools = hasCapability(resolvedModel, 'tools');
  const allow = opts.allow ?? new AllowList();
  const system = opts.system ?? buildSystemPrompt();

  let messages: ECodeMessage[] = opts.resumed
    ? [...opts.resumed.messages, { role: 'user', content: task }]
    : [{ role: 'user', content: task }];

  const tools = useTools ? toolDefinitions : [];
  // runtime-log（CLAUDE.md §1.6：日志保存到文件供排查）—— 与原 runAgent 一致
  const logFile = initRuntimeLog(task, resolvedModel, provider.baseURL);
  // session 落盘上下文（与原 runAgent 一致：首轮/每轮末/压缩后/结束落盘）
  const sessionId = opts.resumed?.id ?? timestampId();
  const sessionTask = opts.resumed?.task ?? task;
  const createdAt = opts.resumed?.createdAt ?? new Date().toISOString();
  const stats: ECodeSessionStats = { rounds: 0, compressed: false, toolCalls: 0 };
  const buildSession = (): ECodeSession => ({
    id: sessionId, task: sessionTask, model: resolvedModel, messages,
    createdAt, updatedAt: new Date().toISOString(), stats: { ...stats },
  });

  yield { type: 'start', task, model: resolvedModel, provider: provider.name, logFile };

  let lastSignature = '';
  let iteration: number;

  try {
    for (iteration = 0; iteration < MAX_ITERATIONS; iteration++) {
      // 上下文压缩（复用 maybeCompress，降级提示走事件）
      const compressResult = await maybeCompress(messages, {
        model: resolvedModel,
        system,
        summarize: async (prompt: string) => {
          const resp = await provider.complete({ model: resolvedModel, system: '你是对话历史压缩器。', messages: [{ role: 'user', content: prompt }], tools: [] });
          return resp.content.find((b) => b.type === 'text')?.text ?? '';
        },
      });
      if (compressResult.compressed) {
        messages = compressResult.messages;
        stats.compressed = true;
        yield { type: 'warning', message: '上下文已压缩' };
      }

      // 流式调用 LLM
      logApiRequest(iteration, messages, tools);
      const streamGen = provider.stream({ model: resolvedModel, system, messages, tools }, { signal: opts.signal });
      const { text, toolCalls } = await consumeStream(streamGen, opts.signal);
      stats.rounds = iteration + 1;

      // 构造本轮 assistant 完整回复 blocks（供日志 + push messages，无论有无 toolCalls 都先记录响应）
      const assistantBlocks: ECodeContentBlock[] = [];
      if (text) assistantBlocks.push({ type: 'text', text });
      for (const tc of toolCalls) {
        assistantBlocks.push({ type: 'tool_call', id: tc.id, name: tc.name, input: tc.input });
      }
      // 流式下 usage 可能不完整（OpenAI 需 stream_options，Anthropic 在 message_delta）；先记 0，保时序
      logApiResponse(
        iteration,
        assistantBlocks,
        { unified: toolCalls.length > 0 ? 'tool-use' : 'stop' },
        { inputTokens: 0, outputTokens: 0 },
      );

      if (toolCalls.length === 0) {
        if (text) yield { type: 'text_delta', text };
        // 最终落盘
        persistSession(buildSession());
        yield { type: 'completed', rounds: iteration + 1, toolCalls: stats.toolCalls, reason: 'done' };
        return;
      }

      // push assistant 本轮完整回复（text + tool_call blocks）
      messages.push({ role: 'assistant', content: assistantBlocks });

      // 执行工具（含权限拦截）
      for (const tc of toolCalls) {
        const def = toolDefinitions.find((t) => t.name === tc.name);
        const isDangerous = def?.dangerous ?? false;

        if (shouldAsk(tc.name, isDangerous, allow)) {
          yield { type: 'permission_request', toolUseId: tc.id, toolName: tc.name, input: tc.input };
          if (opts.permissionGate) {
            const decision = await opts.permissionGate.ask({ toolName: tc.name, input: tc.input });
            if (decision === 'deny') {
              const denyMsg = `用户拒绝执行工具 ${tc.name}`;
              yield { type: 'tool_result', id: tc.id, name: tc.name, content: denyMsg, isError: true };
              messages.push({ role: 'user', content: [{ type: 'tool_result', tool_use_id: tc.id, output: { type: 'error', value: denyMsg } }] });
              continue; // 跳过 executeTool，但 tool_result 已配对
            }
            allow.add(tc.name); // allow → 会话记住
          }
          // 无 gate = 默认放行（兼容无 UI / 测试）
        }

        yield { type: 'tool_call_start', id: tc.id, name: tc.name };

        let result: { content: string; isError: boolean };
        try {
          result = await executeTool(tc.name, tc.input);
        } catch (err) {
          result = { content: `工具执行异常: ${err instanceof Error ? err.message : String(err)}`, isError: true };
        }
        logToolExecution(iteration, tc.name, tc.input, result.content, result.isError);
        yield { type: 'tool_result', id: tc.id, name: tc.name, content: result.content, isError: result.isError };

        const output: ECodeToolResultOutput = result.isError
          ? { type: 'error', value: result.content }
          : { type: 'text', value: result.content };
        messages.push({ role: 'user', content: [{ type: 'tool_result', tool_use_id: tc.id, output }] });
      }

      // 重复动作检测
      const signature = toolCalls.map((t) => `${t.name}:${JSON.stringify(t.input)}`).sort().join(' || ');
      if (signature === lastSignature) {
        yield { type: 'warning', message: '检测到连续重复的工具调用，终止以防死循环' };
        persistSession(buildSession());
        yield { type: 'completed', rounds: iteration + 1, toolCalls: stats.toolCalls, reason: 'repeated' };
        return;
      }
      lastSignature = signature;

      stats.toolCalls += toolCalls.length;
      persistSession(buildSession());
    }

    // 达上限
    persistSession(buildSession());
    yield { type: 'completed', rounds: iteration, toolCalls: stats.toolCalls, reason: 'max-iterations' };
  } catch (err) {
    // 中断 / 其它异常：为所有本轮已 start 但未配对的 tool_use 补发 error tool_result（保配对不断裂）
    // —— 实际上本实现里 tool_result 在工具执行后立即 push，中断多发生在 stream 迭代中（tool_use 尚未确认），
    //    故此处主要兜底：记录错误事件 + 落盘，不破坏既有配对。
    const aborted = err instanceof DOMException && err.name === 'AbortError';
    persistSession(buildSession());
    if (aborted) {
      yield { type: 'completed', rounds: stats.rounds, toolCalls: stats.toolCalls, reason: 'aborted' };
      return;
    }
    yield { type: 'error', error: err instanceof Error ? err.message : String(err) };
  } finally {
    finalizeRuntimeLog(stats.rounds);
  }
}
```

> **关于中断配对的精确说明**：阶段①中断最可能发生在 `provider.stream()` 迭代中（此时本轮 tool_use 尚未完全收到、未 push assistant 消息）→ messages 里没有 dangling tool_use → 无需补 tool_result。真正需要补配对的场景（工具执行中途被中断）留 M4 配合更完整的 abort 语义细化；本 task 先保证「中断不破坏已落盘的配对历史」。

- [ ] **Step 4：跑测试确认通过**

Run: `npx vitest run tests/agent-stream.test.ts`
Expected: PASS。

- [ ] **Step 5：补关键测试（权限拦截 + 工具调用序列）**

在 `tests/agent-stream.test.ts` 追加：
```ts
  it('工具调用 → tool_call_start / tool_result 事件序列正确', async () => {
    const provider = mockProvider([
      { type: 'tool_call_start', id: 't1', name: 'read_file' },
      { type: 'tool_call_delta', id: 't1', inputDelta: '{"path":"package.json"}' },
      { type: 'tool_call_end', id: 't1' },
      { type: 'stop', reason: { unified: 'tool-use', raw: 'tool_calls' } },
    ]);
    // 简化：只测第一轮事件，不构造完整多轮（多轮需 provider 按 messages 返回不同流，复杂度高）
    const events = await collect(runAgentStream('读 package.json', { provider }));
    const types = events.map((e) => e.type);
    expect(types).toContain('tool_call_start');
    expect(types).toContain('tool_result');
    const tr = events.find((e) => e.type === 'tool_result') as Extract<AgentEvent, { type: 'tool_result' }>;
    expect(tr.id).toBe('t1');
    expect(tr.name).toBe('read_file');
  });
```

> 用 `package.json`（确保存在）避免依赖真实 README；read_file 读到即 isError=false，事件序列成立。

> 注：read_file 读真实 README.md（若不存在 isError=true，但事件序列仍成立）。若想避免依赖真实文件，可 mock executeTool，但本 task 不强求 —— 用一个确保存在的路径（如 `package.json`）。

- [ ] **Step 6：跑全部测试 + tsc clean + commit**

Run: `npx vitest run` && `npm run build`
Expected: 全绿 / 0 error。

```bash
git add src/agent.ts tests/agent-stream.test.ts
git commit -m "feat(m3.5): runAgentStream AsyncGenerator 核心（事件化+权限拦截+中断）"
```

---

## Task 10：runAgent 改 wrapper + index.ts 接线

**Files:**
- Modify: `src/agent.ts`（旧 `runAgent` 改为消费 `runAgentStream` 打印，保持 `Promise<void>` 签名向后兼容）
- Modify: `src/index.ts`（构造 AbortController + 读 CLAUDE.md 注入）

**Interfaces:**
- Consumes: Task 9 `runAgentStream` + Task 2 `loadInstructions`
- Produces: 旧 CLI 行为不变（console 打印），但底层走事件流；Ctrl+C 可中断。

**设计说明：**
- `runAgent` 改为 thin wrapper：迭代 `runAgentStream`，按 event type 打印（复刻原 console 输出：text → stdout，tool → ⚡/✅/❌，start/completed 打印摘要）。
- `index.ts`：构造 `AbortController`，监听 `SIGINT`（Ctrl+C）调 `controller.abort()`；读 CLAUDE.md（`loadInstructions([homeDir, projectRoot, configDir, cwd])`）传给 runAgentStream 的 `system`。
- home/cwd 路径解析遵循 CLAUDE.md §9.3：不散用 `os.homedir()`。M3.5 阶段①先用最小版（`process.cwd()` + `os.homedir()` 直接传，但**集中在这一个调用点**，后续若上 WSL 再换 `resolveDataDir`）。**本 task 范围不引入 resolveDataDir 重构**（YAGNI + 不重构周边）。

- [ ] **Step 1：改 runAgent 为 wrapper（先保证现有 CLI 测试不回归）**

`src/agent.ts`，把原 `runAgent` 函数体替换为：
```ts
export async function runAgent(
  task: string,
  model?: string,
  opts?: RunAgentOptions,
): Promise<void> {
  // 阶段①：runAgent 退化为 runAgentStream 的 console 打印消费方（向后兼容旧 CLI）。
  // UI 化（阶段② Ink REPL）将直接订阅 runAgentStream，不走此 wrapper。
  for await (const event of runAgentStream(task, { ...(opts ?? {}), model })) {
    switch (event.type) {
      case 'start':
        console.log(`\n🤖 ECode (model: ${event.model}, provider: ${event.provider})`);
        console.log(`📝 任务: ${event.task}`);
        if (event.logFile) console.log(`📋 日志: ${event.logFile}\n`);
        break;
      case 'text_delta':
        process.stdout.write(event.text);
        break;
      case 'tool_call_start':
        console.log(`\n\n⚡ [${event.name}]`);
        break;
      case 'tool_result':
        if (event.isError) {
          console.log(`❌ 错误: ${event.content.slice(0, 300)}`);
        } else {
          console.log(`✅ 完成 (${event.content.length} 字符)`);
        }
        break;
      case 'warning':
        console.log(`\n⚠️  ${event.message}`);
        break;
      case 'completed':
        if (event.reason === 'max-iterations') {
          console.log(`\n⚠️  达到最大迭代次数 (${MAX_ITERATIONS})，自动终止`);
        }
        break;
      case 'permission_request':
        // 旧 CLI 无 UI：默认不拦截（runAgentStream 无 gate 时自动放行），此事件仅日志
        console.log(`\n🔒 权限请求: ${event.toolName}（旧 CLI 自动放行，REPL 模式将弹窗）`);
        break;
      case 'error':
        console.error(`\n💥 ${event.error}`);
        break;
    }
  }
}
```

> 保留原 `runAgent` 顶部所有 helper（`timestampId` / `persistSession` / `MAX_ITERATIONS` 等）—— `runAgentStream` 也要用它们（同文件内，无需 re-export）。原 `runAgent` 函数体内的 `summarize` / session / log 逻辑已搬进 `runAgentStream`，删除旧函数体即可（注意 `noUnusedLocals`：搬走后若有 helper 变未用，删除或确认仍被引用）。

- [ ] **Step 2：跑全部测试确认无回归**

Run: `npx vitest run`
Expected: 全绿（runAgentStream 测试 + 原 session/context 等测试）。若原 `tests/` 有直接测 `runAgent` 的集成测试，确认打印行为一致。

- [ ] **Step 3：index.ts 接入 AbortController + CLAUDE.md**

`src/index.ts` 顶部 import 追加：
```ts
import { loadInstructions } from './instructions-loader.js';
import { homedir } from 'node:os';
import { resolve } from 'node:path';
```

在 `runAgent(task, values.model)` 两处调用（continue/resume 分支 + 新会话分支）前，构造 signal + system：
```ts
// Ctrl+C 中断：首次 abort，触发 runAgentStream 的 aborted 完成
const controller = new AbortController();
process.on('SIGINT', () => {
  if (!controller.signal.aborted) controller.abort();
});

// CLAUDE.md 4 层注入（home / project root / .ecode / cwd）
const projectRoot = process.cwd();
const instructions = loadInstructions([
  homedir(),
  projectRoot,
  resolve(projectRoot, '.ecode'),
  projectRoot, // cwd 与 root 同；保留以对齐 4 层模型
]);
const system = buildSystemPrompt(instructions);
```

把新会话分支的 `runAgent(task, values.model)` 改为：
```ts
runAgent(task, values.model).catch(...) // 保持不变 —— runAgent 内部调 runAgentStream，
                                        // 但 signal/system 需透传 → 见下「Step 3b」
```

**Step 3b（透传 signal + system）**：因 `runAgent` 签名是 `(task, model?, opts?: RunAgentOptions)`，而 `RunAgentOptions` 不含 signal/system —— 改 `runAgent` 第三个参数类型为 `RunAgentStreamOptions`（它 extends RunAgentOptions，已有 signal/system 字段），调用处传：
```ts
runAgent(task, values.model, { signal: controller.signal, system }).catch(...)
```
continue/resume 分支同理（把 `{ resumed: {...} }` 合并进 options：`{ resumed: {...}, signal: controller.signal, system }`）。

并把 `buildSystemPrompt` 加入 index.ts import：
```ts
import { runAgent } from './agent.js';
import { buildSystemPrompt } from './system-prompt.js';
```

- [ ] **Step 4：tsc clean**

Run: `npm run build`
Expected: 0 error（重点查 `noUnusedLocals` —— homedir/resolve 是否都用上）。

- [ ] **Step 5：手动冒烟（可选但推荐）**

Run: `npm run dev -- "用一句话介绍你自己"`
Expected: 控制台逐字流式输出（若模型支持流式）+ 结束摘要。Ctrl+C 中断应优雅退出（不抛未捕获异常）。

> 若默认模型（deepseek）端点流式未开，可能仍一次性返回 —— 正常，事件流会把整段 text 作为一个或多个 text_delta 发出。

- [ ] **Step 6：跑全部测试 + commit**

Run: `npx vitest run`
Expected: 全绿。

```bash
git add src/agent.ts src/index.ts
git commit -m "feat(m3.5): runAgent 事件化 wrapper + CLI 接入 AbortController/CLAUDE.md"
```

---

## 验收标准（阶段①完成定义）

跑完 10 个 task 后，以下全部满足才算阶段①交付：

1. **事件化**：`runAgentStream` 是 `AsyncGenerator<AgentEvent>`，消费方可订阅 start/text_delta/tool_call/tool_result/permission_request/warning/completed/error 全部事件。
2. **流式**：OpenAI + Claude 两个 Provider 的 `stream()` 真实实现（不再抛 not implemented），chunk 正确翻译为 `ECodeStreamPart`。
3. **CLAUDE.md**：项目根放 `CLAUDE.md` 或 `ECODE.md`，`loadInstructions` 读取并经 `buildSystemPrompt` 注入 system（ECODE.md 优先）。
4. **权限**：`bash` 标 dangerous，`shouldAsk` 判定正确；注入 `permissionGate` 时 deny → 回喂 is_error tool_result（id 配对）。
5. **斜杠**：`/help` `/clear` `/model` `/exit` 可解析（执行留阶段②）。
6. **中断**：AbortController 传到 `provider.stream`，中断后已落盘的 tool_use/tool_result 配对不破坏。
7. **不回归**：`npm run build` 0 error；`npm test` 全绿（含原 151+ 单测 + 本计划新增）。
8. **向后兼容**：旧 `runAgent(task, model?)` CLI 调用行为不变（打印格式一致）。

---

## 不在本计划范围（YAGNI / 留后续）

- **Ink REPL**（阶段②）：App 组件、输入框、Markdown 渲染、工具面板、PermissionDialog —— 独立计划。
- **权限持久化**（`.ecode/settings.local.json`）：M4。
- **四档模式**（default/acceptEdits/plan/bypass）、deny/ask 规则、Bash 命令解析：M4。
- **并行工具执行**：M5。
- **resolveDataDir 统一入口**（WSL 混合环境）：本阶段先在 index.ts 单点用 `os.homedir()`，上 WSL 时再引入（不预先重构，§1.7）。
- **多轮流式 mock 测试**（provider 按 messages 返回不同流）：Task 9 只测单轮事件序列，多轮交互测试成本高，留集成测试阶段。
