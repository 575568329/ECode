# M1~M3.5 欠账核查与 M4 前置清单

> **目的**:核查 M1 / M2 / M3 / M3.5 四个里程碑【规划了但没实现】的欠账 + 进 M4 的前置必做,为后续"补实现"提供蓝本。
>
> **范围**:M1~M3.5 欠账 + M4 前置;**不含** M5+ 未开垦(子代理 / MCP / Hooks / Skills——属未来新功能,非"之前没实现")。
>
> **方法**:逐项读源码核实(三 agent 并行核查,实证),非文档转述。
>
> **核查时间**:2026-08-07。文中行号为此时快照,可能随代码漂移——**以符号名为准**(见 [debugging #010](../memory/debugging.md))。
>
> **三色图例**:🔴 真缺口(代码未写)｜⚠️ 代码已写待真机验证｜🟡 已撤销·defer·待决策｜🟢 文档滞后(标待补但已实现)

---

## 一、汇总统计

| 类别 | 项数 | 说明 |
|------|------|------|
| 🔴 真缺口 | 16 项（去重后） | 分 A 阻塞M4 / B M4本身 / C M3 P5 / D M3.5 / E M1 五组 |
| ⚠️ 待真机 | 5 项 | 代码写了没跑通,需用户真机验证 |
| 🟡 待决策·defer | 2 项 | RepoMap 决策、LLM VCR(M5+) |
| 🟢 文档滞后已做 | 多项 | M1/M2 主线验收全达成,部分文档仍标"待补" |

**进 M4 最该先做**:🔴-A 工具集缺口(write_file + delete/move/ls 专用)——M4 权限要给每个工具配策略,工具不全则权限配不全。

---

## 二、🔴 真缺口

### 🔴-A 阻塞 M4 的硬前置

| # | 缺口 | 出处 | 代码实际(实证) | 说明 |
|---|------|------|---------------|------|
| A1 | **write_file** 工具 | 支点3 P0 | `registry.ts` 仅 5 工具(read_file/bash/edit_file/grep/glob),无 write_file | M4 要给它配"该问"策略 |
| A2 | **delete_file** 专用工具 | 支点3(跨平台倒逼) | 无,现走 bash 代办 | M4 计划:专用工具免审批,绕开 bash 分级 |
| A3 | **move** 专用工具 | 同上 | 无,走 bash | 同 A2 |
| A4 | **ls** 专用工具 | 同上 | 无,`read-search-group.ts` 仅把 ls 当 bash 命令识别 | 同 A2 |

> TodoWrite / fetch 也缺(支点3),但权限决策轻(TodoWrite 不问 / fetch 待定),**不阻塞 M4 主体**,可后补。

### 🔴-B M4 本身必做(非前置,登记)

| # | 缺口 | 出处 | 代码实际 | 说明 |
|---|------|------|---------|------|
| B1 | **🔴-2** allow vs allow_always 语义塌陷 | P0 表 | `permission.ts` `AllowList.add` 只收 toolName 无 scope;`agent.ts` 无条件 add | M4 核心交付:扩 `add(toolName, scope)` + ask 返回 allow_once/allow_always/deny |

### 🔴-C M3 P5 收尾(M3 验收第十条「P5 借鉴点落地」未达成)

| # | 缺口 | 出处 | 代码实际 | 说明 |
|---|------|------|---------|------|
| C1 | 并行只读工具(Promise.all) | P5#13 / P1-4 | `tools/types.ts` `parallelizable` 字段留位但 5 工具无一标注;`agent.ts` 顺序 for 执行 | 类型契约留位,语义未实现 |
| C2 | `errors.ts` 结构化错误 | P5#14 | `src/errors.ts` 不存在;`withRetry` 抛原 SDK 错误原样传播 | 借鉴点④未落地 |
| C3 | retry 读 Retry-After | P5#14 | `retry.ts` 仅看 HTTP status,不读 `Retry-After` header | 429 服务端等待提示被丢 |
| C4 | usage 五项归一化(cacheRead/cacheWrite/reasoning) | P5#15 / 支点17 | `agent-events.ts`/`transform.ts` 仅两字段(input/output);五项模型未建 | 连带 🔴-3:`claude.ts` 流式 inputTokens 写死 0 无法修 |

### 🔴-D M3.5 收尾

| # | 缺口 | 出处 | 代码实际 | 说明 |
|---|------|------|---------|------|
| D1 | **/model** 命令 | 支点14 | `slash-commands.ts` 已注册,`app.tsx` case 合并 fall-through 显示"尚未实现" | 照搬 SessionPicker 二级菜单,~30 行 |
| D2 | **/compact** 命令 | 支点4/14 | 同上未实现;`use-agent-stream` 无 `compact()` 方法 | `forceCompact` 已就绪,接线即可 |
| D3 | 上下文阈值进 config | 支点4/15 | `context-manager.ts` `COMPRESS_THRESHOLD_RATIO`/`keepRounds`/`keepRecent` 全硬编码 | 与 ECodeConfig 收口一并做 |
| D4 | 多行输入 | 支点16 | `input-bar.tsx` Enter 即提交,单行语义 | 需多行光标状态机,中等改造 |
| D5 | 用户插话(drainQueued) | 支点16 | `app.tsx` 运行时 InputBar disabled;无 queuedInput/drain 机制 | CC mid-turn drain 未实现,功能架构已给 6 文件清单(~80 行) |

### 🔴-E M1 欠账

| # | 缺口 | 出处 | 代码实际 | 说明 |
|---|------|------|---------|------|
| E1 | **P0-5 强制后置验证** | 认知盲区 P0-5 / M1-补全⑥ | 仅 `system-prompt.ts` 软约束("改完要验证"),无硬性 hook | 规划三层只做第一层;建议 onPreStop hook 触发 build/test(可配超时/跳过) |

---

## 三、⚠️ 待真机验证(代码已写,未在真机跑通)

| # | 项 | 代码状态 | 说明 |
|---|----|---------|------|
| W1 | #40 真机冒烟(D 按对话分组 / C 修双❯ / G 提示蜂鸣) | `format-transcript.ts` / `app.tsx` 代码完整 | 逻辑自洽,待真机 |
| W2 | Ctrl+O pager 真机 | `app.tsx` openPager + `pager.ts` 完整 | 待真机 |
| W3 | A 重复文本(streamingText 跨轮累加) | reducer 静态分析无累加路径(completed 清零) | 待 reducer 日志实证 |
| W4 | B 乱码(Windows find.exe + GBK) | `bash.ts` / `grep.ts` encoding 写死 utf-8 | **代码确有缺陷**,Windows 真机复现后需修 |
| W5 | 🔴-3 Ctx% 累计 | `reduce-agent-event.ts` per-call 覆写已修 | 代码层已修,待真机验证 |

> 这 5 项**不算 🔴 缺口**(代码已写),但需用户真机跑一遍确认。其中 W4(B 乱码)代码层确有 encoding 缺陷,真机复现后需修(可升级为 🔴)。

---

## 四、🟡 待决策 / defer(备案,非缺口)

| # | 项 | 现状 | 建议 |
|---|----|------|------|
| Y1 | Repo Map(P1-2) | `src/` 零实现,24 支点未单列 | 决策:YAGNI 砍掉(grep+glob 够用)或 M5+ 立项做 v1 简化版 |
| Y2 | LLM VCR record/replay(P1-7) | vi.mock + provider 注入底座已做 | M5+ 可测试性,YAGNI 暂缓合理 |

---

## 五、🟢 文档滞后已做(可顺手更新文档)

M1/M2 主线验收全达成(19 项),部分文档仍标"待补",核查时可顺手更新:

- M1:tool_use 协议 / id 配对 / messages 累加 ✅
- P0-1 System Prompt 分层 ✅｜P0-2 edit 匹配失败恢复 ✅｜P0-3 截断(阈值对得上)✅｜P0-4 错误恢复四层兜底 ✅
- P1-1 CLAUDE.md 注入 ✅｜P1-3 中断 AbortController ✅｜P1-5 成本追踪(API usage)✅｜P1-6 可观测性(三层日志)✅
- M2:transform 双向翻译 / Claude·OpenAI Provider / 配置系统 / 能力查询 / `--model` 全 ✅

---

## 六、结论与建议补的顺序

**进 M4 还差**:🔴-A 工具集(4 个专用工具)是**唯一硬前置**;其余要么 M4 本身(🔴-B)、要么收尾(🔴-C/D/E)不阻塞 M4。

**建议补的顺序**(待拍):

1. **工具集**(🔴-A:write_file + delete/move/ls 专用)——M4 硬前置,TDD,跨平台(Node `fs`)
2. **M4 本身**(支点7 三档模式 + 规则引擎 + 🔴-B 🔴-2)——当前里程碑
3. **M3.5 收尾**(🔴-D:/model·/compact·阈值·多行·插话)——可与 M4 并行,体验项
4. **M3 P5**(🔴-C:并行·errors·Retry-After·usage 五项)——质量提升,usage 连带修 🔴-3
5. **P0-5 后置验证**(🔴-E)——质量门
6. **RepoMap 决策**(🟡-Y1)——砍或立项

> ⚠️ 待真机项(W1-W5)穿插由用户真机验证,不阻塞开发。
