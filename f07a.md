# F-07 档A 任务书：edit/write 会话级 remember 第三键（用户已拍板档A）

**目标**：审批疲劳根治第一步——仅 `edit_file`/`write_file` 的审批卡给「本会话记住此工具」第三键；会话内后续同工具免审批；**敏感路径（isSensitivePath/isProjectEcodeSettings 命中）永不给第三键**（M14-D6 sensitive 永远交互）。

## 实施要点

1. **基建已有**：ConfirmPrompt 三键基建（rememberLabel 通用化，M9-P5）+ MCP 卡的 a=「本会话记住」先例（server 级放行链路）。接入点：普通 tool-confirm 卡对这两个工具给第三键（键位 a；canAlways 语义已有）。
2. **放行语义=会话级工具白名单**：approve 时带 always → broker/宿主记「本会话 edit_file/write_file 放行」集合，后续 hostConfirm 对命中工具直放（参考 MCP a 键的 server 级放行实现，降为会话级）。**注意与 accept-edits 档正交**：档位是用户显式切的，remember 是审批时顺手按的——两者并存时 remember 集合仍要过 sensitive 硬门（P0-1 同款判定）。
3. **敏感不给键**：buildPreview/卡构造处，isSensitivePath 或 isProjectEcodeSettings 命中的卡不渲染第三键（MCP 卡同理不动）。
4. **bash 不给键**（档A 范围外，档B 另批）。

## 测试

- 档A：edit_file 卡有第三键 → 按 a → 本会话后续 edit_file 零审批 + write_file 也放（同集合）或仅同工具（你选，汇报说明）；新会话不残留。
- 敏感路径卡无第三键（.env/.ecode/settings*）。
- bash 卡无第三键。
- 协议面：web/飞书端 approval/respond decision='always' 对这两工具生效（复用既有 always 枚举——检查 web 端是否已透出第三键 UI，没有则记档不扩面）。

## 验收

全量+tsc+`node scripts/pty-confirm-keyboard-probe.cjs`（动卡面必跑）。汇报：改动文件+测试+放行集合的存储位置与语义。
