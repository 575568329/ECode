# Dogfood 实测修复方案（v1.9 全批次收官+清账批 I/II）——安全根修 + 探针真裁决 + 交互批

---
layer: 详设
status: done
---

- 日期：2026-08-28
- 状态：**批2a~批2b-fix+功能测试批全部完成**（批2a §12.4 / 批2c be3ccd7 / 批2d 15b9957+9c87945 §13.1 / serve §13.2 / 批2b 9c12ab1 §10.5 / 批2b-fix 7b88970 §13.8a / 功能测试批 §13.4a 六项全通+F-24/F-25/F-28 修复+F-23/F-26 入档）；界面批 §13.5（7eb13c5）+清账批 I §13.9（50898f2）+清账批 II §13.10（eb95a18）——**全部批次收官**
- 输入：2026-08-28 三轮 dogfood 实测 + Claude Code 对比实验（docs/解析/2026-08-28_CC与ECode对话界面对比_Claude产出.md——批3 界面批输入）+ 四角色审阅
- 关联：`tui-streaming-turn-wedge` 挂账（**§12 终裁：核心症状未复发，待 S2/S3 探针修复后正式销案**）；M14 V+C 线；审阅批4（694b9c8）

---

## 0. 一句话定位

dogfood 实测抓到的 6 个问题里，**最重要的不是修代码，是先复测**：实测跑的 dist 比 9ad1a83（轮末 UI 状态修复）早 1.5 小时，P0 症状大概率已被 9ad1a83+V4 治愈——本方案批1 主体是「build 新 dist + 探针升级（功能键探针）+ 复测裁决」，探针是本批最关键的新基建；批2 才是确定的代码修（CLI 参数校验/diff 文件名转义/审批疲劳）。

## 1. 版本基线重大发现（方案的地基）

| 事实 | 证据 |
|------|------|
| dogfood 实测跑的是 **8-26 09:54 的 dist** | `dist/tui/TuiApp.js` mtime |
| 9ad1a83（修「placeholder 恒『处理中，Ctrl+C 中断』」）在 **8-26 11:15** | `git show 9ad1a83`：根因① busy 判据 `streamingText!==''` 轮末延迟 commit 下永不清空 → 改 thread/status 驱动 running 镜像 |
| 探针「3 连跑全过」跑的是**源码**（npx tsx），已含 9ad1a83 | wedge 探针 spawn 方式 |
| 8-27~28 又叠了 V4 轮末即 commit（9d95fd7）等 TUI 打磨 | git log |

**推论**：
- F-02（占位符未复位）：**最新源码已修**（9ad1a83 正是修它的）——dogfood 复现的是修复前症状，销案证据 +1。
- F-08（半活楔死：Enter 首按丢/Ctrl+C 失灵/退出无门）：**待新版复测裁决**。与挂账「疑似 9ad1a83 顺带治愈（机理未定位，待真机确认）」正好互为印证——dogfood 旧版复现 ≈ 反向支持治愈假说，但**新版不复发**必须由升级后的探针实证。
- 教训入方法论：**dogfood/真机验收前必须核对 dist 构建时间 vs 源码 HEAD**（本批验收标准第 0 条）。

## 2. 问题清单定稿

| # | 级别 | 状态（v1.4 回填） | 问题 | 证据/锚点 |
|---|------|------|------|-----------|
| F-02 | ~~P0~~ | **已修销案**（9ad1a83；wedge 哨兵 4 连轮实证） | 轮末 placeholder 恒「处理中」 | 9ad1a83（§1） |
| F-08 | P0 | **已销案**（§12.4：矩阵 24 项全过 S1-S3 全绿，7e01439；旧 dist 现象=9ad1a83 前版本） | 半活楔死：字符输入活/Enter 走死队列/Ctrl+C 无效/退出无门（/stats 后彻底卡死，只能杀进程） | dogfood 8-26 dist 实证；新代码 S1-S3 全绿 |
| F-01 | P1 | **已修**（批1 4575e92，端到端+10 用例） | CLI 只认 `serve` 子命令，`--version` 等静默进 argv 单次模式当 prompt 烧钱 | cli/index.ts:110 `/stats` 记账 ¥0.029 实证 |
| F-07 | P1 | 批2b 已做止痛点①（审批期可打字排队插话），三键 remember 接入仍待拍板（§4 档A/档B） | 连续 13+ 写操作逐一弹审批；bash `readonly:false`（bash.ts:61 设计如此）使 grep 也逐一审批 | dogfood 按 13 次 y；ConfirmPrompt.tsx:68 已有 remember 三键基建，仅未接入 |
| F-05 | P2→P1 | **已修**（批1 4575e92，用户视角升 P1：天书文件名=盲批） | edit_file 审批 diff 中文文件名八进制转义（jsdiff createTwoFilesPatch header） | edit_file.ts:104 |
| F-06 | P2 | 未修（不急） | 工具输出展开无轮次/时间标记，历史快照易误读为当前状态 | dogfood 观察者被误导 1 次 |
| F-20 | P1 | **已修**（批2d，§13.1 实施记录+探针六项） | 审批挂起本地完全沉默：无 hook 事件/无响铃/无桌面通知——用户切窗后互相等死，15min 超时自动拒任务全灭 | 批2d 前六事件无 Notification；probe 六项全过 |
| F-21 | P1 | **已修**（批2b 9c12ab1：loop exhausted onWarn，+2 用例） | **迭代上限截断零提示**：50 轮耗尽后 TUI 直接回空闲输入框（轮 N/M 归零），无 systemMsg/告警，用户以为完成实际半途而废——批2d 实撞（117.6k tok 被拦腰，三交付全欠靠监看方续接补齐）；不监看=拿到假完成 | 批2d logstore 12:23:21 UTC iter 50/50 后无 stop 事件（loop.ts:136 循环耗尽静默 return，对照 length 截断有 onWarn） |
| F-22 | P2 | **已修**（批2b 9c12ab1：bash foldNodeWarnings，+4 用例） | **子进程 Node 警告直打 TUI 屏幕**：会话内跑 vitest 时 `MaxListenersExceededWarning: 11 resize listeners` 原样出现在对话界面（用户视角=陌生英文堆栈，不知是否要紧） | 批2d 实录快照留证；机理=测试套件多 Ink 实例累积 listener+vitest 子进程 stderr 直通回显 |
| F-04 | — | 并入 F-07 | bash 只读命令走审批：设计如此（M9「confirm/allow 归 loop confirm 层」） | bash.ts:61 |

方法论坑（不进 ECode 账，随本批记录）：Bash 工具 tail 直出大块中文偶发乱码（改 node 读文件）；Ink spinner 动画打穿「输出静默 1.5s」判定（driver 需节拍快照）；dogfood 驱动基建在 `%TEMP%\ecode-dogfood\`（driver.cjs/inbox 协议/view-tail.cjs，可复用）。

## 3. 批1：复测裁决 + 探针升级 + 两小修（建议 ≈1 人天）

### 3.1 探针升级（本批核心交付，先行）

现状缺口：`scripts/pty-wedge-probe.cjs` 的调度活性判定=**stdin 回显**，半活态下回显活但功能键死——**探针全过 ≠ 没楔死**。

新增 `scripts/pty-semialive-probe.cjs`（或并入现有探针作第二判定组）：

1. **Enter 提交探针（不烧 token 设计）**：轮末写 `/stats` + `\r`，判定任一预期帧出现：
   - 统计输出出现（Enter 生效、命令执行）；
   - 或忙碌拦截提示出现（InputStream.tsx:152 `onSlashBusy` 分支——Enter 生效、被分流）。
   - 两者皆无且超时 → **半活实锤**（Enter 未被处理）。
2. **Ctrl+C 语义探针**：空闲态双击 Ctrl+C（9ad1a83 语义：优雅退出），判定进程在 N 秒内退出；不退 → 键消费死。
3. **场景矩阵**（每场景后跑 1+2）：
   - S1 纯文本轮 ×3（对齐现有 wedge 探针）；
   - S2 **本地命令轮**：发 `/stats` → 等输出 → 探针（dogfood 卡死场景首次进探针覆盖）；
   - S3 工具轮：mock SSE 带 tool_use/tool_result 往返（注意挂账坑「loop 要求工具调用但未给出工具」——mock 必须带 tools 定义）。
4. 判定输出：每场景 `OK/FAIL` 一行 + FAIL 时末 20 帧 + CPU 差分留痕（沿用现有探针取证格式）。

### 3.2 复测裁决流程

1. `npm run build`（tsc 产出新 dist；**不重启任何运行中进程**，Windows 下已加载的 js 可安全覆盖）；
2. 外部驱动侧跑升级探针（新 dist）：S1-S3 全过 → **F-08 销案**（销案记录回填挂账 memory），探针入库防回归；
3. 任一场景 FAIL → F-08 转入实修，按机理定位修：
   - 机理 A（running 卡 true）：TuiApp 终局事件兜底复位（`turn/completed`/`error` 均解除 busy，不单依赖 thread/status）+ `session.ts` 审查 busy:true 发送点（:620/:682）与 finishTurn（:790）严格配对（dev 断言）；
   - 机理 B（面板吃键）：复核 /stats 输出路径是否 setOverlay/面板态（commands/registry action 枚举现无面板项，倾向排除，以复测为准）；
   - 修复后同矩阵复测全过才关单。

### 3.3 F-01 CLI 参数校验（确定性修）

`src/cli/index.ts` 入口（:110 前插解析层）：
- `-v`/`--version` → 输出 `package.json` version，exit 0（不进 LLM）；
- `-h`/`--help` → usage（REPL/单次/serve 三形态一行式），exit 0；
- 其余 `-` 开头 token → stderr `未知参数: xxx` + usage，exit 1；
- `serve` 子命令族不变；非 `-` 开头参数继续走 argv 单次模式（保留脚本/管道用法）。
- 测试：三 flag 路径 + 未知 flag 拒绝 + 无参进 REPL（现有行为回归）。

### 3.4 F-05 diff 文件名转义（确定性修）

`src/tools/builtin/edit_file.ts:104`：`createTwoFilesPatch` 对非 ASCII 文件名生成 `"\350\257\246..."` 八进制转义 header。修：patch 生成后对 `---`/`+++` 两行 header 做八进制序列解码还原（执行时先读 jsdiff 转义实现确认编码形态，宁可 unit test 锁行为）；`write_file.ts` 同款检查。测试：中文路径文件 diff 输出含可读文件名。

### 3.5 提示词一句（免费）

系统提示词或 bash 工具描述补：「文件重命名/移动后，引用检查范围为整个项目根，不限于当前工作子树」（治 dogfood 指导点：AGENTS.md 根目录盲区）。

## 4. 批2：F-07 审批疲劳（待拍板后 ≈1 人天）

基建已备（ConfirmPrompt 三键 + `resolve(ok, always)` + `rememberLabel` 通用化），只差接入。**两档粒度待拍板**：

- 档A（保守，推荐先做）：仅 `edit_file`/`write_file` 给「本会话记住此工具」第三键；`sensitive.ts` 命中（M14-D6「sensitive 永远交互」）不显示第三键。
- 档B（激进，观察档A 后再开）：bash 按**命令首词**会话级放行（grep/ls/mv…），敏感首词（rm/del/format/sudo…）与 blockedCommands 永不给第三键。

## 5. 分工与流程（本批即 dogfood 第二轮）

- **ECode 执行**：按本方案批1 全部编码（3.3/3.4 TDD 先测后修；3.1 探针脚本；3.2 的 build）。
- **外部驱动执行**：跑升级探针、复测裁决、dogfood 观察记录（避免 ECode 在自己会话内嵌套跑 pty 探针的复杂性）。
- **铁律（用户拍板）**：全程禁 `npm run dev`（嵌套交互 TUI 会打断 dogfood 对话）；验证 = vitest 单测 + `npx tsc` 类型门 + build + 外部探针；不重启 dogfood 会话进程。
- 提交节奏：批1 一笔（feat/fix 混合按主体归类），探针脚本随批入库。

## 6. 验收标准

1. **第 0 条**：动手前记录 dist mtime 与 git HEAD，build 后复核 dist 已更新（方法论教训固化）；
2. 探针矩阵 S1-S3 在新 dist 上全过（或 FAIL 后修复至全过），结果留痕；
3. `ecode --version/-h`/未知 flag 三路径行为正确且零 LLM 调用（/stats 记账无新增）；
4. 中文路径 edit_file 审批 diff 文件名可读；
5. 全量测试门：`npx vitest run` 零红 + `npx tsc` 净（当前基线 1355+3skip）。

## 7. 待用户拍板（v1.0 原文）

- F-07 粒度：档A 先行 / A+B 一起（§4）；
- F-06（输出展开加轮次/时间标记）批2 顺手做 or 挂账；
- 半活楔死若复测治愈：楔死挂账 memory 销案回填由本方案验收记录承载。

---

# v1.1 追加（2026-08-28 下午）：批1 实录 + 复测裁决 + 第二轮发现与批2 定稿

## 8. 批1 实施记录（2026-08-28 15:15 完成）

### 8.1 交付明细

| 提交 | 内容 | 执行者 |
|------|------|--------|
| 19d94a8 | 本方案 v1.0 | 外部（ZCode） |
| 4575e92 | 批1 主体：`src/cli/args.ts`（F-01，10 用例 TDD）/`src/tools/builtin/patchHeader.ts`（F-05，4 用例；考据出 jsdiff 双缺陷：`\NNN` 八进制 + 新版 timestamp `[object Object]`）/`scripts/pty-semialive-probe.cjs`（§3.1）/`src/core/system.ts` 提示词句（§3.5）/`npm run build` 新 dist（8-28 15:15，旧 dist 8-26 09:54——§1 版本基线发现坐实） | **ECode**（dogfood 轮2，全程未跑 npm run dev） |
| ddd9c20 | 探针可跑性修复：Windows spawn 统一 cmd.exe 壳（直 spawn `node` 报 File not found）+ S1 文本轮 mock 补 `content_block_start`/`content_block_stop` 补 `index:0`（缺 start 时 delta 落未建 block，provider 报 `undefined.type` 重试 3 次熔断） | 外部（ZCode） |

验证：`npx tsc` 净；新增 14 用例全绿；全量（--exclude config.test.ts，外部实证）**1342 passed | 1 skipped 零红**；`--version`/`-v`/`-h`/`--bogus`（exit 1）端到端验证过。

### 8.2 复测裁决（§3.2 执行结果）：~~F-08 复现~~ → **v1.2 勘误：四角色审阅翻案，复现系探针假阳性**

> v1.1 原判「F-08 复现、销案不能销」**被 §11 角色 A/D 独立一致的取证推翻**，原文保留如下供追溯，结论以本节勘误为准。

- ~~wedge 哨兵（纯文本 4 连轮+回显探针）在新 dist 全过~~（仍成立：9ad1a83+V4 治愈纯文本轮）；
- ~~半活探针 S1#2 稳定失败= F-08 复现~~ → **实为探针三重 bug 制造的假阳性**（§11 角色 A P0-1 / 角色 D P0-1/2/3）：
  1. **两段式命令协议未遵守**：ECode 斜杠命令首 Enter 只回填（InputStream.tsx:185-195），探针只发一次 `\r`——只完成回填未执行；
  2. **判定指纹被 SlashSuggest 污染**：判定词「跨会话|token|模型」恰是 /stats 补全描述文案（registry.ts:133）——打字本身渲染的补全列表即命中，「OK Enter 生效」与 Enter 无关；
  3. **S1#2 消息被回填残留吞掉**：消息追加到残留 `/stats ` → 被当命令参数——完美解释「消息消失、输入框空、轮 0/50」；
  4. 探针 onExit 冲突（Ctrl+C 探针成功退出反而触发「意外退出」exit 1）+ fail-fast 丢矩阵——「全过销案」路径在现探针上不可达。
- **裁决修正**：F-08（新 dist）待探针修复后重测；旧 dist（轮1 dogfood）Ctrl+C 失灵现象无法用两段式解释，仍存疑待真裁决。**副产品真 bug**：两段式回填残留吞用户消息（角色 A 根因候选二，P1）；`--history=` 烧 token 回归（角色 B/D P0）。
- 方法论教训（角色 A P2-1）：探针判定指纹必须来自被测行为独有输出，禁用常驻 UI 元素（补全列表/状态栏）作指纹。

## 9. 第二轮 dogfood 新发现（用户视角；F-09 起）

> 全部来自「让 ECode 修 ECode」过程实录；定义方式=用户在终端前遭受什么。

| # | 级别 | 用户遭遇 | 锚点/证据 |
|---|------|---------|-----------|
| F-14 | **P0** | 审批弹窗打开时打字+回车被**静默吞掉**——用户说了「停」，ECode 根本没收到，TUI 无任何提示。外部驱动两次止损指令均此消失；第 3 次靠「拒绝当前卡→抢 3 秒无卡间隙发送」才入队（屏幕出现「已排队」提示为证） | ConfirmPrompt 模态与输入框键盘路由 |
| F-15 | P0 | 连续拒绝工具调用，模型把拒绝当技术失败**换手法规避**（git clone→worktree→archive 三连）继续跑偏——用户说「不」它听成「换个姿势再来」 | 拒绝反馈（tool_result is_error）语义不足以传达用户意图 |
| F-16 | P1 | 验收指令（「全量零红才算完」）与现场冲突（环境性失败）时，模型**死磕 25 分钟/50k tok 修不属于自己的问题**而不回来报告冲突请求放宽 | 提示词层（标准冲突→报告） |
| F-12 | P1 | `tests/services/config.test.ts` 11 用例在 ECode 会话内跑红、外部跑绿——beforeEach 复制 `process.env` 不剔除 `ECODE_*`/`ANTHROPIC_API_KEY`，用户以为自己的代码坏了 | ECode 轮2 取证（stash 对照/HEAD worktree 对照均复现） |
| F-18 | P1（待证） | 同一测试会话内外结果不同的更深解释：**ECode 疑似给 bash 子进程注入 ECODE_BASE_URL/ANTHROPIC_API_KEY/ECODE_MODEL**——用户在会话里跑测试被自己的配置污染 | §11 待审阅角色C 证实/证伪 |
| F-13 | P2 | bash 读敏感文件（`head ~/.ecode/config.json`）走常规审批即可执行——审批卡无敏感高亮，用户可能盲批密钥读取 | bash.ts + sensitive.ts 只覆盖文件读取工具 →已修（批2b：detectSensitiveCommand 黄字 advisory） |
| F-09 | P2 | 工具组行标签 "bash" 渲染成 "bas"（截断丢字） | TUI 工具行渲染 |
| F-10 | P2 | write 审批长文件显示「⋯ 省略 N 行」但无「看全文」入口——半盲批 | 审批卡内容截断 →已修（批2b：v 键看全文） |
| F-11 | P2 | 40+ 工具的长任务全程不用 todo——用户看不到它的计划与进度，只能从工具流水猜 | todo 触发引导 |
| — | P2 | 50 迭代上限把长任务拦腰截断（轮2 触顶一次，需手动催续；截断瞬间未送出的中间叙事一次性 flush） | maxIter 交互 |

分工边界观察：**写探针的跑不了探针**（方案 §5 分工）→ 交付即未验证（4575e92 的探针首跑即崩，ddd9c20 补修）——批2 起探针类交付以「外部实跑通过」为验收前置。

## 10. 批2 实施方案（v1.2 依四角色审阅重排）

> 优先级依 §11 审阅修订：**P0 安全根修 → 探针修复（真裁决 F-08 的前置）→ P0 回归修复 → F-14/F-15 交互批（方案待用户拍板）**。

### 10.1 F-18 密钥泄漏根修（P0 安全，角色 C 证实）

泄漏链（全部实证）：`config.ts:210-212` dotenv 把项目 `.env`（实测含 apiKey 明文）提升进 `process.env` → `proc.ts:66-72` spawnShellCommand 不传 env 全量继承 → bash/后台任务/quality/hooks 子进程全部可见密钥（`echo $ANTHROPIC_API_KEY` 即读走；prompt injection 叠加即 exfil 链）。MCP stdio 已有 `sanitizedProcessEnv()` 净化（adapt.ts:103）而 hooks 裸继承——自相矛盾。daemon 多项目形态放大（P1-2）。

修复（两级）：
- a. **根修**：`config.ts` 改 `dotenv.parse(readFileSync(...))` 局部 map，不 mutate `process.env`；优先级解析改 `dotenvMap.X ?? process.env.X ?? rawCfg.X`（用户 shell 自身 export 的变量仍继承=用户信任域，与 claude-code 缺省一致）。顺带根治 config.test.ts 11 红；
- b. **纵深**：`spawnShellCommand` 统一接 `sanitizedProcessEnv()`（现成，含单测）一处收口。
- 参考：claude-code `subprocessEnv.ts` 注释点名同一威胁并全子进程 scrub；opencode 不做 dotenv 提升故无此面——「文件密钥主动提升」是多余环节。

### 10.2 探针修复批（P0，F-08 真裁决的前置；角色 A P0-1 + 角色 D P0-1/2/3/P1-1/2）

`scripts/pty-semialive-probe.cjs` 六处：
1. enterProbe 发两次 `\r`（或整写 `/stats \r`）；
2. 判定指纹收紧到命令输出独有行（`/总计：输入|个会话|暂无数据|统计不可用：|运行中暂不能执行命令/`），**mark 移到 `\r` 之后**；
3. `expectExit` 标志：ctrlCProbe 进入置位，spawnTui 的「意外退出」兜底跳过——否则 Ctrl+C 探针成功即自杀；
4. FAIL 改 `failed=true`+evidence+respawn 续跑（矩阵不丢场景），结尾统一 exit 汇总（0 过/2 FAIL）；
5. Ctrl+C 判定窗 6s→9s（graceful 预算 5.5s 余量过窄）；
6. 补回显层探针（enterProbe 前 `write('zz')` 断言回显——区分「调度死」vs「半活」）。
修完重跑 S1-S3 对 F-08 做真裁决（含 S2 本地命令轮、S3 工具往返首跑）。

### 10.3 `--history=value` P0 回归修复（角色 B P0-1 / 角色 D P0-4）

`args.ts` 白名单豁免 `--history=` 前缀但解析不认——`ecode --history=abc` 整串当 prompt 发 LLM（F-01 同型复发，测试恰是盲区）。修：解析层前置拆解 `--history=value`（与豁免对齐），补测试（含互斥）。顺带 args 边界收尾（角色 B P2/D P1-3 择要）：`--history` 值形态校验（接 `isValidSessionId`——CLI 路径全程裸拼 sessions 目录外可读）、indexOf 重复 token 错位、`serve --version` 语义决策+锁测、version 优先序声明。

### 10.4 F-12 测试隔离 patch（角色 D P1-5 精确版）

`tests/services/config.test.ts:14`：`baseEnv` 剔除 `ECODE_` 前缀（toUpperCase，覆盖 ECODE_TYPE 遗漏）+ `ANTHROPIC_API_KEY`（与 loadConfig 读取面对齐，剔多失真）；保留 env 通道活用例防退化。与 10.1a 根修**都做**（根修治源头，隔离治未来）。同类排查：仅此一处需修。

### 10.5 F-14/F-15 交互批（拍板-2 a 五条全做；已实施 9c12ab1，见下方实施记录）

角色 A 否了原方案 a/b，给出 **opencode 实证第三方案**（permission.tsx:567-626/443-523）：
1. 审批卡激活时**字符不吞**——打字可见地进输入框（复用 insert 通道），Enter 语义留给审批；应答后字还在可继续编辑/经 M11 插话发出（同时是 F-07 审批疲劳止痛点：连弹期间用户能打字排队「后面的都拒绝」）；
2. **y/n 单字母快捷在有输入草稿时失效**（裁决口径见 §13.8：空草稿首字母 y/n 仍立即快捷=保留老习惯；「打 yes 误触发」不再静默——①字符不吞+插话队列兜底，误批可见可补救）；**Esc=拒绝**补直觉出口；
3. **Enter 默认 y 误批防护**（ConfirmPrompt.tsx:72/92-94——现状「吞消息+误放行」双害）：草稿非空或未显式选择时 Enter 不确认审批；
4. **F-15 拒绝带理由**（RejectPrompt 实证）：拒绝时弹一行理由随 tool_result 回传——比纯提示词层治本。
配套 P1：回填态 Esc 取消+「再回车执行/Esc 取消」提示（两段式残留吞消息的出口）；审批期输入框 inactive 视觉指示；busy 拦截保留命令文本只提示。
> 因涉及键位/交互习惯变更，**本节实施前需用户拍板**。

#### §10.5 实施记录（批2b，2026-08-28 夜完成，ECode serve 会话三轮交付；拍板-2 a 五条全做 + F-10/F-13/F-21/F-22 随批）

**改动文件**：
- `src/tui/ConfirmPrompt.tsx`：重写键盘路由（新纯函数 `confirmKeyAction`——可单测的按键语义核）+ F-10 看全文（v 键，`previewMaxLines(rows, expanded)` 展开态预算×3 仍 viewport 封顶）+ F-13 `detectSensitiveCommand`（词法提路径段→`isSensitivePath` 判定，黄字 advisory 不做硬门）+ ⑤理由模式（r 进/一行编辑/Enter 提交/Esc 返回）。五条语义：①非快捷字符经 `onDraftKey` 转发主输入框；②`draft` 镜像非空时 y/n/a/r/v 失效；③Esc=拒绝；④`selected` 默认 null，Enter 未显式选择时不确认（旧「默认 y+CR=静默批准」废除）；⑤拒绝理由经 resolve 第三参回传。
- `src/tui/types.ts`：`ConfirmState.resolve` 加第三参 `reason?`。
- `src/tui/Conversation.tsx`/`App.tsx`：`onDraftKey`/`draft` 透传链。
- `src/tui/TuiApp.tsx`：`confirmDraftRef`+`handleConfirmDraftKey`（草稿 buffer 权威 ref，经既有 insert 通道实时写入输入框可见渲染；Enter 提交走 `submit`→审批期 running=true 自然入插话队列=「后面的都拒绝」可排队；应答后 cur 保留=字还在）；approval/respond 带条件 `message`（协议已有字段，零改动复用）；审批期 placeholder「审批中…打字进草稿」视觉指示。
- `src/tui/InputStream.tsx`：回填态（尾随空格无参数）Esc 清空；SlashSuggest 提示行加「填入后：再回车执行 / Esc 清空」；busy 拦截删去 `setCur(createCursor(''))`（保留命令文本只提示）。
- `src/core/loop.ts`：F-21——`exhausted` 标志（初始 false，仅 iter===maxIterations 完整执行未 break 置位），耗尽时 `onWarn` 提示+logger 告警（对照 length 截断 onWarn 先例；abort/正常 end break 不误报）。
- `src/tools/builtin/bash.ts`：F-22——`foldNodeWarnings`（`(node:pid) XxxWarning` 行折叠为一行去重提示，正常输出保留），execute 输出链 `truncateOutput→foldNodeWarnings` 接线。选工具层而非视图层：LLM 与用户两侧同时受益（视图层折叠模型仍吃到噪声）。

**测试对账**（vitest 1411 项全绿+tsc 净；git.test.ts 全量并发下 20s 超时为环境 flake，单跑绿）：
- `tests/tui/ConfirmPrompt.test.tsx` 17 项：五条各一（④含「←→显式选择后 Enter 才确认」对照）+ F-10/F-13 各一 + 旧「CR=批准」用例翻转为防误批断言。
- `tests/core/loop.test.ts` +2：耗尽 onWarn 提示 / 正常结束不误报。
- `tests/tools/builtin/bash.test.ts` +4：折叠全文不出/纯警告一行/无警告原样/同类去重。
- `tests/tui/InputStreamSkill.test.tsx` +3：回填态 Esc 清空 / busy 保留文本 / 空闲清空对照。
- `tests/tui/interject.test.tsx` 旧用例（busy 拦截不执行不排队）零改动通过——拦截语义未变，只是不再吞文本。

**关键决策**：
1. 字符转发选「ConfirmPrompt 消费快捷键+其余转发」而非「TextInput 保持 active 双组件并存」——单组件消费避免双吃 Enter/y；转发走既有 insert 通道（S-P6 回填基建）零新机制。
2. Enter 语义：未显式选择时 Enter 走草稿提交（插话）而非无操作——审批等待期用户 Enter 大概率想发草稿。
3. F-13 用词法提取（token 白名单形态：`/`、`~`、`./`、盘符）而非全句判定——避免 `npm test` 误报；advisory 黄字一行，敏感**读**仍走 sensitiveGate 硬门不动。
4. F-21 判定用「最后一轮完整执行」而非「循环自然退出」——break 路径（end/abort/length/content_filter）都不该报耗尽；abort 尤其不能叠加假告警。
5. 拒绝理由回传复用 approval/respond 既有 `message` 字段（Broker→loop confirm→`用户拒绝了本次操作：{reason}` 已有链路）——治 F-15 换手法规避零协议改动。

**外部验收（监看方复跑，2026-08-28 夜）**：①全量 **1408+3skip（1411 总）** 与自报吻合、tsc 净；②diff 审过（confirmKeyAction 纯函数/F-21 exhausted 标志/F-22 foldNodeWarnings 实现干净）；③**pty 交互探针 `scripts/pty-confirm-keyboard-probe.cjs` 五项全过**：B1 字符不吞（hello 回显且卡仍在）/B2 草稿 Enter=插话排队不误批/B2b 无选择 Enter 不确认/B3 空草稿 y 快捷放行/B4 Esc=拒绝卡消。**已知偏差（②）——已裁决（§13.8 四面一致）**：空草稿时首字母 y 仍立即快捷（实现与单测按「草稿非空后失效」口径）。裁决=明示口径转正：y 单键是审批卡主应答路径（改两步选中+确认是净损失），首 y 触发用户可见非静默、误批后剩余字符经①进草稿排队插话可补救——与 F-14「静默危害」不同类；opencode 根本无单字母立即确认键（只有 ←→/h/l+Enter+Esc），「opencode 是这么做的」对照约束不存在。真病灶在草稿判定基准与可见输入框脱钩（§13.8 代码 P1-1，随批2b-fix 修）。

**CC 审批交互对比（ECode 交付，读 claude-code-main PermissionRequest.tsx/FilePermissionDialog.tsx/FileEditPermissionRequest.tsx 实证）**：选项形态——CC 按工具分派十数个专属组件（Bash 有 classifier 自动批准/IDE diff）vs ECode 单卡+preview 策略保持轻；键位——CC Up/Down 选 option+Left/Right 切 tab vs ECode ←→ 三键循环（键数少不需列表导航）；Esc 语义——同款 reject（本批③对齐）；理由字段——CC「Tab to amend」feedback 随 onAllow/onReject 回传 vs ECode r 键理由模式+message 字段回传，语义等价（都喂回模型治瞎猜拒绝原因）；记忆粒度——CC accept-once/dont-ask 多档+按命令前缀持久化 vs ECode a=always 仅 MCP 前缀（有意收敛防权限蠕变，方向差异非落后）；防误批——CC 无「默认选中+CR 批准」+onUserInteraction 抑制自动批准抢答 vs ECode 本批④对齐（ECode 无 auto-approve 交互面暂不需要后者）；**审批期打字——ECode 超越项**（字符不吞+草稿排队插话，CC 输入框被对话框独占）；长内容——CC 全 diff+fullscreen diff 视图 vs ECode v 键展开×3+viewport 封顶。

### 10.6 体验小件（P2，顺手）

F-10 审批「看全文」与 F-13 敏感命令 advisory 高亮**前移**（半盲批/盲批密钥=安全问题，角色 A/C 一致意见；F-13 用 `isSensitivePath` 现成判定+advisry 形态不做硬门）；F-09 标签截断；F-11 todo 引导提示词句；/doctor 成本与边界（实测 39.1k tok≈¥1 且扫描范围失控倾向——手册/提示词加边界）。

### 10.7 排期与验收

- 批2a（P0，先行，≈1 人天）：10.1 F-18 根修+10.2 探针六修+10.3 --history=+10.4 F-12——**完成后重跑探针矩阵对 F-08 真裁决**（旧 dist 现象若不复发即销案）；
- 批2b（已实施，见 §10.5；≈1 人天）：10.5 F-14/F-15 交互批+10.6 小件；
- 验收：探针 S1-S3 全过（真指纹）；`ecode --history=x` 行为正确；干净 shell+污染 shell 双环境 config.test.ts 绿；`echo $ANTHROPIC_API_KEY` 在 bash 工具子进程内为空；全量测试门零红。

## 11. 四角色审阅记录（2026-08-28，四 agent 并行，全部独立实跑取证）

### 角色 A（交互体验）核心结论
- **P0-1 F-08 定性翻案**：新 dist 复现=探针假阳性（两段式回填+单 `\r`+SlashSuggest 指纹污染三重叠加，S1#2 消息被回填残留 `/stats ` 吞成命令参数）；命令输出路径无未复位态（busy 卡 true 方向被「统计输出出现过」反向排除）；先修探针再裁决，§10.1 实修暂缓。
- **P0-2/3 F-14 第三方案**（opencode permission.tsx 实证）：字符不吞进输入框+y/n 有草稿失效+Esc=拒绝+Enter 默认误批防护；F-15 拒绝带理由（RejectPrompt）同组件域合并设计；反对「审批后自动发送」语义。
- P1：回填态 Esc 取消（两段式残留吞消息=真实产品缺陷）；审批期输入框 inactive 指示；busy 拦截保留文本。P2：探针指纹方法论（禁用常驻 UI 作指纹）；F-10/F-13 前移；maxIter flush 与 F-16 同属失控感家族。

### 角色 B（代码质量）核心结论（全部实跑验证）
- **P0-1 `--history=value` 静默变 prompt 烧 token**（白名单豁免与解析自相矛盾，实测 `parseArgv(['--history=abc'])` → input 整串，测试盲区）。
- P1×4 全在探针：onExit 冲突（S1 过则 Ctrl+C 探针自爆，「全过」不可达）；CPU 差分测的是 cmd 壳恒失真；fail-fast 丢矩阵（「触发因素=本地命令轮」是倒推推断）；Enter 判定锚缺「暂无数据」形态（新机器必假阴）。
- P2×15：args 边界（--history 值无校验 CLI 全程裸拼可路径穿越/indexOf 错位/serve 注释失真/config 失败语义变化未声明）；patchHeader（二次转义变形/`[object Object]` 现依赖下是死代码注释归因错/误伤面极低但存在/断言弱于实现）；探针 500ms 定长回车慢回显破防；提示词句宜放动态段+术语统一。**总评：解析层抽取干净、解码核心实测正确，扣分集中在 args 白名单半支持与探针自身撑不起裁决**。

### 角色 C（安全）核心结论（F-18 证实）
- **F-18 证实（P0）**：泄漏链三段实证——`config.ts:210` dotenv 提升 `.env`（实测含三键）→ `proc.ts:66` spawn 无 env 全量继承 → bash/tasks/quality/**hooks**（第三方命令，信任级同 .mcp.json，MCP 已净化 hooks 裸继承=自相矛盾）子进程全可见密钥；因果复现（注入假 env 跑 config.test.ts → 11 红精确吻合）；修法 a 根修（dotenv.parse 不 mutate）+ b 纵深（sanitizedProcessEnv 接入 spawnShellCommand）；daemon 多项目形态放大（P1）。
- **F-13 评估**：bash 敏感路径做审批卡 advisory 高亮（isSensitivePath 现成，词法硬门勿做——天然漏+虚假安全感）；claude-code 对齐实践（env/printenv 踢出只读白名单）。
- args 安全：`--history` 值无形状校验+CLI 全程未接 `isValidSessionId`（sessions 目录外任意 .jsonl 可灌入）；`--history=` 漏网；indexOf 错位。

### 角色 D（测试质量）核心结论
- **P0-1** Enter 探针假阳污染（同角色 A，判定词与补全描述三重合）+ **P0-2** onExit 兜底先跑（多监听器按注册序实证）+ **P0-3** fail-fast 丢矩阵（exit 语义被架空）+ **P0-4** `--history=` 双料（实现 bug+测试缺口）。
- P1-5 F-12 精确 patch：剔 `ECODE_` 前缀（toUpperCase，覆盖 ECODE_TYPE）+ANTHROPIC_API_KEY，与 loadConfig 读取面对齐；保留 env 通道活用例防退化；全 tests/ 排查仅此一处。
- P1-6 F-08 验收设计六场景（连续两本地命令/多命令变体/带参/忙碌态拦截后/Ctrl+U/单元层下沉状态机）。
- P1-3/4 测试缺口清单（serve --version 契约/version 优先序/空参/序不一致；emoji 4 字节纯函数直测/diff 体字面量防误伤）。P2×7。**总评：既有断言质量好但只锁 happy path 邻域；现探针上「全过」验收不可能成立，批2 开工前必须先修探针**。

### 审阅对方案的修订汇总（v1.1→v1.2）
1. §8.2 勘误：F-08「复现」翻案为探针假阳性（四票一致：A/D 独立同结论、B 实测印证 fail-fast、C 无异议）；
2. 批2 重排：F-18 安全根修升 P0 首位 → 探针六修（真裁决前置）→ --history= P0 → F-12 patch → F-14/F-15 交互批（**待用户拍板键位变更**）→ 小件前移 F-10/F-13；
3. 新增方法论两条：探针指纹禁用常驻 UI；「分工边界交付物」须外部实跑验收。

## 12. 批2a 实施与终裁记录（v1.3，2026-08-28 晚）

### 12.1 交付

- **cd174cf（ECode 执行）**：§10.1 F-18 根修（dotenv.parse 局部 map + spawnShellCommand 接 sanitizedProcessEnv 一处收口）/§10.2 探针六修/§10.3 --history= 拆解+isValidSessionId 堵穿越+7 用例/§10.4 F-12 baseEnv 剔除（toUpperCase）。验证：tsc 净+1370+3skip 零红（**在 ECode 会话内跑**=污染环境实证一半）。
- **0b1fd49（外部复审修复）**：①批2a 引入 P0 回归——dotenvMap 放最前致**外部注入 env 被 .env 文件值压过**（外部验收实证：探针注入 mock 端点失效走真 GLM），改回 dotenv 原生语义 `process.env.X ?? dotenvMap.X ?? rawCfg.X`+回归锁用例（角色C 建议的顺序本身有缺陷，审阅意见也不能盲信）；②探针回显层 zz 残留自伤+双退格吞键两修（探针第五/六次自伤，全在交付后外部实跑暴露）。
- 外部验收：干净 shell config.test.ts 23 passed ✓（F-12/F-18 双环境成立）。

### 12.2 F-08 终裁

**核心症状在当前代码（0b1fd49）未复发**：修好探针后 S1 矩阵全绿——纯文本三轮渲染 ✓+轮末回显 ✓+**轮末 Enter 生效 ✓**+**Ctrl+C 双击优雅退出 ✓**；对照旧 dist（8-26，缺 9ad1a83）的轮1 dogfood 现象（轮末 Enter 丢失/Ctrl+C 失灵/退出无门），**9ad1a83+V4 已治愈该家族**。
S2/S3 的 FAIL 为探针场景级问题（S2 判定锚/时序；S3 mock 工具相位从未跑通=挂账「探针工具轮」延续；末帧实证 /stats 输出实际出现）——待下批修探针场景后矩阵全绿即正式销案。
**销案判定改判记录**：v1.1「不能销」→v1.2「假阳性翻案」→v1.3「核心症状未复发，待 S2/S3 探针修复后销案」——三轮反转全因探针自身 bug，印证 §11 方法论：**裁决工具的可信度先于被测对象**。

### 12.4 二次审阅修复与 F-08 正式销案（v1.4，2026-08-28 晚·四角色对 skill/文档/批2a 代码再审）

四角色再审（A skill 事实核对/B 文档口径/C 代码深审/D 测试验收）产出 P0×5，全部修复：

1. **skill 两条 P0 事实错误**（`~/.agents/skills/agent-dogfood-monitor/SKILL.md` 已重写）：①`--yes` 在 **REPL 完全不生效**（auto-approve 仅单次模式无订阅者时豁免，approval.ts:83——放松轮询会撞 15min 审批超时自动拒），REPL 免卡正解=sandbox full-access；②「吞键」实为**静默批准**（卡默认选中 y：CR=放行工具、文本含 y=放行、MCP 卡 a=放行+记住）——卡开着严禁发任何文本/CR（批2b 后已废除：CR 不批、字符进草稿，见 §10.5）。
2. **S2/S3 FAIL 真凶=探针 respawn 判定失效**（角色C/D 同源定位）：`proc.exitCode` 是 node-pty IPty 不存在的属性且 pid 退出后仍留——三条件恒 false，respawn 永不触发，S2/S3 对死进程 write 必假 FAIL。§12.2 的「末帧实证 /stats 输出出现」实为 **S1 enterProbe 输出的累积残影**（lastFrame 取全程 out），归因勘误。修复（7e01439）：alive 布尔判定+ctrlCProbe 失败路径防自杀+相位复位+S3 协议保真（input_json_delta+stop_reason:tool_use）。
3. **F-08 正式销案**：矩阵 24 项全过 S1-S3 全绿（含轮1 卡死场景 /stats 后 Enter/Ctrl+C 全健康）；wedge 记忆销案回填。「S3 工具相位从未跑通」旧归因一并推翻（respawn 修复后一轮通）。
4. **数字勘误**（角色B/D）：批1 新增用例 14→**13**（args 10+diff **3**，diff-filename 实为 3 用例非 4）；批2a args 净增 **6** 用例非 7；全量基线 cd174cf 时点 1370+3skip → **0b1fd49 后 1371+3skip**（回归锁 +1，19:07 实测零红+ tsc 净）；「23 passed」为 Windows 口径简写（实 21 passed+2 win32 skip）。git.test.ts 全量并行偶发 20s 超时 flaky（单跑 13/13 绿 3s）——验收条款加「全量红先单文件复核再定性」。
5. **F-18 修复留账**（角色C P1-1，批2c 待修）：MCP `${ENV_VAR}` 占位符与 .env 静默解耦（expandStr 只读 process.env，.env 值不再提升→server 静默跳过）；ECODE_SERVE_* 同款失效；净化接入零测试覆盖+「不 mutate process.env」回归锁缺位（角色D P0-3/P1-4）——随批2c 补。`echo $ANTHROPIC_API_KEY``echo $ANTHROPIC_API_KEY` 机制实证子进程 `K=[]` 通过（角色D dist 直调）。

**批2c 已实施（be3ccd7，2026-08-28 晚，ECode 免卡模式 dogfood 执行）**：五项全清——MCP ${ENV_VAR} envFallback 回退（expandEnvVars/mergeMcpServers 参数化+loadDotenvMap 导出+assembly/loader 接线，优先级 process.env > .env 与主链一致）/ECODE_SERVE_* 统一 envOr/净化单测×2（密钥形态不透传+普通键存活，proc 层零覆盖补齐）/不 mutate 回归锁（宿主 env 逐字节+Config 生效双向）/args message 断言+version 优先序锁测。验证：tsc 净+1380+3skip（唯一红=git.test.ts 并行 26.9s flake，单跑 13 全过零交集；改动面测试全绿）。免卡提速实测：批2c 同等五项工作量 ≈15 分钟（批2a 六项 ≈1.5 小时+复审）——监看倍数从 3-5× 降至 ≈1.3×，skill 正解 B（Tab 切 full-access）有效性实证。

### CC 文档勘误（随批）

「三档沙箱」有误——SANDBOX_MODES 实为**四档**（default/read-only/workspace-write/full-access，sandbox.ts:18-20）；该错误不影响 acceptEdits 借鉴结论（default→workspace-write 间粒度论据仍成立，改档数为四）。

### 12.5 遗留与下一步（v1.4 更新）

- 批2b（已实施，见 §10.5）：§10.5 F-14/F-15 交互批（opencode 第三方案：字符不吞+快捷键失效+Esc 拒绝+Enter 误批防护+拒绝带理由）+§10.6 小件（F-10/F-13 前移）；
- **批2c（代码留账）已实施**（F-18 尾巴五项一笔）：MCP `${ENV_VAR}` 占位符 dotenvMap 回退（expandEnvVars/mergeMcpServers envFallback 参数，assembly/setup/plugin loader 接线，config.ts 新增 loadDotenvMap）+ECODE_SERVE_*（含 WEB_DIR/PORT）同款回退+spawnShellCommand 净化 proc 层 env 断言单测×2+「loadConfig 不 mutate process.env」回归锁+loadDotenvMap 用例+args 重复 token「只能出现一次」message 断言与 version 优先序锁测（含「未知 flag 在前先报错」既定语义注释）；优先级统一 process.env > .env 文件（与主链 dotenv 原生语义一致）；
- 界面批（新增输入）：CC 对比文档高价值四项（@文件补全/输入历史持久化+搜索/单工具级展开/transcript 入 scrollback）+中价值六项；
- 轮3 功能测试未完项：MCP 安装使用（智谱 coding plan）、skill 双触发/auto-memory、/rewind /output /config 面板实测——随批2b 或下轮 dogfood；
- 效率基线（2026-08-28 实测）：监看+手动审批 ≈3-5× 单独执行（瓶颈=审批响应延迟均 3min/次）；REPL 免卡（sandbox full-access，--yes 仅单次模式生效）预计 ≈1.2-1.5× 待实测；CC 同任务（子代理并行+accept edits）≈ ECode 纯执行持平或更快。提速已沉淀 skill：`~/.agents/skills/agent-dogfood-monitor/SKILL.md`。

---

## 13. 后续任务清单与拍板项（v1.5，2026-08-28 夜定稿——本轮工作总排期）

> 执行顺序默认：批2d（通知口子）→ serve 实验（监看法升级）→ 批2b（交互批）→ 功能测试批 → 界面批；skill 更新为批2d 与 serve 实验的**硬性收尾项**。标注【拍板】的条目等用户确认后开工。
>
> **对比原则（用户拍板 2026-08-28 夜，主体修正）：同一功能能对比 Claude Code 的都对比，且对比工作由 ECode 执行**——每批任务里把 CC 对比点作为交付项派给 ECode（ECode 读 D:studyclaude-code-main 源码/官方文档，产出「CC 做法/ECode 做法/差异与借鉴」小节），监看方只验收对比质量并从用户视角记录问题；重大差异单独沉淀对比文档（对标 2026-08-28_CC与ECode对话界面对比_已完成.md）。已排对比点：批2d=CC Notification hook（触发条件/超时值/schema/配置面）+CC 响铃行为；serve 实验=CC headless 模式（-p --output-format stream-json 事件流形态/接入成本对照 mux）；批2b=CC 权限审批交互（options 列表/Esc 语义/拒绝注释字段）；功能测试批=CC 的 MCP 配置面与 skill 双触发；界面批=CC 对比文档驱动（已就绪）。
>
> **升级循环（用户确认的总流程）**：①生成批次任务（含 CC 对比点）入文档 → ②ECode 执行（免卡/serve 事件驱动监看），监看方用户视角记录问题（F-编号） → ③外部验收（探针/测试/端到端，监看方执行） → ④问题写入文档+skill 更新 → ⑤多角色审阅（skill/文档/代码/测试四面） → ⑥按意见修复（小修监看方直接改，产品代码 ECode 修） → ⑦回填记录+生成下一批 → 回到①。

### 13.1 批2d：审批挂起通知口子（F-20，方向已批，细节待拍板）

用户视角痛点：审批挂起时本地 TUI 完全沉默（无 hook 事件/无响铃/无桌面通知）——用户切窗后互相等死，15 分钟超时自动拒、任务全灭。

1. **Notification hook（第七事件）**：触发条件=审批挂起（approval/requested 发出时）与等待用户输入超时（idle N 秒，CC 对齐，N 可配默认 60）；hook 脚本可写信号文件/桌面通知/响铃——dogfood 监看与真实用户挂后台共用此口子；【拍板-1】触发时机三选：a. 挂起即触发 b. 挂起 N 秒后触发（推荐，防高频）c. 两者都做可配；
2. **BEL 响铃**：TUI 审批卡**首次出现**时输出一次 `\a`（终端铃声；config `bellOnApproval` 默认开可关）；【拍板-1 附】是否接受默认开；
3. 验收：hook 触发留痕测试+BEL 在 pty 探针断言帧含 \x07；
4. **skill 更新（硬性收项）**：新增「推送式监看」节——Stop hook 信号文件+Notification hook 的 fs.watch 用法。

#### 批2d 实施记录（2026-08-28，拍板-1 落地；实施=ECode 真机（dogfood 轮3），验收=监看方外部复跑）

**改动文件**：
- `src/services/config.ts`：新键 `notificationIdleSeconds`（默认 60，0=关；常量 `DEFAULT_NOTIFICATION_IDLE_SECONDS`）+ `bellOnApproval`（默认 true；`DEFAULT_BELL_ON_APPROVAL`）
- `src/services/hooks/types.ts`：`HOOK_EVENTS` 增第七事件 `Notification`；`HookInput` 增 `reason?: string`（`idle` | `approval-pending`）
- `src/host/approval.ts`：`ApprovalBroker` 增 `pendingNotifier`/`notifyDelayMs` 构造参数——`suspendOnce` 登记 `notifyTimer`（unref，fire 时校验 entry 仍 pending 防重复）；respond/级联/dispose 显式清 `notifyTimer`（timeout 收敛不显式清，靠 fire 前校验 no-op 兜底）
- `src/host/session.ts`：broker 构造接线（approval-pending → `dispatchNotification`）；`finishTurn` 轮末 `scheduleIdleNotification()`（N 秒空闲起表）、`startTurn` 取消（新轮=不再空闲）、`dispose` 清理；`dispatchNotification` 统一出口（fail-open，无 handler 零开销跳过）
- `src/tui/TuiApp.tsx`：`approval/requested` 分支首次出现写一次 `\x07`（`BEL_CHAR` 常量；`bellRungRef` Set 按 requestId 去重防重放/重响；`bellOnApproval:false` 关）
- 测试：`tests/host/approval-notification.test.ts`（4 用例：挂起触发/应答取消/idle 触发一次/dispose 零触发）、`tests/services/config.test.ts`（2 用例：缺省默认+显式覆盖）；探针 `scripts/pty-notify-probe.cjs`（六场景矩阵，254 行）

**关键决策**：①触发时机=拍板 b「挂起 N 秒后触发」（防高频）；②同一挂起只触发一次——broker notifyTimer fire 前校验 `pending.get(requestId) === entry`（应答/级联/超时收敛后自然 no-op）；③双定时器全部 `unref`——旁路观测不拖进程退出；④idle 通知挂在宿主 finishTurn（非 TUI），serve/CLI 形态同享；⑤BEL 只在 TUI 层（终端感知语义在客户端），首次判定用 requestId 去重。审批通知 kind/tool 经 `tool_name`/`tool_input.kind` 透传给 hook 脚本。

**测试结果**：vitest 全量 **1387 passed + 3 skip**（129 文件，基线 1381+新增 6）；`npx tsc --noEmit` 净。TDD 先红后绿（红的具体帧数无留痕，无法独立核实）。注意：git.test.ts /undo 用例全量并行下偶发 20s 超时（单跑 13/13 过，父提交亦现）——复跑「全量红」先单文件复核再定性。

**外部验收（监看方复跑）**：①测试/tsc 独立复跑吻合；②diff 审阅过（生命周期防御四点核实）；③真机探针 `scripts/pty-notify-probe.cjs`（隔离 USERPROFILE 沙箱+mock SSE+hook 落 notify.log）**六项全过**：S1 审批卡 BEL（断言裸 \x07 ≥1，实测=1，已剥 OSC 误报源）/S1b 挂起 3s approval-pending 留痕（探针断言 reason 字段；tool_name/tool_input.kind 为单测断言+代码透传）/S2 轮末 idle 留痕/S2b 继续空闲只触发一次/S2c 新轮完成重新起表/S3 bellOnApproval:false 卡现铃不响。过程实录：ECode 首轮被 50 迭代上限拦腰（F-21 实撞），续接一轮补齐三交付；子代理并行完成 CC 对比调研（M11 task 工具自举）。

**CC 对比小节（ECode 交付，监看方验收通过；源码锚点 D:\study\claude-code-main\src；文档面审阅 P1×2 已由监看方订正：原稿 useNotifyAfterTimeout 机制两半句颠倒、:33 行号错）**：

- **触发条件**——CC：`sendNotification`（services/notifier.ts:25）→ `executeNotificationHooks`（utils/hooks.ts:3570），调用点三类：权限审批弹窗（PermissionRequest.tsx:190，`permission_prompt`）/空闲等待输入（REPL.tsx:3934，`idle_prompt`——轮结束起表，无交互且 `idleTimeSinceResponse >= messageIdleNotifThresholdMs` 才发）/MCP elicitation 等杂项；审批/问询走 `useNotifyAfterTimeout`（useNotifyAfterTimeout.ts，`DEFAULT_INTERACTION_THRESHOLD_MS=6000` 在 :9）：**最近 6s 无交互才通知**（`shouldNotify = !hasRecentInteraction`，:56-61）；有交互则每 6s 轮询直到用户脱离键盘才通知——防「用户正盯着屏还响」。ECode：两条路径（broker 挂起 N 秒+finishTurn 空闲 N 秒），无交互判定（TUI 输入与宿主解耦，宿主看不到键盘）。**借鉴**：CC 的 6s 交互阈值聪明（人在屏前不打扰）——TUI 层 BEL 侧可借鉴（stdin 可见），hook 侧宿主拿不到交互事件维持纯时间阈值合理；CC 多 notificationType（elicitation/收件箱），ECode 先收窄到两痛点场景正确。
- **超时默认值**——CC：idle 通知 `messageIdleNotifThresholdMs` 默认 **60000**（config.ts:612 可配）；审批弹窗固定 6s 交互阈值轮询。ECode：`notificationIdleSeconds` 默认 **60 可配（0=关）**，审批挂起复用同一 N。**差异**：两键合一 vs CC 分离（idle 可配、审批硬编码 6s）；合一更简单，嫌审批通知慢可日后拆独立键。
- **Schema**——CC：`{ ...base(session_id/transcript_path/cwd/permission_mode/agent_id/agent_type), hook_event_name:'Notification', message, title, notification_type }`（utils/hooks.ts:3579-3585）；`notification_type` 兼作 matcher。ECode：`{ event:'Notification', session_id, reason:'idle'|'approval-pending', tool_name?, tool_input:{kind}? }`；matcher 沿用现有字段但 reason 暂不参与匹配。**借鉴**：场景增多后可让 matcher 支持 reason 值（如只监听 approval-pending 写信号文件）。
- **配置面与响铃**——CC：hook 走 settings.json；响铃是**独立通知渠道路由** `preferredNotifChannel`（config.ts:591 默认 `'auto'`，选项 terminal_bell/iterm2_with_bell/notifications_disabled 等），BEL 经 `terminal.notifyBell()`——**默认 auto 不保证响铃**（按终端能力挑 OSC 通知，Apple Terminal 且铃关才 fallback BEL）。ECode：hook 走 config.json `hooks` 键（七事件同面）；BEL 独立键 `bellOnApproval` **默认 true 开**。**差异**：CC 渠道协商优先免打扰，ECode 用户痛点导向直接默认响可关；CC 的终端能力协商（iTerm2/kitty/ghostty OSC 协议）是远期可借鉴面——引入 channel 概念后 `bellOnApproval` 可平滑并入。

**批2d 过程新发现（入 §2 问题清单）**：F-21（迭代上限截断零提示，P1）/F-22（子进程 Node 警告直打 TUI，P2）。

#### 批2d 四角色审阅记录（2026-08-28 深夜，四 agent 并行独立实跑取证）

**四面均无 P0**。汇总：

| 面 | P1 | P2 | 要点 |
|---|---|---|---|
| 代码 | 1 | 10 | **扩展源 Notification hook 权限 ask 自续环**：任一审批挂 60s→通知 dispatch→扩展源 hook 未授权（ask 态）→挂新权限卡→新卡 60s 又触发 approval-pending→循环（稳态约 15 张同 owner 卡堆积，每张新卡响一次铃）。当前无存量扩展注册 Notification，机制性缺陷 M14 前堵。修法：mcp-permission kind 不挂 notifyTimer。P2 主体：timeoutResolve 不显式清 notifyTimer（靠 no-op 兜底）/notifyDelayMs≥timeoutMs 静默失效/两处 `?? 60` 未用导出常量/reason 宽化 string/两处漏起表（保守方向）/askUser 问询与审批语义混淆/matcher 只吃 tool_name |
| 测试 | 7 | 6 | 三个单测断言强度：用例 2 观察窗 <阈值（漏 clearTimeout 回照样绿）、用例 1 隐式走 10s 超时收敛+`toBe(1)` 靠 50ms 侥幸窗、用例 4 标题两断言点均未测（等价空转）；覆盖缺口：0=关/级联清 timer/timeout 先到不通知/validate enum 真链路/同 requestId 重投不重响 均零覆盖。修法约 6-8 个用例收口 |
| 文档 | 2 | 8 | CC 对比 useNotifyAfterTimeout **机制两半句颠倒**（源码 `shouldNotify=!hasRecentInteraction`：最近 6s 无交互才通知）/`:33` 行号错（常量在 :9）。P2：timeout「清」字面过宽/S1b tool_input.kind 非探针断言/UTC 未标/探针未列改动清单/「5 fail」无留痕不可核实——**均已由监看方直改订正**。独立复跑：1390 总数与基线 1384 精确对账，零虚报；git.test /undo 全量偶发 20s 超时（单跑过，父提交亦现） |
| skill | 2 | 3 | approval.ts:83→:92 行号漂移（已改语义引用）/死引用 `_Claude产出.md`（已删）；三处路径「反斜杠丢失」**实为 GBK 乱码字节 \x82 残留**（整行重写根治）；已补 session_id 恒带+matcher 滤 idle+OSC 两结束符——**全部直改完成** |

**修复分工**：文档面/skill 面监看方直改完成（上文订正即终态）；产品码（代码 P1-1 自续环+测试面 P1×7+代码 P2 顺手五项：常量引用/timeout 显式清/reason 字面量联合/注释修正/零开销口径）**派 ECode 修复批 2d-fix**。附带发现：ECode 内建手册 builtin.ts:132 写 `~/.ecode/logs/` 与实际 cwd 级不符（文档债，P2 挂账）。

##### 批 2d-fix 修复记录（2026-08-29，ECode 执行，监看方复验）

**改动文件**（4 个）：

- `src/host/approval.ts`：
  - 【代码 P1-1 自续环】`suspendOnce` 对 `kind === 'mcp-permission'` 不挂 notifyTimer——扩展源 Notification hook 处于 ask 态时，通知 dispatch 挂起新权限卡、新卡再触发 approval-pending 的循环从源头掐断；
  - 【P2 顺手】`timeoutResolve` 显式 `clearTimeout(entry.notifyTimer)`（原靠 fire 前 no-op 兜底，防御口径统一为显式清）。
- `src/host/session.ts`：两处 `?? 60` 改用 config.ts 已导出的 `DEFAULT_NOTIFICATION_IDLE_SECONDS`（import 补入）；`scheduleIdleNotification` 注释「表在 submit/startTurn 清」订正为「表在 startTurn 清」（与实现一致——cancelIdleNotification 仅 startTurn/dispose 调用）；类头「无 handler 零开销」口径改准确——「idle 在起表时查、approval-pending 在挂起触发时刻查——无 handler 不起表/不 dispatch」（approval 路径确实会挂表，只是 fire 时 dispatch 出口再查，非零开销）。
- `src/services/hooks/types.ts`：`HookInput.reason` 由 `string` 收窄为字面量联合 `'idle' | 'approval-pending'`。
- `tests/host/approval-notification.test.ts`：按下述对账重写。

**测试对账**（审阅意见 → 修法）：

| 审阅点 | 修法 |
|---|---|
| 用例 1 隐式走 10s 超时收敛 + `toBe(1)` 侥幸窗 | `approvalTimeoutMs: 0`，显式 `approval/respond` once 收敛后断言仍只 1 次 |
| 用例 2 观察窗 < 阈值、`toHaveLength(0)` 过宽 | 阈值 100ms、应答后观察窗 500ms（5×），断言改按 `reason === 'approval-pending'` 过滤 |
| 用例 4 等价空转 | 重写为两用例：①真发新 prompt（阈值内到达→首表作废；新轮完成重新起表→恰好触发 1 次）；②dispose 清理补时间窗验证（起表后销毁、超阈值窗内零触发） |
| 0=关 双路径零覆盖 | 新用例：`notificationIdleSeconds: 0`，审批挂起+轮末 idle 均零触发 |
| always 级联清 timer 零覆盖 | 新用例：双挂起 mcp__a__t1/t2，respond always 级联放行后超阈值窗零通知 |
| timeout 先收敛不通知零覆盖 | 新用例：超时 100ms < 通知阈值 500ms 反向构造，超时收敛后零通知（同时覆盖 timeoutResolve 显式清） |
| validate enum 真链路零覆盖 | 新用例：`parseHookSpecs` 接受 `event: 'Notification'` + 反例非枚举事件被拒（防 enum 放开到任意字符串的回归） |
| P1-1 防回归 | 新用例：broker 级双挂起（`permission()` + mcp 工具 confirm），80ms 阈值过 3 倍——tool-confirm 恰好 1 次通知、mcp-permission 0 次；显式应答收敛后不再触发 |

**测试结果**：`npx vitest run` 全量 **129 文件 1393 passed + 3 skipped**（本文件 4→10 用例净 +6，总数较批2d 独立复跑基线 1390 差 +3 系他文件此前增量，本批无其他测试改动）；`npx tsc --noEmit` 干净（退出码 0）。审阅挂账 P2 中 askUser 问询语义/matcher 吃 reason/notifyDelayMs≥timeoutMs 静默失效三项未在本批处理（非本批范围，留 §13.6 挂账口径）。

### 13.2 serve 模式事件驱动监看实验（方向已批）

- 起本地 `ecode serve`（daemon token 已有），监看方订阅 `/api/events.mux`——`approval/requested` 到达即应答（curl approval/respond，秒级）、`turn/completed` 到达即验收；
- 实测倍数对比（pty 轮询 1.3× → 目标 ≈1.0×），产出监看法 v2 流程；
- **skill 更新（硬性收项）**：监看法改为「serve 优先、pty 兜底（TUI 交互专项）」双模；
- 风险：serve 会话与 TUI 会话行为差异（M13 已知面）；实验任务选小任务（如批2d 完成后的回归验证任务复用）。

#### 13.2 实施记录（2026-08-28 夜完成，**目标达成 ≈1.0×**）

- **驱动**：`%TEMP%\ecode-dogfood\serve-watch.cjs`（双模式：派活=`session/new`+`prompt`；守护=`--watch=<sid>` 断线重连）——订阅 `events.mux?canAnswer=1`，`approval/requested` 帧到即自动应答（危险模式黑名单 `rm -rf`/`git push --force`/`Remove-Item -Recurse`/`npm publish` 兜底 reject+停自动），`turn/completed` 即验收收尾并落 serve-events.log。
- **实验任务**（一石二鸟）：CC headless 对比调研派给 serve 会话——产出 `docs/解析/2026-08-28_CC与ECode-Headless事件流对比_已完成.md`（44 行 5 节，源码锚点抽查 4/4 全中：stream-json 强制 --verbose/SDKMessageSchema z.union/result 帧 duration_ms·num_turns/permission_suggestions 下发）。**ECode 反超确认**：mux 接入成本显著低于 CC headless（重连/游标/鉴权/多项目聚合内置 vs 编排器自造）；可借鉴点六条入档（result 终帧富化 stop_reason·permission_denials/system-init 自描述帧/hook 与审批竞速/审批 suggestions/streamlined 低带宽帧/stdout 守卫）。
- **效率量化**：任务墙钟 ≈155s（13:22:18 派活→13:24:5x 轮完成），watch 窗口审批 14 次**帧到→应答平均 <1ms**（此前 pty 手动审批均 3min/次、免卡模式牺牲审批可见性）——**监看倍数 ≈1.0× 达标**（pty 免卡 1.3×→serve 1.0×），且 default 档逐条 bash preview 都过了一遍（审批可见性保留，比 full-access 免卡更安全）。usage 窗口累计 ¥0.595（watch 重连前漏计约 1/4）。
- **过程实录**：首跑 serve-watch 前台跑被 Bash 超时杀掉→审批断档挂起（无人应答）→`--watch` 守护模式重连，**mux pending 审批重放兜住了挂起帧**（连接三连之二，与 CC `pending_permission_requests` 对齐已兑现）——断连韧性实证。监看方操作失误两条入 skill 自伤清单：serve-watch 必须后台跑；usage/轮计数按订阅窗口（断连漏计）。
- **serve 会话行为差异面（M13 已知）**：本轮未见新差异；serve 会话审批弹帧与 TUI 卡同源（Broker 唯一），仅呈现形态不同。

### 13.3 批2b：F-14/F-15 交互批（opencode 第三方案，拍板-2 a 五条全做，已实施 9c12ab1 见 §10.5）

五条（详见 §10.5）：①审批卡激活时字符不吞（可见进输入框，应答后可继续编辑/插话）②有输入草稿时 y/n/a 快捷失效 ③Esc=拒绝（直觉出口）④Enter 误批防护（草稿非空或未显式选择时 Enter 不确认审批）⑤拒绝带理由（一行文本随 tool_result 回传，治 F-15 换手法规避）；
- 【拍板-2】a. 五条全做（推荐——①④是安全对，②③是体验对，⑤治 F-15）b. 先做保守子集①+④（安全优先）；
- 配套 P1：回填态 Esc 取消+「再回车执行/Esc 取消」提示；busy 拦截保留命令文本；小件 F-10（审批看全文）/F-13（敏感命令高亮）随批。

### 13.4 功能测试批（轮3 未完项，无需拍板）——**已实施（2026-08-28 深夜，见下方实施记录）**

MCP 安装使用（智谱 coding plan：/mcp 面板配置+工具调用实测）；skill 双触发面（模型自动+手动 /skill）与 auto-memory 归纳实测；/rewind /output /config 面板实测。执行方式：**优先在 serve 实验（13.2）的会话里顺路测**（web 端即 serve 客户端），TUI 面板专项用 pty。

#### §13.4a 功能测试批实施记录（ECode serve 自举测试 + 监看方 pty 走查 + ECode 修复 F-28）

**通过面**（六项全通）：
- **MCP 全链 ✅**：零密钥本地 demo server（%TEMP%\ecode-dogfood\mcp-echo-server.cjs，echo/add 两工具）配进用户级 config → serve 宿主 lazy 注册（状态栏 MCP 0/1）→ pty `/mcp` 面板三层走查（列表「demo ○ 已缓存 2 个工具」/详情（stdio·用户级 config/lifecycle）/工具清单）→ serve 会话工具清单注入+调用成功（echo 回显带时间戳/add 41+1=42）；
- **skill 双触发 ✅（修 F-24/F-28 后）**：清单注入 3 个（ecode-config/agent-dogfood-monitor/test-terminal-app）+ skill 工具加载成功返回最新正文；
- **auto-memory ✅**：首用初始化 ~/.ecode/memory/（索引 MEMORY.md+topic 文件），模型按提示词自维护链路通；
- **/config 三页签 ✅**（常规/Providers key✓✗ 脱敏/高级+逃生口）、**/rewind 空态 ✅**、**/output 开 ✅**（但见 F-26）；
- **CC 对比 ✅**：`docs/解析/2026-08-28_功能测试批-CC对比_MCP与Skill.md`（ECode 两个只读子代理啃 claude-code-main 源码）——锚点抽查 4/4 机制为真（BLOCKING REQUIREMENT/1% 预算/250 字上限/mcp add 命令面），2 处行号漂移监看方订正；结论：ECode 缺 local 作用域/mcp add CLI，多 lifecycle 四态/hash 级批准；skill 触发机制两家本质相同（无代码级匹配）。

**问题清单（F-23~F-28）**：
| # | 级别 | 状态 | 问题 | 锚点 |
|---|---|---|---|---|
| F-23 | P1 | **未修入档** | serve prompt op 无斜杠命令分流——web/serve 端发 `/help` 等命令直接当 prompt 起 LLM 轮烧钱（实测 routed:"Started"），TUI 同命令是免费面板 | host/session.ts:426（prompt 直通 startTurn，命令分流只在 TUI InputStream 层） |
| F-24 | P1 | **已修**（监看方直改+2 用例） | skill 扫描对 junction/symlink 目录 Dirent 报 isDirectory=false **静默跳过**（Windows junction/POSIX symlink 均中招零警告）——junction 共享 skill 目录是既有用户工作流，技能全失效 | services/skill.ts:264（修=stat 跟随判定） |
| F-25 | P2 | **已修**（随批文案） | /config 面板 lintCommand 标签「空=自动探测」与 M9 实际语义（空=关闭不探测，防 RCE 链）**相反**，误导用户 | tui/TuiApp.tsx:137 |
| F-26 | P2 | 未修入档 | /output 子代理列表裸 id（`§ a-43xxx`）无会话归属/描述/时间，新会话也显示历史子代理条目 | OutputViewer 子代理源 |
| F-27 | P2 | 留账（B5 再确认） | serve stop op 未接线（NOT_IMPLEMENTED）——本批停 serve 靠杀 pid | session 命令面 |
| F-28 | P1 | **已修**（ECode ¥1.92） | **skillTool 注册表劈叉**：工具闭包硬接全局单例，serve freshRegistries 每项目新实例 load——清单注入正常但 skill 工具调用恒报「不存在，（无）」（serve 双触发面调用侧全失效；M12 审阅「工具层硬接线」债的又一实例） | tools/builtin/skill.ts:11（修=makeSkillTool 工厂注入，M11 makeTaskTool 先例；assembly 装配期注册项目级实例+单测 globalReg.clear() 锁「fresh 路径不依赖全局单例」） |

**过程发现（环境级）**：ZCode（~/.agents/skills）与 ECode（~/.ecode/skills）skill 目录不互通——同机混用需 junction 桥接（F-24 修后 junction 方案可持续）；ECode 续接截断轮经验补充边界：同 sessionId 续跑上下文全在 ≠ 注册表/系统提示刷新（已由其写入 auto-memory）。

**成本**：主轮 ¥11.65（744k input——CC 源码阅读）+重测/修复/终验 ≈¥5.4，合计 ≈¥17。终态：**1452 总+tsc 净**、F-28 真机终验过（serve 新会话 skill 加载返回正文）。**并行 flake 留观升级**：终态全量 6 红（subagent×3/checkpoint/approvalReason×2）单跑 32/32 全绿——flake 家族较批2b-fix 扩大（subagent 新加入），建议下批调查 vitest 全量并行负载稳定性（时序类用例对 CPU 争抢敏感，可评估 sequence/负载上限）。

### 13.5 界面批（CC 对比文档输入，拍板-3 c）——**已实施（2026-08-29 凌晨，提交 7eb13c5，ECode serve 七轮交付）**

七项全交付（明细见提交/测试文件）：
- **A1 @ 文件路径补全**（atsuggest.ts：fast-glob 目录优先/限 10/deep3/排除面；↑↓/Tab/Enter 补全+Esc 关不吞；@ 无既有语义冲突实证）；
- **A2 输入历史持久化+Ctrl+R**（选型=项目级 `.ecode/input-history.json`——输入历史跟项目走对齐 CC；Ctrl+R 增量搜索循环）；
- **B1 单工具级展开**（Ctrl+E 循环单选，expandCap 入 V2 预算；commit 后清空与折叠同语义）；
- **B2 transcript 入 scrollback**：**设计结论达标零改动**——Ink Static 输出机制即原生 scrollback（V4 轮末 commit 已然，对比文档 3.6 确认等价 CC classic），assistant-text 零截断不变量 2 例钉住；超预算降级行全文下次 commit 进 Static（不硬塞 Ink 进程内 stdout.write——会打乱帧）；
- **C1 acceptEdits 档**（edit_file/write_file 免卡+cwd 逃逸/symlink 拦截；**sensitivePath 硬门不随档降级**——.env 照卡；Tab 循环扩五档 default→accept-edits→read-only→workspace-write→full-access）；
- **C2 档位可视化**（StatusBar ⏵⏵ edits/⏵⏵ write/⏵⏵⏵/⛔——CC ⏵ 式）；
- **C3 双击 Esc 直达 rewind**（空闲态 <500ms；Esc 三态语义不破坏）。

**过程**：ECode 七轮（六轮撞 50 迭代上限——F-21 场景反复实证，宿主旧 loop 无提示）成本 ≈¥70；监看方中途三次干预（①纠偏指令：停 python 补丁改 edit_file/新功能测试进新文件；②破案 Alt+V 回归=legacy InputStream.test 缺 afterEach(cleanup)——遗留挂载实例+新挂载期工作叠加掉帧，监看方直改修复；③S3 协议保真交接：acceptEdits 敏感例的 mock 缺 input_json_delta）。**外部终验**：tsc 净；全量 1493/1488+2 红为已知并行 flake（approvalReason 单跑过）；pty-overscreen 两连跑全过（V 线无 ESC[3J 含新 UI）；pty-confirm-keyboard 五场景全过；敏感硬门/双击 Esc 隔离两处安全关键点抽查通过。

**设计选型已认可（2026-08-29 上午用户拍板）**：B2 零改动收法/A2 项目级存储/B1 Ctrl+E 键位/Tab 五档步进四项全部认可，已推送 origin。
- 【拍板-3】a. 高价值四项全批 b. 先做 @补全+输入历史两项（输入区收益最大）c. 含中价值精选；

### 13.6 远期挂账（不排期，记录在案）

F-06（输出展开历史标记）/F-09（工具标签截断 bas）/F-11（长任务 todo 引导提示词）/maxIterations 可调的 dogfood 建议/POSIX CI 跑批兜底 win32 skip/plugin loader 与 clipboard 净化统一/loader 非 git 命令须接净化的防御注释。

### 13.7 拍板结果（2026-08-28 夜，用户已批）

| # | 项 | 拍板结果 |
|---|---|---|
| 拍板-1 | Notification hook 触发时机 | **挂起 N 秒后触发**（默认 60s 可配）；BEL 默认开可关 |
| 拍板-2 | 批2b 范围 | **五条全做**（字符不吞/草稿时快捷失效/Esc=拒绝/Enter 防误批/拒绝带理由） |
| 拍板-3 | 界面批范围 | **含中价值精选**（高价值四项+acceptEdits 档/档位可视化/双击 Esc rewind；审批附注释随批2b⑤不重复） |
| 拍板-4 | 顺序 | **默认**：批2d→serve 实验→批2b→功能测试→界面批；skill 更新随批2d/serve 实验硬性收尾 |

### 13.7b 原拍板项表（留档）

| # | 项 | 选项 |
|---|---|---|
| 拍板-1 | Notification hook 触发时机 | a 挂起即触发 / b 挂起 N 秒后（推荐）/ c 两者可配；附：BEL 默认开可接受？ |
| 拍板-2 | 批2b 范围 | a 五条全做（推荐）/ b 保守子集①+④ |
| 拍板-3 | 界面批范围 | a 高价值四项 / b 先 @补全+输入历史 / c 含中价值精选 |
| 拍板-4 | 顺序确认 | 批2d→serve 实验→批2b→功能测试→界面批（默认）或调整 |

### 13.8 批2b 四角色审阅记录（2026-08-28，四 agent 并行；代码/测试/文档/skill 四面独立实跑取证）

| 面 | P0 | P1 | 要点 |
|---|---|---|---|
| 代码 | 1 | 5 | **P0-1 F-10 展开态超屏**：`previewMaxLines(rows,true)=(rows−2)×3`，rows=24 时 66 行+卡骨架≈8 行必触发 Ink fullscreen 清 scrollback——V5 总守卫明言不管 confirm，V 线「无 ESC[3J」硬指标直接回归；探针五场景恰好不含 v 展开。P1：草稿状态机三处脱靶（卡弹出覆写丢字 `abc`→`d`/应答后 confirmDraft 不复位→下一卡 hasDraft 假阳性+陈旧草稿当插话提交/注释虚报复位）/ConfirmPrompt 无 key 跨卡 selected 泄漏（同渲染批不卸载，新卡 Enter=静默批准复活，拍板④红线）/F-13 引号路径漏报（实证 `cat "C:\.../.ssh/id_rsa"`→null）/detectSensitiveCommand 渲染热路径同步 IO（每按键 realpathSync）/F-21 exhausted 赋值在两个 continue 之后（CONTEXT_TOO_LONG/empty_tool_use 撞上限时既误导又漏报） |
| 测试 | 2 | 5 | **P0-A ②用例名实脱节**：标题「y/n 快捷失效」实际发 `s`（非快捷字符，与①等价重复），「draft 非空按 y 不触发」从未被测，回归照绿；**P0-B「confirmKeyAction 纯函数单测」声称失实**（全目录零引用，17 项全组件级）。P1：④只负向断言（改 none 也绿）/⑤reason-cancel 零覆盖/①TuiApp 桥三分支零组件级/⑤message 接线中段空白/F-22 截断边界未测 |
| 文档 | 0 | 6 | 硬声称零虚报（文件清单/五条语义/测试计数 1411/探针五项/关键决策链路逐项对上）。P1 全是过时口径：L1/L9/§10.5 标题「待拍板」残留/F-07·F-21·F-22 状态列/README 索引行/TUI 规范 ConfirmPrompt 键位行/skill §三——**均已由监看方直改订正** |
| skill | 2 | 5 | **§三「审批卡交互铁律」整节反转**（旧：默认选中 y/CR=批准/严禁发文本——批2b 后三处全错，照旧文操作既用不上审批期插话又撞新陷阱：空草稿首字母 n=拒卡、ESC=拒卡）；§五.1 自伤条同废——**已重写**（键位表+监看者新陷阱+级联拒前提+Ctrl+U 例外+pty 指纹）。P1：§二末行「默认选中执行」/§四 F-21 零提示/§七免卡「待实测」过时——均已改 |

**偏差②裁决（四面一致，已转正 §10.5）**：维持「空草稿 y 快捷、草稿非空失效」实现，文档明示两级口径。理由：①y 单键是主应答路径，改「选中+Enter」两步为小概率误打牺牲高频主路径，交互账算不过来；②误批可见非静默（卡上明示键位+即时反馈），剩余字符经①进草稿排队可补救——与 F-14「静默危害」不同类；③opencode 无单字母立即确认键（仅 ←→/h/l+Enter+Esc），对照约束不存在；④真病灶是草稿判定基准与可见输入框脱钩（代码 P1-1），修基准后「草稿非空=正在打字=快捷让位」语义才成立。反向出口留档：若后续 dogfood 出现「打 yes 误批」真实案例，最小改法=空草稿 y/n 改两步确认（产品决策重开拍板）。

**修复分工**：文档面/skill 面/TUI 规范/README 监看方直改完成（上文订正即终态）；产品码+测试（P0×3 代码测试在内共 14 项+P2 顺手 7 项）**派 ECode 修复批批2b-fix**（任务书经仓库根文件派发，serve 会话 37ce3993）。

#### §13.8a 批2b-fix 实施记录（2026-08-28 深夜，ECode 两轮交付+监看方外部验收直改）

**ECode 交付**（轮1 665.6s/30 审批/¥8.07 **撞 50 迭代上限被截断**——F-21 自身场景再实证：宿主跑旧 loop 无提示，监看方从历史文件尾部停在 tool_result 识别，serve 续接轮2 460.8s/44 审批/¥10.06 完成）：
- 代码：P0-1 展开态 `EXPAND_RESERVE=9` 经 sectionBudget 封顶（24 行终端 ≈13 行，删 ×3 公式与占位常量）；P1-1 选**方案 B**（新 `src/tui/draftPort.ts` InputDraftPort 端口：InputStream 挂权威 read+onRegisterDraft/onDraftChange，TuiApp mainDraft 镜像+应答三点清+nextInsertSeq 自增计数器治 P2-4）；P1-2 `key={use.id}`；P1-3 剥头引号+`VAR=` 前缀；P1-4 useMemo；P1-5 `done` 标志覆盖全部 8 处 break（两个 continue 路径自然纳入）。
- 测试：新 `confirmKeyAction.test.ts` 22 例纯函数直测（方向键回绕×6/快捷与让位×8/Enter 防误批×3/理由×2 等——测试 P0-B 兑现）；新 `approvalDraft.test.tsx` TuiApp 桥（三分支/显式选择放行/基线播种(a)/②桥级锁）；loop +3（continue×2+break 不误报）；bash +3（30KB 截断边界/空名警告/孤立空行）；ConfirmPrompt ②用例改发 `y` 断言 draftKey（P0-A 修）。

**监看方外部验收三轮直改**（ECode 交付的缺口，逐一定位实证）：
1. **onDraftChange 接线漏**（ECode 漏一根线）：InputStream 提供变化回调但 TuiApp 未传——mainDraft 恒挂载时空串，②运行时失效且组件级测试测不到 prop 缺线。修=接线一行+submit 显式清镜像不变量+`readDraft` 事件时直读权威端口（ConfirmPrompt/App/Conversation 三层透传，hasDraft 判定不再依赖渲染镜像——方案 B「事件时读权威」的彻底化）。
2. **理由两例注释虚报「拦截宿主 send」无实现**：补端到端断言（理由串经 approval/respond.message→broker 反馈→tool_result「用户拒绝了本次操作：{理由}」回喂 mock 第二轮 messages；空理由走「用户已取消」分支）——审阅 P1 缺口4 本体补齐。
3. **负载稳定性**：键路由类用例全量并行下键丢/帧滞（帧 dump 实证 6 个 r 裸进输入框）——幂等重试 guard（r 未生效可重发/理由没打全 Esc 回退重试/Enter 重发安全）+「y 放行」改显式选择（→+Enter，不依赖退格清空）+理由两例拆独立 `approvalReason.test.tsx`；②桥级锁用例草稿首字符不能是 y（空草稿 y=立即快捷批准是裁决保留语义）。
4. 附带实证：ink-testing 多字符单发在卡键路由下的事件拆分语义不可依赖（审批卡测试逐键发）；连续 \x7f 吞键老坑在负载下放大。

**终态**：1449 总（较批2b +38）；tsc 净；**探针双过**——pty-confirm-keyboard 五场景全过（B1-B4）、pty-overscreen O1/O3/O4 全过全程无 ESC[3J（P0-1 真机实证）。**已知并行 flake 留观**：approvalReason 两例+checkpoint rename 例+TuiApp 粘贴例在全量并行下稳定/偶发红——单文件×6、成对跑（与 TuiApp.test 同进程）、4 文件组均绿，根因（端口读数与视觉帧矛盾）在合理预算内未定位，按 git.test /undo 20s 先例记环境性 flake 非回归（判定依据：单跑复核绿+纯函数/组件/真机三层功能验证齐全）；若后续批次复现扩大再立案。批2b-fix 成本合计 ≈¥18.1（两轮 LLM）。

### 13.9 问题账清理批（2026-08-29 上午，用户拍板后执行，提交 50898f2，ECode 四轮 ≈¥11.8）

用户认可界面批全部选型并推送 origin 后的清账批：
- **F-23（P1 已修）**：serve prompt op 前置命令分流——host 可执行（/help /stats /cost /clear /compact）本地跑（`routed:'Command'+output`）；TUI 专属面板/未知名 → `SLASH_COMMAND_TUI_ONLY` 明确报错——**绝不落 LLM**。真机三连验证过（/help 返命令列表、/model 与 /nope 报错）。web 端经 systemMsg 帧可见。
- **F-26（P2 已修）**：/output 子代理条目「§ 裸 id」→「§ 时间+首行摘要（2KB 限读）+（历史会话）」，扫描上限 30 条（transcript 无项目归属标记，以最近跨项目+历史标注取代过滤）。
- **F-27（P2 已修）**：serve stop 接线——回执 `stopping:true` → mux 断开 → registry dispose → LogStore 同步 flush → exit；注册文件先删防 watchdog 误判接管。真机验证优雅退出+文件清理。
- **文档债**：builtin.ts:132 内建手册日志路径修正（~/.ecode/logs→cwd 级）。
- **flake 调查（有界）**：AppWarnLine.test 缺 afterEach(cleanup) 已补（本批全量中它确实掉帧红——佐证 Alt+V 破案真因）；结论=**跨文件遗留挂载叠加**（全量并行时其他无 cleanup 的 render 文件挂载实例与本文件 TuiApp 挂载期工作在共享事件循环上叠加掉帧）——**30+ render 测试文件待批量补 afterEach(cleanup)**（机械卫生修复，记档下批）。
- 终态：全量 1502（净增 9）/tsc 净/3 红均 flake 家族单跑绿；F-23/F-27 真机双验过。

### 13.10 清账批 II（2026-08-29 上午，提交 eb95a18，ECode 三轮 ≈¥6.2）

- **批量 afterEach(cleanup)**（30+ render 测试文件，48 文件改动主体）：**flake 家族从 6 种缩到 2 种**——监看方 3 连跑实测（subagent/git.test/AppWarnLine/TuiApp-paste 全消失），残余 checkpoint rename+approvalReason 两只为深层时序敏感（后者 12 次重试不救=结构性争抢非丢键，另案处理）；
- **F-29（新编，P1 已修）**：web store 帧 switch 缺 `case 'warn'`——maxIter 耗尽/loop-guard 告警在 web 端被 default 丢弃（web 用户拿假完成）；补 case 渲染 ⚠ 行+web 测试；
- **F-09 已修**：工具组标签列宽截断（bash→bas）；**F-06 已修**：展开态时间标记（走 V 线预算）；**F-11 已修**：提示词层 todo 引导；
- approvalReason 重试稳健化 6→12（单跑 6 次 2 红 4 绿定性纯时序）；
- 终验：tsc 净；3 连跑 1510 总（2-3 红均残余家族）；web 30/30+build；键盘探针 5/5；**dist 已重建**（10:25，F-08 教训例行项）。
- 余留在账：**F-16（P1）**（验收冲突死磕不报告——提示词层，下批）/**F-07 三键接入待拍板**/残余 flake 两只（另案）。
- **F 台账收口（F-23~F-29）**：除 F-16/F-07 在账外全部已修——F-23~F-27（§13.9）、F-28（§13.4a）、F-09/F-06/F-11/F-29（§13.10 与 §13.6 挂账档）。

### 13.11 三面审阅+清账批 III（2026-08-29，提交 687a608 文档批+ac2c925 代码批）

用户指令「写文档+压缩前落盘+多角色审阅+skill 优化」触发三面并行审阅（界面批/清账批 I/II 当时未审面）：
- **文档面**（零虚报 P0=0）：六处口径修复——§13.5 选型转已认可/两索引补 F-07 在账/九批计数/F 台账收口行/数字口径/方案转名 `_已完成` 零断链；
- **skill 面**（P0×1+P1×5+结构）：换 v2 终稿 63→35 行——修正**档位可视化反读**（旧文按序读成 full-access=⛔）、新增 F-23 命令分流/自举七步工作流/cwd 双 dist 坑/web 告警可见性、键位三处合并速查表；
- **代码面**（P0×1+P1×4+P2×8）：**P0-1 = accept-edits 漏拦项目级 `.ecode/settings*.json`**（write settings.local.json 自授权 Hook 全程零确认——权限自授权链，真实可利用）——isProjectEcodeSettings 补丁照卡；P1：Ctrl+R 搜索态双写/Tab 切档时滞免确认窗口（选型=忙碌拒绝 BUSY）/web clear 死帧/stop 未拒 device 档；P2 七项（双击 Esc 守卫/fail-open 反转/热路径 IO/两处虚报测试/serve help 过滤/stop 留痕/裸 / 拒绝）。
- **F-30（监看基建，同日）**：serve-watch 危险黑名单第三次误伤迭代——heredoc payload 触发正则（cat >> 测试文件）→ 误拒+停自动应答→审批挂 15min 连环 30min 浪费；修=**DANGER 只扫命令首行**。「自动应答器自己的判定逻辑就是新故障面」三连实证。
- 终态：1526 总+tsc 净+web 30/30+双探针过；残余 flake 家族不变（checkpoint+approvalReason 单跑绿）。

### 13.12 F-16 收官（2026-08-29，监看方直改）

F-16（P1 验收冲突死磕不报告）提示词层修复：system.ts 注意事项段补「验收标准与现场冲突时先报告」——环境性失败先小成本复核定性（单跑/换环境对照），确属环境问题即停下报告+建议放宽，不死磕。F 台账 **P1 清零**（在账仅 F-07 三键待用户拍板）。本方案全部已知问题处理完毕。

### 13.13 F-07 档A 收官（2026-08-29，用户拍板档A，ECode 一轮 ¥5.95）

edit_file/write_file 审批卡「本会话记住」第三键：REMEMBER_TOOLS 会话级白名单+宿主侧双重敏感硬门（敏感永远交互）+broker 级联只放 always 决策 pending（敏感卡防级联误放）。bash 不给键（档B 观察档A 后另批）。**F-07 止痛点闭环：批2b ①审批期打字排队+档A 会话记住，13+ 连弹疲劳双解**。F 台账在账项清零（余均为挂账档/另案）。

### 13.14 TUI 排版批 + F-31 中断 + 交互快批 F-32~F-35（2026-08-29，排版批三笔+快批 d60c18c）

- **排版批**（39ea5ca 前后三笔，CC 对比文档输入）：`src/tui/layout.ts` 排版常量表唯一出处（INDENT{icon:2,gutter:5,content:5,sub:2}/GAP/WIDTH 三公式归一）+ gutter `"  ⎿  "` 内容列悬挂缩进（CC MessageResponse 模式）+ 组内工具行行首图标列（用户对照 CC 指出后补）；差距清单 `layout.md`（五维双锚点）。
- **F-31（P1，e343d51）卡上 Ctrl+C=拒卡+中断整轮**：用户问「为啥按一下不能直接退出 loop」→四点计时插桩（pressed/received/surfaced/turn_finished，debug logstore）+探针 `pty-interrupt-latency-probe.cjs` 三场景实证中断链路 4-10ms 干脆→真凶=审批卡期 Ctrl+C 只拒卡、loop 继续下一迭代；修=Ctrl+C（拒卡+onInterruptTurn 中断整轮）/Esc（仅拒卡）分家；48 测+键盘探针过。
- **交互快批（F-32~F-35，d60c18c）**：F-32 审批卡默认选中 y——空草稿 Enter 直接批准（**用户拍板翻案批2b④**；草稿非空 Enter 仍走插话防误批，防误批面收窄为单场景）+测试/探针断言翻转；F-33 沙箱随时可切（**翻案清账 III P1-2 的 BUSY 拒绝**）——toolCtx.sandbox 轮初快照改 getter 实时 makeSandbox（getter 内 this 指字面量须 hostSelf 捕获，实证 spread getter 行为后修），宿主/TUI 双忙碌拦截废除、提档审批保留；F-34 OutputViewer 全文链路——item/read 内存 mirror miss 时 fallback HistoryStore restoreFull（投影派压缩不删消息、冷会话可得），「全文拉取失败」警告改准确口径；F-35 插话引导包装——pollUserInput 注入带「任务执行中的补充+随后继续原任务」双指令（CC wrapCommandText 同款），防只应插话丢原任务。
- **F-37（销案重定性）**：疑似「maxIterations 截轮静默」——实查 F-21 的 onWarn→warn 帧全端到（TUI 告警中心/web case 'warn'）+maxIterations 本就可配，产品无缺陷；真缺口=监看工具 serve-watch 不打 warn/notice 帧（监看方误判静默）——已补监看脚本两分支。
- **快批过程实证两条**：①50 轮迭代上限两连撞（四项任务书+43 审批往返烧尽 maxIterations=50，warn 帧发了监看看不见）——多任务书派活要么拆批要么调 config.maxIterations；②Ink 把同步连发退格合并成单事件只删 1 字（approvalDraft 实证：残稿 3 字被 Enter 提交成「已排队」插话）——测试退格须逐 write+按帧验证清空。
- **工作方式变更（2026-08-29 用户拍板）**：serve 派活 ECode 的 dogfood 循环停用——后续修复由监看方（ZCode）直改+自测（pty 探针/ink-testing），skill 不再迭代。
- 终态：全量 1536 过（残余 flake 仅 approvalReason×2+approvalDraft 并发时序，单跑绿）+tsc 净+键盘探针 5/5；F 台账在账：F-36 布局统一批 II（下一批，直改模式首单）。

### 13.15 F-36 布局统一批（2026-08-29，直改模式首单，监看方实施）

用户验收快批时定性「markdown 排列乱」收口：**第一列只放图标，所有文字含折行续行从第二列起**。调研双面（CC 源码 `claude-code-main` 组件级实证 + 网上互证：Trevor Fox 复制结构分析/awesome-tui-design spec）→ 规则清单（左轨双槽 2/5、正文列 flex+内容宽预折、续行布局副产品、用户消息例外）→ 留档 `docs/解析/2026-08-29_F36布局统一批-前后帧快照留档.md`。
实施：`MessageRow.tsx` 栅格唯一实现（● 槽+正文列）+ AssistantMessage 两态/压缩回退标记/降级提示全进栅格 + 折行宽改 WIDTH.body（续行对齐关键）+ 间距统一 marginTop 级联（ToolGroupView 去 marginBottom/ActivityBar 补位）。验证：全量 1539+tsc 净+overscreen 4 轮+键盘探针 5/5+pty 真机帧（60 列长中文 markdown 续行四行全对齐第 2 列）。
顺带实证两条探针坑：①`ECODE_HOME` 是臆造——隔离走 ECODE_BASE_URL/ECODE_MODEL env（overscreen 先例）；②文本与 `\r` 合并 write=粘贴内嵌不提交，须分开 write。

### 13.16 F-38 提示体系秩序化（2026-08-29，直改模式，用户验收 F-36 后提出）

用户诉求：中断黄字提示不要；提示类统一最下一行；内容框只放内容（例外=工具/MCP 执行错误属内容）；临时提示一会儿消失；错误分类有秩序；图标与文字拉开间距。实施两层提示体系：
- **底部告警行（notices）**=系统运行时发生的事（协议 warn 帧/loop-guard/工具全文拉取失败/审批认领/skill 冲突）：分级驻留 TTL——error 常驻、warn 12s、info 5s（isFresh+deriveNoticeLine(now)，/warnings 仍看全部历史含过期）；过期条目不上行也不计余量；TuiApp 挂 1s tick 时钟仅在有可过期条目时启用（不空转）；图标与文字双空格（⚠ ⚠ 双图标根治：host guardAbort 文本去 ⚠ 前缀，图标归渲染层）。
- **输入框上方（systemMsgs）**=用户主动操作触发的反馈（/cost 等命令输出多行完整显示、沙箱切档确认、发送失败、hook 拦截）：保留原渲染位但加 5s TTL 自动消失（不再常驻）+分级着色（默认 dim，失败类 9 处调用点传 'warn' 黄色）。命令输出不塞底部行——会被单行截断损可用性。
- ActivityBar aborted 黄字横幅废除（空行占位防跳动），中断提示改「已中断，内容已保留」dim 5s。
- 分类秩序：内容区=对话内容+工具/MCP 执行错误（✗ 红结果）；底部行=系统告警（分级着色+驻留策略）；上方=操作反馈（TTL 消失）。
- 验证：notices 20/20（新增 TTL 边界/过期不上行/error 恒 fresh 用例）+ActivityBar 断言反转+全量 1541（4 失败均已知 flake 家族单跑绿）+tsc 净+新探针 `pty-interrupt-notice-probe.cjs` 3/3（无 ⚠ 横幅/dim 提示出现/TTL 末次重绘无提示行——增量判定需以末次 idle 重绘为界，Ink 小 diff 重绘会重写旧行）。

### 13.17 F-39 bash 输出截断对标 CC（2026-08-29，直改模式，用户问「4KB 有调研吗」引出）

用户三问驱动（4KB 哪来的配置/有调研吗/能对标 CC 吗）→ 全链路盘点：三层截断（工具层 30KB 给 LLM/协议帧 4KB 广播摘要/read 1MB 按需），4KB 是 M14-C1⑤ 拍板值（帧轻量级数）无对标；但深挖发现**真问题：config `bashMaxOutputBytes` 字段定义后零消费——「可配」悬空**（truncateOutput 硬编码 30_720，详设 §5.1「可配」未兑现），且中截的中间部分模型与用户都不可回看（CC 对标差距）。实施对标 CC toolLimits：
- **默认 30KB→50KB**（对标 CC DEFAULT_MAX_RESULT_SIZE_CHARS 50K chars；config.ts 示例+手册表同步）；
- **接线悬空修复**：ToolContext 增 `maxOutputBytes`（宿主装配从 config 填）+ `toolUseId`（loop ctxForCall 直挂——此前只经 onBeforeWrite 闭包间接可及）；
- **persist-to-disk（CC 同策略）**：会话内超限输出落盘 `~/.ecode/sessions/<sid>.outputs/<toolUseId>.log`，中截标记带路径（模型/用户 read_file 回看完整输出——中间部分不再丢失）；无会话信息（argv 兜底）或写失败退化纯中截。
- 边界说明：协议帧 4KB 不动（F-34 后只是折叠摘要长度，全文有 item/read 兜底）；CC 的 UI 行级截断（terminalWidth−10）不适用 ECode 多端广播架构。
- 验证：bash/config/loop/session 组 119 过（新增落盘端到端用例临时 home+os.homedir spy；断言教训 yes X 输出是 X\n 非 XXX）+tsc 净。

### 13.18 F-41 /restart 无法重启（2026-08-29，直改模式，用户真机报告）

用户真机（PS + rpp-web 现场）：/restart 打印提示后直接回 PS 提示符，新实例未接管。探针 `pty-restart-probe.cjs` 逐层剥出**两个叠加根因**：
- **Windows detached = CREATE_NEW_CONSOLE**：原实现 spawn(detached:true) 在 Windows 给子进程开自己的新控制台窗口而非接管当前终端，父 200ms 后 exit → 用户回 PS，观感即「没重启」。POSIX 无此概念（detached=子进程组独立+同一 tty，语义成立）→ 平台分流。
- **spawn 不继承 execArgv（tsx 形态叠加）**：实测子进程 execArgv=[]，tsx loader（--import tsx/dist/loader.mjs）丢失 → node 裸跑 .ts（22.x type stripping 起得来但 '.js' 后缀 import 解析失败）→ ERR_MODULE_NOT_FOUND src/services/config.js 秒崩。修复：execArgv 拼进 spawn argv（spawn 无此选项）。此 bug 使**tsx 开发形态下 /restart 从来就是坏的**。
- 附带：attach 等待模式（Windows 子继承当前控制台跑 TUI、父静默等其退出再退，不再延迟 exit 防 PS 提示符与 TUI 交错）+ process.stdin.pause() 释放 conpty 输入投递。
- 验证：TuiApp /restart 单测两例（onRestart 句柄被调+缺省提示不崩——vi.waitFor 等异步 setTimeout，flush 定长等待会错过）+探针链路实证（修复前子进程秒崩实锤/修复后子进程经 loader 起到 Ink mount）+tsc 净。边界：探针 pty 对双进程 conpty 输入投递模拟有限，回显探测 WARN 挂观察，最终以用户真机 dist 形态复测为准。


### 13.19 F-42 代码块无边框化（2026-08-29，直改模式，用户点名）

用户从终端复制 ECode 输出的代码时，CodeBlock 的 ╭─│╰ round 边框字符混进剪贴板，粘到 F12/编辑器无法直接使用。pty 探针实机复现（mock 回复带代码块→帧内 ╭──╮ 边框实锤）后修：CodeBlock 去 borderStyle，区分靠 backgroundColor（theme 新增 codeBg #1E2126——背景色不进剪贴板）+ 2 空格缩进（合法空格，CC 代码块同为无装饰字符形态）；顺带每代码块省 2 行边框行（V 线预算减压）。快照：`  const x = 1` 纯净输出；36 测过+tsc 净。
> 工作方式备注：rpp-web 图片反查实机测试因 node-sass 4.14 不支持本机 Node 22（441 编译错误）挂起——本机 nvm 有 v14.18.0 可切，待用户确认再继续（切版本影响当前 shell 的 ECode 开发环境）。

### 13.20 F-43 diff 渲染三修（2026-08-29，直改模式，用户贴真机帧点名）

用户贴 ECode 真机 diff 输出帧点名两问题：**路径 4 反斜杠** + **每行 diff 都有 ⎿**（要求调研参考）。调研：CC 源码（F-36 报告已证 MessageResponse=单 ⎿ 5 列+内容列 flex，多行输出仅首行 ⎿）+ opencode diff-viewer（paddingLeft=1 ±着色无逐行符号）+ unified diff 终端渲染惯例——三方一致：逐行符号是反模式。三修：
- **patchHeader 反斜杠翻倍根治**：F-05 的 `.replace(/\/g,'\\')` 在八进制解码后把 Windows 路径反斜杠再翻倍（D:\xunfei→D:\xunfei，终端复制/传输再翻倍成 4 个）——去掉该行，解码结果原样输出；
- **diff 头精简**：裁掉 jsdiff 的 svn 风格 `Index:`/`=====` 冗余行（git diff 无此二行，工具行已显示路径）；路径相对化正斜杠（LLM 常传绝对路径，diff 头占满一行）；
- **ToolGroupView 单 ⎿ 化**：展开输出（diff/普通/todo 三分支）从「每行一个 Gutter」改「单 ⎿ + 内容列 flex」（标题行并入内容列，一个工具结果恒一个 ⎿，续行悬挂第 5 列）——CC MessageResponse 同构；折叠 preview 单行 ⎿ 语义不变。
- 快照新增 ③b edit_file diff 样例留档；50 测过+tsc 净（全量后台跑）。

### 13.21 F-44 上下文占用/余量显示（2026-08-29，直改模式，用户问「占用量和余量没显示吗」）

用户确认 TUI 无上下文占用/余量显示（StatusBar 的 tok 是 usage 计费累计，非上下文占用；压缩判定用的估算值也没透出）。基建本已齐全（estimateContextTokens 压缩判定 + ctxWindowCache runLoop 装配时解析缓存），缺的只是透出——协议帧加两个字段即通：
- **usage 帧增 contextUsed/contextWindow 可选字段**：占用=本轮 prompt 全量 `input+cacheRead`（API 真值，比本地估算准；GLM 端点自动 KV 缓存实证后 cacheRead 恒在）；窗口=宿主 ctxWindowCache（config 覆盖 > models.dev > 兜底表）；
- **StatusBar ctx 段**：`ctx 45k/200k`（k 格式复用 formatTokens），**≥90% 窗口（压缩触发阈值 0.9）转 warn 黄加粗**——余量将尽、下轮即压的提前信号；字段缺省不显示（旧宿主兼容）；
- App/TuiApp 透传（usage case 帧缺省保留上次值）；web 端不消费 usage 帧，不扩。
- 验证：StatusBar 新 3 例（k 格式/阈值不崩/缺字段不显示）+真机探针（110 列帧显 `ctx 10/32.0`——mock 模型走安全默认 32k 窗口）+tsc 净。

### 13.22 F-45 idle 态快捷键提示去除（2026-08-29，直改模式，用户点名）

用户点名状态栏「⏎ 发送 / 命令 · ↑↓ 历史」这些教学提示不用显示。ShortcutHint 的 default 上下文渲染为空（busy 态保留 Ctrl+C 中断——运行中怎么打断是关键信息，不在点名清单）；App 底行分隔符 ` · ` 随 hint 存在性条件渲染（防行尾悬空分隔符）。验证：ShortcutHint 测试翻转为空断言 3 例+全量后台跑。

### 13.23 F-46 子代理运行期可见（2026-08-29，直改模式，用户真机报告）

用户真机：子代理（task 工具）运行期「无法看在干什么、无法展开、无法用斜杠命令」。三堵点三修：
- **busy 斜杠全拦**：InputStream 的 busy 拦截放行只读白名单 `output/warnings/help`（纯读快照零竞态）；/history 不放行（busy 下选择恢复会话危险）。/output 正是看子代理的唯一渠道，被拦即堵死；
- **transcript 结束才落盘**：原实现 finally 全量重写（运行期文件不存在）——改为 execute 开始写 meta 行 + onToolStart/onToolResult/onWarn 逐条 append 事件行（异步 fire-and-forget，同步 IO 冻结渲染的 P2 先例规避；失败静默不掩盖执行）；结束仍全量重写 messages JSONL（权威可重放终态，事件行被覆盖）；
- **subagentSource 只读快照**：渲染格式化（事件行/message 行 → 人读文本，parse 失败原样）+ isGrowing=true + 500ms mtime 轮询 subscribe（照 taskFileSource 先例）。
- SubagentBar 的 activity（当前工具名）保持既有。验证：subagent 15 测（新增运行期快照断言：probe 工具 execute 内读 transcript 已含 meta+tool_start）+InputStream 白名单两段式用例（/output 放行 /clear 仍拦）+全量后台+tsc 净。
- 测试工程教训重申：bash 内联 node -e / python -c 注入含转义序列（\r 等）的代码到源文件必翻车（bash 双引号层级转义）——一律走 Edit 工具或落临时脚本文件。


### 13.24 F-46b Ctrl+T 直达输出面板（2026-08-29，直改模式，用户追问「不能用快捷键吗」）

用户追问实机测试与快捷键入口（busy 输斜杠命令语义怪）。追加：全局 useInput 加 **Ctrl+T**（overlay/confirm 期照旧抑制、busy 可用）——直达 /output 面板（子代理/后台任务/工具全文 transcript），busy 态提示更新为 `Ctrl+C 中断 · Ctrl+T 输出`；busy 斜杠白名单保留为备用入口。**真机探针 `pty-ctrlt-probe.cjs` 实证**：审批卡应答 → bash 运行中 → Ctrl+T → 面板打开（子代理 transcript 条目 § 可见）；判定特征用 PanelShell 底部「回车 查看 · Esc 返回」（首版用「输出」误命中状态栏提示）。TUI 577 过+tsc 净。


### 13.25 F-46c 子代理完整链真机探针（2026-08-29，直改模式，用户追问「实机测试了吗」）

用户二连追问实机覆盖面——补 `scripts/pty-subagent-probe.cjs`（mock 按请求区分主/子：子代理 system 含「独立子任务代理」标识；主循环第 1 轮回 tool_use task、之后回文本防无限派）：主循环派 task（readonly 免确认无审批卡）→ 子代理跑完结论回流 → Ctrl+T → 面板列表含本子代理条目 → 回车查看器渲染格式化 transcript，四项全过。**探针顺带揪出真 bug**：OutputListPage 的 agents 列表是 TuiApp 挂载时 useState 快照——本会话新起的子代理永不在列（面板打开也不刷新）——修为列表轮询刷新（tasks 同款 1s）；readFirstUserText 兼容 meta 事件行取 description 摘要。判定语义注意：结束后事件行被权威 messages 覆盖，面板摘要=首条 user 文本（prompt）。
