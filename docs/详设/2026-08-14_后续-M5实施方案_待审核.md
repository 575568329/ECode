---
layer: 详设
status: 草案待审阅（v8：四家源码调研重写压缩算法——opencode select 骨架 + claude-code 不变量 + aider 兜底 + models.dev + token usage 锚定）
scope: 后续（MVP 后）—— M5 = 上下文压缩；Skill → M6；MCP → M7
depends_on:
  - 解析/2026-08-14_上下文压缩调研_已完成.md（★ 压缩算法设计依据，必读）
  - 详设/2026-08-11_ECode-MVP详设_待审核.md（§0.4 非目标 / §6.2 错误分类）
  - 解析/2026-08-14_M5方案审阅意见_待处理.md（三角色审阅 P0 修订）
---

# ECode 后续 M5 实施方案：上下文压缩

> 日期：2026-08-14 · 状态：**草案待审阅 v4**
> **演进**：v1 三特性打包 → v2 拆分（压缩聚焦）→ v3 自动必做 → **v4 基于压缩调研重写**（[调研报告](../解析/2026-08-14_上下文压缩调研_已完成.md)）。
> v4 核心修正（用户指导 + Claude 官方实践）：
> 1. **Context 与 History 分离**——history 完整 append-only 永不压缩（对话历史是用户数据，保存好）；只压缩 runtime context。
> 2. **两级压缩**——Level 1 Tool Clearing（闲时压缩工具内容，低阈值）+ Level 2 Compaction（主动压缩对话，高阈值）。
> 3. **阈值留缓冲**（land the plane，claude-code 教训：64-75% 触发，留 25% 让任务先完成）。
>
> Skill → M6、MCP → M7 后置不变。标 ★ 已拍板。

---

## 1. 核心架构：Context 与 History 分离（★ 用户核心要求）

```
┌───────────────────────────────────────────────────────────┐
│ History（对话历史 · 永不压缩 · 完整保存）                    │
│ ~/.ecode/sessions/<id>.jsonl — append-only                 │
│ 用途：/history 恢复（拿完整历史）/ 审计 / 导出 / 用户数据    │
│ 压缩永远不动 history 文件                                    │
└───────────────────────────────────────────────────────────┘
                         │ 派生（restore / 新对话初始）
                         ▼
┌───────────────────────────────────────────────────────────┐
│ Context（runtime messages[] · 可压缩 · 喂 provider.run）    │
│                                                            │
│  两级压缩（TokenCounter 每轮 provider.run 前判定）：        │
│   Level 1 Tool Clearing（> 50% 窗口）                       │
│     tool_result 占位符化，保留 tool_use + 最近 N 次完整     │
│   Level 2 Compaction（> 75% 窗口 / 手动 /compact / 400）    │
│     summarize 旧消息 → [summary_msg, ...tail]              │
│                                                            │
│  压缩只改 context，history 文件不动                         │
└───────────────────────────────────────────────────────────┘
```

**与 v3 的关键差异**：
- ~~history boundary（restore 过滤压缩标记）~~ → **history 始终完整**，/history restore 拿全量历史（压缩是 runtime 优化，恢复是新会话起点，重置压缩态可接受）
- ~~单级 compaction~~ → **两级**（Tool Clearing + Compaction）
- ~~buffer 固定 13000~~ → **按 contextWindow 比例**（Level 2 留 25% 缓冲）

---

## 2. 目标 & 验收

**目标**：长对话不因上下文超限崩溃；工具输出不占满窗口；对话历史完整保存不受压缩影响。

**验收**：
- **Level 1 Tool Clearing**：context 超 50% 窗口时，旧 tool_result 占位符化（保留 tool_use + 最近 N 次），模型仍知执行过什么
- **Level 2 Compaction**：context 超 75% 窗口 / `/compact` / 400 兜底 → summarize 旧消息，原地替换 context
- **History 完整**：压缩后 `~/.ecode/sessions/*.jsonl` 仍是全量未压缩记录；`/history restore` 拿完整历史
- **摘要质量**：压缩后 LLM 仍引用压缩前的文件路径/技术决策
- **400 不重试**：context-too-long 走压缩，非退避重试

**不做**（后置）：
- Memory 系统（跨会话结构化记忆，Claude 第三原语）—— ECode 的 history 已承担部分，完整 Memory 后置
- 多级管线高级策略（snipCompact 删整段 / reactive / 锚点摘要）—— MVP 两级够用
- prompt cache 雪崩优化（clear_at_least）—— GLM 端点暂无 prompt cache
- Skill（M6）、MCP（M7）

---

## 3. 压缩算法（★ v8：四家源码调研，opencode 骨架 + claude-code 不变量 + aider 兜底）

> 设计依据：[上下文压缩源码调研](../解析/2026-08-14_上下文压缩源码调研_已完成.md)（claude-code/opencode/aider/codex 代码级细节）
> 组合：opencode `select`（骨架，最干净可抄）+ claude-code（API 不变量保护 + microCompact 占位符）+ opencode 滚动 summary（多次压缩关键）+ aider 递归（L3 兜底）

### 3.1 Level 1: Tool Clearing（闲时清工具内容，抄 claude-code microCompact）

**痛点**：tool_result（bash 30KB、read_file 大文件、grep 命中）是 token 大户。

**算法**（`src/services/toolClear.ts`，抄 claude-code `microCompact.ts`）：
- 只替换 **tool_result 的 content**（tool_use 不动——模型知道调过什么）
- 保留最近 K 条（默认 4）完整结果，其余 content → `'[旧工具结果已清理：约 X tokens，需要时重新执行]'`
- 无 LLM 调用（纯本地，便宜，每轮可跑）
- ★ **API 不变量保护**（抄 claude-code `adjustIndexToPreserveAPIInvariants`）：保留区有 tool_result 必带回配对的 tool_use，**绝不切断对**（否则 API 400）

### 3.2 Level 2: Compaction（主动压缩对话，opencode select + 滚动 summary）

**算法**（`src/services/compact.ts`，抄 opencode `compaction.ts`）：
```ts
export async function compactMessages(
  messages: Message[],
  opts: { provider: LLMProvider; providerReq: ProviderReq; previousSummary?: string },
): Promise<CompactResult>
```
**select 切分**（抄 opencode `:128`）：recent 预算（默认 8000 token）从尾部累加吃满；切分点那条消息按字符精确劈开。head（去摘要）+ recent（原样保留）。head 过滤掉旧 compaction 消息。

**★ 摘要 Prompt**（opencode `SUMMARY_TEMPLATE` 结构化 + claude-code `<analysis>` 草稿剥离）：
```
CRITICAL: 只用文本回复，不要调用任何工具。
先在 <analysis> 里按时序分析对话（用户意图/处理方式/关键决策/代码片段/文件名/错误及修复/用户反馈），
再在 <summary> 里按以下结构输出（保持段落顺序，即使空也保留）：
## 目标
- [用户想完成什么]
## 重要细节
- [约束/偏好/决策及原因/关键文件路径/函数名/命令/错误信息；无则 (none)]
## 工作状态
### 已完成 / ### 进行中 / ### 受阻
## 下一步
1. [立即的下一步]
## 相关文件
- [文件路径: 为什么重要]
规则：精炼要点不要散文；精确保留文件路径/符号/命令/错误串/URL；不要提及压缩过程。
```
产出后 **strip `<analysis>`**（草稿提升质量但无信息价值，抄 claude-code `formatCompactSummary`）。

**★ 滚动 summary**（抄 opencode `buildPrompt` `:161`，处理多次压缩的关键）：
- 有 previousSummary → prompt 加「用上述对话更新下面的锚定摘要：保留仍成立的细节，删除过时的，合并新事实」+ 上轮 summary
- 上轮 compaction 的 recent（上次原样保留的尾部）拼进这次摘要的 context
- **解决「多次压缩信息逐级丢失」**（单次全量重摘要会丢，滚动更新保留连续性）

**summary 消息格式**（抄 opencode Compaction 类型，runtime 结构不落 history）：
```ts
{ type: 'compaction', summary: string, recent: string }  // recent 供下轮滚动
```
喂 LLM 时，compaction 后的真实消息才进 context。

### 3.3 Level 3: 兜底（抄 aider 递归 / codex 开新窗口）

**触发**：L2 摘要后仍超限（极长历史），或摘要 LLM 调用本身塞不下。

**算法**（抄 aider `summarize_real`）：递归二分（尾部留 half 原样，头部摘要，仍超递归 depth+1，base case `len<=4 或 depth>3` 整段摘要）。
**终极兜底**（抄 codex `compact_token_budget`）：递归都失败 → 开新窗口（只保 system + 最近一条 user + 「历史已截断」提示），保证可用。

### 3.4 三级协调

```
每轮 provider.run 前 TokenCounter.estimate：
  tokens > effectiveWindow - 30000（L1 阈值）→ clearToolResults（无 LLM，便宜）
  tokens > effectiveWindow - 20000（L2 阈值）→ compactMessages（select + 滚动 summary）
  /compact 命令 → 直接 L2
  400 CONTEXT_TOO_LONG → L2 兜底（L2 仍超则 L3）
```
L1 先跑（工具大户，清完可能就不超 L2）；仍超才 L2；L2 塞不下才 L3。

### 3.5 失败降级（四家综合）

- **摘要 prompt 自身超限** → opencode 预检闸 `estimate(summaryPrompt) > context - output` 直接放弃
- **LLM 调用失败** → 静默 no-op 下轮重试（opencode）+ 多模型兜底（aider weak model 摘要）
- **熔断器** → 连续 3 次失败停（claude-code `MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3`）
- **compact 调用本身 413** → 按轮分组从头删重试（claude-code）/ 删最旧（codex）
- **都失败** → L3 硬截断保证可用

---

## 4. token 计数（★ v5 修正：usage 锚定 + 估算，不装 tokenizer）

**v4 错误**：装 gpt-tokenizer（~1MB，对 GLM 也是估算，过度）。

**v5（调研修正，抄 claude-code `tokenCountWithEstimation`）**：
- **主路径：API usage 锚定 + chars/4 增量估算**（零依赖，零模型差异）
- 每轮 provider.run 后，从 Delta.usage 的 `input_tokens` 更新**精确基准**（服务端算的，最准）
- 新消息用 chars/4 粗估（JSON/代码 chars/2，图片固定 ~2000）增量累加到基准
- 压缩/clearing 后标记「待重新锚定」（下次 usage 回来重置基准，防漂移）

```ts
// src/services/tokenizer.ts（零依赖，不装 gpt-tokenizer）
export class TokenCounter {
  private anchoredTokens = 0   // 最后一次 API usage 的 input_tokens（精确基准）
  private anchoredMsgCount = 0 // 锚定时 messages 长度
  private pendingEstimate = 0  // 锚定后新增量的估算

  /** provider.run 后调，用服务端精确 usage 重新锚定 */
  anchor(inputTokens: number, msgCount: number) {
    this.anchoredTokens = inputTokens
    this.anchoredMsgCount = msgCount
    this.pendingEstimate = 0
  }

  /** 估算当前 system + messages 的 token 数（锚定 + 增量 chars/4） */
  estimate(system: string, messages: Message[]): number {
    const sysTokens = Math.ceil(system.length / 4)
    if (messages.length < this.anchoredMsgCount) {
      // 压缩/clearing 后 messages 变短 → 全量估算（待下次 usage 重新锚定）
      return sysTokens + estimateMessagesChars(messages)
    }
    // 增量：锚定点之后的新消息
    let inc = 0
    for (let i = this.anchoredMsgCount; i < messages.length; i++) inc += estimateMessageChars(messages[i])
    return this.anchoredTokens + inc + sysTokens  // sys 可能变，每次算
  }
}
// estimateMessageChars: text → len/4；tool_use input JSON → len/2+20；tool_result → len/4+10
```

**为什么不用真实 tokenizer**（调研）：claude-code/codex/opencode 三家都不用——各模型 tokenizer 不同（tiktoken 只准 OpenAI），装重依赖且对 GLM 仍估算。usage 锚定有服务端精确基准防漂移，足够准。

---

## 5. contextWindow 获取（★ v7：models.dev 远程库，抄 opencode）

**v4 错误**：硬编码 128k（错值）。
**v5 不足**：内置查表写死——模型更新/新模型涌现表很快过时。
**v6 偏向 litellm**：但 litellm 是 Python 生态副产品，models.dev 更适合（见下）。
**v7（用户定 + 调研确认）**：用 **models.dev**（opencode 团队维护、自家产品在用、同 TS 栈可参考、收录智谱）。

**选型理由**（vs litellm）：
- models.dev：**opencode 自家产品在用**（吃狗粮，维护上心）、专门做模型库（聚焦）、**同 TS 栈**（`models-dev.ts` 集成代码 ECode 可直接参考）、[收录智谱](https://models.dev/labs/zhipuai)（GLM-5/GLM-5-Turbo/GLM-4.6）
- litellm：Python 生态、模型库是副产物（主业 proxy）

**GLM 真实窗口（★ 纠正 v4/v5 的 1M 错误）**：GLM-5 = **200K**（[z.ai/blog/glm-5 官方](https://z.ai/blog/glm-5)，训练期 32K→128K→200K 渐进扩展），非 1M。GLM-4.6 = 200K。

**四级 fallback**（抄 opencode `models-dev.ts`）：
```ts
// src/services/contextWindow.ts
const MODELS_URL = 'https://models.opencode.ai/api.json'  // opencode 维护，Flag 可切 models.dev
const CACHE_PATH = path.join(os.homedir(), '.ecode', 'cache', 'models.json')
const CACHE_TTL_MS = 5 * 60 * 1000  // 5min（抄 opencode Duration.minutes(5)）
// 构建期快照（抄 opencode OPENCODE_MODELS_DEV）—— esbuild define 注入，离线兜底
declare const ECODE_MODELS_SNAPSHOT: Record<string, unknown> | undefined
const FALLBACK_TABLE = { 'glm-5': 200_000, 'glm-4.6': 200_000, 'glm-4': 128_000, /* 应急最小表 */ }

export async function resolveContextWindow(model: string, configOverride?: number): Promise<number> {
  if (configOverride) return configOverride                          // 1. config 覆盖
  const db = await loadModelsDb()                                    // 2. models.dev（缓存+离线兜底）
  const found = lookupByModelName(db, model)                         //    canonical 名子串匹配
  if (found?.limit?.context) return found.limit.context              //    schema: limit.context
  const fb = matchFallbackTable(model, FALLBACK_TABLE)               // 3. 内置最小表
  if (fb) return fb
  process.stderr.write(`[CONTEXT] 未知模型 ${model}，用安全默认 32000。可配 contextWindow 覆盖。\n`)
  return 32_000                                                       // 4. 安全默认 + 告警
}

/** models.dev 加载（抄 opencode）：缓存命中→用缓存；过期→后台拉新；首次无缓存→同步拉(10s timeout) */
async function loadModelsDb(): Promise<ModelsDb> {
  // ① 缓存未过期 → 直接读（零网络）
  // ② 缓存过期/不存在 → 拉 MODELS_URL（10s timeout + 2 次退避 + 原子写 tempfile+rename + Flock 跨进程锁）
  // ③ 拉失败/无网络 → 构建期快照 ECODE_MODELS_SNAPSHOT（离线兜底）
}
```

**关键设计**（opencode 验证可行）：
- **5min 缓存**（抄 opencode，非 aider 的 24h——models.dev 更新更勤，5min 够新且不频繁拉）
- **离线兜底**：构建期打包 models.dev 快照（抄 opencode `OPENCODE_MODELS_DEV`，esbuild define 注入）+ 内置最小表
- **原子写** tempfile + rename（防半截写）+ **Flock 跨进程锁**（防多 CLI 并发刷）
- **10s timeout + 2 次退避**（不卡死启动）
- **匹配**：canonical 名子串（抄 claude-code longest-id-first）
- **effective = raw × 0.9**（留 headroom，抄 codex）

**阈值（绝对值）**：
```
effectiveWindow = resolveContextWindow(model) × 0.9
Level 1 Tool Clearing = effectiveWindow - 30000
Level 2 Compaction = effectiveWindow - 20000
```
GLM-5（200K）：eff 180k → L1 150k / L2 160k。

---

## 6. loop 集成（onBeforeRequest hook + 原地 mutate + P0 修订）

### 6.1 onBeforeRequest hook（架构师建议，单行注入）

LoopRunOptions 加：
```ts
onBeforeRequest?: (messages: Message[]) => Promise<{ messages: Message[]; compacted: boolean }>
```

loop 每轮 provider.run 前：
```ts
if (opts.onBeforeRequest) {
  const { messages: newMsgs, compacted } = await opts.onBeforeRequest(messages)
  if (compacted) {
    // P0-1：原地 mutate（TuiApp/cli 靠 messagesRef mutation）
    messages.length = 0; messages.push(...newMsgs)
    opts.callbacks.onCompacted?.(messages)  // P1-1：通知 UI 重建 committed
  }
}
```

### 6.2 onBeforeRequest 实现（两级 + 阈值）

```ts
const tokenCounter = new TokenCounter()
// v5：绝对值阈值（派生自真实 contextWindow，非百分比）
const ctx = resolveContextWindow(providerReq.model, providerReq.contextWindow)
const effectiveWindow = Math.floor(ctx * 0.9)
const LEVEL1_THRESHOLD = effectiveWindow - 30000  // Tool Clearing（工具大户早清）
const LEVEL2_THRESHOLD = effectiveWindow - 20000  // Compaction（留 buffer land the plane）

onBeforeRequest: async (messages) => {
  const tokens = tokenCounter.estimate(system, messages)
  // Level 1: Tool Clearing（低阈值，工具输出大户）
  if (tokens > LEVEL1_THRESHOLD) {
    const r = clearToolResults(messages, { keepRecent: 4 })
    if (r.clearedCount > 0) {
      messages = r.messages
      onWarn(`已清理 ${r.clearedCount} 个工具输出（释放 ~${r.freedTokens} tokens）`)
    }
  }
  // Level 2: Compaction（高阈值）
  const afterClear = tokenCounter.estimate(system, messages)
  if (afterClear > LEVEL2_THRESHOLD) {
    if (++compactCount > MAX_COMPACTS) { onWarn('建议 /clear 起新会话'); return { messages, compacted: true } }
    const r = await compactMessages(messages, { provider, providerReq })
    onWarn(`已压缩对话（移除 ${r.removedCount} 条）`)
    return { messages: r.messages, compacted: true }
  }
  return { messages, compacted: false }
}
```
**provider.run 后**：`tokenCounter.anchor(usage.input_tokens, messages.length)`（用服务端精确 usage 重新锚定基准）。

### 6.3 400 兜底分支（loop recoverable 内，错误码专属）

```ts
if (streamError.code === 'CONTEXT_TOO_LONG') {
  if (pushedThisRound) messages.pop()  // 回滚半截（P1-9）
  const r = await compactMessages(messages, { provider, providerReq })
  messages.length = 0; messages.push(...r.messages)  // 原地 mutate
  opts.callbacks.onCompacted?.(messages)
  continue
}
```

### 6.4 /compact 命令（手动 Level 2）

CommandRegistry action 扩 `'compact'` + 注册。TuiApp onCommand → compactMessages → 原地 mutate + setCommitted + tokenCounter.reset。

### 6.5 前置：errors.ts CONTEXT_TOO_LONG 分类（P0）

400 body 防御提取（`e.error?.error?.message ?? e.error?.message ?? e.message`）+ `/context|too long|prompt/i` → `CONTEXT_TOO_LONG` code。

---

## 7. History 不动（★ v4 核心修正：废弃 boundary 方案）

**v3 方案（废弃）**：压缩时 history append boundary 标记 + restore 过滤。——这污染了 history（压缩态写进对话历史）。

**v4 方案**：
- **history 文件永不压缩**：`~/.ecode/sessions/*.jsonl` 始终是完整 append-only 对话记录。
- **压缩只改 runtime context**（messages 内存），不写 history。
- **/history restore**：拿完整历史（从 history 文件全量读），重置 context 到未压缩态。——恢复是新会话起点，context 从完整历史开始（可能立即再触发压缩，但这是正确的——恢复本就是重新开始）。
- **runtime 压缩状态不持久化**：重启后 context 从 history 重建（完整），压缩是 session 内的 runtime 优化。

**结果**：对话历史完整保存（用户可回看全量、导出、审计）；LLM 上下文按需压缩（防超限）。两者职责清晰分离。

---

## 8. 决策结论（v4）

| # | 决策点 | 结论 | 理由 |
|---|---|---|---|
| **M5-D1** | Context vs History | **分离**：history 完整 append-only 永不压缩；只压缩 runtime context | 用户核心要求 + 数据安全 |
| **M5-D2** | 压缩层级 | **三级**：L1 Tool Clearing（抄 claude-code）+ L2 Compaction（opencode select）+ L3 兜底（aider 递归/codex 开窗）| v8 源码调研：四家组合，成本递增破坏性递增 |
| **M5-D3** | L2 算法 | **opencode select 切分 + 滚动 summary** | v8：opencode 最干净可抄；滚动 summary 解决多次压缩信息丢失 |
| **M5-D4** | L2 prompt | **opencode SUMMARY_TEMPLATE 结构化 + claude-code `<analysis>` 草稿剥离** | v8：结构化保字段齐全；analysis 提质量再剥离省 token |
| **M5-D5** | API 不变量 | **tool_use/tool_result 配对保护**（抄 claude-code adjustIndexToPreserveAPIInvariants）| v8：绝不切断对，否则压缩后 API 400 |
| **M5-D6** | 阈值 | **绝对值**（effectiveWindow - buffer），非百分比 | v5 修正：window 差 10 倍（32k-1M），百分比不准；L1=eff-30k，L2=eff-20k |
| **M5-D7** | token 计数 | **API usage 锚定 + chars/4 增量估算**（不装 tokenizer） | v5 修正：claude-code/codex/opencode 三家都不用真实 tokenizer；usage 锚点防漂移 |
| **M5-D8** | contextWindow 获取 | **models.dev 远程库 + 四级 fallback**：config 覆盖 > models.dev（5min缓存+构建期快照离线兜底）> 内置最小表 > 安全默认 32k | v7：内置查表会过时；models.dev（opencode 自用+同TS栈可参考+收录智谱）优于 litellm（Python副产品）；★ GLM-5=200K 非 1M |
| **M5-D8** | messages 替换 | **原地 mutate**（length=0; push）| P0：TuiApp/cli 靠 messagesRef mutation |
| **M5-D9** | CONTEXT_TOO_LONG | 错误码专属分支（非 retryable 死字段）| retryable 只写不读 |
| **M5-D10** | loop 集成 | onBeforeRequest hook（单行注入）| 架构师建议：心脏稳定，多级管线留位 |
| **M5-D11** | history 持久化 | **不持久化压缩态**（history 完整，压缩是 runtime）| v4 核心：history 是用户数据，保存好 |
| **M5-D12** | 压缩次数上限 | MAX_COMPACTS=2/会话 | 防 summary 的 summary 退化 |

---

## 9. 实施拆解

| 步 | 内容 | 依赖 | 心脏 |
|---|---|---|---|
| **P0** | errors.ts CONTEXT_TOO_LONG 分类 | 无 | 动（错误）|
| **P1** | tokenizer.ts（★ v5：usage 锚定 + chars/4 估算，**不装依赖**）| 无 | 零 |
| **P2** | contextWindow.ts（★ v7：models.dev 远程库 + 5min 缓存 + 构建期快照离线兜底 + 四级 fallback）| 无 | 零 |
| **P3** | toolClear.ts（Level 1，tool_result 占位符化）| P1 | 零 |
| **P4** | compact.ts（Level 2，aider 递归 + 强化 prompt + 降级）| 无 | 零 |
| **P5** | contextWindow 链路（ProviderCfg/ProviderReq/buildProviderReq 透传）| P2 | 零 |
| **P6** | /compact 命令 + TuiApp 分发（原地 mutate + committed 重建）| P4 | 零 |
| **P7** | loop onBeforeRequest hook + 两级协调 + 400 兜底 + onCompacted + usage 锚定 | P0-P5 | **动心脏** |
| **P8** | cli makeDeps 注入 onBeforeRequest（TokenCounter + 绝对值阈值）| P7 | 零 |

**顺序**：P0-P4（地基并行）→ P5（手动，独立交付）→ P6（自动两级 + 兜底）→ P7（注入）。
**history 不动**（无 P? 改 history.ts——v4 废弃 boundary，history 保持 M4 原样）。

---

## 10. 测试

### 单测
| 模块 | 场景 |
|---|---|
| errors | CONTEXT_TOO_LONG 分类 / 各端点 body / 非 context 400 不误判 |
| tokenizer | TokenCounter.estimate（锚定+增量 chars/4）/ anchor 重置 / 压缩后全量估算 / 各 block（text/tool_use/tool_result）|
| contextWindow | 四级 fallback（config 覆盖 > models.dev 查询 GLM-5=200K/GLM-4.6=200K > 内置表 > 默认+告警）/ 缓存命中/过期拉新/离线兜底 / 子串匹配 |
| toolClear | tool_result 占位符化 / 保留 tool_use / 保留最近 N 次 / **API 不变量：保留区 tool_result 必带回 tool_use 不切断对** |
| compact | select 切分（尾部累加+字符劈分）/ **滚动 summary（previousSummary+previousRecent 重摘要）** / strip `<analysis>` / L3 递归兜底 / 摘要失败降级硬截断 / **摘要保留文件路径**（质量验收）|

### 集成测
- Level 1：MockProvider 长序列含大 tool_result → 超 50% → clearToolResults 触发 → tool_result 占位符化（断言 tool_use 仍在）
- Level 2：超 75% → compactMessages → messages 原地替换 + onCompacted
- 两级协调：超 50% 先 Level 1，仍超 75% 再 Level 2
- 400 兜底：CONTEXT_TOO_LONG → 压缩 + continue（不退避重试）
- /compact：手动 → 原地 mutate（断言 messagesRef 引用不变）+ committed 重建
- **history 完整性**：压缩后读 session 文件 → 仍是全量未压缩（断言 history 未被压缩污染）

### 验收（摘要质量）
- 构造含具体文件路径 + 技术决策的对话 → Level 2 压缩 → 新轮问路径 → 断言 LLM 响应含路径

---

## 11. 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 两级压缩动心脏（onBeforeRequest + recoverable 分支）| 中 | hook 单行注入；集成测覆盖两级 |
| token 估算误差（chars/4 粗估）| 低 | **usage 锚定**防漂移（服务端精确基准）+ 400 兜底；不装 tokenizer 零模型差异 |
| 查表覆盖不全（新模型未收录）| 低 | **models.dev 远程库自动跟进**（opencode 团队维护，新模型 PR 即收录）+ config 覆盖 escape hatch + 离线兜底表 |
| models.dev 远程拉取失败/网络不可达 | 低 | 5min 缓存优先 + 构建期快照/内置表离线兜底 + config 覆盖 |
| Tool Clearing 丢失模型需要的历史工具结果 | 中 | 保留最近 N=4 次 + 占位符提示「需要时重调」+ 模型可重新调用 |
| 摘要丢关键上下文 | 中 | prompt 显式保留代码/路径/决策；验收含质量场景 |
| 摘要 LLM 失败 | 低 | 降级硬截断 |
| /history restore 后立即再触发压缩（history 完整 → context 大）| 低 | 恢复是新会话起点，压缩是正确行为（非 bug）|

---

## 12. 附录

### A. 关键文件改动
| 文件 | 改动 |
|---|---|
| `src/core/errors.ts` | CONTEXT_TOO_LONG 分类（P0）|
| `src/services/tokenizer.ts` | **新**（★ v5：usage 锚定 + chars/4 估算，**零依赖**）|
| `src/services/contextWindow.ts` | **新**（★ v7：models.dev 远程库 + 5min 缓存 + 构建期快照 + 四级 fallback）|
| `src/services/toolClear.ts` | **新**（L1，抄 claude-code microCompact + 不变量保护）|
| `src/services/compact.ts` | **新**（L2 opencode select + 滚动 summary + L3 aider 递归兜底）|
| `src/services/config.ts` + `providers/interface.ts` | contextWindow 透传（P5）|
| `src/commands/registry.ts` | action 扩 compact + /compact（P6）|
| `src/tui/TuiApp.tsx` | onCommand compact 分支 + 原地 mutate + committed（P6）|
| `src/core/loop.ts` | onBeforeRequest hook + 400 兜底 + onCompacted + usage 锚定（P7）|
| `src/cli/index.ts` | makeDeps 注入 onBeforeRequest（两级协调，P8）|
| `src/services/history.ts` | **不动**（history 完整保存）|
| `package.json` | **不加依赖**（v5：去掉 gpt-tokenizer）|

### B. 调研依据
- [Claude Platform Cookbook — Context Engineering](https://platform.claude.com/cookbook/tool-use-context-engineering-context-engineering-tools)（三原语：Tool Clearing / Compaction / Memory）
- [Hyperdev — claude-code 阈值改进](https://hyperdev.matsuoka.com/p/how-claude-code-got-better-by-protecting)（64-75% 触发，land the plane）
- [Mem0 / LangChain / JetBrains / Factory / Acon / Zylos](../解析/2026-08-14_上下文压缩调研_已完成.md)（迭代摘要 + 滑动窗口共识）

### C. Skill / MCP 后置（→ 合并至 M6）

不变（M5 不阻塞）：原规划 M6=Skill / M7=MCP，现**合并为 M6 一篇**（两个独立子系统，各自可独立交付）。
详设见 [2026-08-14_后续-M6实施方案_待审核.md](2026-08-14_后续-M6实施方案_待审核.md)：
- **Skill**：三层渐进式加载（清单注入 system prompt + LLM 自主调 SkillTool + body 作 tool_result）
- **MCP**：官方 SDK + stdio/HTTP transport + `mcp__server__tool` 适配 + 默认确认
- 两者都注册成 Tool 进 ToolRegistry，**心脏（AgentLoop）零改动**
