---
layer: n/a
status: 已完成（2026-08-16 全量实施 P0-P7；见文末实施记录）
scope: 后续（MVP 后）—— M9 = 改动安全网与质量闭环：hooks 接线缺口修复 / checkpoint//rewind / 编辑后 lint-test 回喂 / git 轻量集成 / 沙箱模式（默认关）/ 权限系统首步 / LSP 占位
depends_on:
  - 详设/2026-08-14_后续-M5实施方案_已完成.md（投影派：rewind 的对话回退复用投影标记语义）
  - 详设/2026-08-15_后续-M8实施方案_已完成.md（顺序依赖；WebFetch 的安全收口在 M9-D7 沙箱）
---

# ECode 后续 M9 实施方案：改动安全网与质量闭环

> 日期：2026-08-15 · 状态：**已完成（2026-08-16 全量实施 P0-P7，含偏差记录与问题清单——见文末实施记录）**
> 主题一句话：**改坏了能回去**（checkpoint + /rewind）、**改完自动验**（lint/test 回喂）、**权限有纵深**（沙箱模式 + 权限规则）；git 做轻量集成。
> 来源：功能差距盘点（四家成熟 CLI 对比）发现三个「四家全有、ECode 全无」的硬缺口中的两个半（安全网 / 质量闭环 / 沙箱纵深）。参考实现集中附录 B。

---

## 0. 目标与验收

**目标**：给「Agent 改文件」这个最高频高危动作建立纵深——事前有围栏（沙箱+权限规则）、事中有快照（checkpoint）、事后有验证（lint/test）、后悔有退路（/rewind 与 /undo）；顺带清偿 hooks 的四个接线缺口。

**验收**：
- **hooks 接线修复**（原观察区 D.5 四项）：SessionStart 的 additionalContext 注入首轮消息；PreToolUse verdict 全字段消费；dispatch 七处透传 AbortSignal；Stop 的 stop_reason 区分 aborted/turn-complete
- **checkpoint**：write_file/edit_file/bash 修改过的文件在改动前被快照；`/rewind` 面板选消息点 → 文件还原 + 对话回退（投影标记，不删历史）
- **lint/test 回喂**：编辑类工具成功后自动跑配置的 lint/test 命令，失败输出作为信息性 tool_result 回喂模型自纠；连续 2 次失败熔断
- **git 集成**：`autoCommit`（默认 **false**）开启时编辑轮末自动 commit（带 ECode 标记）；`/undo` 只回退 ECode 创建的 commit
- **沙箱**（**默认关，用户主动开**——default 档=现状确认弹窗，2026-08-16 拍板）：`/sandbox` 四档（default / read-only / workspace-write / **full-access 总放行**）；write_file/edit_file 越界拦截；read-only 下副作用工具整体拒绝；full-access 全免确认但**内置黑名单 + 用户可配 blockedCommands 通配清单仍硬拒**
- **权限系统首步**（M8 §11 储备前移）：三态规则引擎 + `Hook(owner)` 规则——扩展源 hook（skill/plugin）首次执行前查规则（allow/deny/ask），用户源 hooks 不问
- **快照治理**：每会话上限 100 个快照点（超限淘汰最旧，共享对象回收）；全局上限 50 个会话目录；>10MB 文件跳过快照并 warn；content hash 去重生效
- **还原安全**：还原前自动快照当前状态（撤销可撤销）；外部改动文件在确认弹窗中逐一标注，不静默覆盖

**不做**（后置）：进程级 OS 沙箱（codex 式 seccomp/Job Object——重基建，软沙箱先行）、diff 浏览器面板（会话级改动总览 UI）、LSP 诊断（§7 占位，M9 有余量才做否则 M10）、权限系统完整四维（Skill/Plugin/Mcp 规则与 UI 面板——M9 只做 Hook(owner) 首步）。

---

## 1. hooks 接线缺口修复（P0，~0.5 天）

> 2026-08-15 代码走读发现（原 M9 附录 D.5，2026-08-16 拍板前移为本方案 P0）：协议层已交付、接线层没铺完的四个缺口，量级都是案发现场几行 + 回归测试。

| 项 | 现状锚点 | 修法 |
|---|---|---|
| SessionStart 的 additionalContext 未消费 | `TuiApp.tsx:584/608` `.then` 只取 systemMessages（resume/startup 两处；2026-08-16 复核行号） | 注入首轮消息（典型用途恰是"会话开始注入环境信息"） |
| PreToolUse verdict 的 additionalContext/systemMessages 未消费 | `hooked.ts:67-74` 只看 block/updatedInput | block 时一并展示；context 可注入 tool_result |
| dispatch 的 `opts.signal` 七处接线点均未传 | 全部调用形态①（不传 opts） | 案发现场透传 AbortSignal，hook 子进程随 Ctrl+C 中断（当前仅超时兜底） |
| Stop 的 stop_reason 固定 `'turn-complete'` | `TuiApp.tsx:328`（2026-08-16 复核行号） | abort 路径带 `'aborted'` 再 dispatch |

## 2. checkpoint 文件快照 + /rewind（P1，~3-4 天）

**问题**：edit_file/bash 改坏文件后没有任何退路——不总在 git 里、也不该被迫为 Agent 的每次尝试污染 git 历史。

**流程图（事中快照 + 事后反悔两条线）**：

```
【事中：每次改文件前先拍快照】
  LLM 要执行 write_file / edit_file / bash
    └─► 弹确认，用户同意
          └─► ★ 快照：把即将被改的文件当前内容复制到
                ~/.ecode/checkpoints/<会话ID>/<序号>/
                （附 meta：时间 / 工具 / 改了哪些文件）
                └─► 才真正执行修改

【事后：用户反悔了 /rewind】
  面板列出快照点（时间 + 当次改了什么）
          └─► 选一个点，界面再确认一次（列出将还原的文件）
                └─► 文件还原：选中点（含）往回按时间倒序放回去
                （后改的先还原，一层层退回去）
                └─► 对话回退：追加一行 rewind 标记
                      ★ 历史文件一个字不删——只是之后的对话不再喂给 LLM
                      （与压缩 boundary 同机制，/history 仍能回看全部）
                      └─► 回到那个时刻，继续聊
```

**详细讲解**：

**快照机制**（`src/services/checkpoint.ts`）：
- **触发点**：副作用工具（write_file/edit_file/bash）`execute` **前**、确认通过后——通过 `ToolContext` 注入可选 `onBeforeWrite?(paths: string[])` 回调，cli/TuiApp 的 makeDeps 装配（**心脏只在 ToolContext 透传，不认识 checkpoint 概念**）
- **快照内容**：「本次将修改的文件」的改动前副本（write/edit 从入参取 path；bash 不解析命令——快照 cwd 下**近修改集**：git 有则 `git status` 变更文件，无 git 则跳过 bash 快照并记 warn）
- **存储（content-addressed 布局，M9-D14）**：`~/.ecode/checkpoints/<sessionId>/` 下两半——`objects/<内容hash>` 对象库 + `<seq>/meta.json`（消息 id / 工具 / 时间 / 文件清单：原路径 → hash 引用）。快照 = 读文件算 hash → objects 未有才写入（**同内容多轮未变只存一份**，2026-08-16 一手核读吸收，opencode `snapshot.ts` 同款，100 点 × 多文件的全量 copy 膨胀解药）→ meta 记引用；还原 = 按 meta 引用从 objects 取内容写回。对象本体 **copy 而非硬链接**（Windows 下工作区与 home 常跨卷，硬链接不可用；快照对象是文本文件，体量可接受）
- **治理**：每会话 100 点上限（淘汰最旧——删 `<seq>/` 目录后重扫剩余 meta 引用集，未被引用的 objects 一并回收；多点共享对象不可直接删）；**全局会话目录上限 50 个**（按目录 mtime 淘汰最旧会话，防跨会话无限累积）；单文件 >10MB 跳过 + warn；会话保留跨重启（`/rewind` 恢复历史会话后仍可用）

**/rewind**（命令 + overlay 面板）：
- 面板列出快照点（时间 + 工具 + 文件摘要）→ 选中：
  1. **文件还原**：**选中点自身及其之后**的全部快照按时间逆序还原（后改的先还原）——语义 = 撤销该点起的全部改动、回到该点**执行前**；结果等价于每个受影响文件取它在被还原范围内最早的快照副本（opencode「每文件取最早快照」同款）
  2. **对话回退**：**投影派做法**——往 history 追加一行 `{"rewind_to": <seq>}` 标记，`buildContextMessages` 识别最后一个 rewind 标记截断投影，投影保留至该 seq 对应消息**之前**（当次改动不进上下文；与 boundary 同机制、append-only 不删历史；`/history` 回看完整）
     **类型与 UI 配套**（审阅补）：`HistoryLine` 联合扩展 `RewindLine` 变体 + `isRewind` 守卫（core/types.ts，与 BoundaryLine 同层）；`commit.ts` 的 `messagesToCommitted` 对 rewind 行生成 `⇺ 已回退至 N` 标记（boundary 的 compacted 标记同款处理——否则新变体进 filter 会被当 Message 渲染炸 UI）
**/rewind 面板设计（2026-08-16 对照代码补全；overlay kinds 加 `rewind-panel`，PanelShell 基线零新键位）**：
- **行结构**：`HH:MM  工具名  文件摘要（≤3 个，多则 +N）`——meta.json 的时间/工具/文件列表直拼；空态「（本会话还没有任何改动）」；输入即搜索（工具名/文件名过滤）
- **二级确认页**（面板内导航，McpPanel 三级同款）——**不用 ConfirmPrompt**（2026-08-16 代码核读：其 `ConfirmState` 是纯字符串 preview 的工具授权语义，装不下文件清单与着色标注；还原确认是独立语义）：
  列出将还原的文件清单；外部修改的文件独立成组、黄色 ⚠ 标注「快照后有外部修改，还原将覆盖」；底部 `y 还原 / Esc 取消`
- **运行中守卫**（2026-08-16 补）：`runningRef` 为 true 时面板**可开可看但确认禁用**，底部提示「本轮运行结束后才能回退」（loop 与 rewind 同时写 messagesRef/history/文件会竞态——现有 runningRef 只守 submit，命令路径无守卫）
- **恢复会话快照跟随**（2026-08-16 补，断链修复）：restoreSession 起新 sessionId（TuiApp.tsx:581，M5 投影派）而快照按 `<sessionId>` 键控——恢复时同步把 `checkpoints/<旧id>/` 拷贝到 `<新id>/`（CC `copyFileHistoryForResume:922` 同款），否则「跨重启可用」落空
- **还原前先快照当前状态**（2026-08-16 吸收，opencode `revert.stage` 同款）：执行还原的那一刻先拍一个新快照点——回错了可以再 `/rewind` 回来（**撤销可撤销**），还原操作本身不留盲区
- **外部改动检测**（2026-08-16 吸收，CC `fileHistory.checkOriginFileChanged:600` 同款）：还原前比对该文件当前内容与最近快照基线——若快照后被用户手动改过（外部编辑器），确认弹窗里逐个列出「这些文件在快照后有过外部修改，还原将覆盖」，由用户决定——**绝不静默覆盖用户的手改**

## 3. 编辑后 lint/test 自动回喂（P3，~2-3 天）

**问题**：模型改完就说「完成」——编译错/测试红要等用户自己发现再贴回来。四家里 aider 的 auto lint/test 是质量闭环标杆，ECode 完全没有。

**流程图（改完自动验，红了自动修）**：

```
一轮对话中 LLM 改了文件（write_file / edit_file 成功）
  │
  ▼
等这轮工具都执行完再跑（★同轮改多个文件只跑一次）
  │
  ▼
自动跑 lint / test（命令自动从 package.json 的 scripts 里找，找不到即关闭）
  │
  ├─ 全绿 ──► 静默，不打扰任何人
  │
  └─ 有失败 ──► 失败输出作为提示信息喂回给 LLM
                  │
                  ▼
               LLM 看到具体报错，自己修
                  ├─ 修好了 ──────────► 回到「静默」
                  └─ 连续 2 次修不好 ──► 停止自动跑，提醒用户接手
                                          （防死循环烧钱）
```

**详细讲解**（`src/services/quality.ts`）：
- **配置**：`lintCommand` / `testCommand`（config 顶层；**默认自动探测**——package.json scripts 里的 `lint`/`test`，探测不到即关闭，零配置可用）
- **触发**：一轮工具执行中若有 ≥1 个编辑类工具成功（write_file/edit_file），**轮末聚合跑一次**（同轮多编辑不重复跑）——**afterTools 回调注入**（LoopRunOptions 可选字段，onCompacted 同款先例；loop 只在轮末透传工具结果清单，编辑检测与聚合触发全在宿主回调里，TuiApp/cli 各自装配）
- **回喂**：输出作为**信息性 tool_result** 追加（`is_error: false` + `[lint] 失败输出...` 前缀）——模型看到即自纠；成功则静默（不打扰）
- **熔断**：连续 2 次 lint/test 失败且模型修复无效（输出无变化）→ 停止自动跑，onWarn 提示用户接手
- 超时 60s；输出截断复用 bash 截断（30KB 头尾中截）

## 4. git 轻量集成（P6，~2 天）

**问题**：想要干净历史的用户需要 Agent 改动可追溯；但 ECode 哲学是**不静默改用户 repo**——所以默认关、显式开。

**流程图（自动提交 + 安全撤销）**：

```
【自动提交】（config 开启后，默认关）
  LLM 改完文件 + lint/test 跑完
    └─► git add 改动文件 → git commit
          message 由模型一句话生成（失败用固定模板）
          ★打标记：commit 尾部带 Ecode-Commit: 会话ID

      ……用户反悔……

【撤销 /undo】
  用户敲 /undo
    └─► 检查最近一次 commit：带 Ecode-Commit 标记吗？
          ├─ 带（是 ECode 提交的）──► git 回退，文件还原 ✓
          └─ 不带（用户自己的提交）──► 拒绝并提示
                                       ★绝不动用户自己的提交
```

**详细讲解**（`src/services/git.ts`）：
- `autoCommit: true`（config，默认 false）时：编辑轮末（quality 跑完后）`git add <改动文件> && git commit`，message 由当前模型一句话生成（复用 callLLM 通道，失败 fallback 固定模板 `ecode: <工具与文件摘要>`），commit trailer 打 `Ecode-Commit: <sessionId>` 标记
- **/undo**：`git reset --soft HEAD~1` + 文件还原——**只对带 Ecode 标记的最近 commit 生效**（`git log -1` 查 trailer，非 ECode 的 commit 拒绝并提示，绝不动用户自己的提交）
- 与 checkpoint 互补：checkpoint 总在（不依赖 git/不污染历史）；git 模式给要干净历史的人。两者共存时 /rewind 优先（粒度到消息点）

## 5. 沙箱模式（P4，~2 天）

**问题**：现在的唯一防线是确认弹窗（人盯）——读多写少的日常场景里，一次误确认就是一次事故。需要模式级的纵深。

**流程图（三档各拦什么）**：

```
/sandbox 切换档位（StatusBar 常驻显示当前档），四档各拦什么：

  default（默认档）
    读文件 / 搜索      ✓
    写 / 改文件        每次弹确认（现状，无越界分流）
    bash              每次弹确认

  read-only
    读文件 / 搜索      ✓
    写 / 改文件        ✗ 直接拒绝
    bash              ✗ 直接拒绝

  workspace-write
    读文件 / 搜索      ✓
    写 / 改文件        仅限项目目录内；越界 ✗ 直接拒绝（能拦 .. 逃逸）
    bash              弹确认

  full-access（总放行，2026-08-16 用户拍板新增）
    读 / 写 / 改 / bash   全部免确认 ✓
    例外（无条件硬拒 ✗）：① 内置八条危险黑名单（rm -rf / 等永远拦）
                        ② 用户自配 sandbox.blockedCommands 通配清单
                           （如 ["git push --force*", "npm publish*"]）
    （对齐 CC：bypass 模式下 deny 规则仍强制；codex danger-full-access 同位）

诚实声明（软沙箱的边界）：
  bash 命令无法可靠解析它要写哪 → 靠确认弹窗兜底，不假装拦得住
```

**详细讲解**（**软沙箱**——工具层路径校验，进程级围栏后置）：
- 三档（`/sandbox` 切换 + StatusBar 标识 + config 默认档）：
  | 档 | read_file/ls/grep/glob/WebFetch | write_file/edit_file | bash |
  |---|---|---|---|
  | `default` | ✓ | 确认（现状，无越界分流）| 确认（现状）|
  | `read-only` | ✓ | **整体拒绝**（is_error「只读模式」）| **整体拒绝** |
  | `workspace-write` | ✓ | **仅 cwd 内**（越界直接拒绝）| 确认 + 输出审计提示 |
  | `full-access` | ✓ | ✓ 免确认 | 免确认；**黑名单+blockedCommands 命中仍硬拒** |
- 实现：write_file/edit_file 的 execute 前置 `resolve()` + 前缀校验（路径净化防 `..` 逃逸）；bash 无法可靠解析目标路径——read-only 档直接拒绝、其余档维持确认（**诚实声明软沙箱边界**，不假装拦得住）
- **Tab 专职档位热键**（2026-08-16 拍板）：Tab 在主输入空闲态与 /sandbox 面板内直接循环切档（default → read-only → workspace-write → full-access → 环绕），StatusBar 闪示当前档；进 full-access 弹确认（M9-D12）；输入焦点/搜索态不拦截。**代价与清偿**（2026-08-16 已随键位改版落地）：①斜杠命令 Tab 补全删除——回车两段式本就逐字覆盖同功能（`InputStream.tsx` 已改+防回归测试）；②规范里的全局焦点循环**实现层面从未落地**，记载撤销，焦点态收敛为「输入框 ↔ 面板/弹窗」两态；③工具块展开从「Tab 聚焦+Enter」两步并为 Ctrl+O 一步（Ctrl+O 已在产）；④ ask_user 多问 chip 导航改 ←→（QuestionPanel 无 tabs 不冲突）——键位规范同步改
- **full-access 档**（2026-08-16 拍板新增，对标 codex `danger-full-access` / CC bypassPermissions）：跳过全部 ConfirmPrompt，但两道闸无条件保留——① `proc.ts` DANGEROUS_PATTERNS 八条（本就无条件 throw，任何档都拦）；② config `sandbox.blockedCommands: string[]` 用户通配清单（`*` 前后缀匹配，语义对齐 opencode wildcard；命中 is_error 硬拒非确认）。**与权限系统（P5）的合成规则**：deny 规则仍强制（CC 同款），ask 规则降级为 allow（bypass 语义）。切换进该档时 ConfirmPrompt 确认一次（防误切）
- MCP 工具：read-only 档下按其 `readonly` 元数据分流（M6 已有）
- 与 M8 WebFetch 的收口：read-only/workspace-write 档下 WebFetch 照常（已是只读+SSRF 拦截）

## 6. 权限系统首步（P5，~2-3 天）

> 2026-08-16 用户拍板前移（原 M8 §11 设计储备，"启动时机用户定"→ 定了：M9）。完整设计（双维模型/三态规则/三层存储求值/与沙箱正交）见 M8 方案 §11，本节只定 M9 首步范围。

**首步 = `Hook(owner)` 三态规则**（四维模型中离危险最近、M7 刚交付的扩展面）：
- 扩展源 hook（skill:xxx / plugin:yyy@mkt）**首次执行前**查规则：allow（放行）/ deny（跳过+告知）/ ask（ConfirmPrompt，可记住）
- **用户源 hooks 不问**——用户自己写的 config，所有者侧不是被管控对象
- 规则字符串式（可手编可 UI 列）：`Hook(owner)` + `prefix:*`，三层存储（local > project > user）
- 落点：HookRunner.dispatch 前置检查（装饰层 getter 之内、runOne 之前）；零心脏改动

## 7. LSP 诊断回喂（P7 占位，有余量才做）

编辑后查询已装 LSP server 的 diagnostics，与 lint 结果同通道回喂。依赖子进程/语言服务管理（重），M9 只留接口位（quality.ts 的 provider 抽象），实现视余量或整体后置 M10。

---

## 8. 实施拆解

| 步 | 内容 | 依赖 | 心脏 |
|---|---|---|---|
| P0 | hooks 接线缺口修复四项（原观察区 D.5 吸收；各案发现场几行 + 回归测试）| 无 | 零 |
| P1 | checkpoint.ts + ToolContext.onBeforeWrite 注入 + 快照治理 | 无 | ToolContext 加可选字段（透传，非逻辑）|
| P2 | /rewind 命令 + 面板 + rewind_to 投影标记（context.ts 扩展）| P1 | context.ts（投影函数加标记识别）|
| P3 | quality.ts（探测/轮末触发/回喂/熔断）+ afterTools 回调注入 | 无 | LoopRunOptions 加可选回调（onCompacted 同款）|
| P4 | 沙箱四档 + /sandbox + Tab 专职热键 + StatusBar 标识 + full-access 的 blockedCommands（default 档=现状=关）| 无 | 零（工具 execute 前置校验）|
| P5 | 权限系统首步：三态规则引擎 + Hook(owner) 首次执行确认（设计=M8 §11 储备）| P4（正交可合）| 零（HookRunner dispatch 前置检查）|
| P6 | git.ts（autoCommit + /undo + trailer 标记）| 无 | 零 |
| P7 | （余量）LSP provider 接口位 | P3 | 零 |

## 9. 测试

| 模块 | 场景 |
|---|---|
| hooks 接线（P0）| SessionStart additionalContext 进首轮消息 / PreToolUse verdict 全字段消费（block 时 context 一并展示）/ dispatch 透传 signal 后 hook 子进程随中断停 / Stop stop_reason 区分 aborted 与 turn-complete |
| checkpoint | 副作用工具前快照（mock fs）/ 只读工具不快照 / 100 点淘汰（含共享对象回收）/ >10MB 跳过 warn / bash 无 git 跳过 / 全局 50 会话目录淘汰 |
| rewind | 面板列表 / 逆序还原（选中点含自身，回到执行前）/ rewind_to 投影截断（context.ts，截至对应消息之前）/ 还原前确认 / **还原前自动快照（revert 后可再 revert 回来）** / **外部改动检测与弹窗标注** / **content hash 去重（同内容只存一份）** / **运行中确认禁用** / **恢复会话后旧快照可见（目录拷贝跟随，跨重启可用）** |
| quality | package.json 探测命中/未命中 / 轮末聚合（同轮两编辑只跑一次）/ 失败回喂格式 / 连续失败熔断 / 超时 |
| 沙箱 | 四档矩阵（每工具×每档）/ `..` 逃逸拦截 / 越界拒绝文案 / MCP readonly 分流 / full-access 下黑名单与 blockedCommands 硬拒 / 切档确认 |
| 权限首步 | Hook(owner) 三态（allow 放行 / deny 跳过+告知 / ask ConfirmPrompt 可记住）/ 首次执行前查、记住后不再问 / 用户源 hooks 不问 / 三层存储优先级 local > project > user |
| git | autoCommit trailer / /undo 只退 ECode commit（用户 commit 拒绝）/ fallback 模板 |

## 10. 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 快照磁盘膨胀 | 中 | 单会话 100 点 + 全局 50 会话目录上限 + 10MB 单文件跳过 + content 去重（实测校准） |
| bash 快照盲区（无 git 时）| 中 | 诚实降级：warn + 文档明示；有 git 的项目用 git status 兜 |
| /rewind 与运行中 loop 竞态（2026-08-16 代码核读发现）| 中 | runningRef 入口守卫：面板可看、确认禁用 + 提示 |
| 恢复会话起新 sessionId 致快照目录断链（2026-08-16 代码核读发现）| 高 | restoreSession 时拷贝 checkpoints/<旧id>→<新id>（CC copyFileHistoryForResume 同款）|
| lint/test 死循环（失败→修→再失败）| 中 | 连续 2 次熔断 + 60s 超时；输出无变化判定 |
| /undo 误伤用户 commit | 高 | trailer 标记强校验，非 ECode commit 一律拒绝 |
| 软沙箱被绕（bash 写 cwd 外）| 中 | 明示边界：bash 档位靠确认兜底；进程级沙箱后置声明 |
| full-access 被误切/滥用 | 中 | 切档 ConfirmPrompt 确认 + StatusBar 危险色常驻 + 黑名单无条件保留（连 CC 都在 bypass 下跑分类器，ECode 用静态黑名单兜底，不引 LLM 分类器——零依赖哲学）|
| rewind 与压缩交互 | 低 | rewind_to 与 boundary 同为投影标记，取「最后」语义天然共存（测试覆盖同存场景）|

## 11. 决策表

| # | 决策点 | 结论 | 理由 |
|---|---|---|---|
| M9-D1 | 快照存储 | copy 到 `~/.ecode/checkpoints/`（非硬链接）| Windows 跨卷硬链接不可用；文本体量可接受 |
| M9-D2 | 快照触发 | ToolContext.onBeforeWrite 回调注入 | 心脏只透传不认识 checkpoint；与 confirm 后耦合（确认通过才快照）|
| M9-D3 | 对话回退 | rewind_to 投影标记（append-only）| 与 M5 boundary 同机制；历史永不删 |
| M9-D4 | 质量回喂形态 | 信息性 tool_result（is_error:false）| 模型自纠语义；与工具错误（is_error:true）区分 |
| M9-D5 | autoCommit 默认 | **false**（显式开启）| 不静默改用户 repo（项目哲学）|
| M9-D6 | /undo 安全 | trailer 强校验只退 ECode commit | 绝不动用户提交 |
| M9-D7 | 沙箱形态 | 软沙箱（工具层校验）+ bash 诚实声明 | 进程级围栏重基建后置；不假装拦不住的东西拦得住 |
| M9-D8 | LSP | 接口位占牌，实现视余量/M10 | 依赖重；quality provider 抽象预留 |
| M9-D9 | 沙箱默认态 | **默认关**——default 档=现状（确认弹窗），用户经 /sandbox 主动升级；软沙箱即终态（进程级围栏维持后置） | 2026-08-16 用户拍板："沙箱有就行，默认不开用户自己开，软沙箱就行" |
| M9-D10 | 权限系统归属 | 前移进 M9（原 M8 §11 储备"启动时机用户定"）；首步只做 Hook(owner) 三态，Skill/Plugin/Mcp 四维完整版后置 | 2026-08-16 用户拍板"权限系统需要往前排"；与沙箱 P4 正交可合 |
| M9-D11 | hooks 接线缺口 | 四项从观察区 D.5 吸收为 P0 首项 | 2026-08-16 用户拍板"hooks 的问题往前排"；协议层已全、接线各几行 |
| M9-D12 | 总放行档 | 新增第四档 `full-access`：全免确认，内置黑名单 + 用户可配 `sandbox.blockedCommands` 通配清单仍硬拒；deny 权限规则仍强制、ask 降级 allow；切档需确认 | 2026-08-16 用户拍板"加一个总放行的模式，特殊命令不放行用户可自配"；对标 codex danger-full-access（无命令级例外）/ CC bypassPermissions（deny 仍强制 + dangerousPatterns + bypass 分类器兜底）/ opencode wildcard reject 规则 |
| M9-D13 | 档位切换键位 | **Tab 专职**（主输入空闲态+面板内循环切档）；斜杠补全删除（回车两段式覆盖，代码已落地）、焦点循环撤销（实现从未落地，收敛输入框↔面板两态）、工具展开并为 Ctrl+O、ask_user chips 改 ←→ | 2026-08-16 用户拍板"Tab 直接切换、其他职责去掉"；Tab 语义唯一可记（档位=模式）|
| M9-D14 | 快照存储与还原安全 | content-addressed 布局（`objects/<hash>` 对象库 + seq/meta 引用清单，同内容只存一份）+ 还原前自动快照（撤销可撤销）+ 外部改动检测不静默覆盖 | 2026-08-16 一手核读吸收：opencode snapshot 内容寻址与 revert.stage 先 capture；CC checkOriginFileChanged——三者均与「绝不动用户的东西」哲学同源 |

---

## 附录 B：参考来源（启发映射）

| ECode 设计 | 启发来源（本地源码） |
|---|---|
| 文件快照 + 消息点回滚 | claude-code `utils/fileHistory.ts`（copyFile 全量备份非硬链非 patch/上限 100/按消息关联/`checkOriginFileChanged:600` 外部改动检测/`copyFileHistoryForResume:922` 恢复跟随）；opencode `session/revert.ts`（边界 seq 反查 + 每文件取最早快照 + `stage` 还原前 capture）+ `snapshot.ts`（content-addressed，内容 hash 去重） |
| rewind 的投影标记 | ECode 自身 M5 boundary 机制延伸（非外部）|
| 编辑后 lint/test 回喂 + 熔断 | aider `base_coder.py` auto_lint/auto_test（质量闭环标杆）|
| autoCommit + /undo + 安全标记 | aider 自动 commit/undo；「只退自己的 commit」为本项目安全加固 |
| 沙箱三档 + 审批策略 | codex `sandbox_mode`（read_only/workspace_write/danger_full_access）——软实现为本项目裁剪 |
| LSP 诊断回喂 | claude-code `services/lsp/`（passive feedback）；opencode `lsp/` |

## 附录 C：代码锚点

| 落点 | 现状 |
|---|---|
| `src/tools/interface.ts` ToolContext | 加可选 `onBeforeWrite`（M9-P0）|
| `src/core/types.ts` HistoryLine | 加 `RewindLine` 变体 + isRewind 守卫（与 BoundaryLine 同层）|
| `src/core/context.ts` buildContextMessages | 加 rewind_to 标记识别（与 boundary 同层）|
| `src/core/loop.ts` LoopRunOptions | 加可选 `afterTools` 回调（onCompacted 同款先例）|
| `src/tui/commit.ts` messagesToCommitted | rewind 行 → `⇺ 已回退` 标记（compacted 标记同款）|
| `src/tools/builtin/{write_file,edit_file}.ts` | 沙箱前置校验注入点 |
| `src/tui/` | /rewind /sandbox 面板（PanelShell）+ StatusBar 档位标识 |

## 附录 D：后置需求观察区（未排期储备）

> 2026-08-15 盘点入库：全量 grep `详设/` 各方案的「不做/后置/暂不/预留/占位/接口位」标记，剔除已进 M8/M9 正式范围者，**悬空项统一收口在此**——此前散落 7 份方案，无排期的裁剪只有决策号没有回收点。此表不是承诺，是「什么时候捡、捡哪」的触发器登记簿。
>
> **使用规则**：某行触发条件出现 → 该项进对应里程碑方案（或开新方案 + ADR 记录"何时为何放开"）；实现时回来源列标注的决策号核对当时裁剪理由是否仍成立。新里程碑的方案定稿时应把本表相关行吸走。
>
> **已吸走**：D.5 hooks 接线缺口四项 → 本方案 §1 P0（2026-08-16 拍板，M9-D11）；正文所引"原观察区 D.5"即指此段，附录内不再保留。

### D.1 上游先例触发（别人做了我们跟）

| 项 | 来源 | 触发条件 | 补齐落点 |
|---|---|---|---|
| mcp_tool / prompt 型 hook 执行体 | M7 §H0 / M7-D9 | codex 或 Claude Code 真实现（codex schema 已在途，标注 not supported yet） | `exec.ts` 形态分支 + `validate.ts` schema 放开（`types.ts` 类型位已留，三层防御齐） |
| proxy 元工具模式（工具极多收敛为单一 `mcp` 元工具） | M6-D28 | 单 server 工具数让 schema 上下文占用成为实测负担 | McpManager 注册层 |

### D.2 依赖就绪触发（等某系统先有）

| 项 | 来源 | 触发条件 | 补齐落点 |
|---|---|---|---|
| PreCompact / PostCompact hook 事件 | M7 §H2 后置 | 压缩行为需要门控/观测（M9 checkpoint × 压缩联调是自然回看点） | HOOK_EVENTS + 压缩编排器接线 |
| SubagentStart/Stop 事件 + plugin `agents` 组件 + skill `fork` | M7 §H2 / 附录C；M6 附录C | sub-agent 立项 | 事件集扩展 + 清单组件发现；立项前调研已完成（[解析/2026-08-17_Subagent机制调研](../解析/2026-08-17_Subagent机制调研_已完成.md)：调用纪律/硬限制/模板/UI 四问的事实底座 + S1-S7 决策清单） |
| LSP 诊断回喂实现 | M9 §7（本方案占位） | M9 余量或 M10 | `quality.ts` provider 抽象（接口位随 M9 留） |

### D.3 实践信号触发（用出需求再上）

| 项 | 来源 | 触发条件 | 补齐落点 |
|---|---|---|---|
| Skill 增量推送 | M6 S7.2 / M6-D6 | skill 清单 token 占用成负担（变化签名接口已留） | `listing.ts` 增量三态 |
| 客户端图片缩放链 | M10-D10 + 感知扩展调研 | 图片 token 成本成痛点（大截图频繁读入；现状守卫拒绝+服务端自动缩） | sharp（原生依赖）或 photon WASM；CC 渐进降质链参照 |
| skill 级 disable | M7 P4.5 | 自然语言消解不顺手（频繁冲突的团队项目） | config + SkillPanel |
| bash 免确认白名单 | M3 §14 | 确认弹窗噪音实测烦人 | `proc.ts` 白名单 + ConfirmPrompt 跳过 |
| 失败 2 次强制确认（替代每次副作用 confirm） | M3-D6 | 同上，且跨迭代计数器归属有定论 | loop/装饰层计数 |
| grep -C 上下文 | M3 §14 | LLM grep 后频繁 read_file 浪费轮次 | grep 工具参数 |
| 外溢到文件 spill / 反应式重放 | M5 §2 | 摘要损失信息成实际痛点 | 压缩策略接口（可插拔位已留） |
| MCP resources / prompts 原语 | M6-D16 | 常用 server 普遍带这两类能力 | adapt 层扩展 |
| MCP OAuth needs-auth / elicitation | M6 附录C | 常用 server 需要认证流 | 六态状态机扩展 |
| per-tool 白黑名单 | M6 附录C | 单 server 只用个别工具（权限系统可先覆盖） | 与权限规则合流 |
| tools/list_changed 增量同步 | M6 附录C | server 运行期热更工具 | manager 订阅 |
| MCP 面板高级项（直连 toggle/token 估算/自动关/键位/鼠标）+ 禁用 server 启用操作 | M6 T4 / §面板 | 单条用户反馈 | McpPanel |
| reduced-motion | M2 §10 | 无障碍诉求 | Spinner 退化静态 |

### D.4 生态与安全演进触发

| 项 | 来源 | 触发条件 | 补齐落点 |
|---|---|---|---|
| npm:spec 安装源 | M7 附录C（v3.1 评估完） | 官方市场成型或插件分发诉求 | PluginLoader source 解析 |
| autoUpdate + commit sha 比对 | M7 附录C | 插件安全更新诉求 | loader + known 注册表 |
| 企业策略 allowlist/blocklist | M7 附录C | 企业场景 | config 层级 |
| keychain 安全存储 | M7 附录C | 合规硬要求出现（**2026-08-16 用户拍板：key 无法被完全保护，优先级极低，基本永不**） | services/config |
| matcher ReDoS 限长/预检 | M7 风险表 | 第三方 matcher 来源不可信 | `matcher.ts`（可与权限系统合做） |
| `${ECODE_PROJECT_DIR}` / `${user_config.K}` 占位符 | M7 §占位符 | 插件配置需要项目/用户值 | expandPluginRoot 白名单扩 |
| plugin 依赖解析 + 跨市场白名单 | M7 风险表 | 出现互相依赖的插件生态 | manifest + loader |
| git-subdir 部分克隆 | M7 附录C | 大仓库插件 | install 链 |
| 批量安装 / 热度排序 / userConfig 收集 UI | M7 附录C | 市场规模化 | PluginPanel |
| 项目级 hooks 源（`.ecode/hooks.json`） | M7 §H1 | 团队共享 hooks（信任模型先定，批准机制可对齐 .mcp.json） | specsFor 第三源 |
| 进程级 OS 沙箱 | M9 §0 不做 / M9-D7 | 软沙箱边界被实际绕过 | 平台层 |
| diff 浏览器面板 | M9 §0 不做 | 会话级改动总览诉求 | TUI |
| memory 管理命令（/memory 等） | M8 §0 不做 | M8 memory 落地后文件操作不顺手 | commands |
| ask_user preview 分栏 | M8 §0 不做 | ask_user 高级需求 | TUI |
| 跨进程事件总线 | M8 §0 不做 | 多进程形态出现（单进程哲学下基本永不） | — |

---

# 实施记录（2026-08-16，全量交付 P0-P7）

> TDD 全程（每步先红后绿）；终审修复后 956/956 测试 + tsc 零错误（终审时点曾 951/952——git 用例偶发超时未真正加 20s，见终审 P1-6）。提交序列：P0 `76406b4` / P1 `7b6125e` / P2 `bac205c` / P3 `623653c` / P4 `9b6e32b` / P5 `4d19f6e` / P6 `23858d1`（+ 防超屏审阅修复 `3362b0f` 穿插 + 文末终审修复批）。

## 交付清单

- **P0 hooks 接线四项**：SessionStart additionalContext 暂存注入首轮 user 消息（`[hook context]` 同款、一次性消费；UserPromptSubmit 收原文）；PreToolUse verdict 全字段消费（block 时 ctx/systemMessages 一并展示、非阻塞 ctx 注入成功 result 前缀）；dispatch 透传 AbortSignal（UserPromptSubmit controller 前置 + Pre/Post 用 ctx.signal）；Stop stop_reason 区分 aborted（中断信号双通道：onActivity('aborted') 回调为主 + catch isAbortError 兜底——loop 内部消化 AbortError 不抛出，纯 catch 判不到）。
- **P1 checkpoint**：`services/checkpoint.ts` content-addressed 布局（`objects/<sha256>` + `<seq>/meta.json` 引用清单）；治理三线（100 点淘汰+孤儿对象引用集 GC / 全局 50 会话目录 mtime 淘汰 / 单文件 >10MB 跳过 warn）；bash 近修改集 git status 兜底；ToolContext.onBeforeWrite 接线（write/edit 绝对路径、bash []，工具侧 catch 不挡主流程）+ HistoryStore.currentSessionId + TuiApp/cli 装配（`~/.ecode/checkpoints`）。
- **P2 /rewind**：RewindLine 投影标记（区间跳过模型——见偏差③）+ isMessageLine 守卫收敛全部消费点（防标记行漏进 LLM 上下文）+ commit ⇺ 标记渲染 + appendRewind 持久化（重启恢复后投影仍正确）+ /rewind 命令 + RewindPanel（PanelShell 列表：最新在上/HH:MM 工具 文件摘要≤3+N/输入即搜索；二级确认页自建：文件清单+外部改动 ⚠ 黄标注+y/Esc；运行中可看确认禁用）+ messageId 链路（loop executeTool 包装透传 use.id）+ restoreSession 快照目录拷贝跟随。
- **P3 quality 回喂**：QualityGate（探测 config>package.json scripts>关闭；轮末聚合本轮 ≥1 编辑成功才跑；回喂见偏差②；熔断连续 2 次输出 hash 无变化→附提示回喂+warn 接手+此后短路；60s 超时；4k 截断）+ loop afterTools 钩子（onCompacted 同款注入）+ config lintCommand/testCommand + CONFIG 模板补 M9 键。
- **P4 沙箱四档**：`services/sandbox.ts`（checkWrite：read-only 拒/workspace-write resolve+前缀校验拦 .. 逃逸（win 大小写/分隔符归一）；checkBash：blockedCommands 通配全档硬拒/read-only 拒/full-access 免确认）+ 工具前置接入 + config sandbox.{defaultMode,blockedCommands} + /sandbox 命令 + SandboxPanel（Tab·↑↓ 循环/full-access 二级确认）+ Tab 专职热键（InputStream 空闲态；循环到 full-access 转面板确认）+ StatusBar 档位段（full-access ⚠ 危险色）+ confirm 集成（full-access 放行/read-only MCP 副作用拒）。
- **P5 权限首步**：`services/permissions.ts`（Hook(owner) 三态规则：Type(param) 解析+括号内尾 * 通配；求值 deny 任一层终局>local>project>user>默认 ask；三层 settings 文件，交互记住落 local）+ HookSpec.owner（registry 注入；用户源不问）+ HookRunner.checkHookPermission 门控 + ConfirmPrompt 第三键通用化（rememberLabel）+ TuiApp asker 桥（argv/无 UI 默认拒绝 fail-closed）。
- **P6 git 轻量**：`services/git.ts`（ecodeCommit 双 -m trailer/只 add 指定文件；/undo 仅退带 Ecode-Commit trailer 的最近提交 reset --hard，用户提交绝不动）+ config autoCommit（默认 false）+ afterTools 链 quality 之后提交（绿了再提交；subject 见偏差④）+ /undo 命令。
- **P7 LSP**：DiagnosticProvider 接口位占牌（quality.ts 尾部；实现视 M10）。

## 与设计的偏差记录（实施中决策）

1. **dispatch signal 7 处中 4 处实传**：UserPromptSubmit（controller 前置）/PreToolUse/PostToolUse（ctx.signal）实传——有真实中断链路（等待 hook 时 Ctrl+C 可杀子进程）。SessionStart×2（启动/恢复瞬间无交互等待窗）/SessionEnd（退出清理路径）/Stop（轮末后台 fire，传入已 aborted 的 signal 反而误杀）不传——当前仅超时兜底，理由：无中断窗口处传 signal 是形式主义。
2. **M9-D4 回喂形态**：信息性 tool_result → **user 文本消息**（`[lint] ...` 前缀）。协议约束：tool_result 必须配对 tool_use（无主 result 会 400），信息性回喂不能造无主 result；user 文本语义等价（模型同轮可见自纠）。
3. **rewind 投影模型细化为区间跳过**：设计稿写"截断到锚消息之前"，实施修正为 `[锚消息..rewind 行]` 区间跳过——锚之前的行（回退点）**加** rewind 行之后的新行（回退后继续聊的对话）保留。纯前截会把回退后新对话也吃掉，违背"回到那个时刻，继续聊"。
4. **autoCommit message**：设计写"模型一句话生成（复用 callLLM）"，实施改为取本轮最后 assistant 文本首行（模型自己的改动描述，零额外调用；callLLM 通道复用成本高）+ 固定模板兜底。
5. **MCP read-only 拒绝**：经 confirm callback return false（loop 固定文案"用户已取消"——LLM 侧文案不准）+ setSystemMsgs 用户侧准确提示。本地 write/edit/bash 在工具层拦（文案准确）；MCP 经 server 无法工具层拦，局限记录。
6. **/undo 无二级确认**：trailer 强校验是主安全线（用户提交绝不动）；reset --hard 的破坏面仅限 ECode 自己刚提交的文件集 + checkpoint 兜底。后续反馈烦可加确认页。

## 问题清单（后续审阅/观察）

- **PanelShell 窗口 12 固定未高度感知**（防超屏审阅 P1-2 残留）：Select 已收口 min(12, 视口-14)，PanelShell（skill/mcp/plugin/warnings 面板）同病待同款处理——80×24 中段双指示态自身顶满。
- **防超屏审阅 P2 未修项**：①横向防线（preview 长行 wrap 后物理行数仍可超视口——按列截断破坏 diff 可读性，未做）；②AssistantMessage 长回复动态区无上限（M8-D12 已拍板砍渐进固化，观察区——轮末 commit Static 自愈）；③Select key=位置键整批重挂载（与 PanelShell 一致，代价可忽略）。
- **checkpoint bash 快照时序**：git status 在工具确认后同步执行，大 repo 秒级拖慢工具链——后续可异步预热/缓存 dirty 集。
- **权限 ask 桥与运行中 confirm 的并发**：权限弹窗复用 active.confirm 单槽——loop 工具确认与 hook 权限确认理论上不并发（权限门在 dispatch 内、工具确认在 executeTool，串行），极端嵌套场景未验证。
- ~~git.test 偶发超时~~ **终审 P1-6 已修**：全量并发下 git 子进程慢，/undo 两用例真正补上 20s 超时（首批替换静默未生效）；CI 化时继续关注。
- **autoCommit 文件集语义**（终审 P2-6）：只含 write_file/edit_file 改动文件（onBeforeWrite 收集）；bash 产生的变更不进集——与直觉有差，特此声明（checkpoint 的 bash 快照独立兜底）。

## 终审修复批（2026-08-16，独立 agent 审 M9 全部提交：1 P0 + 6 P1 + 7 P2）

> 三个跨模块集成缺陷均经真实代码运行实证复现（各自单测绿、组合漏网）；修复 + 补对应集成测试。

- **P0-1 rewind×压缩参考系分叉（孤儿 tool_result → Anthropic 400）**：orchestrator 翻译 boundary.tailStartIndex 用全量参考系、context 使用端用 rewind 跳过后参考系，RewindLine 存在时漂移=丢弃区间消息数。修：rewind 区间跳过逻辑提炼为 `context.ts rewindSubset()` 共享纯函数，翻译端 filter 前先 rewindSubset——两参考系强制一致。补集成测试（真实 orchestrator+mock 策略+含 RewindLine，断言二次投影 tail 完整无孤儿；首版测试用自建投影对象 indexOf 引用比较失配，改走 buildContextMessages 真实引用）。
- **P1-1 loop afterTools isError 位置错配**：executeTools 重排 [readonlys..., sideEffects...]，按位置配对在混排时把别人的结果配给自己（write 失败被读成成功 → QualityGate 误跑）。修：按 tool_use_id 建 Map 配对。补混排回归测试。
- **P1-2 git 提交混入用户 staged + /undo 清掉（数据丢失）**：两层修——①commit 加 `--only -- <files>` pathspec（裸 commit 提交整个 index）；②/undo 从 `reset --hard HEAD~1` 改为 `reset --soft` + 只对 ECode 提交自己的文件集恢复（HEAD 有→checkout HEAD --（注意不能裸 checkout --，那只回 index；HEAD 无=新建→rm），用户 staged/工作区改动分毫不动）；**首个提交无父**走 update-ref -d HEAD（reset HEAD~1 会 128）。补 staged 混入/undo 保留集成测试。
- **P1-3 revert 自动快照 GC 竞态**：会话满 100 点时 revert 内的还原前自动快照触发 enforceLimits，可能淘汰选中点/删掉待还原对象 → 还原静默不完整。修：snapshot 加 skipLimits 选项，revert 自动快照跳过治理（延到下次正常快照）。
- **P1-4 rewind-auto 点缺投影锚**：「撤销可撤销」场景选自动点回退时 toolUseId 缺失 → 截断整体失效、被撤销区间复活。修：revert 自动快照带 messageId 锚（还原范围内最新点的锚）+ RewindPanel 兜底（缺锚时用 seq 更小的最近点锚）。
- **P1-5 autoCommit 红灯也提交**：与「绿了再提交」声称不符。修：QualityGate 加 lastRoundFailed 信号，TuiApp 判 feedback===undefined && !lastRoundFailed && !tripped 才提交。补单测。
- **P1-6 测试基线不可复现**：终审实测 951/952（git 用例超时）且上批「已加 20s」的替换静默未生效（无 assert）。修：真补两用例 20s；全量重跑勘误。
- **P2 顺手 2 项**：restoreFull 按标记字段三分发 cast（类型卫生）；copyForResume 失败从静默吞改为 warn 进告警中心。
- **P2 登记未修**（观察）：权限 deny 无记忆/同 dispatch 连环弹窗；多 rewind「取最后」使先前丢弃区间复活（改区间并集，复杂）；blockedCommands 字面匹配可被引号/变量绕（软沙箱声明内残余）；settings 三层每 dispatch 同步读（可缓存）；quality 命令探测启动期一次性；edit_file 内容校验失败仍进 editedFilesRef（"nothing to commit" 误报一次，无害）；TuiApp 层 autoCommit 端到端测试仍缺（toolCtx.cwd 硬编码 process.cwd() 导致测试会写真项目文件——待 cwd 可注入）。

## 终审修复批勘误与补充修复（2026-08-16 会话复核）

**终审批 9 项声称逐条核对结论：7 项落地，P1-3 / P1-4 两项「声称已修实际未进代码」**：

- **P1-3 skipLimits 实未落地**：终审提交 949dcff 未触碰 checkpoint.ts，`skipLimits` 标识符对该文件全历史检索为零。真修：`snapshot()` 拆出私有 `snapshotCore()`（不含治理），revert 的还原前自动快照改走 core——治理淘汰/GC 不再可能删掉本次正要写回的基线；跳过的治理延迟到下次正常快照无害。回归测试锁定（点数满 + 选最旧点，修复前实测 a.ts 停在 v2 还原不完整）。
- **P1-4 两半均未落地，且声称修法本身有缺陷，处置为「确认缺锚是正确语义」**：revert 自动快照未带 messageId、RewindPanel 无缺锚兜底（949dcff 的 TuiApp 改动仅含 P1-5 与 copyForResume 告警）。复核推演：补「范围内最新点的锚」会让撤销回退的复活不完整——最新锚那轮仍被截掉，文件已还原回改后状态、模型却只记得一半（半截状态）；**缺锚 → rewindSubset 全量返回恰是「撤销回退」的正确语义**：上下文与文件一起完整回到回退前。处置：checkpoint.ts / context.ts 注释写明设计意图，context.test 新用例锁定（回退 → 撤销回退 → 投影全量、原区间复活）——防止后人「好心补锚」引入半截状态。上文终审批 P1-4 条目与「P2 登记未修」中「区间复活」观察项的定性据此修正：撤销场景的复活是特性不是 bug。

**复核中发现的新问题（同批修复，各带测试）**：

- **/undo 中文文件名直接坏（中危）**：`git show --name-only` 默认 core.quotepath=true 输出带引号八进制转义路径，cat-file/checkout/rm 全链路找不到文件；且 reset --soft 已执行后 rm 失败抛异常——留下「提交已撤 + 文件新状态 + 用户看到报错」的半完成态。修：show 加 `-c core.quotepath=false`；tracked 恢复 / 新建删除两用例锁定（中文文件名在 ECode 用户环境并不罕见）。
- **MCP 副作用工具无快照（覆盖缺口）**：onBeforeWrite 只接 write/edit/bash，MCP 工具改的文件不在 /rewind 保护面。修：adaptTool execute 在 callTool 前调 `onBeforeWrite([], mcp 名)`——bash 同款 git status 近修改集兜底（第三方入参路径不可信，不提取），经 loop 包装自动带投影锚，快照失败静默不挡主流程（与内置工具一致）。
- **gitDirtyFiles rename 假路径（低危）**：porcelain -z 的 rename old 路径是独立 NUL 项（无状态码前缀），裸 `slice(3)` 产生假路径（实测 "xyzkeepme.txt"→"keepme.txt"），与真实文件撞名时误拍进快照。修：解析前校验第三字符为空格。
- **ecodeCommit 失败 stage 残留（低危）**：add 已执行而 commit 失败（pre-commit hook 拒 / 无 identity）时，本轮 add 残留在用户 index——「不碰用户 repo」承诺的豁口。修：catch 内尽力 `reset -q -- <files>`（回滚自身失败不掩盖原始错误）。

测试基线 956 → 963（+P1-3 回归 1、中文×2、rename 1、stage 回滚 1、MCP 快照 1、撤销回退锁定 1）；tsc 干净。
