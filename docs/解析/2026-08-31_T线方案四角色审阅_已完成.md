# T 线方案四角色审阅（2026-08-31）

> 审阅对象：`docs/详设/2026-08-31_后续-T线-TUI附着daemon实施方案_待审核.md` v1.0
> 四席并行：架构师 / 资深开发 / 安全 / 测试（各席对照 src/、tests/、scripts/ 逐点核验）
> 结果：**四席一致「修后可实施」，零方向性否决**；P0×7、P1×18、P2×20 全部吸收进 v1.1。

## 总裁决与修复去向

| 席 | 裁决 | P0（照做必翻车） | 修复落点 |
|---|---|---|---|
| 架构师 | 修后可实施 | ①detached 拉起无 stdio/平台策略——win32 detached=CREATE_NEW_CONSOLE 弹窗+stdio 继承在 TUI 退出后 EPIPE 杀 daemon（index.ts:308-314、serveMain.ts:157/271-275）②「serve 已在跑→附着」误读接管语义——真语义是 killServeByReg 杀旧重建（serveMain.ts:70-83），冷启动竞态下后起 serve 杀先跑任务 | §4.1 重写：stdio:'ignore'+windowsHide+拉起锁+serve 接管改造（健康+版本一致→退出 0 不接管）+G-T 断言 12（并发冷启动） |
| 资深开发 | 修订后通过 | ③§3.3 漏整个 `host.*` 直调面（transcript×10/compactManual/restoreFrom/appendRewind/mountBridges/subscribe/dispose——T2 真正主体）④#14 等价列张冠李戴：mcpPendingApproval 是项目 .mcp.json 首用批准门（setup.ts:32-36），≠mcp-permission hook 授权帧，且无任何协议帧可走 ⑤§4.3 现状失真：UserPromptSubmit 宿主 dispatch 已存在（session.ts:817-833，b652968），web 触发现有；真缺口=SessionStart 宿主化+插话路径+additionalContext 跨进程承载 | §3.3 补 B1-B5；#14 改写+D-T7；§4.3 按现状基线重写 |
| 安全 | 驳回直改 | ⑥TUI 附着凭据语义全文缺失（必然 primary token；T8 窗口随常驻扩大未披露）⑦静默拉起+项目 .env 可注入 serve 绑定三元组——恶意仓库 .env 写 HOST=0.0.0.0+密码即经日常 `ecode` 静默制造局域网常驻暴露（serveMain.ts:123-131 dotenvMap 回退链） | §4.5 新增节：凭据显式决策+env 白名单（auto-spawn 不回退项目 .env）+身份双验+T8 披露+安全断言 6 条入 §7 |
| 测试 | 有条件通过 | ⑧hook 宿主化事实滞后（同⑤）；25 个 pty 探针默认路径被 T3 静默改变（裸入口 spawn→attach），形态开关与聚合 runner 未落纸；T2 破 5 个测试文件装配+4 面板组件测试未计估时 | §3.4 盘点入档；T3 加 ECODE_FORCE_EMBEDDED+`npm run probes`；T2 估时含测试重建；pty-attach-probe 12 断言成 §7 主体 |

## 各席 P1 摘录（全部已吸收）

- **架构席**：HttpTransport 连 multi 不通（裸命令+已退役端点）→T4 新写 MultiTransport（信封+游标 sinceSeq/gap）；§4.4「不自动降级」与 G-T4「自动重拉」矛盾→统一为「重开才自动、运行中永不」；server.json/health 无 version 字段→schema 扩展入 T3；防脑裂 watchdog 已存在（不重复计入）；**装配面机制拍板：单路径全协议化**（Deps 拆客户端面/宿主面，宿主面字段删除由编译器逼出全部消费点——§4.6）；muxFilter 术语失准→web 现行「全量收+本地过滤」模式照抄（切会话零重连）。
- **开发席**：fork 语义归位——forkSession+copyForResume+SessionStart(resume) 属 restoreSession 恢复流程非 rewind→§4.2 session/restore 扩 fork:true 宿主化；rewind 回执三面（CheckpointMeta[]+external 标注+restored[]）shape 冻结先行（3 探针压面板文案）；session/list 须带 includeArchived:true（行为差）；/skill-create 断链（providerRegistry+callLLM 在宿主）→D-T2；SkillPanel 模块单例暗道（SkillPanel.tsx:12）；17 非 18、doctor 非面板、compactManual 无命令→session/compact 补命令。
- **安全席**：§4.1 流程图把「版本不符」导向拉起=变相杀旧重建，与 D-T1a 矛盾→改为不拉起只提示；hook 宿主化 cwd 破坏（exec.ts:73 process.cwd()——宿主化后守卫 hook 跑错树）→hook cwd=会话项目 cwd（Embedded 同修）；触发面扩大（LAN 客户端可经 prompt 触发 hook）→审计接入；--yes 附着态传播→D-T6（会话级声明，禁 daemon 全局）；daemon 身份残余（health 无鉴权可伪造）→pid 探活+health.id 双验+披露 R1 收口；Windows 0600/配置快照陈旧/watchdog DoS/env 继承入附带记录。
- **测试席**：G-T5 哨兵清单与实物不符→改为全量 25 探针+聚合 runner；附着形态测试金字塔（L1 协议适配器单测/L2 InMemory 双客户端集成/L3 pty-attach-probe 端到端）；rewind 全流程 MockProvider 断言六件（契约冻结/契约 shape/留痕/busy/篡改/行为对拍）；SessionStart additionalContext 承载设计（宿主暂存拼 input，不加帧）；mcp/action 是新 op 非「接线」；G-T1 声明浏览器渲染不在自动化断言面。

## 估时演进

v1.0：6.5-8 人天 → 四席分别上调至 9-11 / 8-10（测试席，含测试缺口）→ v1.1 合并：**10.5-12 人天**（T1 3-3.5 / T2 2.5-3 / T3 2-2.5 / T4 1.5 / T5 1-1.5）。

## 遗留拍板点（带审阅后倾向，见方案 §8）

D-T1 版本策略（a 拒绝附着）/ D-T2 skill·plugin 首版范围（b 附着态禁用挂账）/ D-T3 退出提示（a 轻提示一次）/ D-T4 Embedded 进入（a 自动+--local，用户已认可）/ **D-T5 插话 hook 语义（a 触发，对齐两形态）** / **D-T6 --yes 传播（a 会话级声明）** / **D-T7 .mcp.json 门（a 本线协议化）** / **D-T8 审批超时（a 无人值守调大可配）**——后四项为 v1.1 审阅新产。
