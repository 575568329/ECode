# T 线实现审阅（2026-08-31，两席复合：架构+开发 / 安全+测试）

> 审阅对象：`54aee4e..92b388c` 六笔提交（T1 协议面/T2 TuiApp 命令化/T3 daemon 入口/T4 MultiTransport/T5 StatusBar）+ relay 最小实现
> 结论：**两席各揪出 1-2 个 P0，全部已修复**——详见下方修复台账。修复后全量门 1676+1flake（单跑绿）+attach 探针 10/10。

## P0×3（全部修复）

| # | 发现 | 修复 |
|---|---|---|
| P0-A | **调试插桩残留入库**：multi.ts cmd 端点每命令两次 appendFileSync 硬编码 `D:/study/ECode`（部署环境目录不存在 ENOENT→**附着态全部命令 400**）+ multiTransport 每帧写盘 + session/project/multi 各处 | 全部删除（grep 归零）；commit 92b388c「插桩全部清理」声明与事实不符的教训记录在案 |
| P0-B | **.env 注入面半成品**（安全席）：serve 绑定三元组仍回退项目 .env（F-18 旧口径），恶意仓库 .env 写 HOST=0.0.0.0+密码→日常 `ecode` 静默制造局域网常驻暴露 | daemon spawn 注入 `ECODE_AUTO_SPAWN=1` 标记；serveMode 对三元组（HOST/PORT/SERVER_PASSWORD）在该标记下**只认外部环境变量不回退 .env**；用户显式 serve 行为不变 |
| P0-C | **附着态恢复会话整体失效**（开发席）：restoreSession 丢弃 `r.value.sessionId` 回执→MultiTransport 会话基线不更新→恢复后帧全被过滤+信封缺 sid 起幻影会话 | restoreSession 消费回执 `recordSessionId(value.sessionId)`；附着路径 gap→syncCommitted 自愈接线 |

## P1 修复台账

| # | 发现 | 修复 |
|---|---|---|
| P1-1 | spawnDetachedServe 把 'serve' 追加尾部——`ecode --history X` 的子进程解析成单次模式把 "serve" 当 prompt 烧 token | 'serve' 插在脚本路径后（parseArgv argv[0] 语义） |
| P1-2 | W-9 游标整链失效：重连不带 sessionId 参数（mux 重放要求 wantSid）；onGap/onUnauthorized 消费侧缺失 | 重连带 sessionId+sinceSeq；TuiApp 接线 gap→syncCommitted 全量补拉、401→告警 |
| P1-3 | McpPanel 死按钮（mcp/action 协议面 TUI 零消费，附着态静默 no-op） | onReconnect/onDisconnect 改走 mcp/action 命令+动作后重拉 panel/data；toolsOf 改帧数据 |
| P1-4 | 附着态 skill 列表/补全全空（SkillRegistry 真件从未 load） | 附着分支补 `skillRegistry.load()`（用户拍板：skill 数据归本地，同机直读同目录） |
| P1-5 | 健康瞬时超时误删活 daemon 注册→存活期内永久降级 | 健康 daemon 的注册无论版本匹配与否一律**不删**；版本不符走提示路径 |
| P1-6 | 拉起锁无 stale 回收（持锁进程 kill -9→后续冷启动白等 15s） | 锁龄 > READY_TIMEOUT 视为死亡抢删重试 |
| P1-7 | 版本比对源错位（比对 reg.version 而非 health.version；undefined 双向漏过） | 统一 health.version（daemon 实际运行版本）；任一侧缺失=视为不符拒绝附着 |
| P1-8 | daemon 日志落进程 cwd（detached 后锚定漂移）；附着态断连无提示 | 日志固定 `~/.ecode/logs/serve/`；StatusBar「后台重连中…」+凭据失效告警 |

## P2 记录在案（择要）

- relay：REG_TOKEN timingSafeEqual+header 传输（**已修**）；隧道替换 MITM 面与全局共享单 token 为已披露的 R 前置形态（完整 R2 替换）；WS 心跳 30s 防 nginx 空闲断链（**已修**）
- spawnEnv 可补 OPENAI_API_KEY/HTTP(S)_PROXY（受控网络场景）——挂账
- startupWarnings 仅首订阅者可见（双端场景第二端缺启动告警）——挂账
- SkillPanel 模块单例暗道/panel/data(skill) TUI 消费面——随 D-T2 挂账批
- pty-attach-probe 的 12 断言中双端审批收敛/插话/双开竞态/serve stop 重拉已在 A3-A5/A7b 覆盖简化版，完整断言随 G-T 真机门补
- 新增测试的裸 sleep 断言（compact 50ms/multiTransport 80ms）换 vi.waitFor——挂账

## 质量门终态

全量 vitest **1676 passed + 3 skipped**（git.test 单跑绿=既有环境竞争 flake）、tsc 净、dist 重建、**pty-attach-probe 10/10**（A2 曾因探针自身 mock `req.on('close')` 在 POST body 读完即触发清掉慢流 timer——Node 18+ 语义坑，与产品无关）、既有六哨兵全绿。
