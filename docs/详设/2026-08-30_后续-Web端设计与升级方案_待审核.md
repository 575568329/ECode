---
layer: web
status: 待审核
depends_on:
  - 详设/2026-08-24_后续-M13-实施方案_已完成.md
  - 解析/2026-08-30_Web端调研-opencode-app_已完成.md
  - 解析/2026-08-30_Web端调研-deepseek-harness_已完成.md
  - 解析/2026-08-30_Web端调研-OpenHands_已完成.md
  - 解析/2026-08-30_Web端调研-LibreChat_已完成.md
  - 解析/2026-08-30_Web端调研-lobe-chat_已完成.md
  - 解析/2026-08-30_Web端调研-NextChat_已完成.md
---

# ECode Web 端设计与升级方案

> 日期：2026-08-30 · 状态：待审核（六项目源码调研汇总，Q1-Q6 待拍板后分批实施）
> 本文基于六份对标分析（opencode app / deepseek-harness / OpenHands / LibreChat / lobe-chat / NextChat，源码已克隆至 `D:\study\`，路径证据见各分析文档）汇总而成。

## 1. 现状盘点

当前 web 端（M13/M14 交付）的资产与缺口：

**已有**：daemon 同源部署的单页端（Vite8 + React19 + Tailwind4 + Zustand5，~394KB）；项目→会话两级列表；会话流（react-markdown 全量渲染 + 工具卡 + 图片直渲）；审批/单选/问答 takeover；SSE mux 单流 + 断线重连 + 全量补拉（restore→read）；`/stats`、ModelChip 顶栏、移动两态 + KeyboardAware；PWA 可安装壳；bundle 内 git 23 测。

**缺口（本方案的靶子）**：

1. 流式渲染是 react-markdown **全文重渲**——长回复/长工具输出流式期间卡顿且高亮反复重跑。
2. 长会话无容量策略——数千条工具事件全量挂 DOM。
3. 会话列表只有「项目→会话」裸列表——无分组/搜索/置顶/重命名/删除，运行态只有布尔点。
4. 工具卡无 diff 展示（TUI 已有全量 diff 数据源，web 点开是原始文本）。
5. 插话在 web 端仅 queue 文本条，无用户消息留痕语义对齐（TUI 已补，web 需对齐核查）。
6. 重连靠全量 `session/read` 补拉——无游标续传。
7. 样式硬编码色值散落，无语义令牌层；暗色是唯一形态，无主题演进位。

## 2. 调研结论矩阵

| 维度 | 最强供给 | 供给内容（详见分析文档） |
|---|---|---|
| 事件消费 | opencode | 合并-分帧（入队→delta 拼接→16ms 帧 flush）、目录 refcount 订阅 |
| 流式 markdown | LibreChat + harness + opencode | 分块 memo/冻结尾块/块投影三族同源方案；worker 高亮；完成后才高亮 |
| 长列表容量 | lobe-chat / LibreChat / NextChat | 虚拟化三规则 / 渐进挂载窗口 / 3 页窗口（三档由轻到重） |
| 状态机渲染 | OpenHands | 双轨事件 store + observation 原位替换 + rAF 批处理 |
| 会话列表 IA | LibreChat + harness | 时间分组/置顶区/运行徽标 + 四态点语义 + 快照帧 |
| 审批 UX | opencode + harness | dock 卡形态 + once/always 作用域 + 父链继承 + 按钮行恒在滚动区外 |
| 工具卡体系 | OpenHands | defineVisualizer 注册表、无依赖 LCS DiffView |
| composer 扩展位 | harness | 分区命名法（input.dock/left/right…） |
| 移动端 | NextChat + LibreChat | iOS 细节包、抽屉手势参数 |
| 反面清单 | 全部 | 四套状态体系、偏好矩阵、react-virtualized 手动缓存、多后端代理层、市场 monorepo |

## 3. 设计原则（不改的三条底盘）

1. **daemon 权威，前端纯视图**：协议不为本方案让路；唯一例外是 W9 的 since 游标（协议增强，向后兼容）。
2. **单层状态**：Zustand 分片 + 少量 SWR/React Query 语义的服务器数据钩子；禁止引入第二套状态体系。
3. **轻量端定位**：不引组件库、不引重运行时；每项设计标注「轻做法」，优先 NextChat 式解法。

## 4. 现有功能升级（W 线，按优先级）

### W-1 流式渲染管线（P1，实测后收缩为两项廉价保险）

实测结论（见 W-2 数据）：已提交行 memo 化后，流式期间只有尾行重渲，450 行场景无 longtask、帧时间健康——**「分块冻结」对常规消息（3-4 块）无收益**（memo 比较成本 ≈ 重渲节省，实测反而略负），降级为按需优化。本项保留：

- SSE delta 进 **rAF 批处理缓冲**（OpenHands batcher 蓝本，76 行零依赖）——高频 delta 每帧至多一次 store commit。
- 长消息行内分块冻结（marked.lexer 分块 + 稳定块 memo，尾块 live）挂账：单条消息超长（>30 块/超大代码块）的真实会话出现时再启用。
- 锚点：`D:\study\LibreChat\client\src\components\Chat\Messages\Content\splitMarkdown.ts`、`D:\study\OpenHands\src\utils\streaming-delta-batcher.ts`。

### W-2 虚拟化长会话（P0，Q1+实测升格：虚拟化「非常需要」）

实测（`web/src/perf` 基准页：450 行合成会话 + 120 tick 流式，Playwright 采集）：

| 方案 | 首屏挂载 | 流式平均帧 | P95 帧 | DOM 节点 |
|---|---|---|---|---|
| 全文渲染（现状等价） | 103ms | 8.6ms | 12.2ms | 1492 |
| **virtua 虚拟化** | **23ms** | **6.9ms** | **9.3ms** | **61** |

- 首屏 4.5×、DOM 24×；真实行更重（工具卡/diff/图片），倍数只会更大。合成负载下 longtask 全 0——差异体现在帧时间与内存规模，长会话/低端设备上放大。
- 实现：`virtua` VList 包裹会话流（Q1 拍板用库不手写）+ 行级 memo；harness 深读结论作行为参照——它**不用虚拟化库**，靠 `ChatNodeSeat`（每 key 独立订阅，delta 只重渲自己）+ `loadOlder` 前插分页锚 + 增量 markdown 三件套撑长会话，代价是回滚越深 DOM 越多（其源码注释自认"虚拟器才能天然封住"）——virtua 直接封住该缺口。
- 排期：**批 1**（原第三档升格）。
- 基准页保留（`web/src/perf`，dev-only `?perf=1`），后续任何渲染改动可复测。

### W-3 会话列表升级（P1）

- **时间分组**：今日/昨日/过去 7 日/更早（`groupConversationsByDate` 同款固定组）。
- **搜索**：客户端子串（title+firstUser）起步，协议演进项见 W9。
- **会话操作（Q4 拍板：归档替代删除）**：重命名（pin 语义：手动命名即固化标题，harness 同款）、**归档**（daemon 增补 `session/archive` 协议：session meta 打 archived 标记 + `session/list` 默认过滤；web 归档会话默认不显示，**侧栏项目名称行加「已归档」入口按钮**——查看该项目归档会话并可恢复）、置顶（独立分组）。
- **状态点语义**：待交互（amber，审批/提问挂起）> 运行中（蓝，脉冲）> 完成提醒（绿，仅未打开过）> idle（harness 四态 + OpenHands 视觉词汇）；daemon 已有 running 帧与审批帧，`pendingInteraction` 由客户端从 mux 帧推导，零协议改动。

### W-4 工具卡 diff 展示（P1）

编辑类工具（edit_file/write_file）结果已含 unified diff（TUI 侧已全量化）。web 工具卡展开态渲染 **+/- 着色 diff**（Q2 拍板：复用 `diff` npm 包——与 TUI 同一依赖生成/解析 diff，不手写 LCS；呈现参考 harness DiffBlock：IN/OUT 标注、max-height 内部滚动、超长截断）。截断帧（>4KB）沿用 item/read 补全。

### W-5 消息操作集（P1）

消息级操作：复制（文本/代码块）、重发该问题（startTurn 同文本）、插话引用。用**操作注册表**组织（每操作一文件，icon/label/handler/hide 条件），后续操作零侵入扩展（lobe-chat defineAction 模式）。不做消息树/分支（ECode 会话模型是线性+checkpoint 回退，无 sibling 语义）。

### W-6 审批交互升级（P2，Q3 拍板：dock 化后置，按需另行排期）

- takeover 保留（sensitive 语义不变），非敏感审批远期从「整卡替换 composer」演进为 **dock 卡**：钉在 composer 上方、与输入并存、三键层级 Deny(ghost)/Once(primary)/Always(secondary)。
- auto-accept 作用域：会话级记忆（localStorage）——服务端 once/always 语义不变，纯客户端快捷层。
- 锚点：opencode `session-permission-dock.tsx`、harness `ApprovalPanel.tsx`（按钮行恒在滚动区外 + 一次性 latch + 失败 re-arm——ECode takeover 已有 latch，补 re-arm 核查）。

### W-7 移动端细节包（P2）

iOS 四件套（NextChat）：输入 16px 防缩放、body touch-action、viewport 禁双击缩放、iOS 侧栏 transition 修复；抽屉手势（LibreChat useDrawerSwipe 参数：10px/轴锁定 1.5/35%/0.3px/ms，直写 DOM transform）；`interactive-widget=resizes-content` + `viewport-fit=cover` meta（opencode，已部分有，补齐核查）。

### W-8 语义令牌层（P2）

现状硬编码 Tailwind 色值（neutral-800 等）散落。建立语义令牌 CSS 变量层（`--ec-bg-surface`/`--ec-text-primary`…）喂给 Tailwind4 `@theme`，组件只消费语义类；暗色为唯一形态不变，但留出亮色/主色切换的演进位（lobe-chat cssVar 思想的轻量版，不引 antd 栈）。

### W-9 重连游标（P2，协议增强）

`session/read` 全量补拉演进为「REST 拉尾页 + SSE `since <seq>` 续传 + 事件 id 去重」（OpenHands 模型）。mux 帧已带 seq 语义（harness subscribed lastSeq 同构），daemon 侧增量实现。**向后兼容**：老客户端不传 since 走全量。

### W-10 插话留痕对齐核查（P1，小）

TUI 已实现插话留痕（排队行 + 轮末转写）；web 端核查三点：queue 帧驱动的排队条视觉升级为用户消息行样式、注入后 appendUser 时序、轮末补拉包含注入消息。零协议改动。

## 5. 扩展预留（E 线，只留位不做）

| 项 | 预留位 | 参考锚点 |
|---|---|---|
| E-1 工具卡注册表 | `toolVisualizers` Map（按工具名查渲染组件，缺席走通用卡）；新增 MCP/内置工具零侵入 | OpenHands defineVisualizer |
| E-2 消息操作注册表 | W-5 即按注册表实现，后续加导出/分享等只增文件 | lobe-chat defineAction |
| E-3 composer 分区 | 布局按分区命名预留：附件栏（input.left）、斜杠/@补全层、队列/todo 条（input.dock）、统计读出（composer.dock） | harness slots 命名法 |
| E-4 工作区右栏 | 会话页右侧可折叠 tab（files/输出/终端）——数据源已有（/output、item/read、task 日志）；本期不做只保证布局可挂载 | OpenHands conversation-tabs |
| E-5 i18n | key 抽取用常量联合类型；en 内联 + 其他 locale 动态 import 的目录结构预留（本期不引 i18n 框架） | LibreChat i18n.ts |
| E-6 只读分享视图 | 路由预留 `/share/:sessionId`（daemon 出只读 token 时即插即用） | OpenHands shared route |
| E-7 主题生成 | 令牌层（W-8）就绪后，seed 色 → CSS 变量生成器可后期加入 | opencode oklch resolve |

## 6. 不做清单（反面集）

- 第二套状态体系（Recoil/Jotai/30 Context——LibreChat 教训）；ECode 保持 Zustand 单层。
- react-virtualized + CellMeasurer 手动缓存（停维护库 + 4 个失效 effect）。
- 多后端代理层/双认证/云本地双模（OpenHands 连接复杂度放大器）。
- 会话偏好矩阵（pinned/标签/分组文件夹/compact 双形态 8+ 开关——桌面重度产品形态）。
- 市场供给端 monorepo、Next+SPA 双构建、Electron 桥（ECode 无此产品面）。
- 消息树/分支（与 checkpoint 回退模型冲突，线性会话不做 sibling）。

## 7. 实施排期建议

| 批次 | 内容 | 估时 |
|---|---|---|
| 批 1 | W-2 虚拟化（virtua）+ W-1 rAF 批处理 + W-10 插话对齐核查 | 2-2.5 天 |
| 批 2 | W-3 会话列表升级（含 session/archive 协议增补+归档入口） + W-5 消息操作注册表 | 2-2.5 天 |
| 批 3 | W-4 diff 卡 + W-7 移动细节包 + W-1 长消息分块（按需） | 1.5-2 天 |
| 批 4 | W-8 令牌层 + W-9 重连游标（协议侧） | 2 天 |

每批一提交一全量门（vitest + tsc + web build + pty/web 冒烟），同 M13 惯例；Q5 拍板小步走——批 1 先行验证收益再启动批 2。W-6 dock 化已后置（不在四批内，按需另排）。

## 8. 决策记录（2026-08-30 用户拍板，Q1-Q5 全闭）

- **Q1 虚拟化**：采用 **`virtua` 库**（lobe-chat 生产在用、变高行聊天列表友好、支持 keepMounted——流式行/选区行不回收）；harness 侧只借鉴其「按 key 独立订阅行」的行设计，列表器不手写。**用户追问后追加实测**（web/src/perf 基准页，数据见 W-2）：首屏 4.5×、DOM 24×——虚拟化从「第三档按需」**升格为批 1 首位**。
- **Q2 diff 实现**：**直接用 `diff` npm 包**（TUI 已在用的同一依赖，生成 unified diff），web 端只做 +/- 着色与折叠展示；参考 harness DiffBlock 的呈现（IN/OUT 标注、max-height 内部滚动）。
- **Q3 审批 dock 化**：**后置**。takeover 现状保留，dock 化移出批 3，按需另行排期。
- **Q4 会话删除 → 归档语义**（改设计）：不做删除。daemon 增补 `session/archive` 协议（session meta 打 archived 标记 + session/list 默认过滤）；web 端归档会话默认不显示，**侧栏项目名称行增加「已归档」入口按钮**——点开查看该项目归档会话并可恢复（取消标记）。进批 2。
- **Q5 排期**：**小步走**——四批串行，批 1（流式管线+容量第一档+插话对齐）先行验证收益，不与 M15 R 线穿插。

## 附录：调研来源

| 项目 | 本地路径 | 分析文档 |
|---|---|---|
| opencode | D:\study\opencode | 解析/2026-08-30_Web端调研-opencode-app_已完成.md |
| deepseek-harness | D:\study\deepseek-harness | 解析/2026-08-30_Web端调研-deepseek-harness_已完成.md |
| OpenHands | D:\study\OpenHands | 解析/2026-08-30_Web端调研-OpenHands_已完成.md |
| LibreChat | D:\study\LibreChat | 解析/2026-08-30_Web端调研-LibreChat_已完成.md |
| lobe-chat | D:\study\lobe-chat | 解析/2026-08-30_Web端调研-lobe-chat_已完成.md |
| NextChat | D:\study\NextChat | 解析/2026-08-30_Web端调研-NextChat_已完成.md |
